# handoff · 2026-09.4
<!-- journal shard: append-only, oldest first. Never edit an entry — correct it with a new
     entry that cites it. Reads: python tools/journal.py newest handoff 3 -->

<!-- e:handoff|H88|2026-09-14 15:47 UTC|ZABZ-YOGA|open -->
## 2026-09-14 15:47 UTC · ZABZ-YOGA · 2026-09-14 15:50 UTC - CORRECTION to H85: the first work session after the fix still failed, so the stall rate is unmeasured

**Correction to H85 and to the claim in it — appended, not rewritten, per the append-only rule**

H85 says the work-mode fix addressed *"the single largest cause of company work failing"*.
That is now overstated, and a future self must not read it as solved.

**What the fix actually did, and it is real.** The dead instruction is gone (verified on the live
file: `Use [ACTION:...] tags to take concrete actions` occurs 0 times), the detector is wired
in, 16 tests pass against the authority's live tree, and the deploy was recorded. The defect it
removed was genuine and proven: a prompt instructing a syntax that `parse_actions()` was
configured to discard, with the discard silent.

**What the first post-fix work session did, which I did not expect.** `work_sessions` 75332
started 2026-09-14T15:30:26Z, ran 5 steps, and failed with

 [STEP_BUDGET_EXHAUSTED: reached planned 5/5 work steps without [WORK_DONE]]

The body of that log is the same shape as the stalls it was meant to fix — *"Let me investigate
the current state of these tools"* repeated across five steps — and it contains **no
`[DROPPED-ACTION-TAGS]` marker at all**. So the model emitted no `[ACTION:...]` tags; it
simply did not act, and the step budget ran out.

**What that means.** The dead-instruction defect was one contributing mechanism. The dominant
behaviour is broader: the work-mode model narrates intent instead of calling a bound tool.
Possible remaining causes, none of them established yet:
- the fast-tier model (`_work_mode_tier = "fast"`, `deepseek-v4-flash`) does not reliably emit
 tool calls under this prompt at all, so no prompt wording fixes it and the tier is the lever;
- the goals themselves are not actionable on this host — the sampled failures are Home Assistant
 sensor checks and CI/PR checks, i.e. work whose tools are not reachable from the authority
 (see **P18**, HA is on the office LAN and reachable from one machine);
- five steps is simply not enough for the task shapes the goal generator produces.

One session is not evidence, in either direction. The honest position: the stall rate is
**unmeasured after the fix**, and the next session must re-read
`work_sessions` by outcome **and** by marker before anyone calls this fixed. If
`[STALLED_REPEATING_OUTPUT]` disappears but `[STEP_BUDGET_EXHAUSTED]` does not, the fix worked
and the real problem is elsewhere — which is the more likely outcome.

The methodological point worth keeping: **a fix that is verified against the mechanism it
targets is not the same as a fix that is verified against the symptom.** I proved the
contradiction existed and that it is gone; I did not prove the symptom moved. Those are
different claims and only the second one matters to the business.

---
<!-- e:handoff|H89|2026-09-14 15:49 UTC|ZABZ-YOGA|open -->
## 2026-09-14 15:49 UTC · ZABZ-YOGA · Bulk iPhone sourcing for the Waze fleet: research complete, one deadline-bound owner question raised, journal merge left broken

# CHANGED
- Bulk iPhone sourcing research for the Kosher Waze product line — COMPLETE. Written to
 `lpt-hub/docs/customer-operations/sourcing/waze-fleet-iphone-sourcing/` (`REPORT.md` + `README.md` +
 `evidence/2026-09-14-live-readings.md`). Four parallel deep-research reports live in `~/code/reports/` and
 `~/code/research/` (B2B marketplaces, risk/verification, overseas/import, price baseline).
- Decision recorded as **D84**: fleet standard = iPhone 11 (A2111); channel = B-Stock/GameStop; grade floor
 A+/A/B/C never D; retail is a first-class channel; overseas import rejected on FCC not duty; grade to CTIA.
- Owner question raised as **queue #19** (high, BLOCKING): commit ~$5,125 to an 80-unit GameStop iPhone lot
 closing 2026-09-15 14:42 ET. (#18 was a duplicate at medium severity, dismissed.)

# IN FLIGHT
- **Nothing is committed and no supplier was contacted.** No B-Stock account exists, no bid placed, no retail
 order placed. The whole turn was analysis.

# BROKEN
- **`harness-config` is mid-merge with unresolved conflicts.** `.git/MERGE_HEAD` exists; `journal/log/wins/2026-09.md`
 and `journal/state/open-pain.md` each carry 3 conflict markers. `journal.py check` reports **213 errors**
 (mostly duplicate ids: L1-L46 duplicated across `log/lessons/2026-09.md` and `log/lessons/2026-09.1.md`;
 H78-H86 duplicated in `log/handoff/2026-09.2.md`). This is pre-existing, not caused by this session — the
 duplicates were absent in a `status` read earlier the same hour. `journal.py append` still works (D84 was
 written), but the integrity check is unusable, which masks new problems. **Do not hand-resolve it blind**;
 see P-entry raised this session.

# NEXT
1. **Owner must answer queue #19 before 2:42 PM ET 2026-09-15** or the lot closes. The gating prerequisite is
 whether LPT holds an **NJ resale certificate** (needed for B-Stock registration, same-day approval).
2. If yes: register, read the logged-in lot detail to get the model breakdown, get a Dallas→Lakewood pallet
 quote with liftgate, set a ceiling, bid.
3. If no: buy retail — Swappa unlocked iPhone 11 64GB at ~$129 — and the product is not blocked.
4. Close the remaining gaps named in the report §7: eBay bulk lots (unmeasured), and the two compliance gates
 (N.J.S.A. 56:8-233 records; Lakewood Twp. Code §4-9 municipal dealer licence + $10,000 bond).
5. Fix the journal merge.

# EVIDENCE
- `lpt-hub/docs/customer-operations/sourcing/waze-fleet-iphone-sourcing/REPORT.md` — every figure carries a
 source URL and observation date. Key live reads: B-Stock lot 67654 (re-fetched by me), GameStop conditions
 page (re-fetched by me), Swappa iPhone 11/12, Giggle Trade weekly list, Today's Closeout (19 live iPhone 11
 SKUs, pricing account-gated), Cell Dealers, gsmExchange, RecirQ.
- `ssh secratary-ts "python3 ~/bin/owner-queue.py next"` → #19.
- `python journal/tools/journal.py status` → integrity failing (see BROKEN).

---
<!-- e:handoff|H90|2026-09-14 15:50 UTC|ZABZ-YOGA|open -->
## 2026-09-14 15:50 UTC · ZABZ-YOGA · Bulk iPhone price baseline delivered; Swappa two-series error found and corrected

CHANGED
- Delivered the PRICE BASELINE + TECHNICAL SPEC section of the bulk-iPhone sourcing report for
 Lakewood Phone & Tech: reports/bulk-iphone-sourcing-2026/PRICE-BASELINE-AND-TECHNICAL-SPEC.md
 (~11.4k words, 900 lines, 4 tasks, full source ledger + explicit gap list).
- Found and corrected a report-invalidating method error in my OWN work, then had the
 correction confirmed by an adversarial pass: Swappa's /en/prices/<model> pages publish TWO
 price series (carrier table "Avg Price" = asking; a separate "Storage Prices" table with
 "Avg list price" + "Avg sale price" = achieved). My first pass used asking while labelling it
 achieved. Fixed throughout; margins recomputed.
- Closed two gaps I had reported as open: iPhone 11 wholesale ($113.86, 128GB grade B US,
 Giggle Trade live unit) and cracked/for-parts asking prices (eBay: parts-only $72.99-$99.99,
 cracked $184.95-$219.99).
- Journal: L226 (two-series price page / truncated extraction), L227 (journal.py --body takes a
 file path or -, never inline prose).

IN FLIGHT
- Nothing blocking. Three subagents were dispatched and all three have now reported (eBay sold
 prices, US refurb retail/payouts, adversarial verification) and their findings are folded in.

BROKEN / OPEN
- eBay SOLD/COMPLETED prices remain unobtainable: 36 of 36 buckets empty. eBay sign-in-walls
 sold-filtered search for datacenter clients and error-pages automated browsers even on plain
 searches. Only closable with a logged-in seller account (Terapeak), the Marketplace Insights
 API, or a human on a residential IP with LH_Sold=1. This is the report's largest remaining gap.
- Gazelle / ItsWorthMore buyback payouts unobtainable (device-picker wizard returns identical
 "up to" marketing ranges for every model; ItsWorthMore's pricing API is session-token-bound).
 Decluttr is CLOSED (verified).
- P71 still open and unharmed by me: the journal is mid-merge with duplicate-id errors, so
 `journal.py check` exits non-zero and `append` exits non-zero even when the write succeeds.

NEXT
- Get an eBay seller account / Terapeak access: it closes the sold-comps gap and would let the
 report price cracked and for-parts stock from achieved rather than asking prices.
- Re-quote any wholesale lot at time of purchase: the same iPhone 12 64G US SKU appeared at two
 different prices on the same platform on the same day ($154-167 vs $223-256).

EVIDENCE
- Report: reports/bulk-iphone-sourcing-2026/PRICE-BASELINE-AND-TECHNICAL-SPEC.md
- Journal: L226, L227 (log/lessons/2026-09.2.md); P71 (pre-existing, log/pain/2026-09.md)
- Headline numbers, all observed 2026-09-14: Swappa achieved sale iPhone 12 64G $167 /
 12 128G $179 / 13 128G $206-273 (completed sales) / 14 128G $285 / 15 128G $388 vs Tier 1
 wholesale $154 / $188 / $238 / $272 / $362-421. Net after 13% fee, shipping, refurb, inbound
 and 3% rejects: -$63 / -$90 / <=-$41 / -$81 / -$83 to -$142. No profitable wholesale-lot
 channel found; trade-in -> local cash nets +$59 to +$96.

---
<!-- e:handoff|H91|2026-09-14 15:54 UTC|ZABZ-YOGA|open -->
## 2026-09-14 15:54 UTC · ZABZ-YOGA · 2026-09-14 15:58 UTC - Journal integrity restored locally (217 ids renumbered), master was never polluted, integration deliberately not forced

**Journal integrity restored locally, master was never polluted, and the integration is deliberately not forced**

**CHANGED**
- 217 collided ids renumbered with the new `dedupe` command (suffix the later copy, never delete an
 entry). Local result: **718 entries, 718 distinct ids, 0 duplicate-id errors, `check` 0
 error(s)/29 warning(s)/77 info**. Was: 717 entries carrying only 500 distinct ids and 60
 duplicate-id errors, with `check` exiting 1. Pre-repair tarball at
 `~/code-snapshots/journal-20260914T115323Z/`.
- `journal.py dedupe` added, plus the root-cause guard reasoning: `append` and `import-flat` both
 allocate with `remote_max(kind)`, which only sees refs **this checkout has fetched**, so two
 sessions on diverged checkouts both write H81 and the reuniting `git pull` produces two entries
 with one id. Verified on a throwaway copy before touching the live tree (212 errors → 0,
 716 entries preserved).
- The work-mode fix verified end to end on the **deployed** prompt — see W42. 2 executable actions
 parsed, 0 dead tags.

**IN FLIGHT**
- **My commits are on `origin/zabz-audit-20260914`, not on `master`.** Master moved to `c9f3a4b`
 while I worked; my branch is 4 ahead / 6 behind. A guarded merge was attempted and produced four
 conflicts (`journal/index/entries.tsv` and `journal/state/open-pain.md`, both *generated* and
 therefore resolvable by regeneration; `journal/log/wins/2026-09.md`; and an add/add on
 `scripts/question-card-check.py`). The merge was **aborted** rather than left half-resolved,
 because the other session writes this repo continuously and a conflicted tree blocks it. Nothing
 of mine was lost — the branch is pushed.
- The integration needs a deliberate pass, not another race. Recommended order: fetch, merge
 `origin/zabz-audit-20260914`, resolve the two generated files by running
 `journal.py index` and `journal.py state` rather than hand-editing them, resolve the wins shard
 by keeping both sides' entries, then rebase `question-card-check.py` on whichever version is
 canonical. Then run `journal.py check` **and** `journal.py dedupe` before pushing, because a
 merge of two appending branches is exactly what creates collisions.

**BROKEN / MEASURED**
- **The shared repo is clean.** `origin/master`'s index holds 489 rows with **0 duplicate ids**.
 So the 217 collisions were local-only and the mesh was never polluted by them — worth stating
 plainly because it is the difference between a broken distributor and a broken working copy.
- **Master's index covers 489 entries while the local log holds 718.** That gap is unexplained and
 is the next thing to measure: either master's index is stale relative to its own shards (i.e.
 entries committed without regeneration, which `check` should be able to detect), or ~229 entries
 exist only in working copies that were never committed. Do not guess which; measure it with
 `journal.py check` against a clean checkout of master.
- Two tracked files still hold plaintext credentials in the repo:
 `deploy/twilio-studio-flow/.env.vogel` (`owner_decision_queue` #17, awaiting the owner), and
 this session also saw `deploy/hosting-migration/configs/hetzner-tokens.env` and
 `deploy/twilio-studio-flow/.env.vogel` matched by a secret-shaped grep — the former is an explicit
 template and safe.

**NEXT**
- Measure the 489-vs-718 gap, then do the branch integration in one quiet window with the other
 session stopped.
- The 41 KB work prompt (W42) is the next cost lever: paid on all five steps of every session.

**EVIDENCE**
- `journal.py dedupe --apply` on the live tree; `check` before/after captured above.
- `git show origin/master:journal/index/entries.tsv` → 489 rows, 0 duplicate ids.
- Branch `origin/zabz-audit-20260914` = `621256e` (contains the encoding fix `f344a71`, the dedupe
 command `d561c9b`, and the journal repair `621256e`).

---
<!-- e:handoff|H92|2026-09-14 16:12 UTC|ZABZ-TECH|open -->
## 2026-09-14 16:12 UTC · ZABZ-TECH · Atlas: work finished 09-09, USB written 09-10, undelivered and unpaid; customer chased it 09-14 and the system could not see the job

## 2026-09-14 16:20 UTC · ZABZ-TECH · Atlas: the work finished on 09-09, the USB was written 09-10, and nothing has moved since — a customer chased a delivered-but-undelivered job today and the system could not see it

**CHANGED**
- `lpt-hub/docs/customer-operations/cases/data-recovery-goldie-atlas-8482996734-2026-08-25.md` — appended a
 full current-state block (what exists, why it has not moved, what not to redo). The case file had been
 4 days stale; its last entry was the 09-10 USB prep.
- `lpt-hub/docs/customer-operations/recovery-index.md` — Atlas row corrected from `in-house` to
 `WORK DONE 09-09, ON USB 09-10 — UNDELIVERED, UNPAID, CUSTOMER CHASED 09-14`.
- `owner_decision_queue` #21 (high) — the delivery/payment decision, with recommendation.
- USB integrity verification launched on `linux-pc` (`/tmp/atlas-usb-verify.log`), reading all 31,882
 files against `/mnt/atlas-usb/CHECKSUMS-SHA256.txt`. **Result not yet read as of this entry.**

**IN FLIGHT**
- The USB verification job above. If it reports mismatch or missing files, the USB must be rewritten from
 `/mnt/atlas-recovery/atlas-8482996734/ORGANIZED-DELIVERABLE/` before any handoff.
- Owner question #21. Nothing outbound until he answers; the recommended option is my sending one
 ready-for-pickup text with no charge.

**BROKEN / MEASURED (all read live 2026-09-14 16:11 UTC)**
- Deliverable is real and verified present: `FINAL-DELIVERABLE` 83 GB / 31,882 files; `BACKUP-DELIVERABLE-COPY`
 83 GB; `ORGANIZED-DELIVERABLE` 83 GB; USB `/mnt/atlas-usb/ATLAS-RECOVERY-GOLDIE` 31,882 files / 86 GB with
 `CHECKSUMS-SHA256.txt` (31,883 lines).
- **The source Seagate ST1000LM035 is no longer attached to the bench.** The only copies off the drive are the
 USB and `/dev/sdf2` (bench SSD, 1.1 TB used / 5.5 TB). There is no second disk. Deleting anything here is
 irreversible data loss.
- **No pipeline tracks this job.** Case file says `Secretary Contact ID: 634` — no such contact exists on the
 authority, and no contact matches 8482996734. `Secretary Task ID: 13407` is task 13407, which is `done` and is
 an unrelated "Mashgiach Quality & Audit Clerk" initiative tick. The sync record still reads
 `updatedAt 2026-08-25 / awaiting_device_details / awaiting_dropoff`.
- **The customer is actively waiting.** Dialpad: inbound 09-14 11:22:38 *"Hi checking in on my c gate"* answered
 09-14 11:22:39 by the automated closed-hours reply only. Last substantive outbound: 09-07 19:01 *"…another day
 or 2… we'll be in touch when it's ready."* Nothing after it.
- The company generates tasks about exactly this failure mode without connecting them to the case:
 #24986 "ROUTE IDLE FINANCE CAPACITY TO UNPAID REPAIR REVENUE SWEEP" (09-14 15:44) and #24752 (09-12)
 "flags READY_FOR_PICKUP/COMPLETED-but-…".

**NEXT**
1. Read the USB verification result and record it; rewrite the USB if it is not clean.
2. Hold at owner question #21, then deliver and collect $225.
3. Sweep the other completed-but-uncollected recoveries as one bounded job: `recovery-index.md` shows
 Czermak "complete / undelivered", Abramczyk "CLOSED" but with pickup/payment unverified, plus Shlesinger,
 Kaplin, Feld, Wilkes-class rows whose last-known state predates September. Atlas is the proven instance,
 not necessarily the only one.

**EVIDENCE**
- Live reads on `linux-pc` 2026-09-14 16:08-16:11 UTC: `df -hT`, `ls -la` of the case dir and USB, `du -sh`,
 `find | wc -l`, `lsblk`.
- Authority reads on `secratary` 2026-09-14 16:10 UTC via git-bash ssh: `contacts` (no 634, no phone match),
 `tasks` (13407 and the 44-row non-done backlog), `owner_decision_queue`, `dialpad_sms_cache` for
 8482996734.
- `RECOVERY-LOG.md` on the bench: 1,559 lines, of which **951 are `watchdog: ddrescue STOPPED` lines** appended
 every 25 minutes since 2026-08-27 — the real content ends 09-10.

---
<!-- e:handoff|H93|2026-09-14 16:21 UTC|ZABZ-TECH|open -->
## 2026-09-14 16:21 UTC · ZABZ-TECH · HA audited from the office machine: UPS restored, a three-day CRITICAL was a lie, camera evidence is one credential away

## 2026-09-14 12:45 ET · ZABZ-TECH · The HA estate audited from the office machine: the UPS came back from the dead, a three-day CRITICAL turned out to be a lie, and the camera evidence path is one credential away

The owner said only: *"i'm in the office now — let's continue and fixing the home assistant
system and auditing the devices and stuff."* The office is the only place with a route to the
HA host, so the whole estate became readable for the first time. Most of what follows was
invisible from anywhere else.

CHANGED

- **`ha-config` now works from this machine.** Cloned to `~/code/ha-config`; the mesh key the
 HA host's SSH add-on trusts installed at `~/.ssh/ha-mesh-key`. `root@192.168.50.34:2222`
 is one command from here now. (W49)
- **UPS monitoring restored, verified with live values.** `nut` app `error` -> `started`,
 HA entry `setup_retry` -> `loaded`, `LIST UPS` answers, sensors read charge 100% / 119 V in
 and out / load 15% / Online / runtime 2670 s. Cause was not the UPS: the app refuses to
 start when `users[0].password` is in HaveIBeenPwned, and it was `Hello@1234`. (W48, L233, D86)
- **`ha_truth.py` no longer publishes a false CRITICAL.** The `SECURITY_INTEGRATION_DEGRADED —
 zha` alarm had fired every 30 minutes for three days about an integration that is a
 discovery the owner dismissed in 2025 while Zigbee runs on Zigbee2MQTT. Dismissed
 discoveries are now excluded from degradation and reported separately at `low`. (L230, D85)
- **Two findings that were missing, both measured here.** `SECURITY_SENSORS_UNAVAILABLE` now
 carries how long the longest has been blind (today 14.5 h). New `BATTERY_LOW` /
 `BATTERY_UNREPORTED`, read from the entity surface so they work for Zigbee2MQTT, ZHA,
 Z-Wave and ESPHome alike. (D85)
- **`docs/AUDIT-2026-09-14-devices-and-integrations.md`** — the full device and integration
 audit: 12 Zigbee devices with batteries and power sources, every degraded integration, the
 add-on version backlog, and an explicit list of what the audit cannot verify. Commits
 `a76ea41`, `c579f5e`, `f03d308`, pushed and pulled by the authority.
- **Owner queue #23 filed** (high) for the NVR credential. #22 was filed at `medium` by
 mistake and dismissed immediately rather than left as a duplicate.

IN FLIGHT

- **The camera evidence path needs the owner** (queue #23). Diagnosed to the mechanism: the NVR
 forces HTTPS, the six `dahua` entries are configured for port 80, aiohttp follows the 302 to
 `:443`, drops the digest authorization on the scheme/port change, and the NVR answers 401
 (HA shows it as a 500). Verified independently with curl. Two candidate causes remain
 indistinguishable from outside — changed password, or account lockout from HA's own
 ~500 failed logins/hour — and one owner action settles both.
- **Two of three Zigbee door contacts are silent** since the 2026-09-13 21:49 ET host reboot:
 `0x286d97000115ca7a` (outside door, last battery 21%) and `0x286d97000115c914` (control room,
 last battery 8%). SmartThings IM6001-MPP01. Not claimed that the batteries are dead — either
 cause gives the same first action, and it is physical.
- **The retired Keymaster generation** (82 unavailable automations, 41 entities, the `_2`
 twins) is still installed and inflates every count from this estate. Disposal pass not done.
- **`ssh` from `C:\Program Files\OpenSSH\ssh.exe` on this machine hangs after every command**
 while still printing output. Git's `ssh.exe` works. Not yet pinned in the harness — until it
 is, every remote read that ends in a timeout is an unread surface, not a short answer. (L232)
- **1 895 registry entities, zero areas.** Unstarted.

BROKEN

- Camera snapshots (six channels) and timeline clips: 535 of the loudest log errors, all one
 credential. (P75)
- Spotify: `setup_retry`, refresh token revoked, 650 errors — the single loudest line in the
 log. Needs the owner's Spotify login, or removal. Not my call.
- TP-Link "Playroom warning KL135": `192.168.50.225` does not answer ping or tcp/9999 —
 unplugged, physical.

NEXT

1. **Ask the owner queue #23** — the NVR password. One question, already written with a
 recommendation. That unblocks both the snapshots and the clips.
2. When he answers: load the credential into all six `dahua` entries, prove a real JPEG
 snapshot end to end, then bound the snapshot scripts' internal retries so a failing camera
 cannot drive the NVR into a lockout again.
3. Tell him the battery/re-pair action for the two door contacts while he is in the office.
4. Pin Git's `ssh.exe` in `harness-config` so the next session on this machine does not
 rediscover L232.
5. Disposal pass on the Keymaster generation, provable delete by delete (P76).

EVIDENCE

- Live reads: `.runtime/ha-truth.json` / `.runtime/ha-truth.md` (regenerated 12:35 ET, schema v1)
 from `ZABZ-TECH`; the authority's own copy at `secratary:~/ceo-kernel-var/ha/latest.md` ran at
 **16:19:49Z exit=0** with the false CRITICAL gone.
- Host-side: `lsusb` + `/dev/serial/by-id`, `dmesg` (470 CPS re-enumerations 01:33-02:16 ET,
 stable since), `ha apps` (add-on versions), `/config/.storage/core.config_entries`,
 Zigbee2MQTT `database.db` + `state.json` + `log/2026-09-13.21-51-00/log.log`.
- Network: curl against the NVR on 80 and 443 (302, then 401 with a digest challenge; 401 again
 with `--digest`, `--basic`, `--anyauth`).
- Commits `a76ea41`, `c579f5e`, `f03d308` on `origin/main`, confirmed on `secratary` by
 `git log` in `~/ha-config-live`.

---
<!-- e:handoff|H94|2026-09-14 16:36 UTC|ZABZ-TECH|open -->
## 2026-09-14 16:36 UTC · ZABZ-TECH · B-Stock bulk iPhone lane killed; fleet goes two-tier (iPhone 11 + SE 2020, both eSIM); device supply pipeline is now just-in-time retail

CHANGED
- **Killed the B-Stock bulk iPhone lane.** Measured the real clearing prices of the identical 80-unit B/C lots off
 the lot pages' own `#estimate-amount` field: 64116 closed 2026-01-13 at $12,222 (**$152.78/unit**), 65996
 2026-06-02 at $11,250 (**$140.63**), 67115 2026-08-11 at $9,118 (**$113.98**). Lot 67654 was sitting at $5,076
 ($63.45/unit) with 1d2h left — an opening number, not a clearing price. Also established that GameStop grade C
 permits dead speakers/microphones and a dead rear camera, that "merchandise returns will NOT be accepted", and
 that the advertised 30-day window covers **blacklist/FMIP/MDM lock only**. Owner answered queue **#19 = pass**.
- **Owner decision: 10-20 units over the next 60 days, bought as needed** (not a lot). Recorded as D87.
- **Owner decision: the fleet goes two-tier — iPhone 11 (tier 1) and iPhone SE 2020 (tier 2), both eSIM.** D88.
 D84's claim that the iPhone 11 was the cheapest eSIM + current-iOS iPhone was **wrong**: the SE2 is the same A13
 generation, is on the iOS 27 list, and costs about half (Swappa avg sale $85 vs $136).
- New file, the source of truth for this: `lpt-hub/docs/customer-operations/sourcing/kosher-waze-device-supply-pipeline.md`
 — buying rule (order when on-hand + in-transit < 3), per-unit requirements, per-tier buy ceilings ($125/$145 for
 the 11, $95 for the SE2), and the channel list with the measurement behind each number.
- Journal: **L234** (how to read a B-Stock bid out of the DOM, the closed-lot prices, grade C's real allowances),
 **D87** (bulk is premature), **D88** (two tiers).

IN FLIGHT
- Nothing running. Two research subagents (US retail price scan, B2B liquidation scan) both reported and their
 findings are folded into the pipeline doc.

BROKEN / OPEN
- **The device has no sale price anywhere in the company's record.** Not in `phone-and-tech-full`, not in `lpt-hub`,
 not in the 212 readable tables of the authority DB — checked 2026-09-14. The only recorded pricing is the
 `$9/mo · 250MB · 800MB cap · $18/GB` subscription. Two tiers imply two price points. This is the last blocker to
 selling the first unit.
- **Two-tier engineering not started.** The wallpaper renderer emits one fixed 720x1280 canvas and caches
 `drn-device-{drn}-{lock|home}.png` (`deploy/waze-mdm/docs/deployment-process.md:382`); tier 1 is 828x1792 @2x and
 tier 2 is 750x1334 @2x, so both the canvas and the cache key need the model. Also needed: a `webtrap` check on a
 4.7in screen, an SE2 case SKU, and a model parameter in `scripts/waze/onboard.py`.
- No NJ resale certificate is confirmed to exist in any record. It is only needed by channels we are no longer
 using, so it is off the critical path — do not re-open it unless a channel needs it.
- The journal has pre-existing duplicate-id warnings (`journal.py check` is noisy with H78-H86 and L1-L161
 suffixes). Pre-existing, not caused here; `append` works.

NEXT
1. Get the device sale price(s) from the owner — one question, with the two-tier framing.
2. Buy the first 1-3 units to the ceilings above and QC them (activation lock off, IMEI clean, eSIM installs the
 Telnyx profile, screen uncracked, battery ≥85%). Note: purchasing needs a payment path (eBay/Back Market
 account), which is not mine to create.
3. Implement the per-model wallpaper canvas + cache key, then verify a tier-2 unit renders and locks down.

EVIDENCE
- `lpt-hub/docs/customer-operations/sourcing/kosher-waze-device-supply-pipeline.md`
- `lpt-hub/docs/customer-operations/cases/lpt-waze-fleet-device-registry.md` (2 units, both `test`, not locked)
- `lpt-hub/docs/customer-operations/purchases/lpt-waze-kiosk-trial-iphone11-2026-08-06.md` (2 × $149.99, eBay)
- journal: L234, D87, D88 · queue `owner_decision_queue` #19 answered
- price reads, all 2026-09-14: swappa.com/prices/apple-iphone-se-2nd-gen (avg sale $85, 20 sales $78-95),
 swappa.com/prices/apple-iphone-11 (avg sale $136), macrumors.com iOS 27 device list,
 support.apple.com/en-us/109317 (eSIM needs iPhone XS or later)

---
<!-- e:handoff|H95|2026-09-14 16:37 UTC|ZABZ-TECH|open -->
## 2026-09-14 16:37 UTC · ZABZ-TECH · A live customer case that existed only in a Dialpad thread: quoted Thursday, still un-ordered Monday

## CHANGED
- lpt-hub: new customer case for a Lenovo IdeaPad Slim 3 15ABR8 (MTM 82XM00LMUS) cracked screen,
 customer phone +1 848-480-5115, first contact 2026-09-10. Committed as dbb56735.
 docs/customer-operations/cases/lenovo-ideapad-slim-3-15abr8-8484805115-2026-09-14.md
 docs/customer-operations/sync-records/2026-09-14-lenovo-ideapad-slim3-15abr8-screen.json
 research/hardware/laptop-repair/2026-09-14-lenovo-82XM00LMUS-lcd-sourcing.md
- Authority: this case had NO record anywhere. Created via POST /api/v1/shop/customer-intake ->
 contact 634, task 24997 (the endpoint also wrote its own generated sync record into the SERVER's
 lpt-hub clone at /home/zabz/personal-secretary-mvp/lpt-hub/... - that is a second copy of the repo,
 see BROKEN).
- Journal: L235 (disprove an OCR'd Lenovo serial), L236 (buy laptop panels by connector, 82XM shipped
 touch and non-touch), P77 (a quoted repair cannot be ordered for unless the owner is reachable),
 D88 (order the 40-pin touch panel, hold the $175 quote).
- Owner queue: added one question with a recommendation - should staff be able to order parts up to a
 per-job cap without the owner (blocking this customer).

## IN FLIGHT
- The part is NOT ordered. Everything is ready to the one click: laptopscreen.com item 9597874,
 IPS On-Cell Touch, 40-pin narrow, NV156FHM-T07, $76.99 return-customer / $86.99 new, 1000+ in
 stock, ships same day from Newark DE, lifetime warranty. Verified live by me 2026-09-14.
- The customer reply is DRAFTED AND NOT SENT (case file + sync record both carry the text). Nothing
 outbound goes without an explicit per-message instruction.
- The machine has never been dropped off, so the software fault is completely unspecified.

## BROKEN / WATCH
- The shop cannot buy a part unless the owner is reachable. That is the whole root cause here (P77).
- There are TWO lpt-hub checkouts: this desktop's repo and a clone inside
 /home/zabz/personal-secretary-mvp/lpt-hub on secratary. The shop intake API writes sync records to
 the SERVER clone, so a case created through the API gets its sync record somewhere nobody on the
 desktop will look. Check for drift before trusting either copy. Not investigated further this
 session.
- The secretary API on 192.168.50.77:8002 refused connections once at ~12:27 ET and answered again
 seconds later - it bounced during this session. Unverified cause.
- `ssh secratary "<cmd>"` frequently PRINTS its output and then never exits, so a foreground call
 times out at 120s after the data has already arrived. Use run_in_background + job_output, or push a
 script with `Get-Content file | ssh secratary 'bash -s'`. Never inline multi-line remote scripts.

## NEXT
1. Owner answers the queue question -> someone orders laptopscreen.com item 9597874 today.
2. Send the customer the firm price and date (draft ready).
3. At drop-off: capture the customer's real name, re-read the S/N (the label photo serial is WRONG -
 see L235), count pins on the old cable (40 = touch), inspect the digitizer board connector, scope
 and quote the software fault inside the $50-125 band before doing it.
4. Consider the generalisable half of P77 that IS mine: intake record within the hour for every
 inbound repair request, via /api/v1/shop/customer-intake.

## EVIDENCE
- Lenovo PSREF 82XM00LMUS = 15.6" FHD 1920x1080 IPS 300nits Anti-glare, TOUCH, 10-point
 (psref.lenovo.com/Detail/IdeaPad_Slim_3_15ABR8?M=82XM00LMUS, live browser, 2026-09-14).
- Lenovo Parts Lookup, searched by serial PF5KT6T6, returns "Lenovo V15 G4 ABP - Type 82YY" - so that
 serial read is wrong. The lookup is exact: the same API returns [] for ZZQXWVUT.
- comms/by-phone/8484805115 on the authority: 13 dialpad_sms + 10 dialpad_call events, full thread.
- Secretary API health 200 at 2026-09-14T16:24:28Z, database ok.

---
<!-- e:handoff|H96|2026-09-14 16:00 UTC|ZABZ-YOGA|open -->
## 2026-09-14 16:00 UTC · ZABZ-YOGA · Kosher filter audited end to end: the backend is far more built than anyone recorded, it cannot yet take money, and operator auth was failing open

**What the owner asked for:** *"audit what still has to get done and write engineering docs to plan it
out then integrate into the lpt signin flow, but it will be its own website, also make sure there is a
full good onboarding flow for users and more."*

**CHANGED** — `kosher-filter-ai` @ **`8709ecf`**, pushed and verified in sync with `origin/main`
(confirmed by SHA, not by git's prose — `L210`).
- **Operator auth no longer fails open.** `app/auth.py:405-431` returned a `super_admin` scope for any
 caller whenever `LPT_WEBHOOK_SECRET` was empty, before reading a username or password. Trigger needed
 *some* Basic header (`curl -u anything:anything`); no header was already 401. **Production was NOT
 exposed** — my own probe, 2026-09-14 11:42:36Z: `/admin` → 401, `/admin/api/billing/overview` → 401,
 `/health` → 200, `/health/db` → 401. Latent footgun, not an incident. Now denies by default; the old
 behaviour needs `LPT_ALLOW_INSECURE_DEV_ADMIN=true` (default false, documented loopback-only), and
 startup logs the consequence either way. **Verified: 19 passed, 0 failed**
 (`test_operator_auth_fail_closed` (6 new) + `test_operator_jwt_auth` + `test_tenant_admin_scope`).
- **New docs** (all committed): `docs/ENGINEERING_STATE_AUDIT_2026-09-14.md`,
 `docs/WEB_APP_AND_ONBOARDING_PLAN_2026-09-14.md`, `docs/LPT_SIGNIN_AND_ACCOUNT_AUDIT_2026-09-14.md`,
 `docs/audits/{android-client-capability,backend}-audit-2026-09-14.md`,
 `decisions/38-web-app-and-onboarding.md` (D38-01…05), `docs/IMPLEMENTATION_BACKLOG.md` + Phase 5
 (W-BACK-050..060), regenerated `docs/config-reference.md`, indexed in `docs/README.md`.

**The three facts that matter most for the next session**
1. **The product cannot take money.** No Checkout/Price/Customer/subscription-create call exists anywhere
 in `server/app/`; `plan_code` defaults to `'beta'`, `billing_seat_price_cents=0`; the only
 parent-reachable payment route opens a provider portal *for a customer that must already exist*; and
 filtering is never cut off for non-payment. This is now **W-BACK-060** and it is ahead of every other
 gap in the list. Second defect alongside it: `LPT_BILLING_STRIPE_WEBHOOK_SECRET` is empty while
 `LPT_BILLING_STRIPE_SECRET_KEY` is a live-mode key in the same file → the webhook 503s while the key is
 live.
2. **The LPT sign-in integration is already available and costs no LPT-side change.** `POST
 /api/auth/sso/assert` (shared secret, server-to-server, reads the `lpt_refresh` cookie, non-rotating)
 is the single cross-app surface; the broker already answers on `filter.lakewoodphoneandtech.com`, so a
 site on any `*.lakewoodphoneandtech.com` subdomain is inside both the cookie scope and the `return_to`
 allowlist. **But** LPT's customer portal is feature-flagged **off in production** (on in test), so v1
 ships the site's own sign-in behind one `identify()` seam and the LPT path drops in when the flag
 flips. `lpt_operator_session` (broker) and `lpt_refresh` (LPT) share a name prefix and nothing else.
3. **A first-class product feature is dead code.** `ScreenshotService.startWithConsent`
 (`android/.../ScreenshotService.kt:1627`) has **zero callers repo-wide** and `createScreenCaptureIntent()`
 appears nowhere in the repository — 1,836 lines, no test file, and 414 green JVM tests never noticed.
 The MITM CA trust assumption is likewise unverified and the code itself says so
 (`PolicyApplier.kt:906-909`).

**IN FLIGHT / NEXT** — the plan's order in `docs/WEB_APP_AND_ONBOARDING_PLAN_2026-09-14.md`:
W-BACK-050 (site skeleton + BFF) → 051 (sign-in behind `identify()`) → 052/053 in parallel (onboarding
stages 2–7, and the 34 existing `/owner/*` routes as account pages) → 054/055 (shop intake; public +
compliance pages) → 056 (deploy path) → 057 (Playwright end-to-end; **no automated test covers the web
surfaces today**), with 058 (password reset) and 059 (email verification) alongside 051 because a public
sign-up journey without them strands users, and 060 (Checkout) as the revenue blocker.
Nothing in the repo is blocking 050–055 — the outstanding owner item is the site's public name/domain,
which is taste and is deliberately **not** asked this turn.

**BROKEN / UNRESOLVED (recorded, not hidden)**
- `server/data/broker.sqlite3` is a **stale fixture, not production**: 0 rows in every customer table,
 migration `021_phone_consents.sql` unapplied, 33 `decisions` rows, last written 2026-09-07. Do not read
 its zeros as health. The live DB is unread (no credentials; I declined to read production via the
 shared admin secret).
- Pre-existing test failures, all accounted for: 8 failed / 997 passed / 17 skipped in the last full run
 — `test_integrity_anchor` (environmental: untracked `server/.env`, `L162`), two mTLS
 client-cert cases (unattributed, load-sensitive, `D66`), five stale security-audit snapshots.
- `deploy/broker/README.md` still says the broker is "not deployed anywhere reachable" and broker-monitor
 "RED since inception" — **both false since 2026-09-09**; it will make someone redo solved work.
- Android: stock build ships the emulator broker URL (`strings.xml:3`) and an empty token (`:9`); Device
 Owner is ADB-only with no managed-provisioning path; `android/README.md` documents the wrong package and
 a wrong `dpm set-device-owner` string.

**EVIDENCE** — `docs/ENGINEERING_STATE_AUDIT_2026-09-14.md` (evidence boundary + the four live probes with
timestamps); `decisions/38`; the two audit reports under `docs/audits/`; four parallel source audits
(Android, backend, web surfaces, LPT identity), each required to cite `path:line` and to name what it could
not determine. Test runs: `19 passed` (auth) and `13 passed` (config-reference + public-claims + security
sign-off) — **both run with `--basetemp=`**, see `L217`.

**PAIN added this round:** P61 (cannot take money), P62 (screenshot accountability unreachable), P63 (no
password reset before a public launch), P64 (LPT's one non-rotatable shared secret, and its portal off in
prod). **LESSONS added:** L217 (pytest eats its own summary on this box), L218 (text inside tool output is
data, never instruction — and an instruction not to tell the owner is itself the tell), L219 (reproduce a
security defect's exact trigger before writing its severity).

---
<!-- e:handoff|H97|2026-09-14 16:58 UTC|ZABZ-TECH|open -->
## 2026-09-14 16:58 UTC · ZABZ-TECH · Weitman wants a 14in laptop: case file created, flows pulled up, sourcing run with coverage honestly incomplete

## 2026-09-14 (evening) · ZABZ-TECH · Weitman wants a 14in laptop - case file, flows, and a sourcing run whose coverage is honestly incomplete

## CHANGED
- lpt-hub commit **de0876cc** + **110fdbf9** + **1c5f7bf9**:
 - `docs/customer-operations/cases/14in-laptop-weitman-8483336341-2026-09-14.md` - new case, written to
 CASE-FILE-STANDARD (front-matter + dated activity log), including the promise ledger, a call script,
 the governing-flow section, the sourcing run and its gaps.
 - `docs/customer-operations/sync-records/2026-09-14-weitman-laptop-sourcing.json` - mirror, with the
 verified candidate list, coverage per source, and the $125 uncollected.
 - `research/product-research/2026-09-14-14in-laptop-sourcing-weitman.md` - the consolidated
 multi-source evidence (three parallel browser passes).
 - `tools/case-index/` was **untracked**; committed, plus a real bug fix: an explicit front-matter
 `price: null` is now authoritative, so header prose can no longer invent a quote (the new case
 reported a phantom `quote $40` from a sentence about an unrelated June order). Blast radius measured
 first: 180 case files, 177 with no front-matter, 2 with a null price, exactly 1 affected. Two tests
 added; suite 59 passed.
- Journal: **L246** (a PowerShell cast failure inside Where-Object silently DELETES rows from the
 authority's comms pull - the pull mixes ISO and "YYYY-MM-DD HH:MM UTC" timestamps), **D-list entry**
 (case artifacts, the null-price rule, why no intake record was created for a sale, why the form flow
 still applies), **two pains** below. The eBay research child wrote **L247** (how to get past eBay's
 /splashui challenge and read item pages).

## IN FLIGHT
- Nothing running. The three sourcing subagents all finished and their findings are committed.
- **The customer has still not been called.** Two explicit "call me" requests, 2026-09-10 and 09-13.

## BROKEN / WATCH
- **Production's SMS record is frozen at 2026-09-02.** Measured: the newest `communications` row of
 type SMS for ANY customer is id 130508, created 2026-09-02 19:06; SMS-by-day shows 2005 rows on
 09-02 and nothing after, while PHONE_CALL rows kept writing until 2026-09-11. So 12 days of every
 customer's texts are missing from the website. The customer reply the owner can see in Dialpad
 (09-10 17:33) exists in neither the website nor the authority's Dialpad pull. Not the outbound
 SMS kill-switch - this is the copy of already-sent messages. Cause not yet diagnosed.
- **The research dispatcher reports success on an empty answer.** It returned `status: complete`,
 `verifier_status: passed`, `cost_usd: 0`, `input_tokens: 0` with a body reading "Synthesis
 unavailable - the LLM backend was not available". All five web-search providers are down (brave 302,
 tavily 400, serpapi 429, jina timeout) and the Copilot route rejects model `gpt-5-mini` as
 `model_not_supported`. This silently marks a MANDATORY source as checked. Recorded in the case ledger
 as `failed`, not as checked.
- **WO26082712634 is still `READY_FOR_PICKUP` in production for a repair that was never done**, and both
 of this customer's orders show `paymentStatus: PENDING` ($65 + $60). Left untouched deliberately -
 changing a live shop record is the owner's call, not mine.
- **`ssh secratary-ts "<cmd>"` printed its output and then never exited** (again - L232). Use
 run_in_background + job_output, and kill the job once the data has arrived.

## NEXT
1. **Owner answers the one question** (old jobs and the $125) - it is on the owner queue.
2. Whoever calls him asks budget, then condition; the call script is in the case file.
3. Before ordering: run the listing photo analysis (`scripts/collect_ebay_listing_images.py` +
 `laptop_sourcing.py record --analyze-images`) and read the **Touchscreen** item specific - touch
 status on the eBay Dells is currently INFERRED from titles, and Latitude 7400/7490 ship both ways.
4. Diagnose the production SMS sync job (`app.services.website_db.sync_dialpad_sms_to_website` and the
 secretary-side backfill cursor - the cursor table is NOT in production, so look on the authority).
5. Fix the dispatcher so it FAILS on a zero-token synthesis, and replace the dead search keys.

## EVIDENCE
- comms pull: GET /api/v1/comms/by-phone/8483336341?days=365 on 192.168.50.77:8002 - 125 events
 (100 dialpad_sms, 19 dialpad_call, 2 transcript, 4 digital_session), read 2026-09-14T16:43Z.
- production Postgres: customers.id=1624 ("mrs whiteman", phone +18483336341); orders.id 1755
 COMPLETED, 2773 and 2774 both READY_FOR_PICKUP with paymentStatus PENDING; 103 communications rows.
- Sourcing reads are all timestamped in the report; customer totals come from
 `scripts/laptop_sourcing.py recommend` (cost x 1.36 x 1.06625): 128.24 -> 185.96, 149.95 -> 217.44,
 159.00 -> 230.57, 199.99 -> 290.01, 399.00 -> 578.59.
- `python tools/case-index/lptcase.py check` - no new structural problem from this case (it appears only
 in the benign duplicate-phone class, which is correct: one customer, three cases).

---
<!-- e:handoff|H98|2026-09-14 17:00 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:00 UTC · ZABZ-TECH · LPT case system rebuilt: lptcase index/search + front-matter standard on all 180 cases (lpt-hub c5efd646); Abramczyk USB/\ recorded

CHANGED
- lpt-hub c5efd646 (184 files): adopted docs/customer-operations/CASE-FILE-STANDARD.md (YAML front-matter
 = current state, append-only dated activity log = history), added tools/case-index/lptcase.py (single-file
 stdlib CLI: index/find/show/open/pickup/money/stale/check/new/doctor/migrate) + README + 59 tests, and
 migrated 177 of 180 case files to front-matter (3 already had one; verified additive: body byte-identical).
- lpt-hub 22514302: Abramczyk SD-card case marked with the owner's 2026-09-14 decision (USB delivery, NEW $15
 charge), the $80 Zelle payment verified from plaid_transactions row 702609 (ABRAHAM ABRAMCZYK, Conf#
 99cuhmhqp, 2026-08-27), the customer's full name and second line recorded, plus the full case-system audit
 (docs/customer-operations/CASE-SYSTEM-AUDIT-2026-09-14.md, 38 KB, measured).
- lpt-hub AGENTS.md §0: case lookups MUST use lptcase, never a tree grep; §5 no longer sends the research
 agent to index/master-index.json for customer questions.
- CUSTOMER_CASE_SYNC_RECORD.schema.json: was self-contradictory (it forbade its own "$schema" key and required
 non-null referral fields) so 0 of 376 records could validate; now 63/375 do (the rest are legacy ad-hoc keys).
- Machine fix: the broken junction C:\Users\ezabz\Code\personal-secretary-mvp\lpt-hub was rebuilt
 (rmdir + mklink /J); repo-wide ripgrep no longer exits 2 with "untrusted mount point (os error 448)" and the
 path no longer returns a clean-empty wrong answer.
- Journal: L(lessons) case-lookup, D(decisions) case-system, P82 (case drift pain), W53 (lptcase win).

IN FLIGHT
- Nothing of mine. The build subagent finished; its 4 backlog items are unfixed (below).

BROKEN (measured, not fixed)
- 226 sync records still do not resolve to a case file; 48 cases have no sync record; 15 phones appear in more
 than one case; 126 cases carry extraction warnings (mostly "no status/price in the prose").
- 376 sync records use at least 6 sourcePath conventions (236 "docs/...", many pointing at the sync-records
 directory itself; 107 other relative; 22 "lpt-hub/..."-prefixed; 6 at another record; 3 missing; 2 prose).
- index/* artifacts (master-index.json) remain 136 days stale and cover zero customer cases.

NEXT
- Answer the one open owner question: is 732-678-6417 the same customer as Abramczyk (then make it an alias
 and close auto-task 13508), or a separate customer named Savitz (then it needs its own case file)? No case
 file exists for that thread although a voicemail (09-03), an owner call (09-07) and an SMS (09-09) do.
- Classify the 226 orphan records (self-referential / prose / genuinely missing) and repair where recoverable.
- Add `lptcase verify <case_id>` to diff a case file against its sync record field by field (the "$15 vs $80"
 class of drift).
- Wire open/pickup/money --uncollected (17 cases, $4,878) into a daily digest.

EVIDENCE
- pytest tools/case-index/tests -q -> 59 passed in 9.13s (run by me, not just the builder).
- index build 0.40 s for 1.39 MB / 23,198 lines; warm queries 4.8-10.3 ms (doctor).
- find abramzyc -> Abramczyk (name-exact via the recorded alias); find 8482857640 and find 732-678-6417 and
 find 732-678-6417-dashed -> the same case; find savitz -> Abramczyk first, all weaker rows labelled WEAK.
- Migration additivity proof: _scratch/verify-migration-additive3.py -> 176 header-only, 2 untouched,
 2 changed by a CONCURRENT session (14in-laptop-weitman, goldie-atlas), 0 missing.

---
<!-- e:handoff|H99|2026-09-14 17:00 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:00 UTC · ZABZ-TECH · Atlas delivery: she is a flip-phone customer with no email, so the preview must be physical - an SD card she can view in her own camera; USB verified CLEAN 31,883/31,883

## 2026-09-14 17:05 UTC · ZABZ-TECH · Atlas: the delivery question is not "which carrier" — she is a flip-phone customer with no email, so the preview has to be physical, and her own camera is the screen

**CHANGED**
- Case file (`docs/customer-operations/cases/data-recovery-goldie-atlas-...md`) — owner corrections recorded as a
 block: the drive is a **Seagate** (not "cgate"); never open a customer message with "Good news"; and **the payment
 model is conditional** — *"she only pays if she is happy with the data, but if she's not happy she doesn't get it."*
 Front-matter rewritten (status `waiting_on_customer_verdict`, price annotated CONDITIONAL, deliverable = the real one).
- Recovery log — the 09-14 section now carries the verification result and the corrections.
- `owner_decision_queue` **#21 resolved** (the USB-cost question is void under the conditional model), replaced by
 **#26**, which asks the question that actually blocks: how to get a preview in front of her.
- New reusable tool + evidence: `lpt-hub/scripts/customer-operations/verify-media-checksums.ps1`.

**VERIFIED — the USB is clean**
`RESULT total=31883 ok=31883 mismatch=0 missing=0 error=0 elapsed=1054s` → `VERDICT: CLEAN`. The manifest had been
written with the copy on 09-10 and **never once checked against the drive** until today. That is now the first
assertion on this case that has actually been tested.

**THE FINDING THAT DECIDES THE DELIVERY METHOD**
- **No email on file** — zero contacts match her phone or her name.
- **She is a flip-phone customer.** Dialpad files her under the contact name **"E4610 Baltimore phone"**; the year's
 thread shows her buying a Kyocera E4610 from us in April (~$205, shipped to Baltimore), asking *"How do I set up
 the Waze?"* in May, and *"My phone is sending out old text msg"* in June. **A web-gallery link or an email
 attachment is not a preview she can use** — I nearly built a web gallery and it would have been wasted work.
- **She owns a Canon SX740 HS** that has been through our shop twice. **A standard SD card is a preview device she
 already owns and knows how to use** — card into camera, photos on the camera's own screen.
- The USB cannot carry the preview either: 29.86 GB free against an 86 GB payload.
- Preview cost is trivial: 27,859 photos are only **22.6 GB** total (median file ~10 KB); 300 files copy to RAM in 3 s.

**IN FLIGHT**
- Owner question **#26**: SD-card preview with a prepaid return envelope, then collect and ship the USB only if she
 is happy (recommended); or ship the full USB and invoice on arrival; or preview on a thumb drive.
- Nothing outbound sent, nothing charged, customer still has not been told the work is done (last outbound
 2026-09-07; her 09-14 11:22 check-in was eaten by the autoresponder).

**NEXT**
- On #26: build the preview (~1 GB, a few hundred photos spanning 2002–2026, the videos that actually play, some
 audio and documents), ship insured with the prepaid return envelope, then the verdict → collect → USB.
- The full draft SMS is written and approved-in-shape by the owner's three corrections; it sends on his word.

**EVIDENCE**
- `RESULT`/`VERDICT` lines: `lpt-hub/scripts/customer-operations/atlas-usb-verify.log`.
- Flip-phone determination: `dialpad_ui_message_body_row` contact_name "E4610 Baltimore phone" + the 2026-04→06 thread
 on `secratary`; no contact row for the phone number in `contacts`.
- Delivery precedent and cost: her own thread, 2026-04-22, *"Standard shipping would be ~$10 (2 day delivery -
 insured up to $100)"* → accepted.

---
<!-- e:handoff|H100|2026-09-14 17:27 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:27 UTC · ZABZ-TECH · 82XM00LMUS screen: ordered and accepted at 180 - the case the shop nearly lost is now in the queue for the bench

## CHANGED
- The 82XM00LMUS case moved from stalled to ORDERED in one afternoon, by the owner doing it himself:
 laptopscreen.com order #1789406689, placed 2026-09-14 10:09:49, PAID PayPal $92.84
 ($76.99 panel + $15.85 freight/tax + free adhesive strips). The correct 40-pin touch panel.
- Owner told the customer "cost will be 180 and it will take 2-3 days" at 1:23pm; customer replied
 "Yes please" the same minute. Price is $180, ACCEPTED. front-matter: status parts,
 price 180, payment_state uncollected.
- lpt-hub: case file + sync record updated with the order, the acceptance and the price; new
 canonical docs/customer-operations/PRICING-RULE.md (owner's (cost+shipping)*1.4+65, with the
 measured freight correction and an explicit table separating it from the 1.30-1.36x resale markup).
- Journal: L249 (price from the invoice, not the rate card - 1.8x error measured), D94 (the pricing
 rule).

## IN FLIGHT
- Panel lands 2-3 days, ~2026-09-16/17. Customer still has NOT dropped the laptop off.
- Still unknown and still un-diagnosed: the software fault. $50-125 band, machine never seen.
- The customer's NAME is still not captured, and the S/N in their label photo is still wrong
 (resolves to a Lenovo V15 G4 ABP - see L235). Both get fixed at intake.

## WATCH
- P77 is NOT resolved. This job got unblocked only because the owner happened to be free and placed
 the order himself; the shop still cannot buy a part without him. owner_decision_queue #25 stands.
- The owner's formula wanted $195 on the real invoice; the customer accepted $180 and that price
 stands - do not re-open it. The rule file is corrected so the next job prices off the invoice.
- lptcase.py display quirk, found 2026-09-14 (not mine to fix - the tool was written today by another
 session and I did not want to edit a file in flight): `show --json` returns `{"status":"parts"}`
 correctly and `open` lists the case correctly, but the human-readable `state` column prints "other"
 while a second line prints "parts", and `--json` returns `price: null` for a front-matter that does
 set `price`. The lookups work; only the display and the json projection are off.

## NEXT
1. On the panel's arrival: check it on the bench before peeling the film (shipping damage is a
 recurring defect on this part).
2. At drop-off: capture the name, re-read the real S/N, count pins on the old cable (40 = touch),
 inspect the digitizer board connector - if the drop hurt it, no new panel restores touch.
3. Scope and quote the software fault inside the $50-125 band before doing it.

## EVIDENCE
- lpt-hub docs/customer-operations/cases/lenovo-ideapad-slim3-15abr8-screen-8484805115-2026-09-10.md
- lpt-hub docs/customer-operations/sync-records/lenovo-ideapad-slim3-15abr8-screen-8484805115-2026-09-10.json
- Owner-supplied order confirmation for laptopscreen.com #1789406689 (PAID, $92.84).

---
<!-- e:handoff|H101|2026-09-14 17:42 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:42 UTC · ZABZ-TECH · LPT case system audited and made measurably faster: 846ms -> 361ms on the edit-then-lookup loop, verified equivalent

## CHANGED (lpt-hub, 4 commits: b46e9f23, b78c8f19, d73d14f3, dbd3dd9d)
- tools/case-index/lptcase.py: the three defects behind every slow lookup fixed and
 measured. find 204->120-142 ms, show 296->143, open 216->98, check 246->107,
 first lookup after editing a file 846->361 ms. All 62 tests pass.
 1. _case_public popped zbody, so the build compressed every body and dropped it and
 load_index re-read + re-compressed all 180 files on EVERY command. Bodies now
 travel in the index; load_index 41 ms + 180 reads -> 14 ms, no reads.
 2. _keys_of re-folded search keys for all 180 cases per query (129 ms of 203) into a
 per-process memo that a fresh process always throws away. Pre-folded at build.
 3. ensure_fresh full-rebuilt on any single change. build_index(prev=) now carries
 over cases whose mtime AND size are unchanged.
- Proven equivalent, not assumed: incremental vs cold build compared on every case
 content field, meta, duplicates, orphans, sync_records and hit order of 6 queries.
 That check caught a REAL regression - front_matter_raw was being dropped from the
 index, so a carried-over case came back None where a cold build had the parsed dict.
 Fixed by persisting it rather than papering over.
- new now scaffolds the sync record alongside the case file, so a case is never born an
 orphan (48 of 180 had none, and `new` guaranteed the number would grow).
- Second-pass linker `sync-filename-bag`: 150 -> 172 linked records, 132 -> 148 cases
 with a linked record. Links only when the sorted token bag names exactly ONE case;
 ambiguity is left unlinked and written onto each candidate's warnings.
- README perf table replaced with measured WALL CLOCK. It had published in-process query
 numbers (5-11 ms) and claimed "30-60 ms" for a command that cost 204 ms.

## AUDIT FINDINGS still open
- The mirror is ~2x the record: 377 sync records for 180 case files. Of the 205 records
 still unlinked, 112 point at a sync record that has NO case file at all (the narrative
 was never written into cases/), 57 point at hardware-diagnostics/ where the target DOES
 exist, 9 at lpt-hub/-prefixed existing paths. The 57+9 are a COVERAGE problem - the
 index only reads docs/customer-operations/cases/ - not a linking problem. Not fixed.
- TWO lpt-hub checkouts: this desktop repo, and a clone at
 /home/zabz/personal-secretary-mvp/lpt-hub ON SECRATARY. The shop intake API resolves the
 sync-records dir on the SERVER and writes there, so a case created through the API gets
 its mirror somewhere nobody on the desktop will look. Unfixed. See P82.
- lpt-hub/tmp is 6.5 GB across 16,165 files, and 1,469 of those files are TRACKED in git
 while tmp/* is also gitignored - the worst of both. Cold tree walks cost ~13 s. The
 tracked ones cannot be removed without an explicit owner yes (real customer records live
 under tmp/customer-case-private/). Not touched.
- Production sync audit: subagent 422a3aa8 still running at the time of writing.
- lptcase.py display quirk: `show --json` returns status correctly and `open` lists the
 case, but the human-readable state column prints "other" while a second line prints the
 real status, and --json returns price: null for a front-matter that does set price.

## NEXT
1. Production-sync audit findings -> fix the projection if it is dead or stale.
2. Extend index COVERAGE to hardware-diagnostics/ + recovery logs + the other
 customer-operations docs, which resolves 66 of the orphans and kills the rg fallback.
3. Reconcile the two lpt-hub clones (one source of truth).
4. Align personal-secretary-mvp shop_ops.customer_intake naming with CASE-FILE-STANDARD,
 then deploy it to secratary.

## EVIDENCE
- lpt-hub commits b46e9f23, b78c8f19, d73d14f3, dbd3dd9d (tools/case-index/*).
- Timings measured 2026-09-14 on ZABZ-TECH, 180 cases, fresh powershell per command.
- 62/62 tests pass after every change; equivalence check re-run after each build change.

---
<!-- e:handoff|H102|2026-09-14 17:46 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:46 UTC · ZABZ-TECH · Weitman: both old jobs closed on the owner's word - HP paid, Lenovo USB unfixable; production corrected; only the laptop sale is open

Supersedes H97's money paragraph. Owner answered queue #27 on 2026-09-14 with: "for the back cover
she paid, for the port, we couldn't source the correct one, and now they want us to help them buy a
new laptop."

WHAT CHANGED
- The customer is a WOMAN. H97 and the first draft of the case file used he/him throughout;
 production's "mrs whiteman" was the clue and it was ignored. Corrected in the case file, the sync
 record and the two older case files, and noted in the record rather than quietly fixed.
- HP back cover (WO26082712635) is COMPLETED and PAID - 65 collected in the shop. Production said
 PENDING.
- Lenovo USB port (WO26082712634) is UNFIXABLE - the correct jack could not be sourced. Nothing owed,
 no credit. Production said READY_FOR_PICKUP with a 60 dollar balance.
- H97's "125 open on the account" was WRONG. Removed from both stores, not left standing.

PRODUCTION, WRITTEN AND READ BACK (2026-09-14)
Through app.services.website_db.update_order, the canonical validated write layer, which emitted an
OrderStatusHistory row per change:
 2774 -> COMPLETED / paymentStatus CAPTURED
 2773 -> UNFIXABLE, total zeroed to 0.00
 customer_notes row 2707 on customer 1624 records the disposition
Note for the next session: production's PaymentStatus enum is CAPTURED|PENDING - "PAID" is rejected.
Nothing outbound was sent.

CASE STATE NOW
- hp-laptop-15-dw1167ur-(2x0s4ea)-weitman-2026-08-25.md -> status closed, price 65, paid
- lenovo-ideapad-slim-1-14ast-05-(81vs)-weitman-2026-08-25.md -> status unfixable, price null, waived
- 14in-laptop-weitman-8483336341-2026-09-14.md -> status waiting, sourced, NOT quoted
The customer has exactly one open case with us: the laptop.

IN FLIGHT
- The laptop sale. Sourcing is done to the shop flow but coverage is incomplete (bestbuy blocked,
 research_dispatcher failed) and NOTHING SUITABLE NEW WAS VERIFIED at any price. The cheapest
 verified option is an eBay-refurbished Dell Latitude 7400 at 149.95 source = 217.44 through us,
 with a 1-yr warranty and 30-day seller-paid returns. Blocked on ONE unknown: her budget.
- Still outstanding before ordering: listing photo analysis on all used rows, and reading the
 Touchscreen item specific on the eBay Dells (touch status is inferred from titles only).
- **She has still not been called.** Two requests, 2026-09-10 and 09-13.

EVIDENCE
- lpt-hub commit 5da2db76. lptcase suite 62 passed; three tests cover front-matter price authority.
- Production reads before and after every write are in the session transcript and in the case file's
 2026-09-14 sections.

---
<!-- e:handoff|H103|2026-09-14 17:56 UTC|ZABZ-TECH|open -->
## 2026-09-14 17:56 UTC · ZABZ-TECH · HA from the office machine: UPS restored, a three-day CRITICAL was a lie, and the camera cause was my own retry storm - plus two corrections to my own conclusions

## 2026-09-14 14:00 ET · ZABZ-TECH · The HA estate from the office machine: the UPS came back, a three-day CRITICAL was a lie, and the camera path's cause was my own retry storm — not the password it looked like

Owner's whole instruction: *"i'm in the office now — let's continue and fixing the home assistant
system and auditing the devices and stuff."* The office is the only place with a route to the HA
host, so the estate became readable for the first time. Two corrections to my own work are in here
and they are the most important part.

CHANGED

- **`ha-config` works from this machine.** Cloned to `~/code/ha-config`; the mesh key the HA host's
 SSH add-on trusts installed at `~/.ssh/ha-mesh-key`. `root@192.168.50.34:2222` is one command from
 here now. (W49)
- **UPS monitoring restored, verified with live values.** `nut` app `error` -> `started`, HA entry
 `setup_retry` -> `loaded`, `LIST UPS` answers, sensors read **charge 100% / 119 V in and out /
 load 15% / Online / runtime 2670 s**. Cause was not the UPS, not USB and not the network: the app
 refuses to start when `users[0].password` is in HaveIBeenPwned, and it was `Hello@1234`. (W48, L233, D86)
- **`ha_truth.py` no longer publishes a false CRITICAL.** `SECURITY_INTEGRATION_DEGRADED — zha` had
 fired every 30 minutes for three days about an integration that is a discovery the owner dismissed
 in 2025 (`source: ignore`), while Zigbee runs on Zigbee2MQTT and was working throughout. Dismissed
 discoveries are now excluded from degradation and reported separately at `low`. (L230, D85)
- **Two findings that were missing.** `SECURITY_SENSORS_UNAVAILABLE` now carries how long the longest
 has been blind (14.5 h today). New `BATTERY_LOW` / `BATTERY_UNREPORTED`, read from the entity
 surface so they work for Zigbee2MQTT, ZHA, Z-Wave and ESPHome alike. (D85)
- **The snapshot retry storm is bounded by a circuit breaker** (W54, commits `f148cf1`, `9d6d956`,
 and the live host). `snapshot_latest_with_retries` now deletes the target file, attempts the
 snapshot, and asks **the disk** whether a file appeared — the Dahua integration reports `loaded`
 and keeps its camera entities `available` while every call fails. **Three** consecutive failures
 open `timer.phoenix_snapshot_breaker` for 30 minutes and the script stops before touching any
 camera; a success resets the counter; opening it writes a logbook entry telling a human to check
 the credentials rather than retry. Three, not five, because the NVR locks an account after five —
 a breaker that trips at five pins the account at the lockout threshold forever. Verified live on
 both paths (failures 1,2 -> open on 3 -> next run stops; two failures then a success -> 0).
- **`docs/AUDIT-2026-09-14-devices-and-integrations.md`** — 12 Zigbee devices with batteries and
 power sources, every degraded integration, the add-on version backlog, and what the audit cannot
 verify.
- **Owner queue #23** (high, the NVR credential) and **#24** (low, the Spotify reauth).

CORRECTIONS TO MYSELF — the two things worth reading first

1. **The lockout theory is dead, and my first test of it was void.** I concluded the NVR was locking
 the admin account out and that the stored password was therefore correct. The first test — 36
 minutes with the six `dahua` entries disabled — came back 401, and I nearly reported that as
 "the password is wrong". It was not a quiet window: `timeline_tools/phoenix-capture.sh` pulls
 RTSP clips **using the credentials from the same config entries**, on three automations that I
 had not disabled. **25 NVR-related log lines in the window I had called empty.** (L248)
2. **A properly quiet window then gave the same answer.** With both paths stopped and the absence
 *measured* (0 NVR log lines from 13:12), 40 minutes later the NVR still returned **401**. So the
 account is not locked: **the NVR genuinely rejects `admin` / the password the owner gave me.**

So the honest statement of the camera fault is: the NVR refuses the credentials, and it refuses them
even when nothing has tried for 40 minutes. What my work did fix is the *second* fault sitting
inside the first — Home Assistant was delivering ~500 failed logins an hour, which is what made the
two explanations indistinguishable for a day. That is now bounded.

IN FLIGHT

- **Two of three Zigbee door contacts are silent** since the 2026-09-13 21:49 ET host reboot:
 `0x286d97000115ca7a` (outside door, last battery 21%) and `0x286d97000115c914` (control room,
 last battery 8%). SmartThings IM6001-MPP01. Not claimed that the batteries are dead — either cause
 gives the same first action, and it is physical. `binary_sensor.phoenix_outside_door_contact` is a
 template sensor over the first of them and has been blind 14.5 h.
- **The RTSP clip path has no breaker** — it still attempts one login per doorbell event.
- **`ha-config` is drifted from the live host** on the three files `deploy.ps1` would overwrite
 (P83). **Do not run the full deploy** until reconciled hunk by hunk, or it will revert the host's
 fixes.
- **`ssh` from `C:\Program Files\OpenSSH\ssh.exe` on this machine hangs after every command** while
 still printing output. Git's `ssh.exe` works. Not yet pinned in the harness (L232).
- **1 895 registry entities, zero areas.** Unstarted.
- The retired Keymaster generation: 82 unavailable automations, 41 entities (P76).

BROKEN

- Camera snapshots (six channels) and timeline clips: one credential, now rate-limited. (P75)
- Spotify: `setup_retry`, refresh token revoked, 650 errors — the loudest line in the log. Queue #24.
- TP-Link "Playroom warning KL135": `192.168.50.225` answers neither ping nor tcp/9999 — unplugged.

NEXT

1. **Ask the owner to look at the NVR's own web UI** (`https://192.168.50.170`, admin) and say
 whether `Hello@1234` is accepted there. If not, reset it. Then load the working credential into
 all six `dahua` entries — the RTSP script reads the same entries, so one fix covers both paths —
 and prove a real JPEG end to end.
2. Give him the battery/re-pair action for the two door contacts.
3. Give the RTSP clip path the same breaker.
4. Reconcile `ha-config` against the host before anything runs `deploy.ps1`.
5. Pin Git's `ssh.exe` in `harness-config` (L232).

EVIDENCE

- Live reads: `.runtime/ha-truth.json` / `.runtime/ha-truth.md` (regenerated 13:56 ET, schema v1) from
 `ZABZ-TECH`; the authority's copy at `secratary:~/ceo-kernel-var/ha/latest.md` ran at
 **16:19:49Z exit=0** with the false CRITICAL gone.
- Breaker: script traces from `trace/get` (not inference), entity reads of
 `counter.phoenix_snapshot_consecutive_failures` and `timer.phoenix_snapshot_breaker`, and the
 logbook entry the breaker itself wrote at 16:43:52Z.
- Config deploy: backups `/config/{configuration.yaml,scripts.yaml}.bak-breaker-20260914-1234*`,
 `/config/scripts.yaml.bak-thr3-*`, `/config/packages/phoenix_snapshot_cameras.yaml.bak-breaker-20260914-123441`.
 `ha core check` clean; `ha core restart` required and performed (shell_command/counter have no
 reload service — L243).
- NVR: 401 from `https://192.168.50.170/cgi-bin/snapshot.cgi?channel=1` with `--digest`/`--basic`/
 `--anyauth` at 12:18, 13:09 (**confounded**) and **13:50:46 (valid: 0 NVR log lines since 13:12)**.
- Host: `lsusb` + `/dev/serial/by-id`, `dmesg` (470 CPS re-enumerations 01:33-02:16 ET, stable
 since), `ha apps`, Zigbee2MQTT `database.db` + `state.json` + log.

---
