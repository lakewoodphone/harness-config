<!-- e:handoff|H296|2026-09-15 21:12 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:12 UTC · ZABZ-TECH · Walk-in hours sign delivered + Wednesday-closed propagated to the site (PR #142); GBP still wrong

CHANGED
- Delivered the walk-in hours door sign the owner asked for:
 `personal-secretary-mvp/docs/marketing/assets/walkin-hours-sign.png` (2550x3300 @300 DPI) and
 `.pdf` (letter, 1 page, 612x792pt). Generator: `scripts/make-walkin-hours-sign.py`, built as a
 sibling of `make-review-sign.py` (same teal band, logo mark, khaki footer, `brand-palette.json`).
- Corrected the new hours (Wednesday closed) across six surfaces in `phone-and-tech-full`
 (`backend/src/lib/business-hours.ts` + 5 frontend strings/tests). Branch
 `fix/walkin-hours-20260916`, commit `ff319106d`, pushed.
- `personal-secretary-mvp` GBP docs updated to the new hours.
- Journal: D173 (decisions), L1603 (worktree/node_modules lesson), P173 (hours drift).

IN FLIGHT
- **PR #142** `fix/walkin-hours-20260916 -> test`: https://github.com/lakewoodphone/phone-and-tech-full/pull/142
 State at close: `mergeable_state: unstable` (checks pending or failing), mergeable. The stored
 `GITHUB_PAT` in `personal-secretary-mvp/.env` can create PRs but returns 403 on the
 `/commits/{sha}/check-runs` API, so the check state could not be read. `gh` itself is logged out --
 its stored token for user `lakewoodphone` is invalid (`gh auth login` fixes that).
- `_worktrees/lpt-walkin-hours` still exists with node_modules installed. Remove with
 `git worktree remove` when PR #142 lands.

BROKEN
- **Google Business Profile still advertises Wednesday as open.** Read live 2026-09-15 21:0x UTC from
 the Maps place page for place_id `ChIJ84QUDnh5wYkRlI4WemcjSKU`: *"Closes soon · 5:30 PM · Opens
 10:30 AM Wed"*. Not fixable from here -- the GBP API path is not enabled and it needs the owner's
 Google login.
- **Production website hours.** `test` auto-deploys to `test.lakewoodphoneandtech.com`; production is
 a manual Netlify deploy and there is no Netlify token on this machine. The live site still says
 "Mon–Thu 10:30 AM–5:30 PM".
- `docs/operations/dialpad-ivr-audio.md` and `docs/operations/dialpad-after-hours-bachrach-plan-2026-09-06.md`
 still state the old hours as current fact (the Dialpad closed-hours script is what callers hear).

NEXT
1. Land PR #142 into `test`, verify on `test.lakewoodphoneandtech.com`, then produce the production
 build for the Netlify deploy.
2. Get the Dialpad closed-hours script and the two docs corrected in the same pass.
3. Owner question raised this turn: Google Business Profile access (it cannot be solved from here).

EVIDENCE
- Backend `business-hours.test.ts`: 37/37 pass (was 35; +2 for the Wednesday-closed case).
- Frontend `ContactSection` + `HomePage`: 42 pass / 3 fail -- **the same 3 failures on pristine
 `origin/test`** (stashed the worktree and re-ran). Pre-existing: two `getByText` multiple-element
 errors and `HomePage > should explicitly mention supported devices and services`.
- Sign verified by reading the rendered PNG and parsing the PDF (`/Type /Page` count 1,
 `/MediaBox [0 0 612.0 792.0]`).

<!-- j2 tags=lpt,hours,sign,website refs= alias_of= legacy_id= sha=e0f8aba863171cea -->
