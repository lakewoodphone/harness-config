<!-- e:handoff|H366|2026-09-16 01:13 UTC|ZABZ-YOGA|open -->
## 2026-09-16 01:13 UTC · ZABZ-YOGA · journal.py was mojibake again and corrupting other sessions entries; repaired with the cp1252 inverse (not latin-1) and the writer now refuses to emit mojibake; check 1095 -> 0 errors

## 2026-09-16 01:10 UTC · ZABZ-YOGA · Round 5 addendum: journal.py was mojibake again and it was corrupting OTHER sessions' entries; repaired (cp1252, not latin-1) and the writer now refuses to emit mojibake

CHANGED
- **`journal/tools/journal.py` was mojibake, live, and DIRTY.** Its UTF-8 bytes had been decoded as
 cp1252, so all 71 U+00B7 MIDDLE DOTS in it were the two characters `·` — **including inside its own
 heading regexes**. Measured consequences, both:
 - `journal.py check` reported **1,095 errors** (the whole corpus fails when the regexes expect `·`
 and every entry contains `·`), and since the cache stores headings too, the corruption reached the
 cache.
 - The WRITER emitted `·` into new headings. It corrupted **H365 (mine)**, and — the part that makes
 this a shared-memory fault rather than my mistake — **L1680, which is a CONCURRENT SESSION's
 lesson**. A broken writer here damages other people's memory, not just its own appends.
- **Repaired with the cp1252 inverse, and the codec choice was diagnostic, not arbitrary.**
 `latin-1` REFUSES this file (`ordinal not in range(256)`) because it contains U+20AC, U+201D, U+201C
 and U+2022 — which are cp1252 mappings, not latin-1. That is positive evidence the bytes were decoded
 as cp1252, and the sequences present are exactly the cp1252 shadow of UTF-8 punctuation:
 `â€"` (E2 80 94) = em dash, `·` (C2 B7) = middle dot, `Â¦` (C2 A6) = broken bar, `â€¢` (E2 80 A2) = bullet.
 `text.encode('cp1252').decode('utf-8')` is lossless for it and clears every sequence.
- **Only 29 of 127 changed line-pairs were pure re-encoding, so `git checkout` would have destroyed
 real work** — verified by undoing the mojibake per-line and comparing. Another session's substantive
 edits are in that file; they were preserved. A byte backup of the broken file was taken first, and the
 repair was only KEPT because the measured error count fell (1,095 -> 2 -> 0).
- **The two corrupted entries repaired, verified by the check's own numbers:** heading `·` -> `·`, then
 the footer sha re-recorded with the tool's OWN `entry_hash` (imported, not reimplemented).
 H365 `e18487cc40ba6775` -> `cc77878ff03ce849`; L1680 `d953861f5a4ec7fb` -> `50fd7b5876c3721f` — and
 both new values match exactly the body hashes `check` had already computed, which is independent
 confirmation that the recomputation is right.
- **Closed at the source: `build_heading()` now REFUSES to return a heading containing U+00C2.** This is
 the guard P200 asked for after the first occurrence; it recurred, damaged another session, and cost
 about ten tool calls to repair, so it is now built. The check is written as `chr(0xC2)` and not as a
 string literal **on purpose**: a literal U+00C2 in that source would itself be mangled by the very
 corruption it is guarding against, making it useless exactly when needed. A computed codepoint is
 plain ASCII in the source and survives.
- **Proven both ways:** a clean heading builds (`'**L9999 · a clean title**'`), all five real kinds
 build, and a heading containing the mojibake sequence raises with a clear message and writes nothing.
 `journal.py` compiles. Final state: **`check` -> 0 errors**.

BROKEN / NOT TRUE
- **The root cause is not fixed, only its symptom at the writer.** Something in this fleet rewrites
 `journal.py` through a cp1252 round trip — twice now, both times while a session was live. The guard
 now makes that version fail loudly on append instead of corrupting the record, but the process that
 produces it is unidentified. **A future session should find the writer, not just the writes.**
- journal.py is DIRTY with a concurrent session's work plus my guard; it is NOT committed. I did not
 commit another session's uncommitted file, and I did not discard it either.

NEXT (unchanged from H365, plus the above)
- Regenerating the canonical identity surface is still the top in-scope item: `person_canonical` /
 `person_phone` were built from axis C's frozen snapshot and today's indexer changes invalidated it.

EVIDENCE
- `repair-journal-mojibake-20260916.py`, `recompute-entry-shas.py`, `_test-mojibake-guard.py`;
 `check` 1,095 -> 2 -> 0; the sha values quoted above; backup `journal.py.mojibake-backup` in /tmp.

<!-- j2 tags= refs= alias_of= legacy_id= sha=84a381a29459d5b6 -->
