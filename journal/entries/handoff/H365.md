<!-- e:handoff|H365|2026-09-16 00:52 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:52 UTC · ZABZ-YOGA · Round 5: indexer identity trio applied - C-007 verified, C-008 applied but unmeasurable, C-009 hit the wrong path for 13 of 14; shared helper took concatenations 14 to 6; canonical surface is now stale

## 2026-09-16 00:58 UTC · ZABZ-YOGA · Round 5: axis C's indexer trio applied — C-007 verified, C-008 in place but UNMEASURABLE, C-009 fixed the wrong path for 13 of 14; the real lever took concatenations 14 -> 6

CHANGED (all on `secratary:/home/zabz/.fsearch/commsindex.py`, backup
`commsindex.py.bak-identity-20260916T0045Z` then `.bak-phonetokens-20260916T0055Z`,
md5 `eb2ae0a2...` -> `5147788c...` -> `5120d48b98933f0b869f2f10a41e0cca`)
- **C-007 applied and VERIFIED — the `Private Name` false merge is gone.** `_NOT_A_PERSON` gained
 "private name", "private caller", "private number", "anonymous", "local", "caller wireless",
 "unknown name", "no name"; `person_like_name()` now rejects two-word privacy strings and business
 suffixes. Measured: `Private Name` rows **1 -> 0**, and `Local` / `Caller Wireless` / `Merck Co Inc`
 / `Inc Nitto` all **0**. The three strangers it had fused no longer share an identity; they now sit
 in the 1,463 bucket that needs a NAMING SOURCE, which is the honest outcome rather than a merge.
- **C-008 applied but its effect is NOT MEASURABLE, and I am not claiming it works.** The change
 (`else: pass` -> `union.find("phone:" + d)`) is a genuine structural fix — a real number should
 exist as its own person — but the aggregate moved the wrong way: `people` **1,483 -> 1,477**,
 `named` 1,475 -> 1,466, `unnamed` 8 -> 11. A direct question settled it: **ZERO rows have exactly
 one phone and no name**, and all 11 unnamed rows are email-only (`basis=address_book:email_auto_discovered`).
 So it creates no person rows on today's data. **My first A/B attempt was flawed** — it removed the
 line, leaving the `else:` block empty, and the control variant failed to compile; the script's own
 md5-verified restore worked, which is the only reason that was safe. Settled instead by asking the
 table a question whose answer differs under the two hypotheses.
- **C-009 could not be applied verbatim: axis C's diff OMITS THE CLOSING PARENTHESIS** of the
 `add_contact(` call and would not have compiled. Closed and applied — and measured, it cleared
 **1 of 14** concatenated phone numbers.
- **The real answer came from grouping the survivors by `basis`.** 11 of the 13 remaining were
 `address_book:manual` — **the owner's own contact book** — read at `commsindex.py:642` as
 `add_contact(v.get("name"), [v.get("phone")], ...)`, so a cell holding
 `+17127758888+01197236194444` became ONE 26-digit person with a real customer's name attached.
 `dialpad_ui_contact_row`, the table C-009 targeted, is not even in `comms.db`. **Added a shared
 `_split_phone_tokens()` used by BOTH call sites** (two copies of one rule is how they drifted).
 Measured: **concatenated entries 14 -> 6.**
- **The last 6 come from a THIRD layer, named not guessed:** four are `basis=name`
 (`'Margulies Chaim (SMS)'`, `'Matatov (SMS)'`, `'safrin smart phone (SMS)'`, `'Alert (SMS)'`), plus
 `'Bison Commerce'` and `'Yelp'`, and they originate in **`comms.address` itself** — which is what
 `phone_names` is built from — not in `add_contact`. That is a different function.
- Recorded as **L1681**. Every change compiled before install, backups kept, and the layer was
 rebuilt and re-measured (`comms-refresh.py` rc=0).

IN FLIGHT / CONSEQUENCE I CAUSED AND MUST NOT FORGET
- **`person_canonical` / `person_phone` / `v_person_canonical` / `v_identity_conflict` in secretary.db
 are now STALE.** I built them last round from `plan/C-identity/identity-truth.json`, which is a
 frozen snapshot of the OLD person layer. Today's indexer changes materially alter that layer
 (junk identities removed, phone concatenations split), so the canonical surface no longer describes
 it. **Regenerating that surface is the top in-scope item left** — it is what makes "one canonical
 reconciled state" true rather than approximately true.
- The last 6 concatenations need the `comms.address` extraction fixed, which is a different path again.
- F-005 (email/stats reads an empty catalog) and F-007 (stop skipping `dialpad_ui_*` by name) remain.
 **F-001 is effectively DONE** — it targeted `app/communications_contract.py`, which B-004 repointed.
- `scripts/ps_mcp_server.py` (B-006) is DIRTY and waits for the other operator.

BROKEN / NOT TRUE
- Axis E's money operations and axis C's 18 merges/splits remain owner-gated (queue 65 asked).
- The `.fsearch` mesh connection dropped once mid-session (`Connection timed out` / `reset`); retrying
 worked, and axis A hit the same thing earlier. Expect it.

EVIDENCE
- `ab-test-C008.sh` (the flawed attempt, and its safety net), `patch-commsindex-identity.py`,
 `patch-commsindex-contact-phones.py`, and the read-back measurements quoted above.
- Report committed to lpt-hub as `df01139c3`.
- `journal.py check` -> 0 errors.

<!-- j2 tags= refs= alias_of= legacy_id= sha=cc77878ff03ce849 -->
