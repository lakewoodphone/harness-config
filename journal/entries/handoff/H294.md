<!-- e:handoff|H294|2026-09-15 21:03 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:03 UTC · ZABZ-TECH · 2026-09-15 night: fleet-api, Caddy gate and the portal all deployed; DRN 45 is visible at last

Three things were deployed and one open question was answered tonight, all on 2026-09-15
between 20:02 and 21:03 UTC.

1. FLEET-API, revision e88c3054 -> c029f049 (in-container hash verified): the silent-failure
 fixes, then the stuck-state monitoring. The zombie is now visible: POST /fleet/monitor/run
 answers complete:false with incomplete_reasons ["1 device(s) stuck in a non-settled state
 for >= 24h"], and alert #171 exists - "DRN 45 (M267V7C6JJ) has been stuck in state
 'deploying' for 366.1 hours (15.3 days, since 2026-08-31 14:58Z)". It was previously
 invisible because /fleet/sweep selects only state='registered' and the monitor had no
 time-in-state clock. Nothing regressed: health 200, /api/fleet/devices 200 (65),
 /api/waze/device 200, /version 200, public :8003 still refused.
 Rollback: /opt/waze-mdm/fleet-api/fleet_api.py.bak-stuck-20260915-210228.

2. OPEN QUESTION CLOSED - the monitor IS scheduled. A subagent flagged that it could not find
 any crontab, systemd unit or script calling POST /fleet/monitor/run and warned the fix
 would be inert if unscheduled. The live crontab line is
 */30 * * * * FLEET_TIMEOUT=180 /opt/waze-mdm/operator-tools/fleet-api-call.sh POST /fleet/monitor/run
 so the alert fires within 30 minutes on its own. No action needed. Lesson for the pattern:
 search the RUNNING system, not the repo - this system keeps its cron on the box, not in git.

3. CADDY gate moved out of the repo: the basic_auth hash is now ${DASHBOARD_PASSWORD_HASH}
 from .env; the tracked Caddyfile has 0 bcrypt strings and 2 placeholders; repo files are
 md5-identical to the live ones, so a redeploy cannot break Caddy. The trap in this one is
 worth remembering: Compose interpolates .env values, so an UNQUOTED bcrypt hash resolved to
 8 characters instead of 60 - the value must be single-quoted (agent lesson L1599).

4. PORTAL: origin/test is now df6d02c43 (fast-forwarded from 0c8786068). It carries the auth
 guards that production already had but the branch had LOST (terminal 0->13 manager,
 a2pAutoReply 0->6, smart-fields 0->4, ui 0->3 protected, systemLog deliberately unguarded),
 the waze contract fix (usage_known, null-safe figures, cap_band unknown), and the
 CUSTOMER-denial pin on ui.navigationContext. Branch guardian/integrate-waze-guards-20260915
 is pushed. A subagent is deploying it to lpt-test-backend now.

STILL OPEN, and it is the owner's: the dashboard password itself must be rotated. The hash is
byte-identical everywhere it should be, but it remains in git history, in origin/master, in the
secratary remote and in older Caddyfile.bak files on the box. Owner row #53 (medium) carries
the recommendation to rotate now; the new password is his to choose.

ALSO QUEUED for him tonight, from the question triage: #50 (the pricing numbers to print on
the customer panel), #51 (customer-initiated Lost Mode - staff-only recommended), #52 (location
retention for the sold fleet - keep with a 30-day window recommended). Queue now 19 pending.

<!-- j2 tags=waze-mdm,deploy,handoff refs= alias_of= legacy_id= sha=a96f87e282445f25 -->
