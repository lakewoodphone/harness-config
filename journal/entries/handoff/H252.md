<!-- e:handoff|H252|2026-09-15 18:51 UTC|ZABZ-TECH|open -->
## 2026-09-15 18:51 UTC · ZABZ-TECH · Mac mini dictation: the missing text was a click on the pill, not a broken pipeline

## 2026-09-15 ~19:00 UTC - ZABZ-TECH - Mac mini dictation: the missing text was a CLICK, not a failure

CHANGED
- **The recording pill is gone** (`overlay_style: "none"`). Its X discards a recording while the Windows
 voice-typing bar's X keeps the text, so the Windows habit "click the box to finish" silently destroyed two
 dictations on day one (log: `Initiating operation cancellation` at 18:46:59 and 18:47:19, with no key event
 near either -- it came from the UI, not the keyboard). Start/stop beep plus the menu-bar icon are now the
 indicators, and the only way to finish is pressing Command+H again.
- Everything else unchanged and re-verified after the restart: `command+h` toggle, `paste_method: CtrlV`,
 `auto_submit: false`, whisper-large-v3-turbo on MTL0, LaunchAgent running.
- Docs updated on the machine, in lpt-hub (ac4a70058) and in harness-config (8c5c638); her Desktop guide
 rewritten around "press the two keys again to finish; there is deliberately no box to click".

VERIFIED END TO END, from the log and history.db (not from configuration)
- start 18:49:01 -> stop 18:49:04 -> 48,000 samples -> `Using paste method: CtrlV` ->
 `Text pasted successfully in 562ms`, sentence present in history.db. The toggle stop path and the paste
 path both work; the earlier "no text appears" reports were cancellations.
- Real human speech transcribed exactly earlier the same evening ("Does this work?", "Hello, what's up?").

BROKEN
- Nothing.

NEXT
1. Ask her to dictate one real sentence with the new gesture (press, talk, press again) and confirm the text
 lands. If it does not, read the log and use the three-way signature: no `handy-keys event` = the key never
 arrived; `TranscribeAction::stop` without a paste = the paste step; `Initiating operation cancellation` =
 something in the UI discarded it.
2. Still open, and now written down as a limitation rather than a task: Handy cannot insert text WHILE you
 speak, so it is not a perfect Windows clone (see P-entry on live insertion). Apple's dictation is the
 live-typing fallback at the cost of shop vocabulary.
3. Shop vocabulary test out loud -- customer name, IMEI, price -- then Custom Words if a word recurs.

EVIDENCE
- ~/Library/Logs/com.pais.handy/handy.log ; `sqlite3 ~/Library/Application Support/com.pais.handy/history.db`
- harness-config/docs/talk-to-text-lakewooechsmini.md ; harness-config/scripts/install-handy-dictation-macos.sh

<!-- j2 tags= refs= alias_of= legacy_id= sha=64bc0a4d1dfb6c65 -->
