<!-- e:handoff|H329|2026-09-15 14:45 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:45 UTC · ZABZ-YOGA · The in-app reminder channel now reaches the owner's badge, and the badge line shows live items instead of stale criticals

**CHANGED**
- **The in-app reminder channel exists** (`cf800076`). One writer, three writers, no reader: a due
 reminder was marked `delivered` and reached nobody. Now the in-app channels enqueue into
 `owner_message_queue` (what the badge renders, what the shelf life ages), as `held` so a reminder
 can never become an outbound SMS on its own; the poll reports `queued` beside `delivered`; and
 reminders age out in 24 h. Verified end to end with a probe reminder: `{checked 1, queued 1,
 sms_sent 0}` -> one held row `reason=reminder:398`; the probe was then cancelled and dismissed
 with a note (one extra `reminders` row, id 398, stays as `dismissed` -- the record of the probe).
- **The badge's waiting line shows what is live** (`f86d671`). Wiring the channel was not enough:
 the reminder reached the queue and the badge still showed two 5.9-day-old criticals, because
 `reason, urgency, created_at` let urgency outrank freshness. Freshness now comes first (same 48 h
 window, same observation clock as the severity rule). Live line: `[Netlify] Action needed:
 ableTelSolutions... | 🌙 Evening briefing...`.
- Both phases still run in the live sweep (`owner_messages_expired': 0`,
 `agent_questions_expired': 0`); health 200; app and kernel commits pushed.

**RESOLVED**
- **P186 done** -- the in-app reminder channel reaches the owner, verified at the reader.
- **P185 done** -- the "nothing asserts the app can narrate itself" half was fixed by the
 `app_voice` kernel check (green: 489 of 2017 journal lines carry the app's own INFO format), and
 the retention half is closed by measurement: ~11 MB/day against a ~47.8 GB default cap and 209 GB
 free. No explicit cap set, and the number is recorded rather than guessed.

**NEXT**
1. Finish the pain triage: ~86 entries remain open out of 91 (only the first 52 were read in round
 37, and each resolution needs its own evidence). The method is unchanged: read the claim, verify
 it against the live system, then resolve with the evidence in `--why`.
2. Observe the sync-alert clear at a real `amazon_sync`/`ebay_sync` success -- still unverified
 since round 35, and the two `sync_needs_login` rows remain `held` until then.

**EVIDENCE**
- Commits `cf800076` (app, on `secratary-master-preserved-20260915`) and `f86d671` (kernel, pushed);
 the probe numbers and the badge line quoted above; `.runtime/deployed-commits.log` updated;
 health 200 after restart.

<!-- j2 tags= refs= alias_of= legacy_id= sha=e5594fb803a2a98b -->
<!-- renumbered H235 -> H329 on 2026-09-15 (ZABZ-YOGA): id H235 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
