<!-- e:handoff|H300|2026-09-15 21:17 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:17 UTC · ZABZ-TECH · 2026-09-15 21:15Z: the waze contract fix is deployed and truthful; the customer-visible half needs the frontend, and the build recipe is finally in git

Deployed 2026-09-15 21:14:17Z by subagent 2e7c26bf to lpt-apps-01. Verified by artefact
identity inside the RUNNING container, not by "the build succeeded":
 grep -rn usage_known /app/backend/dist -> waze-device.service.js:64-67, the new logic
 before: 14959 B, 0 hits, `function toNumber(value, fallback = 0)` at :24
 after: 15822 B, sha256 8691cecd..., and `{{.Mounts}}` is EMPTY so no host file can
 shadow it (this box has already hidden a change behind a bind-mount inode once today).
Behavioural contrast on the real artefacts, same failed-carrier payload:
 OLD {consumedDataMb:0, remainingMb:0, percentUsed:0, capBand:"ok"}
 NEW {usageKnown:false, consumedDataMb:null, ..., capBand:"unknown"}
Image 8f57b87e2a09 -> 758d08cff70e; container b17bb2346198, healthy, 0 restarts. Rollback is
exact (savepoint tag pre-wazefix-20260915-210430 + commit 65740b4e) and is written into
/opt/lpt-test/rollback/. Migrations were deliberately NOT applied; read-only
`prisma migrate status` says 147 found, schema up to date, so there was nothing to apply.
Production lpt-backend is identical to its pre-deploy baseline (same id, image, start time).

THE VISIBLE HALF IS NOT DONE, and that is the point to remember. The API is now truthful,
but the customer UI at test.lakewoodphoneandtech.com is served by Cloudflare Pages from a
PRE-FIX bundle: 0 occurrences of usage_known / cap_band / consumed_data_mb, and it still
ships `formatMb(){ if(!Number.isFinite(value)) return '0 MB' }`. So a failed read now
returns honest nulls and the stale UI still prints "0 MB" over them. Killing that numeral
needs a separate frontend deploy from origin/test. An API fix is not a customer fix.

BUILD RECIPE, FIXED: backend/Dockerfile.production was the node:18 non-workspace version
that cannot build; the recipe that actually builds lived only as an uncommitted file on the
box (`git log --all -S "FIXED 2026-08-31..."` matched nothing, stash empty). A fresh clone
or a `git clean` would have lost it and broken deploys. Committed the byte-exact file
(sha256 0cd1c9fd2d...) and pushed to origin/test as 8c62a17fc. Still open and different: the
PRODUCTION checkout /opt/lpt carries its own local variant (sha256 afd2ce6e...) over a tree
that is not a git repo at all - that one needs the same treatment before anything rebuilds
prod.

NOT A CRASH LOOP, checked because a subagent flagged the container changing mid-window:
lakewood-rentals is `restarts=0`, running since 21:09, logs normal - it was RECREATED (new
container id), not crash-restarting. Something on that host recreates containers; worth
knowing before blaming a deploy. Its log does carry a real but graceful degradation:
"PHOTO_CONTENT_MODEL_API_KEY is unset: uploaded images are stored and QUEUED for review but
are not content-screened" - rentals uploads are not being screened, and that key is not mine
to invent.

<!-- j2 tags=waze-mdm,lpt-apps,deploy,handoff refs= alias_of= legacy_id= sha=1abed0fbedba071a -->
