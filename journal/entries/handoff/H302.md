<!-- e:handoff|H302|2026-09-15 21:28 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:28 UTC · ZABZ-TECH · The deterministic half of the suite is GREEN: 9 failures to 1, and the worst one was 41 tests that had never run because of a duplicate vitest import

CHANGED
- **`9a372fd19`** — fixed the last four deterministic suite failures. **Deterministic baseline under
 `--maxWorkers=1` went from 9 failures to 1**, and the remaining one is **interference, not a defect.**

THE WORST ONE WAS INVISIBLE IN EVERY COUNT
`target-total-regression.test.ts` reported as a **failed suite with 0 tests** — which reads in a summary
count as neither pass nor fail, and is the easiest thing of all to miss. It declared
`describe, it, expect` from vitest **twice**, at line 23 and again at line 473, which is a **PARSE error**,
so the file never loaded. **41 tests were not running and had not been for some unknown period.**
Two halves of a file concatenated in a merge. Removed the narrower first import.

THE OTHER THREE, ONE CAUSE EACH
- **`customer-portal-ops.routes` (2 failures → 1 passing).** Two defects, both producing a **404** rather
 than an assertion about the code: it mounted the router at `/api/internal/customer-portal-ops` while
 `index.ts:363` mounts it at **`/api/internal/customer-portal`**, *and* it requested
 `/customer-backups/config-status`, which exists **nowhere** in the codebase. **The route is gone; the
 test kept asking for it.** Removed the dead block, kept the live `customers/resolve` test and its setup.
 **The mount mismatch is documented and NOT silently changed** — changing it would either fail the test or
 require confirming what `index.ts:363` is actually for.
- **`customerPortal.debug-logging` (1 → 9 passing).** `getMyProfile` was expected to return `createdAt` as
 an **ISO string** and returns a **Date**. This test calls the procedure directly, so the tRPC transport
 layer never serialises — a string expectation could never match a value the service produced.
- **`health-slow-logging` — not fixed, because it is not broken.** It fails in the full suite and passes
 **twice in isolation**: alone (13/13) and with all 138 route tests. Reported as the **interference**
 class. Attributing it to the code would have been wrong.

THE MEASUREMENT AFTER
```
 Test Files 1 failed | 434 passed | 11 skipped (446)
 Tests 1 failed | 6198 passed | 363 skipped (6562)
```
The single remaining failure is the interference case above. **So the deterministic half of the suite is
now green**, which is the precondition for `guard-coverage.test.ts` to function as a control.

> **NOTE ON QUOTING THIS NUMBER.** It is a `--maxWorkers=1` measurement. The default 8-fork run reports
> far more failures because of the concurrency-induced interference (round 17: 38 vs 9). **Report the two
> classes separately** — `N deterministic + M interference` — never one number, or the next reader will
> see it change and lose trust in all of it.

MY OWN FAULTS THIS ROUND, ALL THE SAME SHAPE
- My first removal of the dead `describe` block left its **closing brace orphaned**, turning a working file
 into a parse error. Caught by running the tests, not by reading my diff.
- **Two more PowerShell invocations mangled inline Python** (a `Substring` length error, a quoting break).
 The file-based scripts worked first try, as they have every time. The rule is already in my notes and I
 did not follow it twice more.
- Pattern, fourth round running: **act, then read the artefact — never assume the edit did what its name
 says.** Every fault above was caught by reading output; none by re-reading my own command.

BROKEN
- Nothing in production. Posture intact; workspace clean at `9a372fd19`.

NEXT
1. **The interference class** as its own investigation — now the only thing between the suite and green.
 Run each remaining failing file alone, split the list, then look at shared mutable state (module-level
 Maps at `account-change-guard.ts:19` and `security.ts:20`; `pool: 'forks'` with `isolate: true` is
 already set, so a shared file or env var is more likely than module memory).
2. **The proper rebuild** — still the only thing making the audit's source fixes real.
3. Then `agent/guard-ui2`, the original plan's 78 never-started parts, the 17 REVISION parts, and one
 consolidated pass over the owner decisions (C-117-1, C-119-1, C-123-1, C-126-1, C-129-1, plus the
 queued Stripe question).

<!-- j2 tags= refs= alias_of= legacy_id= sha=f0d945b07162dad8 -->
