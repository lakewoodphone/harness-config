<!-- e:handoff|H289|2026-09-15 20:50 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:50 UTC · ZABZ-TECH · 2026-09-15 end: 12 commits on master, the base convention closed, one path left unverified

Pushed to lakewoodphone/personal-secretary-mvp origin/master, ending at d025e1e5 (12 commits
this session). The last four: the canonical-base change (2d5008be - "FLEET_API_URL" now
means one thing: base stops at /api, every consumer appends /fleet/... or /waze/...), its
docs record (667887f9), and the Hetzner SSH-key fix (d025e1e5).

The SSH-key fix, reproduced directly: Invoke-SqlOnHetzner.ps1, Invoke-DockerOnHetzner.ps1,
Invoke-HetznerPython.ps1, Invoke-HetznerCommand.ps1, run-on-hetzner.ps1 and drn-remote-run.ps1
all defaulted to ~/.ssh/zabz-tech-ed25519 for root@87.99.141.172, which the box rejects
("Permission denied (publickey,password)"), while the ~/.ssh/config alias `hetzner` works with
id_ed25519_hetzner. Every SQL section of Get-WazeMdmFleetStatus.ps1 therefore printed
"SCP failed: ... Connection closed" while its API sections passed - a half-broken status tool.
Defaults changed to id_ed25519_hetzner.

UNVERIFIED AND SPECIFIC: after the key fix, Invoke-SqlOnHetzner.ps1 exits 0 but prints no
result rows, so the SQL path is not confirmed end to end - do not quote it as working.
Get-WazeMdmFleetStatus.ps1 could not be exercised from the integration worktree at all because
the worktree has no .env (FLEET_API_TOKEN not found); its API sections were verified by the
subagent in the main checkout, its SQL sections were not.

Also verified during integration: 159 tests pass on the merged tree
(pytest scripts/waze tests/test_fleet_api_url_convention.py tests/test_mdm_fleet_registry.py).
.mvn: the integration worktree still holds pytest basetemps scripts/waze/_pt3/ and
_pt_integrate/ - untracked, harmless, not deleted (removal needs an explicit per-action yes).

<!-- j2 tags=waze-mdm,git,handoff refs= alias_of= legacy_id= sha=cf01a81d54f73f9d -->
