<!-- e:handoff|H304|2026-09-15 21:28 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:28 UTC · ZABZ-TECH · 2026-09-15: interface audit - 55 findings, five workstreams, and the staff fleet page nobody could reach

Ran a seven-agent read-only audit of both interfaces, from the DEPLOYED refs
(phone-and-tech-full@origin/test 8c62a17fc, personal-secretary-mvp@origin/master 4b0170d4) -
not from any working tree, because the on-disk checkouts are stale and five agents had
worktrees open. Result: 55 findings, 5 critical, 20 high, and NOT ONE needs the owner.
The full structured output is committed to lpt-hub main as
docs/customer-operations/waze/INTERFACE-AUDIT-2026-09-15.md (10fec5f12), with the five
workstreams it opened.

The dominant class is the one this system keeps paying for: a surface that looks healthy while
it is broken or empty. The five criticals are all that shape:
- the staff fleet page's /shadow rewrite adds no Authorization header and can never
 authenticate, and a 401 does not reject fetch, so the page renders "No devices registered"
 with every stat "—" and re-confirms it every 30 s;
- the portal's toNumber(value, fallback = 0) coerces the fleet-api's DELIBERATE null
 (queue_issues/push_issues on a failed read) into 0, so a dead command queue reads as
 "0 queue issues" - the same file already fixed exactly this for usage figures and did not
 generalise it;
- /fleet/devices and /fleet/devices/{drn} never return last_seen or ever_replied, so the staff
 panel renders "Never seen" for every device forever, including phones that checked in
 minutes ago, and the silent-device count is structurally always zero (fleet_devices.last_seen
 is NULL for all 65 by the control plane's own admission at fleet_api.py:97);
- every MDM dashboard command - including ERASE and LOCK - toasts success on an ok:false
 response, because commands.py returns HTTP 200 for a failed enqueue and the UI never reads
 the body;
- the three portal env templates advertise FLEET_API_URL="https://mdm.abletelsolutions.com"
 (no /api), which 404s every call, and 404 becomes [] becomes "0 devices in two fleets".

The customer-facing one to keep in view: CustomerSettings has no error branch, so a failed
profile read renders blank fields as fact and the next Save writes the customer's address as
JsonNull. Silent customer-data destruction on a page that never says anything went wrong.

FIVE FIX WORKSTREAMS ARE RUNNING (disjoint file scopes, one repo each side): portal backend
(contract honesty + env templates), portal frontend (error states, null rendering, the retry
predicate that greps the error message for "status 4NN" and burns rate-limit attempts, the
missing nav entry), fleet-api (real liveness fields), fleet-dashboard (command honesty), and
web/ (proxy auth). Each must prove its fixes with tests that fail before and pass after, and
none may push or deploy; I integrate and deploy.

Also on the record: the audit found the staff Waze Fleet page has NO navigation entry anywhere
(the route exists at MVPRouter.tsx:426-433, nothing links to it), so the surface the fleet
router's own comment calls "the best point of entry" is reachable only by typing the URL. Every
defect above was invisible to the people it was built for.

<!-- j2 tags=portal,fleet-dashboard,audit,handoff refs= alias_of= legacy_id= sha=702b41acd4bbd981 -->
