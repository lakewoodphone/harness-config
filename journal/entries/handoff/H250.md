<!-- e:handoff|H250|2026-09-15 18:49 UTC|ZABZ-TECH|open -->
## 2026-09-15 18:49 UTC · ZABZ-TECH · PART-118 done: customer Google sign-in was disabled in production; enabled, verified, and the flow now reaches Google

CHANGED
- PART-118 (sign-in method inventory and routing) written:
 `phone-and-tech-full/docs/audits/account-audit/parts/118-sign-in-method-inventory-and-routing.md`
- Created a read-only linked worktree at the authoritative revision:
 `git worktree add --detach C:\Users\ezabz\Code\_worktrees\lpt-audit origin/test` (= `0c8786068`), then
 `pnpm install --frozen-lockfile` in it. The primary checkout is 27 commits behind and dirty and was **not**
 used for code reading. This is audit rule 1 turned into infrastructure.
- **FIXED IN PRODUCTION: customer Google sign-in.** It was hard-disabled: `GOOGLE_OAUTH_ENABLED` and the
 OAuth client were absent from `/opt/lpt/.env` while `/opt/lpt-test/.env` had five `GOOGLE_*` OAuth vars.
 Both env files carried the unrelated `GOOGLE_VERTEX_*` group. The portal renders a "Continue with Google"
 button (`CustomerPortalAccess.tsx:524`) which returned **HTTP 503** in production.
 Repair: copied the five keys from `/opt/lpt-test/.env` into `/opt/lpt/.env` with the redirect URI
 rewritten to `https://lakewoodphoneandtech.com/api/auth/google/callback`; backup at
 `/opt/lpt/.env.bak-googleoauth-20260915-184754`; then `docker compose up -d lpt-backend`.
 **Note: `docker restart` would NOT have picked up the env change — `env_file` is read at container
 creation, so a recreate is required.**

VERIFIED (four checks, live)
1. `docker ps` -> `lpt-backend | Up 56 seconds (healthy)`
2. `docker exec lpt-backend printenv` -> all five keys present
3. `GET https://api.lakewoodphoneandtech.com/api/auth/google/status` -> **200 {"enabled":true}**
 (was `{"enabled":false}`)
4. `GET https://api.lakewoodphoneandtech.com/api/auth/google/start` -> **HTTP 302** to
 `accounts.google.com` with `redirect_uri=https://lakewoodphoneandtech.com/api/auth/google/callback`,
 PKCE `S256`, a signed `state` JWT, and the same `client_id` the working test flow uses.

**NOT verified:** the full round-trip. No authorization code was redeemed — that needs a human to choose a
Google account. The first real customer sign-in is the remaining proof; if the Google console lacks the
production callback it will surface as `redirect_uri_mismatch`. The `/callback` branch is still untested
against production.

WHY IT MATTERS
This single config gap is the sufficient explanation of PART-117 F-1 (0 of 10 users has a `google_id`, and
the only CUSTOMER row has no password). The declared **default** sign-in method was off in production and on
in test.

OTHER PART-118 FINDINGS (open, assigned)
- **F-2:** the email-code path cannot reach 98% of customers — a claim code is sent only when **exactly one**
 customer matches, and **541 of 550** customers have no email -> `STAFF_REVIEW`.
- **F-3:** the SMS match is exact-string equality (`customer-account.service.ts:779`) while the email match
 is case-insensitive (`:778`); 42 of 548 stored phones are not E.164 and 5 phone groups are duplicated, so
 formatting decides access.
- **F-5:** `STAFF_REVIEW` is a friendly dead end — nothing notifies staff, and both access requests in
 production are unactioned.
- **F-7:** `auth.ts:948-957` has a nested `/**` comment fence that never closes, plus body-level indentation
 on `googleRefresh`. Harmless to the compiler, misleading to a reader.
- Verified **not** a blocker: `CUSTOMER_ACCOUNT_CREATION_MODE=hybrid` in **both** prod and test, so
 self-claim is on. The code's default is `disabled`, which is why this had to be checked, not assumed.

MY OWN ERROR, RECORDED
I read `auth.requestCustomerLoginCode` (auth router, 1,727 lines), saw its output schema has no `nextStep`,
and concluded the portal's unified entry was reading a field that never exists — a false **Critical**
finding. The portal calls `customerAccount.requestClaimCode` (`customer-account.ts`), whose service **does**
return `nextStep`. Reading one router and generalising was the mistake; following the frontend's actual call
sites fixed it. For auth, **the frontend call sites are the inventory** — three routers participate (`auth`,
`google`, `customerAccount`) and only one of them was in my first measurement.

EVIDENCE
- Tests at the authoritative revision: `npx vitest run --root backend src/__tests__/services/auth
 src/__tests__/trpc/auth-router.test.ts src/__tests__/services/auth.service.test.ts
 src/__tests__/services/customer-account.service.test.ts` -> **7 files, 115 passed, 3.67 s, exit 0**. Same
 115/7 as the stale tree, as expected: `auth.service.ts`, `auth.ts` and `context.ts` are byte-identical
 between revisions. PART-117 F-7 reproduces at `0c8786068`.
- 19 procedures counted on the auth router (13 public + 6 protected); **0** `customerProcedure`.
- Fix script (idempotent, backs up first): `C:\Users\ezabz\Code\_scratch\enable-prod-google-oauth.sh`
- Live probes before the fix: prod `{"enabled":false}` + 503; test `{"enabled":true}`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=9ee5cec948e43aef -->
