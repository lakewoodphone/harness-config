<!-- e:handoff|H355|2026-09-15 23:38 UTC|ZABZ-YOGA|open -->
## 2026-09-15 23:38 UTC · ZABZ-YOGA · Reconciliation closing state: axis G found the watchdog watching the wrong clock, the gap is 111 messages, three owner rows queued, and a concurrent mojibake regression in journal.py was diagnosed and repaired to 0 errors

## 2026-09-15 23:40 UTC · ZABZ-YOGA · Reconciliation closing state (addendum to H352; axis G landed and three owner rows were queued after H352 was written)

CHANGED since H352
- **Axis G landed — the production comms pipeline is diagnosed.** Writer is
 `app/services/website_db.py:1578/:1671`, called ONLY from `daily_reconciliation.py:1582/:1591/:2122`
 when `autopilot._maybe_check_comms_freshness()` declares `dialpad_sms` stale, behind a 24 h alert
 backoff and `reconciliation_interval_hours=24`. **It is not scheduled.** The only Dialpad cron is the
 30-minute harvest, which writes secretary.db only (its own header: "It does not harvest SMS").
 Proven: run 259 finished 11:34:03Z, `website_sync_cursor` = 11:34:25.730Z, production newest row
 11:34:24.649Z, then nothing; both triggers spent (last `anomaly_log` row is still id 514 at 11:05:37Z).
 Stall of run 260 NOT proven (`job_queue` 84665, "Reconciliation timed out after 600s", 11 of the last
 12 such jobs). The `database is locked` storm is **ruled out** — all 198 lifetime lock errors are
 AFTER 11:34 and on two unrelated endpoints.
- **THE REAL DEFECT: the watchdog watches the wrong clock.** `capture_freshness()` measures
 `MAX(dialpad_sms_cache.fetched_at)` — the LOCAL receive clock, which the live webhook keeps fresh —
 so the staleness signal said healthy while the website received nothing. Fix is G-004 (repoint it at
 `website_sync_cursor.last_synced_at`) plus G-001 (give the website a clock).
- **The 4,873 answer: a join-to-customer FILTER, not a cap.** `website_db.py:1628/:1720` both
 `continue` when the phone yields no customer, and the 7-day window in
 `daily_reconciliation.py:1897/:1921` makes the loss permanent. `dialpad_call_link` is the same 4,873
 rows as production's calls (one sweep at 20:35:27Z), so it is a downstream mirror, not a bridge.
 Cumulative: production is missing **7,857 call ids and 20,641 SMS ids**.
- **The gap: 111 items** — 35 SMS + 76 call legs (~38 calls), 10 known customers (rabbi weissman 10,
 mrs whiteman 7, mr shlezinger 5, mrs atlas 5), plus **74 items across 24 threads that match no
 production customer at all**. The webhook path is a genuinely dead second path (6,764 rows, died
 2026-08-31) whose receiver is the production site itself; the 427 sourceless rows are inert.
- **Three owner decisions queued: `owner_decision_queue` ids 65, 66, 67** (all pending) — the deposit
 question (recommend insert-as-PENDING against the real order), the five committed customer-credential
 files (recommend secret store + pre-commit guard, NO history rewrite), and the missing SMS that
 contains a full card number + CVV (recommend redact and backfill the rest).
 Written directly with a 120 s busy timeout because `owner-queue.py add` failed three times on
 `database is locked`; verified from a separate read-only connection.
- **Report committed to the company's own convention dir**: `/home/zabz/repos/lpt-hub`
 `docs/customer-operations/reconciliation/2026-09-15-full-reconciliation.md` (`e668ed5f1`), alongside
 the 2026-08-27 one. Case-to-order links `173a411ee`. Neither pushed to origin.
- **My own tools repaired a live regression:** a concurrent session briefly rewrote
 `journal/tools/journal.py` with a mojibake bug that made every heading regex expect `Â·`, producing
 **1,065 false errors**. It also corrupted the heading of the one entry I appended during that window
 (D194). Repaired in three verified steps — heading, then the footer sha (proved the footer hashes
 heading AND body by reproducing the recorded `sha=13e64b8b34e7781e` exactly from the old heading),
 then CRLF->LF. `journal.py check` is back to **0 errors**. Recorded as **P200**.

BROKEN / STILL OPEN
- Production comms still silent since 11:34:24.649Z; 116,558 rows, unchanged. Axis G's fix not applied.
- Nothing from `plan.json` in axes B, C, D, E, F was applied — those are specifications and no agent
 tested its own SQL.
- `lpt-recon-check.sh` and `comms-refresh.py --check` are written and **not in crontab**. The same
 class of failure (a surface that exists and is scheduled nowhere) is exactly what let 11.5 hours of
 silence go unnoticed.
- The 8 remaining case pushes: 2 need the owner (money), 1 blocked on an lpt-hub phone fix (A-007,
 Weingarten "PENDING" -> 9179026334, without it a THIRD duplicate Weingarten customer is created),
 1 blocked on a missing phone (A-008 Bloch), 2 blocked on missing names (A-004, A-009).
- `lp140wfh-screen-gutterman-2026-09-01.json` is non-conforming (`recordType='sync_record'`) and
 **silently dropped by the reader**: 371 records reachable of 372 files, and one real customer
 (Gutterman, +17326301660, contact 651) with it. The existing `migrate-sync-records.py` migrated 104
 legacy records and missed this one.

NEXT
- The one thing that most improves the business: schedule G-001 + G-004, then backfill the 111 items
 (G-003) minus the card-number message. All three are in `plan/G-prod-comms/plan.json`.
- Ask the owner ONE question: the deposit question (queue 65). Do not stack it with the others.
- Pass `device_info` in `lpt-sync-to-production.py`, or every future pushed order widens the
 device-information gap (all 62 of the 2026-08-27 orders have none).

EVIDENCE
- `~/code/lpt-recon/`; plan/G-prod-comms/{PLAN.md,plan.json,gap.json}.
- `verify-applied.py` output: customers 540 -> 542, orders 584 -> 588, tickets 613 -> 617,
 `ticket_counters.lastValue = 12715` == `MAX(tickets.number)`, communications 116,558 unchanged.
- `journal.py check` -> 0 errors; `show D194` prints a correct heading.

<!-- j2 tags= refs= alias_of= legacy_id= sha=2bd95167f88616ad -->
