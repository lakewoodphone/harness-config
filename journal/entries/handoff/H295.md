<!-- e:handoff|H295|2026-09-15 21:11 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:11 UTC · ZABZ-TECH · Lakewood Rentals final state: all five workstreams merged, gate green, deployed sha-matched; one human sign-in outstanding

CHANGED (final state, supersedes H290)
- **All five fix workstreams are merged into `master`** (`fe9d9372`), each branch still exists for salvage: `agent/rentals-auth-sso`, `agent/rentals-ui`, `agent/rentals-ops`, `agent/rentals-tests`, `agent/rentals-core`, plus `agent/integration`. The six fleet worktrees are removed; note the `psm-w1..w6` worktrees in `_worktrees/` belong to ANOTHER live session and must not be touched.
- **Gate green on master:** `npm run gate` → `GATE PASS — 3 test file(s), tsc 0 errors, next build OK`; integration 19/19, marketplace 114/114, sso 30/30; `apps/rentals/data untouched` (the old suite really did run against the real DB and `DELETE FROM` 13 tables — that is fixed and proved).
- **Deployed and self-proving:** image built from master, live `/api/health` returns `sha fe9d9372…` == master HEAD, image carries the matching OCI revision label, `database {ok, tables:14, schema_version:2}`, `admin_allowlist_configured:true`. The repo's own `apps/rentals/scripts/deploy-rentals.ps1` passed every check: preflight, container env (22 vars), public HTTP, five `/uploads` confinement probes, content-identical drift.
- **Two deploy-script defects were found by running it, not reading it:** the canonical script's sync was `tar | ssh` (PowerShell turns native stdout into text → "gzip: stdin: not in gzip format", nothing synced, while every preflight passed) — fixed to archive→scp→extract; and a duplicated closing brace from that edit. Both committed.
- **Security:** `/uploads` key confinement is enforced in the app (`lib/services/storage_key.ts`) as well as at the Caddy edge; the SSO shared secret was rotated on both sides in one window and verified (authority: 403 no/wrong, `sso:no_token` with the new one); CSRF fail-closed; rate-limit keys trustworthy; `.env` backups on the box mode 600.
- **The production database is now backed up** (`VACUUM INTO` snapshot + table check + non-empty-set assertion) and **has been restored once**, deliberately, with the real image serving from the restored copy. A daily `box-health-check.sh` on the secretary watches backup freshness, the rentals snapshot's presence, disk and the public health endpoint, and files a row in the owner queue on failure (verified by forcing a failure).

IN FLIGHT
- Nothing in code. **One human action outstanding:** the owner has not yet completed a Google sign-in at `https://rentals.lakewoodphoneandtech.com/auth` (prod `users` = 0), which is the only unproven link in the SSO chain.

BROKEN / OPEN (honest)
- **The full SSO loop has never completed with a real person.** Proven: the start leg reaches Google's consent screen with the correct redirect URI; the authority accepts the rotated secret; the callback demonstrably contacts the authority (`no_lpt_session` without the cookie, `lpt_rejected` with a bogus one); unit tests assert the `Cookie: lpt_refresh=` header; the deployed source is content-identical to the fixed source. Unproven: Google's code → `lpt_refresh` cookie → browser → rentals session.
- **R2/photos:** the app's key still reaches the whole `psm-backups` bucket (the escape is closed; the credential is not scoped). Needs a Cloudflare token with R2 edit — owner queue #47. The R2 key pair has NOT been rotated.
- **Storefront Google sign-in** still links to a relative `/api/...` on an apex that 404s every `/api` path — owner queue #46.
- `next@15.2.4` carries CVE-2025-66478; the CI workflow includes a dependency-audit job so it cannot pass silently, but the bump itself is not done (do it when no worktrees are live — a lockfile change invalidates every worktree's `node_modules`).
- `TEST-9` is open and skipped by name: `setRating` accepts `rater_id === target_id` (the route blocks it, the repository does not).
- The live 27-line verification checklist in `deployment-live.md` §5 has never been executed; lines 18–27 need the sign-in.
- The restore drill proved the mechanism on an empty database — re-run it once real listings exist.

NEXT
1. Owner signs in once; then confirm a `users` row with `sso_subject`, an `admin` role (ADMIN_EMAILS is live) and a session, and finish the checklist.
2. Then: the R2 bucket split + key rotation (needs the token), the storefront login fix (one line + a storefront deploy), the Next bump.

EVIDENCE (final, run by me 2026-09-15 21:0xZ)
- `npm run gate` → PASS (3 files, tsc 0, build OK).
- `curl <rentals>/api/health` → `{"ok":true,"sha":"fe9d937223b9bc57c4403e21866970afc59efcf2","database":{"ok":true,"tables":14,"schema_version":2},...}` and `git rev-parse HEAD` → the same sha.
- `docker inspect lakewood-rentals` → `org.opencontainers.image.revision: fe9d9372…`, healthcheck healthy, log rotation 10m×3, on `lakewood-rentals_default lpt_default`.
- deploy script → `ALL CHECKS PASSED`, all five `/uploads/*` probes 404, drift content-identical.
- rotation → authority 403 (no secret), 403 (wrong), `{"code":"sso:no_token"}` (its own secret).

<!-- j2 tags=rentals,fleet,deploy,sso,final refs= alias_of= legacy_id= sha=6ca38fe1de02b42e -->
