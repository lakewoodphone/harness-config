<!-- e:handoff|H334|2026-09-15 15:21 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:21 UTC · ZABZ-YOGA · Pre-existing failures 9 to 2; cheap-first routing off in production and queued as #39

**CHANGED**
- `test_assistant_chat_prompt_profiles.py` 2 failed -> **6 passed**: the hermetic fixture, the missing
 router stub, and the stub's own wrong `source` (commit `2050543f`, preserved on the GitHub branch).
- Pre-existing suite set: **9 failed / 103 passed -> 2 failed / 110 passed**.

**QUEUED FOR THE OWNER (#39, medium)** -- semantic cheap-first routing is off in production because
`.env` sets `USE_DYNAMIC_REGISTRY=false` while `settings_overrides` says true, and the gate reads only
the `.env` value, so the override is inert. Recommendation: turn it on (it only touches simple tasks
and the 63x cheaper models are what the feature exists for), but it is a money/quality trade, so it is
his. Nothing is blocked by it.

**REMAINING (2)**
- `test_vscode_chat_extractor.py::test_discover_workspaces_aggregates_matching_workspace_profiles` --
 not yet diagnosed.
- `test_ai_gateway.py::test_sanitize_tool_call_out_of_workspace` -- left failing deliberately (L1647):
 it asserts that approving a Windows root makes a Windows path readable, and implementing that broke
 its two siblings. It needs to be made platform-aware.

**NEXT**
1. The vscode test (one failure, unknown cause) -- then P139 is down to the one deliberate case.
2. Make the gateway test platform-aware, so the suite can finally be a gate.

**EVIDENCE**
- `pytest <four files> -q` -> 2 failed, 110 passed (19.7 s); `pytest
 tests/test_assistant_chat_prompt_profiles.py -q` -> 6 passed; the instrumented gate line
 `SEM-GATE tier='ceo' owner=False sem=True dyn=False`; `with .env: False` vs `_env_file=None: True`;
 `.env:409 USE_DYNAMIC_REGISTRY=false` vs `settings_overrides ('use_dynamic_registry', 'true')`;
 commit `2050543f`; queue row #39.

<!-- j2 tags= refs= alias_of= legacy_id= sha=b899f0090967c031 -->
<!-- renumbered H240 -> H334 on 2026-09-15 (ZABZ-YOGA): id H240 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
