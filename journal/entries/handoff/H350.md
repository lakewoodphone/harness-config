<!-- e:handoff|H350|2026-09-15 22:50 UTC|ZABZ-YOGA|open -->
## 2026-09-15 22:50 UTC · ZABZ-YOGA · Full reconciliation with the production database: 4 disagreeing stores mapped, rollback points verified, apply gate self-tested, fleet of 7 read-only axes launched

## 2026-09-15 22:55 UTC · ZABZ-YOGA · Full reconciliation with the production database: fleet of 7 launched, rollback points taken

CHANGED
- Owner asked for a "full reconciliation with the production database" using all Dialpad data, all
 lpt-hub repo info, and everything else, methodically, with the sub-agent flow.
- **Reconnaissance established four stores that disagree, measured, not assumed:**
 1. `secratary:/home/zabz/personal-secretary-mvp/data/secretary.db` — 5.5 GB, 203 tables, carrying
 TWO comms lineages that never met: legacy `call_log` 70 / `sms_log` 859 / `contacts` 627, and
 Dialpad `dialpad_call_full` 12,630 / `dialpad_sms_cache` 127,879 / `comms_identity_link` 31,368 /
 `dialpad_call_link` 4,873 / `dialpad_digital_session` 3,020 / `dialpad_transcript_cache` 4,140 /
 `dialpad_media_file` 6,300 / `dialpad_stats_record` 178,893.
 2. production Postgres (`PHONE_TECH_FULL_DATABASE_URL`, Heroku `phone_tech_prod`, 149 tables) —
 customers 540, orders 584, tickets 613, customer_notes 954, **devices 1**, communications 116,558.
 3. `secratary:/home/zabz/repos/lpt-hub` — 372 sync-records, 174 case files, in sync with origin/main.
 4. `secratary:/home/zabz/.fsearch/comms.db` — 167,144 indexed rows, 1,483 people (1,207 named),
 indexed from secretary.db (mtime 22:12Z).
- **NEW FINDING, mine, customer-facing: production `communications` is a THIRD Dialpad pipeline**
 nobody reconciles. `metadata.source='dialpad_sync'` 109,367 rows (2026-07-09 → **2026-09-15
 11:34:24Z**), `dialpad-sms-webhook` 6,764 (dead since 2026-08-31), null-source 427. Types: SMS out
 57,679 / SMS in 54,006 / calls in 3,309 / calls out 1,564. 0 duplicate external_refs (idempotent).
 **It has written nothing for ~11.5 h** while the Dialpad harvest is live (newest sms 0.6 h). Production
 holds 4,873 calls against Dialpad's 12,552, and ~111,685 SMS against 127,678 — and 4,873 is exactly
 `dialpad_call_link`'s row count, so that bridge is the likely cause, not a coincidence.
- **"Full reconciliation" is this company's own term** and it has run exactly ONCE: `lpt-hub/docs/
 customer-operations/reconciliation/2026-08-27-full-reconciliation.md` — 289 sync-records vs 476
 production customers / 508 orders → 186 matched, 28 junk, 32 true gaps, 55 no-customer → production
 went 476 → 527 customers and 508 → 568 orders. **It has never run since**, and sync-records went
 289 → 372. The pusher is `scripts/lpt-sync-to-production.py`, which has a genuine read-only
 `--dry-run-all`.
- **Rollback points taken and verified:** `secretary.db` copied via the SQLite online backup API to
 `secratary:/home/zabz/recon-20260915/secretary-pre-recon-20260915T224525Z.db` (5.5 GB, 107 s);
 production Postgres dumped with `pg_dump -Fc` to `prod-pg-pre-recon-20260915T224857Z.dump`
 (17 MB, 147 TABLE DATA entries, includes communications/customers/orders/tickets/order_items/
 customer_notes — verified by `pg_restore -l`).
- **Built and self-tested the apply gate** `secratary:/home/zabz/recon-20260915/recon-apply.py`
 (source kept at `~/code/lpt-recon/apply/recon-apply.py`). It validates every operation against a
 kind whitelist, captures a row pre-image before any UPDATE/DELETE, demands `--confirm-op <id>` for
 each destructive op, refuses an unbounded UPDATE/DELETE, refuses a plan op with no `affected`
 count or no evidence, and warns when rows-affected contradicts the plan's own prediction.
 **Verified by a synthetic plan deliberately built to be refused:** 4 refused, 1 awaiting per-op
 confirmation, 2 dry-ran and rolled back. Confirmed afterwards that production still reads 116,558
 communications / 540 customers / 0 self-test rows and sqlite `sms_opt_outs` is still 0 — the gate
 wrote nothing.
- **Fleet launched: 7 read-only axes**, each writing only its own directory under
 `~/code/lpt-recon/plan/`, each producing `PLAN.md` + `plan.json`: A lpt-hub↔production ·
 B legacy↔Dialpad inside secretary.db · C identity (contacts/identity_link/people) · D duplicate
 collapse inside the dialpad tables · E orders/tickets/devices/money · F every lying surface ·
 G the production comms pipeline. Shared context and hard rules in `~/code/lpt-recon/CONTEXT.md`.
- Housekeeping: the 0-byte dump left by a failed first quoting attempt was removed (it was an empty
 artifact I created minutes earlier, and a decoy for any future glob); the temp credential file
 `/home/zabz/recon-20260915/.pgurl` was deleted rather than left in plaintext.

IN FLIGHT
- All 7 axes running as of 22:55Z. Nothing has been applied to either database.
- Phase 2 (serial apply, risk order) and phase 3 (re-runnable job + failure surface) are mine and
 not started.

BROKEN / NOT TRUE
- `GET /communications/summary` returns all zeros (reads only frozen `call_log`/`sms_log`).
- The production comms sync is silent for 11.5 h; nothing reports that.
- `devices` has 1 row for 584 orders — the real device record is unresolved.
- P91's reading stands: 372 sync-records vs 174 case files, and the checker's invariant looks wrong
 rather than the data. Do NOT mass-create case files.
- H278's claim "reconciliation has failed every run since 2026-08-23 (33 consecutive 600 s timeouts)"
 is already suspect: `reconciliation_runs` 258/259 SUCCEEDED today (11:05, 11:15) against 87 error /
 173 ok overall. Axis F is re-measuring it per trigger; do not repeat the claim as fact.

NEXT
- Collect the 7 plans, reproduce every headline claim myself, then integrate serially:
 docs/views → inserts → updates → destructive-only-with-per-op-yes.
- Re-dump both stores after any apply, and re-measure each axis to prove it moved.

EVIDENCE
- `ssh secratary-ts`, queries quoted in `~/code/lpt-recon/CONTEXT.md` and in the axis plans.
- `.fsearch/comms-state.json` (written 22:37:24Z) — the indexer's own duplicate counts:
 dialpad_call_full 12,630→10,348 unique; dialpad_sms_cache 127,879→125,135; dialpad_ui_history_row
 10,703→4,907; dialpad_ui_voicemail_row 3,036→1,534.
- self-test: `python3 recon-apply.py --plan _selftest-plan.json --dry-run` → "refused/error: 4
 awaiting your per-op yes: 1".

<!-- j2 tags= refs= alias_of= legacy_id= sha=a3169c5b62f1a177 -->
