<!-- e:handoff|H214|2026-09-15 01:39 UTC|ZABZ-YOGA|open -->
## 2026-09-15 01:39 UTC · ZABZ-YOGA · 2026-09-15 01:45Z ZABZ-YOGA: round 4 -- fetch complete, last transcripts draining, atomic index swap

## 2026-09-15 01:45Z ZABZ-YOGA: round 4 — the fetch is done, the last transcripts are draining, and readers can no longer see a half-built index

### CHANGED

* **`fetchable: 0`** — the ingestion phase is complete. Every call whose audio Dialpad will
 still serve now has it locally; media files on disk ~4,900 and the fetch dry run selects 0.
* **Transcription sharded** (`--shard I/N`, `dialpad-transcript-gap.py`): one process drains
 ~3 calls/min (Whisper + a cleanup pass ≈ 22 s each), so the last few hundred took hours of
 cron ticks. Four processes partition the queue by `rowid % N` — disjoint slices, so nothing
 is transcribed (or paid for) twice — and the rate went to **~12/min**. Measured: 437 → 524
 transcripts in about seven minutes.
* **The three row-writes that discarded completed work now retry** (`dialpad_media_file`,
 `dialpad_media_download_attempt`, `dialpad_call_retranscript`). The third is the expensive
 one: the write happens *after* Whisper and DeepSeek have been paid, so a `database is locked`
 failure threw the transcript away and the next pass bought it again (**L1484**).
* **Readers no longer see a half-built index.** The rebuild emptied `comms` and refilled it over
 25–90 s; anything reading in that window got a partial answer with no sign it was partial —
 measured: the `waiting` view returned **0 customers at 01:38**, inside the `:37` rebuild, and
 17 minutes later. The build now runs beside the live file and `os.replace`s it in (atomic on
 one filesystem), folding its WAL in and dropping to DELETE journaling first so the finished
 file is self-contained. Verified: the live index answered continuously through a build
 (166,903 → 166,905) and never went empty.
* **A dynamic tool edited in place is now deployed.** The app cached the module, so
 `comms_search` returned old fields over HTTP while the CLI returned new ones for the same
 database. `run_tool` reloads when the file is newer than the loaded copy — proven by editing
 the installed file (next call returned the change) and reverting it.
* **The completeness ceiling is on the health surface**: `calls with words: 3,578/10,347 (35%)
 — the rest have no audio anywhere (source limitation, not a backlog)`, with the 6,129
 permanently unrecoverable calls classified and reasoned in **L1481**.

### IN FLIGHT

The sharded transcription is draining the last of the obtainable audio: **524 transcripts and
climbing at ~12/min**, spend **~$4 of the ~$36 cap**. When its dry run reaches 0, every
obtainable word in this archive is in.

### NEXT

1. Confirm the transcript queue is 0, then re-run the clause-by-clause evidence and close the
 objective.
2. Then, in order of value: the "waiting" line on the owner's digest (**D146**), the kernel's
 index-age metric (**P131**, currently blocked by another session editing `ck/sentinel.py`),
 and the softer re-transcripts (~$45, calls that already have Dialpad text).

### EVIDENCE

`comms_search health` and `comms-refresh.py --check` on the authority; `harness-config` @
`e784a68`; `personal-secretary-mvp` @ `7a566b6e3`; journal 0 errors.

<!-- j2 tags=comms,dialpad,handoff refs= alias_of= legacy_id= sha=b33f0fe263fd6b59 -->
