<!-- e:handoff|H282|2026-09-15 20:20 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:20 UTC · ZABZ-TECH · The backend suite is RED on origin/test - 25 failures in 6 files, pre-existing, and they make the new guard test invisible

CHANGED
- **`b354fd42f`** — new note: **the backend suite is RED on `origin/test`**, 25 failures across 6 files, and they
 are **not mine**. Recorded, triaged, not fixed.
- Also this round: **proved the guard test from round 13 is correctly wired into CI** — by breaking it and
 watching a run that did *not* name it catch the break.

THE RED SUITE, MEASURED ON A CLEAN TREE AT THIS BRANCH
```
 Test Files 6 failed | 99 passed | 2 skipped (107)
 Tests 25 failed | 1398 passed | 86 skipped (1509)
```
| Failing file | Result |
|---|---|
| `laptop-preference.validation.test.ts` | 13 tests, **8 failed** |
| `laptop-preference.contract.test.ts` | 16 tests, **7 failed** |
| `customers.backups.test.ts` | 4 tests, **4 failed** |
| `terminal.error-handling.test.ts` | 4 tests, **3 failed** |
| `google.router.test.ts` | 15 tests, **2 failed** |
| `customerPortal.debug-logging.test.ts` | 9 tests, **1 failed** |

**Pre-existing, verified not assumed:** the only file this branch changed under `backend/` tests or routers is
`auth.service.test.ts` (29 insertions). `terminal.ts`, `laptop-preference.ts` and everything else are
**byte-identical to `origin/test`**, and `laptop-preference` is `customerProcedure` on **both** branches.

WHY THIS MATTERS MORE THAN THE FAILURES THEMSELVES
**It defeats the point of the guard test.** That test IS collected correctly — I injected a bare
`zzCollectedProbe` into `faq.ts` and a run that did **not** name the guard test failed it **by name**. The wiring
is right and the control works. But **a test that fails inside an already-red suite is invisible**: CI was
failing before it existed, so its failure changes nothing anyone acts on, and the next reader sees "6 files red"
and files it as the known noise.

That is the sixth instance this session of the same shape — an instrument reporting confidently while
structurally unable to describe the truth: a harness warning about a fixture (PART-122), an empty
acceptance-request queue read as health (PART-117), mention counts read as reachability (PART-127), a census
read from a stale tree (round 6), a check that could not fail (round 7), and now **a red suite that makes every
new test meaningless until it is green.**

TRIAGE — AND A CORRECTION TO MY OWN INFERENCE
I ran the two clearest groups. **Both are stale tests, not product defects:**
- `customers.backups` calls **four procedures that no longer exist** — `TRPCError: No procedure found on path
 "startCustomerBackupMultipartUpload"` and three more. The router was refactored; the test was not.
- `terminal.error-handling` expects `'Stripe Terminal not configured'` and now receives
 `'Authentication required'`, because **the guard runs before the handler's config check** — an assertion that
 could only ever have passed while the router was **unguarded**. That red test is independent evidence of the
 same defect PART-120 found by probing production.
- **I had written, from the failure *shape* alone, that `customers.backups` failing all four "is the shape of a
 real regression".** Reading them showed the opposite. The correction is left visible in the note: the
 inference was wrong and the measurement is what fixed it.
- **Severity therefore revised down:** the 25 are most likely **all stale**, so this is *"the suite has rotted"*
 rather than *"the suite hides real bugs"*. Still a blocker for the guard test; maintenance, not an incident.

NOT FIXED, ON PURPOSE
Reading 25 failures across six routers touches areas outside this audit's scope (customer backups, portal debug
logging), and doing it at the end of a long session is how a fix becomes a second defect. The note carries the
measurement, the triage method and an ordered recommendation so the next session starts from work rather than
re-discovery.

ALSO VERIFIED THIS ROUND
- `guard-coverage.test.ts` is collected by the broad suite run — **proven by injection, not by reading config**
 (the config's only `exclude` block is for *coverage*, not collection, and I did not want to argue from that).
- The repo's `merge-queue.yml` runs the backend suite on **PR events only**; the `test`-branch push flow used
 here bypasses it. So the guard test's CI value depends on the suite being green *and* on work reaching a PR.

BROKEN
- Nothing. Posture intact; workspace clean at `b354fd42f`.

NEXT
1. **The proper rebuild** — still first.
2. **Green the suite** (or quarantine with per-failure reasons). Until then the guard test cannot function.
3. Land `agent/guard-ui2` and delete the `ui` ACKNOWLEDGED DEBT block.
4. Then the original plan: 78 never-started parts (the dialpad/communications backend and core infrastructure are
 the highest-value untouched areas), 17 REVISION parts, and one consolidated pass over owner decisions
 C-117-1, C-119-1, C-123-1, C-126-1, C-129-1 plus the queued Stripe question.

<!-- j2 tags= refs= alias_of= legacy_id= sha=144374e66e7ef022 -->
