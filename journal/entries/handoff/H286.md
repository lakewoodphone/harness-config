<!-- e:handoff|H286|2026-09-15 20:40 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:40 UTC · ZABZ-TECH · 2026-09-15 late: health sweep after the credential work - everything running, one correction, one hardening

Verified 2026-09-15 20:35-20:40 UTC on waze-mdm-01 (host up 17 weeks).

RUNNING: all 9 waze-mdm containers up (fleet-api healthy, postgres healthy 2 months);
localhost:8003/health 200; TLS /api/fleet/devices 200 (65 devices); /api/waze/device
200; dashboard 401; MDM /version 200; public :8003 still connection-refused. The three
rotated credentials all still authenticate: nanodep 200, nanomdm 500 (auth passed),
postgres select 1 ok. Disk 49% of 38 G, mem 813/1919 MB used.

FLEET TRUTH (from the API): 65 devices - 55 deployed, 9 retired, 1 deploying;
17 open alerts (9 critical, 2 warning, 6 info).

CORRECTION I OWE: my first sweep reported the sweep cron as 401ing. That was my
measurement error (see the lesson entry on newline-less logs). The cron was healthy -
it appended a successful sweep payload while I watched. What IS true: fleet-sweep.log
holds 1155 invalid-token payloads and 86 successful ones, with all the successes most
recent, so the failure period ended roughly 86 minutes before 20:36 (no timestamps in
the log, so that dating is inferred from the run rate, not measured).

CHANGED: the four token-inlining cron lines now call one wrapper,
/opt/waze-mdm/operator-tools/fleet-api-call.sh (source of truth in git). Verified:
sweep ok=True errors=0; monitor/run ok=True complete=True alert_errors=0; heartbeat
returns a payload; a missing token exits 2 loudly; crontab still 11 lines with the
other 7 jobs intact; the next run appended zero invalid-token bytes. Committed and
pushed (origin/master 498b13a5).

NOT AN ERROR, checked so nobody chases it: waze_hotspot_sweep.log's 13 "error-ish"
lines are the JSON key `"errors": 0`; the job is clean. /opt/waze-mdm/.env.filekv-backup
is an old .env copy holding pre-rotation values - dead credentials, worth deleting
when someone is tidying. My earlier "still referenced in .env" hit was a false
positive: I was comparing against the later backup, which already held the new keys.

LEFT OVER: pytest basetemp dirs `_pt3/` and `_pt_integrate/` under the integration
worktree's scripts/waze/ (untracked, harmless, not deleted - removal needs an explicit
per-action yes).

<!-- j2 tags=waze-mdm,health,handoff refs= alias_of= legacy_id= sha=abb981f3192ac515 -->
