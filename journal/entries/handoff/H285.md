<!-- e:handoff|H285|2026-09-15 20:38 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:38 UTC · ZABZ-TECH · Company audit round 14: 4 branches live in production and verified; W1 applied then REVERTED for causing a restart loop; owner direction question still gates the rest

CHANGED (all verified on the live system, not on report)
- **SIX-workstream audit of the AI company, all reported.** Docs in
 `personal-secretary-mvp/docs/audits/company-next-level/` (01 source-of-truth, 02 throughput, 03 economics,
 04 reliability, 05 owner-loop, 06 evolution-loop, 07 security, 08 always-on-layer, plus `00-manager-findings`
 and `99-integration-register`).
- **FOUR branches integrated into the live production tree and verified by assertion:**
 - **W4 `79fe326d`** — phantom work eliminated. ~25 tasks/day about `boa_sync`/`spending_report`
 (subsystems that do not exist, with frozen failure counters that could never clear) → **0**. Verified in
 the data: the hourly histogram stops at 20:00 and 0 tasks exist after the fix. Recorded **W140**.
 - **W3 `ec2107ec`** — cost/prompt. `prompt_prefix_hash` was NULL on 5,951 of 8,050 calls; now produced.
 23 tests on live + 74 neighbours. The ~$39/mo caching saving is NOT booked: it depends on the provider
 honouring cache markers, which W3 documented as a test rather than claiming.
 - **W6 `0331ac70`** — the objective's named deliverable, the self-improvement ignition. Auto-apply stays
 **False**; the applier can now run in an isolated root and the live checkout is hard-refused in three
 places. Its answer to "why 0 fixed 1,439 times": structural — `auto_fixed` is only returned inside
 `if settings.code_edit_auto_apply:`, so the counter cannot move whatever the loop does; and nothing
 autonomous read `data/code_proposals.json`. Also found that `_normalize_rel_path` used `lstrip("./")`
 (a character-set strip), so the `.env` and `.git/` denials **could never fire**.
 - **W5 `55caf1d6`** — customer stall watch. On its first live pass it flagged task **3132 at 106 days**
 (a repair verified stranded by hand three rounds earlier) plus rows at 123 and 118 days, and **0** of the
 40 internal rows. 36 tests; `nothing_is_sent` passes.
- **W1 `b2a41e87` was APPLIED AND THEN REVERTED — do not re-land it without fixing this.** Its new
 **write probe inside `/health`** caused a restart loop: journal showed
 `Health DB write probe failed (transient): OperationalError: database is locked` immediately before each
 restart, systemd `restart counter is at 6`, and **16 API starts in 30 minutes**. The watchdog was NOT the
 cause (0 watchdog restarts; its own log shows my guards correctly declining to act). Reverted from
 `*.bak-w1-20260915-202934`; after the revert, a 90-second observation showed **0 starts, 0 process exits,
 0 probe warnings, 0 locks**. The security guard survived the revert (1 occurrence). **W1's tests also die
 under production config** — the process exits after 4 of 30 cases (`exit=15`), while the same file passes
 `30 passed in 25.86s` in a worktree with NO `.env`. Per-test bisect: the three unit-level probe tests pass;
 every test that calls the real `/health` endpoint times out at 60 s. **Root cause of W1's failure is
 unfound; treat the write probe as unsafe on a 4.48 GB store under contention.**
- **Closed an unauthenticated public surface** (`W138`): `api.abletelsolutions.com` mapped to raw FastAPI and
 the guard exempted everything under `/api/v1/` before any key check. With no credential: `/api/v1/receipts`
 200/**92,986 bytes**, Plaid accounts 200, fleet devices 200, and `POST /api/v1/dialpad/sms/send` reachable —
 outbound SMS as Lakewood. Now 401; loopback untouched. My first attempt at this fix **silently did nothing**
 (its idempotence check matched route decorators); the predicate self-test caught it.
- Journal search fixed (`P163`) and **integrated to `harness-config` master** (fast-forward of a
 non-checked-out branch, proven by blob equality). Found in passing: `--tag` can never match and `backlinks`
 cannot see prose citations (`P168`) — both reproduced by me.
- Crons: removed a backup cron that had **never run** (target dir did not exist); collapsed a
 `PRAGMA integrity_check` that ran **twice** every 15 min into one (**49.3 s → 19.3 s**; the missing `mail`
 binary was why it doubled). Recorded **P167**: the corruption alarm has **no transport** — nothing reads
 the log it writes.
- Watchdog restart storm fixed (`W136`): 339 API starts/7d, median gap 70 s, caused by `systemctl is-active`
 treating `activating` as dead. After: **2 restarts since**, against ~53/hour before.
- Owner queue: resolved 4 rows answered-but-never-closed; verified 5 of the pending rows are genuinely the
 owner's (2 workstream "dismiss this" claims did not survive checking); added **#48** (an Active
 AdministratorAccess IAM key + the Outworq client's committed Twilio token).

IN FLIGHT
- **W2 `agent/w2-conns`** — never committed. Owns the real cause of `database is locked`. Note: lock errors
 fell ~4× (122/hour → 17/30min) **without W2 landing**, most likely because W4 stopped a large volume of
 pointless writes. That attribution is NOT verified — do not claim production lock fixes as verified until
 W2 lands and it is measured.
- Five branches are in the register with reproduction evidence; W2 is the only one still building.

BROKEN / OPEN
- **The register `99-integration-register.md` is STALE on W1** — it still says W1 was applied. W1 was
 reverted. Correct it before trusting that section.
- **Every fleet worktree lacks `.env`** (gitignored, never copied in). So *all* green test output from this
 fleet was measured under **defaults, not production config**. W1 proved this can matter: 30/30 pass in a
 worktree, process death on live. Re-examine other branches' test signal with that in mind.
- Four security items: **~130 guard-exempt `/api/v1/` routes remain reachable uncredentialed** (the real fix is
 inverting to deny-by-default with an allowlist — deferred deliberately, because inverting blind could take
 down the dashboard proxy, the VS Code extension, the badge, MCP and every cron); webhook verification is
 **fail-open** (`if secret:`) with `GITHUB_WEBHOOK_SECRET`/`EVENT_WEBHOOK_SECRET`/`HA_PTT_KEY`/
 `SECURITY_API_KEY` all unset; billing charges have **no idempotency key**; `/home/zabz/secretary-backups` is
 mode **0644** holding plaintext `.env`, Gmail/Calendar tokens and browser sessions with no offsite copy.
- Phantom task rows **25299, 25420, 25421** still open.
- Two cross-cutting defects: `status='in-progress'` (hyphen) holds exactly 3 rows in 25,405 — the 3 forgotten
 repairs — and is invisible to **three separate** staleness mechanisms; `staleness_detector._flag_stale_tasks`
 writes `updated_at = now` while marking work stale, destroying the clock it measures.

NEXT
- Correct the register's W1 section. Then diagnose W1's test deaths (bisect found the `/health`-calling tests),
 fix the write probe, and re-land W1 deliberately.
- Land W2 as soon as it commits, using the guard rule: it must not revert any hotfix. Assert
 `grep -c 'if not _dashboard_path_requires_api_key(path):' app/main.py` == 1 and public `/api/v1/receipts`
 == 401 **after** the merge.
- **The owner question is still unanswered** and gates the whole direction half: with 62.7% of work about
 itself and 9 verified external effects in 30 days, what is the company FOR? Row #40 untouched.

EVIDENCE
- Phantom stop: `select count(*) from tasks where (title like '%boa_sync%' or title like '%spending_report%')
 and created_at > '2026-09-15T20:19:00'` → **0**; hourly histogram stops at 20:00.
- Restart storm: `journalctl -u secretary-api -o short-unix | grep "Started secretary-api.service"` → 339/7d,
 median gap 70 s before; `grep -c restarting .runtime/access-watchdog.log` → 292 → 296 after ~50 min.
- Public surface: `curl https://api.abletelsolutions.com/api/v1/receipts` → 200 (92,986 B) before, **401**
 after; loopback still 200.
- W1 regression and revert: `Health DB write probe failed … database is locked` → `Scheduled restart job,
 restart counter is at 6` → 16 starts/30 min; after revert **0 starts in 90 s**.
- W5 live: task 3132 flagged at 106 days; 22 stall markers present.

<!-- j2 tags=company,audit,integration,revert,handoff refs=W136,W138,W140,P163,P167,P168 alias_of= legacy_id= sha=ef71ecd84ff4c129 -->
