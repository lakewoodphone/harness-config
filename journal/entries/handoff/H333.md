<!-- e:handoff|H333|2026-09-15 15:10 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:10 UTC · ZABZ-YOGA · Config contracts green, the gateway guard fails closed, pre-existing failures 9 to 4

**CHANGED (both commits preserved on `secratary-master-preserved-20260915`)**
- `01ab2dc1` -- config contract counts the env names the app accepts (`validation_alias` replaces the
 field name), `.env.example` documents 21 previously-undocumented settings, `web/.env.local.example`
 created and un-ignored. `missing_keys` 24 -> 0, `extra_keys` 3 -> 0, `test_config_contract.py` 5
 passed. Resolves P188.
- the ai_gateway guard now **fails closed** on a path that is absolute on another platform. The trio
 went 3 failures -> 1; the pre-existing set is **9 -> 4** (108 passed).

**DELIBERATELY LEFT FAILING (1 of the 4)**
- `test_ai_gateway.py::test_sanitize_tool_call_out_of_workspace` asserts that approving a Windows root
 makes a Windows path readable. Implementing it fixes that test and breaks the two sibling tests,
 which approve a Windows root and expect blocking. On POSIX the guard blocks, which is the safe
 direction. The test should be made platform-aware; until then this failure is a **known,
 understood, documented** disagreement, not an unexplained red.

**REMAINING UNKNOWN (3 of the 4)**
- `test_vscode_chat_extractor.py::test_discover_workspaces_aggregates_matching_workspace_profiles` (1)
 and `test_assistant_chat_prompt_profiles.py` routing-telemetry pair (2). Not yet diagnosed.

**NEXT**
1. The prompt-profile telemetry pair -- one path, two assertions.
2. Then `test_vscode_chat_extractor`, then make the gateway test platform-aware.

**EVIDENCE**
- `pytest <four files> -q` -> 4 failed, 108 passed (12.9 s); the four hand-run guard probes above;
 `missing_keys: 0 []`, `extra_keys: []`; commits `01ab2dc1` + the ai_gateway commit; health
 unaffected (the service was not restarted -- `ai_gateway.py` is imported per request path, and the
 running process keeps its loaded copy until the next restart, which is safe because the change only
 tightens a guard that currently never fires).

<!-- j2 tags= refs= alias_of= legacy_id= sha=d720e264d8081365 -->
<!-- renumbered H239 -> H333 on 2026-09-15 (ZABZ-YOGA): id H239 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
