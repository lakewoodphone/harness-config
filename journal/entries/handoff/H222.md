<!-- e:handoff|H222|2026-09-15 13:48 UTC|ZABZ-YOGA|open -->
## 2026-09-15 13:48 UTC · ZABZ-YOGA · CORRECTION to H215: the lineage fork is worse than I recorded (identity-level baseline 477/742/762)

CORRECTION TO H215 — its numbers do not survive measurement. Do not act on H215 as written.

H215 said secratary's harness-config fork was "both lineages hold the SAME 740 entries" with "183 metadata-header-only conflicts, bodies identical". MEASURED (secratary, 2026-09-15 01:58-02:10Z, HEAD 5f59cee, merge-base bda19d2, backup /home/zabz/harness-config.bak-20260915T0200Z):
- The 740/742 figure is ORIGIN's own count (D108 H123 L324 P101 W86). LOCAL is not 740: it is **1316 files / 477 distinct logical identities / 839 duplicate files (64% of the tree)**.
- The conflicts are NOT header-only. Of 516 shared paths, **507 differ in bytes**; stripping every meta line, only **78 are metadata-only** and **429 carry genuinely different logical entries under the same id** (origin D21 = the modesty model, local D21 = "One DSH engine, many windows"). **382 ids collide** across the lineages.
- So the sound baseline is IDENTITY-level, not file-level: **477 local / 742 origin / 762 union** (457 shared + 20 only-local + 285 only-origin). Only 20 logical entries are unique to secratary: H115, H204, H205, H207, H208, D142, D144, P132, P133, P135, P136, L320, L510, L700, L890, L1080, L1270, L1460, L1461, W40.
- WHY FILE COUNTS ARE UNSOUND HERE: the local tree is 64% duplicates, so a LOSSY convergence still RAISES the file count and certifies itself clean. That is exactly how the two earlier migrations dropped 37 then 7 entries undetected. Any future verification must count identities and must prove each of the 20 local-only identities survived.

WHY THE PRESCRIBED SEQUENCE CANNOT CONVERGE IT (measured, not asserted):
- `repair-ids` is structurally a no-op across lineages: it reads ONE tree, and one tree cannot hold two files at one path. On a 1542-file union it printed "no id collides with different content - nothing to do". The collision IS the file set.
- The union step is not expressible as a file copy: 507 paths hold different content in the two lineages, so one variant must be re-identified before it can be placed.
- Measured loss per route against the union baseline of 762: origin wins shared paths -> **13 identities lost**; local wins shared paths -> **132 lost**. A plain fast-forward loses the 800 local-only paths.
- `migrate-v2` is an unregistered dead function (`cmd_migrate_v2`); `absorb` is `import-legacy`; `migrate.py` and `reconcile-questions.py` are one-off scripts, byte-identical on both sides, already run 2026-09-14.

WHAT WAS ACTUALLY DONE, and it is real progress: the TOOL was reconciled on branch `journal-migration-20260915` at commit **750f193** (origin's journal.py as base, plus local's ONE genuinely unique load-bearing fix - `cmd_resolve` flattening `--why`, without which a newline writes continuation lines and corrupts state/status.tsv). `absorb_key` was deliberately NOT ported: origin's `identity_of(kind, heading, body)` supersedes it, and the evidence is decisive - origin 742 files/742 identities/0 duplicates versus local WITH absorb_key 1316/477/839. A REAL CRASH was also found and fixed in the canonical tool: `absorb_all` passed an alias row dict where a canonical id string was required (journal.py:2642), which fails on a PRISTINE origin/master tree too; after the fix `import-legacy --apply` completes rc=0 and selftest passes all 240 checks. Master never moved; nothing was hand-merged; entries/, log/ and archive/ differ from the pre-change backup by ZERO files; phone probe 15/15.

THE MOST DANGEROUS FACT, and it must be fixed before anyone writes on that checkout again: NO entry can be safely appended there by EITHER the local tool or the reconciled upstream tool. Both allocate **P137** while origin/master already holds pain to **P140** (lessons L1488, decisions D148, handoff H217, wins W117) - neither allocator consults origin/*. A guard refused the append and P137 was never created. Any session that writes to this journal on secratary silently worsens the fork. The allocator must consult origin/* before anything writes there.

NEXT, and it is genuinely semantic work for a dedicated session: for each of the 382 colliding ids decide which variant keeps the number, write the other as a suffixed id, fix refs= and in-body citations, then verify at IDENTITY level (>= 762 identities, with all 20 local-only identities surviving). Reference doc committed at journal/reference/lineage-fork-2026-09-15-measured.md; raw evidence /home/zabz/_mig_out/*.txt.

<!-- j2 tags= refs= alias_of= legacy_id= sha=b2d442336294b3f0 -->
