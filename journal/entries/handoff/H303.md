<!-- e:handoff|H303|2026-09-15 21:28 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:28 UTC · ZABZ-TECH · Lakewood Rentals round 4: TEST-9 closed, 0 dependency vulnerabilities, two deploy-only defects fixed; still one human sign-in short

CHANGED since H295 (2026-09-15, later the same session)
- **TEST-9 is closed** (it was the last skipped test and a real hole): self-rating was refused by the route but allowed by `setRating` and by the table, and the earlier suite asserted the hole as correct. Guarded now in `setRating` (same wording as the route's 400) and in the schema by **migration 3**, which rebuilds `ratings` because SQLite cannot add a `CHECK` via `ALTER TABLE`. Verified in production: `user_version = 3`, `CHECK (rater_id <> target_id)` present, temp table cleaned up. Suite: **164 tests, 164 pass, 0 skipped**.
- **Dependencies hardened to 0 vulnerabilities**: `next` 15.2.4 → **15.5.25** (cleared the CRITICAL advisory and the postcss one it dragged in), `sharp` 0.33.4 → **0.35.4** (cleared the libvips/libheif advisories, which matter because this app processes untrusted uploads), and a scoped `overrides` entry moved postcss 8.4.31 → 8.5.28 instead of the breaking major npm proposed. `npm audit` → **0 vulnerabilities**.
- **Two defects the bump surfaced, both found only by deploying:** Next 15.5 types a catch-all route param as `string[]` in its generated validator, so `app/uploads/[...file]` failed the *build* while `tsc` passed; and the image build died because the builder stage reused `--omit=dev` node_modules with no `typescript`, which Next 15.5 hard-requires to load `next.config.ts` (15.2 tolerated it). Both fixed.
- **A silent-success defect in the deploy script:** it ran `docker compose up -d --build 2>&1 | tail -25`, and a pipe returns tail's status — so the failed image build above was reported as success while the OLD container kept serving and the live build stamp never moved. Now captures `BUILD_EXIT` and fails on any non-zero value. This is the second time a pipe-masked exit code has hidden a real failure in this workstream (the first was `tar | ssh`), and both are fixed with a check the far side reports.
- **Live now**: `/api/health` → `sha 103ff958…` (== the deployed revision), `database {ok, tables 14, schema_version 3}`, `admin_allowlist_configured true`. Deploy reported ALL CHECKS PASSED including the five `/uploads` probes and content-identical drift. Master is one docs-only commit ahead of the deployed build.

IN FLIGHT
- Nothing. **Still the one human action:** no Google sign-in yet (prod `users` = 0), so the SSO chain's last link remains unproven.

BROKEN / OPEN (unchanged from H295 unless noted)
- Full SSO loop unproven by a person; everything else verified.
- R2 credential still reaches the whole backup bucket (owner queue #47; needs a Cloudflare R2 token). The R2 key pair is NOT rotated; the SSO secret IS.
- Storefront Google sign-in still links to a relative `/api/...` on the apex (owner queue #46).
- The live 27-line verification checklist in `deployment-live.md` §5 has not been executed (its last lines need the sign-in).
- Restore drill proved the mechanism on an empty database; re-run it once real listings exist.

NEXT
1. Owner signs in once; confirm the `users` row, `sso_subject`, admin role and session.
2. Then: R2 bucket split + key rotation; the storefront login one-liner; run the live checklist end to end.

<!-- j2 tags=rentals,deploy,deps,sso refs= alias_of= legacy_id= sha=3407e0abc0cc81d3 -->
