<!-- e:handoff|H235|2026-09-15 16:27 UTC|ZABZ-TECH|open -->
## 2026-09-15 16:27 UTC · ZABZ-TECH · Newman (347) 768-0413 flash-drive intake: case filed, bench armed, drives not yet connected

## 2026-09-15 ~16:30 UTC · ZABZ-TECH · Newman flash-drive intake: case filed, bench armed, drives not yet connected

CHANGED
- Case created for **Newman, (347) 768-0413** — two USB flash drives in for data retrieval,
 customer says they "have a virus"; owner quoted **$75/drive (2 = $150)**, no charge if
 unrecoverable. Case file `lpt-hub/docs/customer-operations/cases/flash-drives-newman-3477680413-2026-09-15.md`
 (front-matter `status: new`, `price: 150`, `payment_state: quoted`) + sync record of the same
 name; both schema-validated and committed locally as **1e532c120**. `recovery-index.md` gained
 a case row, a new **$75/drive USB-flash-retrieval** pricing row and a maintenance-log row.
- Engine case created and claimed on the bench: `/srv/recovery/cases/20260915-122509-newman-3477680413`,
 agent token `c50388db48b70ccae84250d49b0cf52a`; staging dirs at
 `/mnt/atlas-recovery/newman-3477680413/{images,recovered,deliverable}`; intake note written to
 the case `notes/`.
- **Bench gained an antivirus verdict path: `clamav` 1.5.3 (main 63 / daily 28124, 3.6 M sigs)
 and `exfatprogs` installed on linux-pc.** Until today the bench could not answer "is there
 actually a virus", only work around it.
- Bench state verified before any drive touches it: no live `ddrescue`/`photorec`; GNOME
 automount **off** (no auto read-write mount of a customer drive); `/mnt/atlas-recovery` 4.4 TB
 free; `/` at 96 % (22 GB) so images must never target `/`; sleep inhibited 4 h; `udevadm
 monitor` logging into the case dir; `ddrescue`/`photorec`/`losetup`/`smartctl`/`rsync` present.

IN FLIGHT
- Waiting on the owner to plug the two drives into the Linux PC. Nothing else blocks. When they
 appear the sequence is: identify read-only → ddrescue image to the bench SSD → inspect the
 image → retrieve recordings + all user data → ClamAV → clean deliverable → tell the customer
 what was actually wrong.

BROKEN / OWED
- **This ZABZ-TECH checkout of lpt-hub is `ahead 29, behind 2442` of origin/main.** The commit
 above is local-only; other machines will not see this case until that divergence is dealt with.
 Do **not** "fix" it with a reset or force-push — it needs a real reconciliation pass (L1470 is
 the same disease: parallel lpt-hub checkouts, one case, two records).
- Stale case lock on the bench: `czermak-7183147956` owned by token `a6dc0bc…` since 2026-09-03,
 with three idle tmux sessions (`carve_v4`, `czermak_carve_full`, `czermak_localtest`) and no
 running process. Owner directive is never to touch another agent's case — left alone, but it
 shadows `status` until someone rules it abandoned.
- `/mnt/shlesinger-live` throws `Input/output error` on `df`. Pre-existing, untouched.

NEXT
1. Owner connects both drives → claim each device (`lpt-recovery-manager claim-device /dev/sdX
 <case> --agent <token>`) and image both read-only to the bench SSD.
2. Establish the "virus" claim on the image: `autorun.inf`, `.lnk`/`.exe`/`.vbs`, hidden+system
 attributes, dirty bit / fs damage — and check whether the recordings are even there.
3. Decide the deliverable medium once the size is known (fresh shop USB = the owner's $15 line,
 precedent Abramczyk 2026-09-14; or their own drive wiped clean) **before** pickup.

EVIDENCE
- Call (authority `dialpad_call_full`): 2026-09-15 15:23:53 UTC, 185 s, external +13477680413 —
 "I have two flash drive that have a virus on them… it's a data retrieval… put it on to a clean
 flash [drive]"; owner: "each one is $75 now, but it's $75 for data retrieval… if we're not able
 to do it, we don't charge". Second call 15:27 UTC, 47 s (arrival/directions). Zero SMS rows.
- `lptcase.py show newman` resolves the new case; `lptcase.py check` raises nothing for it; the
 sync record passes `jsonschema.validate` against `CUSTOMER_CASE_SYNC_RECORD.schema.json`.

<!-- j2 tags= refs=newman,linux-pc,lpt-hub alias_of= legacy_id= sha=2ff12aab2a7ef5fc -->
