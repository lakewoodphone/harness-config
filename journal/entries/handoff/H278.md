<!-- e:handoff|H278|2026-09-15 19:57 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:57 UTC · ZABZ-TECH · Company audit: 8 workstreams, 2 production fixes landed (restart storm + dropped money alerts), and a concurrent operator on the live tree

CHANGED
- **Audit of the AI company launched and largely complete.** 8 read-only workstreams, one report each, in
 `personal-secretary-mvp/docs/audits/company-next-level/`: `01-source-of-truth.md`, `02-throughput.md`,
 `03-economics.md`, `04-reliability.md`, `05-owner-loop.md`, `06-evolution-loop.md`,
 `07-security.md` (STILL RUNNING), `08-always-on-layer.md`. Plus the manager's own
 `00-manager-findings.md` and the program in `99-integration-register.md`.
- **TWO PRODUCTION FIXES LANDED, both verified.**
 1. `scripts/server/access-watchdog.sh` — the API was restarting itself **339 times in 7 days, 174 in one
 day, median inter-start gap 70 s**. Two logic errors: `ensure_enabled_active` used
 `systemctl is-active --quiet`, which is false while a unit is `activating`, so it restarted a service
 that was mid-startup and killed the start it had just asked for; and `check_http` restarted on a single
 failed 8 s curl. Backup `access-watchdog.sh.bak-restart-storm-20260915-194301`; four guards added
 (`activating`=alive, 3 consecutive failures, 900 s cooldown, `WATCHDOG_DRY_RUN=1`). Verified by dry run
 with an isolated STATE_DIR, then by a 16-minute background sampler: **watchdog restart counter 292 →
 unchanged for 16 minutes.**
 2. `crontab` — removed a `cp` backup cron that had **never once run** (target `data/backups/` does not
 exist; the real backup system is the systemd timer + online backup API, 165 GB, and it is correct), and
 collapsed a `PRAGMA integrity_check` that ran **twice** every 15 minutes into one run
 (**49.3 s → 19.3 s per cycle**, ~46 min/day of full-store I/O recovered). Backup
 `~/crontab.backup-20260915-195205`.
 3. `app/tool_factory.py` + `app/sms_service.py` — **urgent, money.** `tool_factory.py:1788` imported
 `app.owner_sms_kill_switch`, **a module that has never existed**, so since 00:59 today urgent money
 alerts were **dropped with a warning instead of queued**. The kill switch actually lives in
 `app/sms_service.py` (moved there by `5581569d`, and `tests/test_owner_sms_kill_switch.py` documents
 it). Added `get_owner_sms_kill_switch_path()` to `sms_service.py` and pointed both imports at it.
 Verified: no stale references, both files compile, the import resolves, `data/OWNER_SMS_KILL_SWITCH`
 exists and is active (so the correct outcome is "queued, not sent"), and the API was restarted to load
 it. Buffers: `*.bak-killswitch-import-20260915-195356`.
- **Tailscale was down on ZABZ-TECH** (`failed to connect to local tailscaled process`) — the mesh's only
 home↔office path. Restarted the service; `ssh secratary-ts` verified working. Eight audit agents were
 pointed at that alias when it died.
- **Fixed my own memory tool's root cause:** `journal.py search` matched **titles only**, because
 `_from_tsv()` hard-codes `body=""` and `catalog()` always serves from the TSV cache. Delegated to a
 subagent on branch `fix/journal-search-bodies`; recorded as **P163**.

BROKEN / STILL OPEN
- **A second operator is actively editing the live production tree RIGHT NOW.** Reflog: commit `6a1fd872`
 at 19:55:33, fast-forward merge at 19:55:41, `git pull --rebase origin master` started 19:56:17 and
 **aborted** at 19:56:22. A test caught transient `<<<<<<< HEAD` markers in `app/autopilot.py` mid-rebase.
 HEAD moved `1d134a90` → `6a1fd872` during this session and the dirty set is now **91 files**. My phase-2
 base is therefore a moving target and the fleet must coordinate with that operator.
- `07-security.md` (workstream 07) had not reported when this session ended. **Its secrets inventory gates
 any push**, so nothing gets pushed until it lands.
- Live and unfixed, measured: **7,461 `database is locked` errors/7 days** (~44/hour, 1,128 today) — a
 connection-accumulation leak, not WAL; `/health` cannot report a write-path failure (it returns **200** on
 timeout, and `error_log` records 114 of 7,461 = a 65× undercount); `GET /communications/summary` returns
 **all zeros** while the store holds 12,611 calls and 148 texts/24 h (`communications_contract.py` reads
 only the frozen `call_log`/`sms_log` and never mentions Dialpad); reconciliation has failed **every run
 since 2026-08-23** (33 consecutive 600 s timeouts); one backup set (20260910T180001Z) is 115 MB against
 2.4 GB peers and is not a database; a live 500 from `ImportError: cannot import name 'get_conn'`.

NEXT
- Integrate `07-security.md`; then run the four-workstream phase-2 fleet in `99-integration-register.md`
 (truth instrumentation · connection lifecycle · cost/prompt assembly · stop the garbage generators),
 worktrees cut from the commit that actually runs, **not** `origin/master`.
- The single highest-leverage finding, from WS02: this company produces **9 verified external effects in 30
 days** across 7,635 ticks — **0.12 per 100 ticks** — and 62.7% of sampled work is meta-work about itself.
 WS05 row #40 (cap self-work, re-route ticks) is the company's own operating policy and should be decided,
 not asked.
- One owner question is owed and is drafted in the session's final report (product direction: what the
 company is FOR). **Do not ask it twice.**

EVIDENCE
- Restart storm: `journalctl -u secretary-api --since "7 days ago" -o short-unix | grep "Started
 secretary-api.service"` → 339 starts, 174 on Sep 15; `.runtime/access-watchdog.log` → 145 api + 147
 dashboard restarts in 25.5 h. Fix verified by `C:\Users\ezabz\Code\_tmp\watchdog-verify.log` (16 min,
 counter frozen).
- Kill-switch repair: `grep -rn "app\.owner_sms_kill_switch" --include=*.py app/ scripts/ tests/` → NONE;
 `python -c "from app.sms_service import get_owner_sms_kill_switch_path as g; print(g(), g().exists())"`
 → path printed, `exists: True`; 9 of 10 tests in `tests/test_owner_sms_kill_switch.py` pass (the 1 failure
 was a transient conflict marker in `app/autopilot.py` owned by the other operator, now gone).
- Company output: `03-economics.md`, `02-throughput.md`.

<!-- j2 tags=audit,company,restart-storm,kill-switch,phase2 refs= alias_of= legacy_id= sha=23679904a1af4754 -->
