<!-- e:handoff|H258|2026-09-15 19:12 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:12 UTC · ZABZ-TECH · PART-120 corrected: census was 28 not 43, and the error hid that production runs a stale unguarded build - six routers patched, nine endpoints now 401

CHANGED
- **PART-120 corrected and re-committed: `f9f5fefff`.** The census was wrong and the correction produced a
 worse finding than the error.
- **Six routers that were serving unauthenticated requests in production are now patched and verified.**
 Nine endpoints that returned real data earlier today now return **401**; the public-by-design paths are
 still reachable and the site, portal, Google sign-in and health are all unaffected.
- Reproducible census committed: `docs/audits/account-audit/tools-census-repo.ps1`.

MY CENSUS ERROR, AND WHY IT MATTERS
- I ran the guard census against **`/opt/lpt/src` on the production host**, not against a pinned revision.
 That checkout is **stale**: it does not contain `19f9481dc` (Part 028, 2026-08-31), which guarded `print.ts`.
- So I reported **43 bare procedures across 8 routers** and included `print`'s 10. The authoritative count
 is **28 across 6 routers**: `auth` 12, `faq` 5, `customer-account` 3 (all legitimate public surfaces),
 `smart-fields` 3, `systemLog` 3, `ui` 2.
- **A subagent flatly contradicted my record and was right.** The census is worth re-deriving whenever
 someone disagrees with it, and an environment's source tree is not the repository.

THE ERROR HID A REAL FINDING (F-1b)
- `print` was fixed in **source** on 2026-08-31 but **production was never rebuilt after that**, so the
 deployed `dist/trpc/routers/print.js` (dated 2026-09-10) still declared **10 bare `procedure`** — 0 guarded.
- Live unauthenticated probes: `print.checkStatus` -> **200** (and it disclosed the internal print-service
 URL `http://localhost:8080`), `print.getPrinters` -> **200**. Ten procedures that drive **physical
 printing**, including `printWorkOrder`, which puts customer name, phone and device onto paper.
- **The general finding is bigger than print: every source fix committed after 2026-09-10 is undeployed.**

ALSO FIXED TODAY (all verified 401)
- **F-1c `smartFields` returned customer PII** — `customer_name`/`customer_phone`/`customer_email`/
 `customer_address` run `prisma.customer.findMany` and return name, phone, email and full address, to
 unauthenticated callers, and `git grep` finds **zero** callers of the router. A fleet agent found the PII
 path and probed production with a deliberately non-matching query; **I verified its claim in code before
 acting on it.**
- **F-1d `ui`**: one of its two procedures already enforced auth in its own body, so a declaration census
 cannot see its real state. The count of 28 is an **upper bound on exposure, not a measurement of it**.
- `terminal` (12) and `a2pAutoReply` (5) were fixed earlier in this round.

OPEN BY DECISION, NOT BY OVERSIGHT
- **F-3 `systemLog.logError` is deliberately public.** A fleet workstream guarded all three `systemLog`
 procedures with `protectedProcedure`; I verified the work (one file, 4+/4-, each procedure guarded once)
 and then **rejected** it, because its own caller analysis is the reason: `GlobalErrorBoundary.tsx:172`
 calls it and that boundary is mounted at the **app root**, wrapping public pages including
 `/customer-portal/*`, with no login check. Guarding it would not close an injection path — it would
 **silently delete anonymous error reporting from every public page**, and silently is the problem.
 The service is a **stub** today (the `prisma.systemLog.create` persistence is commented out, TODO #2174),
 so the live vector is log-line spoofing at unlimited rate, not table fill. Correct treatment: a
 rate-limited server-validated public sink, or an authenticated reporter — not a guard. Branch
 `agent/guard-systemlog` reverted (`e123622a7`), kept, not pushed.
- **F-3b `faq.incrementView` is an unauthenticated write** of a view counter. Recorded; rate limiting is the
 right control, not auth.
- **F-3c the deployment mechanism is the real remaining finding.** Production runs hand-patched emitted JS
 over a `dist` whose own source tree is stale and unbuildable in place. Each fix today had to be
 hand-patched into the container, and **the audit's own fixes are one `docker compose build` away from
 being silently lost.**

EVIDENCE
- Final battery (`_scratch/final-verify.sh`): nine endpoints 401; `auth.requestPasswordReset` 200;
 `customerAccount.requestClaimCode` and `faq.search` 400 (reachable); `/health` and `/health/live` 200 on
 three consecutive checks; `lpt-backend` healthy.
- Every patched file backed up to `/opt/lpt/<name>.js.orig-<stamp>` and `node --check`ed before restart:
 `terminal`, `a2pAutoReply`, `smart-fields`, `print`, `systemLog`, `ui`.
- Fleet: four workstreams, four worktrees, all from `b5ad03826`. `print` reported a stale premise and
 corrected it; `smart-fields` found the PII path; `ui` resolved a genuine ambiguity; `systemLog` was
 verified then rejected on its own evidence.

NEXT
- PART-122 (session and token lifecycle) owns PART-119 F-3/F-6/F-7 and gates the 7-day access-token fix.
- The stale-build deployment path (F-3c) needs a properly scoped fix: rebuild, migrations, verify.
- Still open from this round's fleet: `faq.incrementView`, the systemLog sink design, and the 20 in-repo
 callers unanswered because the fleet answered the census question instead.

<!-- j2 tags= refs= alias_of= legacy_id= sha=a6ec46ede0b7ecd7 -->
