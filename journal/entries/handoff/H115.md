<!-- e:handoff|H115|2026-09-14 22:41 UTC|SECRATARY|open -->
## 2026-09-14 22:41 UTC · SECRATARY · ceo-kernel diverged and being edited: sentinel model_failover check is pushed but not applied

CEO-KERNEL IS THE FOURTH DIVERGED CHECKOUT TONIGHT, and this one is being edited by another session - do not force it.

STATE measured 2026-09-14 22:45 UTC on secratary (/home/zabz/ceo-kernel): `main...origin/main [ahead 4, behind 2]`, with ck/sentinel.py and scripts/run-ha-truth.sh DIRTY. Local heads include b471c60 "kernel: stamp the report, and deploy the delivery check" and 76c211d "sentinel: measure delivery, not queue depth (deployed copy)". Upstream has 52e733a (the model_failover check, pushed from zabz-yoga via the Cloudflare tunnel) and 9fc4a0f "sentinel: measure delivery, not queue depth - nothing reached the owner for 57 days".
=> The local 76c211d and the upstream 9fc4a0f are the SAME work done twice on two machines. A rebase here would replay four local commits into two upstream commits that already contain one of them, on a file (ck/sentinel.py) that is currently dirty - i.e. actively being written. DO NOT rebase it blind; let that session settle, then reconcile deliberately.

WHAT IS SAFE AND DONE: commit 52e733a (the model_failover sentinel check plus scripts/verify-model-failover.py) IS on the ceo-kernel remote. The subagent could not push it - it had no route to secratary (its Tailscale was NoState with a zeroed node key, and LAN 192.168.50.0/24 is unreachable from the home network) - so the delegating session pushed it over the Cloudflare tunnel instead. The check was written with 9 offline branches plus a live refusal, 10/10 passing, py_compile clean, and a stated deviation (a local _short() so a <90s move reads "0m" rather than the shared _human()'s "0s", leaving _human() untouched for the other checks that already use it).

TO FINISH IT, once secratary's ceo-kernel tree is quiet: reconcile (backup branch, autostash rebase onto origin/main, resolve only generated/duplicated hunks deliberately, push), then prove it end to end with the live round trip, which is the point of the whole change:
 ssh secratary-ts 'cd /home/zabz/ceo-kernel && git fetch origin && git merge --ff-only origin/main && python3 scripts/verify-model-failover.py --live && /home/zabz/ceo-kernel/scripts/run-sentinel.sh && curl -s http://127.0.0.1:3086/dsh-attention.json | python3 -m json.tool | grep -A6 model_failover'
Then the human half: bash /home/zabz/personal-secretary-mvp/scripts/server/owner-attention-digest.sh | sed -n "/0a\./,/source:/p". No service restart is needed: ck/sentinel.py is a cron job rewriting latest.json, which is what both the badge and the digest read.

PATTERN WORTH NAMING (four instances in one night: ZABZ-TECH harness-config, ZABZ-YOGA harness-config, secratary ceo-kernel, plus the same shape recorded earlier): every one of these repos is synced fast-forward-only, so ANY local commit freezes that machine's copy of the repo without an alarm. Two of tonight's instances were on the OWNER-FACING workspace, one was on the monitoring kernel, and in each case the machine looked healthy. The fix is not another manual rebase; it is a sync layer that reports divergence into a channel someone reads, and that preserves local commits automatically.

<!-- j2 tags= refs= sha=557ae6be2488d4c9 alias_of= -->
