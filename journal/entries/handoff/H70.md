<!-- e:handoff|H70|2026-09-14 06:00 UTC|SECRATARY|open -->
## 2026-09-14 06:00 UTC · SECRATARY · The phone was a shell and is now an agent, and the layer is delivered twice

**CHANGED**
- **The phone path works end to end.** Probe **15/15** (05:53:31Z), and a real browser at 393x852
 through the real URL gets the document *and* every `/api` call, with the WebSocket upgrading 101.
 The refusal (P48b) was **our own config**: `profiles/web/cordis.patch.yml` restated the
 `connection` row's `config` to add the reconnect policy, and a patch replaces the targeted row's
 whole `config` — so `trustedHosts: !!js ctx.webRuntime.trustedHosts`, the only source of the
 `/api` fence's trusted authorities, was silently deleted. Fixed by restating it (25ab7bf).
- **The phone layer is delivered twice** (fe8396a): the gate injects `assets/mobile.css` into
 documents **and** serves it at `/dsh-phone-mobile.css`, which `dsh-plugin-mobile` links at boot
 and re-asserts if the app rewrites `<head>`; the gate also marks its document `Cache-Control:
 no-store`.
- **The cost pill reaches the phone.** `plugin-cost` had **no installer at all**, so the always-on
 host that serves the phone never had it. One installer for every client plugin now
 (`scripts/install-client-plugin.sh`), both packages delegate to it, and `serve-phone.sh` keeps
 both installed (d248118). Measured on the phone: the footer shows `$0.09` on a session with usage.
- **No new-window control on a phone** (d248118): `plugin-windows` renders nothing below 768px —
 the owner's words were "it doesn't need the plus button to open a new window because it's just a
 phone".
- **The drawer closes on any action taken in it** (99f0e3b), not only on a list tap — measured that
 the drawer's New session control lives in the logo row, outside `listArea`, so starting a session
 used to leave the drawer covering it.
- **`[role=dialog] button` gets the 44px minimum** (eb6e09d): the first-load notice's only control
 measured 297x36 behind a blocking mask.
- **Watchdog cadence 10 min -> 2 min** after the gate died with no cause on the record (P56).
- **Parity verified on all three machines**: `dsh 0.1.5-rc.1`, preset `zabz`,
 `deepseek-official/deepseek-flash`; the office desktop already carried the fence fix; the Yoga was
 missing `plugin-mobile` and now mounts it; every mounted bundle resolves on the Yoga
 (`install-client-plugins.ps1 -Check` exits 0).
- Inventory of the whole path, with the command that proves each part: `docs/dsh-mobile/02-SYSTEMS.md`.

**IN FLIGHT**
- P46's remainder: a first run lands with **no session** (measured: 517 bytes of body, no composer)
 until the reader opens the drawer and picks one or taps New session.
- `plugin-attention` is in the phone engine's bundle list but **not** in the browser roster, so it
 contributes nothing client-side there yet — another lane's in-flight work, left alone.

**BROKEN**
- Nothing on the phone path as of 05:53 UTC. Findings still reach nobody (P40/P55): the owner still
 learns about an outage by holding the phone.

**NEXT**
1. P56 — find what killed the gate (it was serving at 05:48:40, the port was gone by ~05:50, no
 traceback in `gate.log`, no OOM line, 17.9 GB free, load 1.3).
2. P46 — seed a default session so the first load is not an empty pane.

**EVIDENCE**
- Probe 15/15 (`~/.dsh-phone/probe.json`, iso 2026-09-14T05:53:31Z); gate document
 `Cache-Control: no-store`, 37,994 bytes; `/dsh-phone-mobile.css` 200 `text/css`, 9,806 bytes.
- Browser at 393x852 on the real URL: frame `393px`, sidebar `-340/339 fixed`, toggle `Open
 sidebar` 44x44, page overflow `hidden/hidden`, `#dsh-phone-mobile-link` present, **0 console
 errors**, 0 controls under 44px in a conversation, drawer closes on New session, cost pill `$0.09`.
- With the injected `<style>` deleted in the live page, the layout still holds (frame `393px`,
 drawer at -340, page scroll locked) because the plugin's link carries it.
- Acceptance answer from a session created on the phone page: `mcp__secretary__ps_db_query` ->
 "**62 ticks today — `/home/zabz/personal-secretary-mvp/data/secretary.db`, table `tick_telemetry`
 … verified by direct sqlite read**".
- Commits: 25ab7bf, d248118, fe8396a, eb6e09d, a7f5b5c, 99f0e3b, da7157a.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=c988841c4b54eab6 -->
