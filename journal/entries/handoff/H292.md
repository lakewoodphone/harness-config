<!-- e:handoff|H292|2026-09-15 20:59 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:59 UTC · ZABZ-TECH · W2-CORE: Tier 1 committed on agent/rentals-core (4 commits); gate green except 2 frozen-test API mismatches

CHANGED
- Branch `agent/rentals-core` in worktree `_worktrees/personal-secretary-mvp/rentals-core`, 4 commits,
 working tree clean:
 - `4820acc9` Tier 0 (`busy_timeout` first statement) + data-layer foundation.
 - `128f702c` Tier 1 items 1-9 + the lazy DB open.
 - `e814f17f` `sha: process.env.APP_BUILD_SHA` in `/api/health`.
 - `87c7f463` `full_address` in the protectable allow-list.
- `apps/rentals/lib/db/schema.sql.ts` no longer opens SQLite at import time (`getDb()` memoises; the
 default export is a lazy Proxy). Proven: 3 consecutive `npm run build`, all exit 0, `data/` absent
 after each.
- New `apps/rentals/tests/marketplace.test.ts` (114 assertions, all green) + `tests/marketplace.env.ts`.

IN FLIGHT
- Nothing of mine. Do not start Tier 2/3 without the manager's go-ahead; Tier 1 is what was asked for.

BROKEN
- `apps/rentals/data/rentals.db` was created and polluted by my diagnostic probes. I moved that file
 aside and deleted it; `data/` is absent now. It was an untracked dev artifact, no customer data.
- The shared gate: `npx tsc --noEmit` is clean for my files, but the tests workstream's untracked
 `tests/zz-w2-tests-integration.test.ts:23` imports a missing `./helpers/db`.
- `npm test` -> 158 tests, 155 pass, 3 fail. Two are `tests/integration.test.ts` asserting the OLD
 signatures I deliberately changed (`createRevealRequest` now returns an event object, not a rowid;
 `voteSuggestion` now returns `{ok,voted,votes}`, not a boolean). The tests workstream must update
 those call sites; I must not edit `tests/**`.
- A test run recreates `apps/rentals/data/` because a suite in the same process does not set
 `RENTALS_DB_PATH` correctly (see L1598 for why that is easy to get wrong).

NEXT
- Manager to route the 2 signature mismatches and the missing `./helpers/db` to the tests workstream.
- Then Tier 2: DATA-7/PHOTO-2 rate-limit keying is already implemented in my commits, so the rest of
 Tier 2 is mostly done as well; a fresh pass should re-read the two audit reports rather than trust
 this note.

EVIDENCE
- `npx tsc --noEmit` -> 0 errors on my files.
- `npx tsx --test tests/marketplace.test.ts` -> 114/114 pass, and `data/` absent afterwards.
- `npm run build` x3 -> `Compiled successfully`, EXIT=0 each, `data dir after build: False` each.
- `npm test` -> `tests 158 / pass 155 / fail 3`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=172fbb51e7f42000 -->
