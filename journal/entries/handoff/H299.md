<!-- e:handoff|H299|2026-09-15 21:16 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:16 UTC · ZABZ-TECH · Window URL fix is on origin/master (265e4e9); ZABZ-YOGA left alone because its checkout is mid-merge

CHANGED
Nothing new on ZABZ-TECH — this is the follow-up to H297, recorded so the next session does not redo it.
The launcher fix is published: pushed to `origin/fix/journal-search-bodies` (bcb7de4, this checkout's
branch) **and** to `origin/master` as `265e4e9` (worktree branch off origin/master, two files only:
`multi-window/dshw.ps1` + `docs/multi-window/ANALYSIS-AND-DECISION.md`; the journal entries were left
off master because entry ids are allocated per machine and the repo already has an id-collision pain).
Any machine that pulls master now gets the tokenized-window fix.

BROKEN / LEFT ALONE
ZABZ-YOGA has the OLD launcher: `git hash-object multi-window/dshw.ps1` there is
`263fdbf...` = the pre-fix blob, i.e. new profiles on that machine still 401. It was NOT touched,
deliberately: that checkout has `MERGE_HEAD` present (a merge in progress), 254 dirty files, and its
master sits on the diverged commit `919cfa6`, so any write or cherry-pick there could strand another
session's in-flight merge. Its own session owns that resolution; the fix arrives with the next pull of
origin/master.

NEXT
If a window on ZABZ-YOGA shows the auth page before that pull lands, the one-line workaround there is to
open the engine's printed URL (it is in `~/.dsh/multi-window/state.json` -> `slots.<port>.url`, and in
the newest `<port>-*.log` as the `dsh web: <url>` line).

EVIDENCE
`git log --oneline origin/master -1` -> 265e4e9; `ssh zabz-yoga-ts` -> branch master, head 919cfa6,
MERGE_HEAD present, dshw.ps1 blob 263fdbf.

<!-- j2 tags=dshw,harness-config,ZABZ-YOGA,multi-machine refs= alias_of= legacy_id= sha=bc38671e44f3885e -->
