<!-- e:handoff|H249|2026-09-15 18:48 UTC|ZABZ-TECH|open -->
## 2026-09-15 18:48 UTC · ZABZ-TECH · Activation Lock fixed into the maintained onboarding path; three items still need the owner; a latent DEP payload bug recorded

CHANGED
- **Activation Lock is now a step in the maintained onboarding path**, so the LPT failure cannot silently recur.
 `scripts/waze/onboard.py` (personal-secretary-mvp) applies org-linked Activation Lock via the fleet-api DEP
 endpoint and, critically: it is **idempotent** (reads the `fleet_alerts` audit row to decide, because calling that
 endpoint is not a read — it ENABLES the lock), it **verifies** (prints `activation lock: applied|NOT APPLIED`), and
 it **exits non-zero** when it cannot apply it. A loud `--no-activation-lock` opt-out exists for bench units.
- The **hardcoded DEP API key** in `archive/scripts/drn-auto-deploy.py` is gone — it reads `NANODEP_API_KEY` from the
 environment like fleet-api already does and refuses to call DEP without it. Host moved to `WAZE_DEP_HOST`.
 **The old literal is still compromised and must be rotated in NanoDEP.**
- The onboarding doc documents the step as 4 of 4 with its rules; the LPT "three steps" heading corrected.
- Stale cross-repo paths fixed: bare `deploy/waze-mdm/...` inside lpt-hub (which has **no deploy/ directory at all**)
 now reads `personal-secretary-mvp/deploy/waze-mdm/...` in the fleet registry and the trial purchase record.
- Pushed: personal-secretary-mvp `48e7a97e` on origin/master · lpt-hub `327f4bead` on origin/main.

IN FLIGHT / STILL NEEDS THE OWNER (three things, none decidable by me)
1. **ABM check** for serials `FFYGNQ8AN72J` / `FFXGT23HN72J`. Org-linked lock is an ABM/DEP property; if they are not
 in Apple Business Manager, that is the root cause and no code change substitutes for adding them.
2. **Running the lock on DRN 2001/2002** — a state change on real devices. `onboard.py 2001 2002` now does it and
 reports success.
3. **Rotating the DEP key** in NanoDEP.

BROKEN
- Nothing. No device was touched, no lock was enabled, no service was changed.

NEXT
- **A latent bug is recorded, not fixed** (audit §8): the archived *working* script POSTs `{device, escrow_key}` to
 the DEP activationlock endpoint while fleet-api PUTs `{serial, escrow_key_hash}`. They disagree on verb AND key
 names, and fleet-api feeds a bypass **code** where the working path feeds a **hash**. The first real run of
 onboard.py will now fail loudly if fleet-api is the wrong one; align the payload then.

EVIDENCE
- `lpt-hub/docs/customer-operations/sourcing/waze-mdm-activation-lock-audit-2026-09-15.md` §7 (fixes) and §8 (the
 latent DEP payload bug).
- `personal-secretary-mvp/scripts/waze/onboard.py` step 7 + `apply_activation_lock()`; compiles, `--help` runs, and
 `_common`'s `pg_query`/`_sql_str`/`api` all resolve.
- Root cause, for the record: org-linked Activation Lock existed ONLY as step 5 of the ARCHIVED DRN script, and the
 LPT onboarding flow never called it — so 2001/2002 were onboarded and deployed with no lock at all, and a factory
 reset left a fully usable phone.

<!-- j2 tags= refs= alias_of= legacy_id= sha=51bbaa34b7204ee4 -->
