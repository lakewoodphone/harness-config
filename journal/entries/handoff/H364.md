<!-- e:handoff|H364|2026-09-16 00:47 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:47 UTC · ZABZ-YOGA · unified-search integrated: 203 tests green, 144k documents answering, the refresh scheduled and observed running

**APPEND to H360 — state at 2026-09-16 00:50Z, after integration.**

## Merged onto `main` and verified
`agent/files`, `agent/journal`, `agent/git`, `agent/query-surface`, `agent/operations` are all on
main. `agent/chats`, `agent/localdb`, `agent/secretary` had not reported when this was written.

**`python -m unittest discover -s tests -t .` → 203 tests, OK.** That is the number to hold: it
includes 32 git-adapter tests, 30 journal, 28 end-to-end, 76 operations, 22 files.

## The index is real and answering
`~/.usearch/usearch.db` (not index.db): **144,451 documents searched**, 583 MB, WAL capped at ~15 MB
by the files adapter's `--checkpoint-every 5000`.

Measured on the real index tonight, in-process latency 0.6-17 ms; CLI wall time 256-754 ms of which
~200 ms is Python startup:
* `stats` — was **157 s**, now **854 ms** (it was counting an FTS5 table; it now sums `docs.n_chunks`).
* `demo.py "sync breaker"` returned the autopilot breaker defect, its root-cause comment in
 `autopilot.py`, and the port script — with line ranges, timestamps and fused scores.

## The refresh runs, and it was seen to run
`scripts/sync.py --source files:local --source journal` executed both sources, ingested 37 documents,
recorded per-source status, duration and exit code, exited non-zero for the failed one and did NOT
stop the other. The failures are the honest kind: the files adapter exited 3 because the full-corpus
pass was still writing the same index (lock contention), not because the pipeline is broken.

**Scheduled: `DSH unified-search refresh`, state Ready, Enabled, next run 01:15Z**, command
`cmd.exe "/c" "~/.usearch/bin/usearch-refresh.cmd"` — re-registered from the PRIMARY checkout, which
was the one thing the operations agent could not do. Linux cron on `secratary` reads back as exactly
one `# usearch-refresh` line pointing at `$HOME/.usearch/usearch.db`.

## Corrections to H360
* H360 said the adapter count was eight; **five merged**, three pending.
* H360 said `stats` was fixed; it was, and the operations layer then reintroduced the same class of
 bug from the other side — it hardcoded `index.db` in 15 places and reported health on a database
 nobody queries. Fixed by importing `usearch.DEFAULT_DB`; one owner for the path.

## What is NOT done, ranked
1. **`agent/chats` is the highest-value pending corpus**: ~9 months of the owner's own reasoning,
 and nothing has ever indexed it.
2. `agent/localdb`, `agent/secretary` unmerged; the authority's `usearch.db` does not exist yet, so
 its cron line currently fails every 30 minutes by design.
3. Two core capabilities the files adapter reached past the contract to work around:
 `delete_documents(con, source, external_ids)` and a `ROLLBACK` that does not mask a lock error.
4. The report-on-the-wrong-file class of bug: with two indexes and more coming, `usearch.py` and
 `ops_common.py` agree now, but a third copy of that path constant would drift again.
5. ~17 GB of superseded index (`~/.usearch/index.db` 2.7 GB + its 14.3 GB WAL) is unreferenced and
 recoverable; not deleted, because deleting data needs the owner's explicit yes.

<!-- j2 tags= refs= alias_of= legacy_id= sha=0e079dcb30fad6db -->
