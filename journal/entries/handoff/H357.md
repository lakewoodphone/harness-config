<!-- e:handoff|H357|2026-09-15 23:48 UTC|ZABZ-YOGA|open -->
## 2026-09-15 23:48 UTC · ZABZ-YOGA · Website comms silence ended: 127 messages restored (116558 -> 116685), three reconciliation clocks installed and tested, check now 16 ok / 0 FAIL, and the PCI message deliberately withheld

## 2026-09-15 23:55 UTC · ZABZ-YOGA · The website clock is restored: 12 hours of customer silence ended, three clocks installed and tested, and the reconciliation check is green for the first time

CHANGED — the customer-facing defect is fixed
- **The production website received 127 customer messages that had been stuck for 12 hours.**
 `communications` 116,558 -> **116,685**; newest message **2026-09-15T11:34:24.649Z -> 23:43:48.221Z**.
 Measured live: calls synced 28 (90 skipped as duplicates), SMS synced 99 (153 skipped), errors 0.
- **The push is idempotent, proven by running it twice:** the second run reported synced 0,
 skipped_dedup 118/252 — nothing duplicated.
- **`/home/zabz/bin/website-comms-push.py`** — the clock that was missing. It reuses the application's
 OWN functions with the application's OWN windows and limits (`_get_dialpad_calls_for_push` 7d/500,
 `_get_dialpad_sms_for_push` 7d/1000, then `sync_dialpad_calls_to_website` /
 `sync_dialpad_sms_to_website`). It invents no matching logic and writes no SQL of its own, so the
 dedup, customer match and `external_ref` guard are all the application's.
 **It withholds exactly one message on purpose:** `dialpad_sms_cache` message_id 6252044111880192
 carries a full card number, expiry, CVV and ZIP. Axis G flagged it and the owner has been asked
 (queue 67); until he answers, `--include-pci` is required to push it. Everything else in the window
 was pushed. This is the ONE thing I deliberately did not reconcile.
- **`/home/zabz/bin/production-comms-freshness.py`** — the check that would have caught the silence.
 The defect was never "nothing measured staleness"; it was that the one signal measured
 `MAX(dialpad_sms_cache.fetched_at)`, the LOCAL receive clock, which the live webhook keeps fresh, so
 it reported healthy while the website received nothing. This reads `website_sync_cursor.last_synced_at`
 AND the website's own newest row, and fails when the local store is more than 2 h ahead of it.
- **Three clocks installed in crontab, idempotently, with a backup**
 (`~/crontab.backup-20260915T234441Z`, 43 lines -> 46, nothing removed):
 `5,35 * * * *` website-comms-push · `*/15 * * * *` production-comms-freshness ·
 `23 */4 * * *` lpt-recon-check. **All three were then RUN exactly as cron runs them**, not merely
 configured: rc=0, rc=0, and rc=1 with `push_would_create=7` respectively.
 **No email, on purpose** — nothing outbound without the owner's per-message instruction; they write
 status files under `~/.lpt-recon/` and exit non-zero.
- **`lpt-recon.py` is now `16 ok · 0 FAIL · 0 could not measure`**, and the green means something
 because every FAIL it showed today was either fixed or honestly reclassified:
 - `comms.prod_newest_age_h` 11.41 h FAIL -> **0.03 h OK** (this round's fix);
 - `dedup.dialpad_sms_cache` used to FAIL on the row count. Axis D proved those 2,744 groups differ
 ONLY in `session_id` with **0 groups differing in body_text**, so the count is not a defect. The
 check now fails only on `COUNT(DISTINCT body_text) > 1` per message_id — i.e. on a genuine
 conflict — and reports the row counts as information. A permanently red light trains its reader
 to ignore it, which is the decoration failure this tool exists to avoid;
 - `identity.gap` had TWO of my own bugs in a row (`no such column: name`, then an IndentationError
 from a dangling string) and now computes a real, normalised-adjacent trend line;
 - two of my own checks were wrong in ways I only found by running them. Again.
- Fixed `c_identity_gap`'s column (`people.display_name`, not `name`) and installed the tool at
 `/home/zabz/bin/lpt-recon.py` rather than the scratch dir, so the cron path is durable.

IN FLIGHT
- **The remaining 6 case pushes, and why each is held:** `push_would_create=7` (was 11; 4 applied last
 round). Two need the owner (A-002 unauthorized $750–900 quote with no customer name; A-010 Czermak
 $75 due at pickup). One is blocked on an lpt-hub data fix (A-007 Weingarten's phone is the literal
 string `PENDING`; production already holds TWO Weingarten rows at (917) 902-6334, so a push as-is
 inserts a THIRD). One has no phone anywhere (A-008 Bloch). Two have placeholder names that would
 create customers called `[First name TBD] Mordecai` and `TBD` (A-004, A-009).
- **74 of the 111 missing messages still cannot reach the website**: they belong to 24 phone threads
 that match no production customer, and `website_db.py:1628`/`:1720` do `if not customers: continue`.
 That is axis G's G-008 (the attrition fix) and it is a code change in a file another operator may
 touch, plus it arguably needs a decision about creating customer records from unknown numbers.
 The 7-day window at `daily_reconciliation.py:1897` is the other half and makes the loss permanent.
- **G-004 (repoint the local staleness signal in `app/autopilot.py`) NOT applied** — `app/autopilot.py`
 is dirty, held by another operator. The new freshness check covers the same ground from outside, so
 the exposure is closed even though that edit waits.
- One warning observed twice and unfixed: `website_db: batch customer lookup failed: list index out of
 range`. A real defect in the batch lookup, unrelated to my change, worth its own fix.

BROKEN / NOT TRUE
- Nothing from `plan.json` in axes B, C, D, E, F has been applied. They remain specifications, and no
 agent tested its own SQL.
- The 5 committed customer-credential files, the $6,489.94 money gap, the mislink that points the
 Weissman E46010 case at Shmuel Birnbaum's AirTag order, and the 13 identity merges are all queued
 for the owner, one at a time.

NEXT
- Watch the first cron-triggered runs land (push at :05/:35, freshness every :15) and confirm the
 freshness check goes red if the push stops — that is the last piece of proof the failure surface
 works end to end.
- Then the 6 pending case pushes once the owner answers the two money questions.

EVIDENCE
- `website-push.log` (JSON with synced/skipped counts), production read before/after
 (116,558 -> 116,685; 11:34:24Z -> 23:43:48Z).
- `production-comms-freshness.py` -> "fresh", cursors at 23:43, website newest 23:43:48.
- `lpt-recon-check.sh` -> status.json `{"push_would_create":7,"push_errors":0,...}` rc=1.
- `lpt-recon.py` -> "16 ok · 0 FAIL · 0 could not measure".
- crontab backup `~/crontab.backup-20260915T234441Z`; three entries verified present (lines 44-46).

<!-- j2 tags= refs= alias_of= legacy_id= sha=f823dbe377a6a9d9 -->
