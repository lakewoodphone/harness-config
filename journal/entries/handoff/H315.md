<!-- e:handoff|H315|2026-09-15 22:09 UTC|ZABZ-TECH|open -->
## 2026-09-15 22:09 UTC · ZABZ-TECH · Full-site audit fleet batch 1 landed; suite green+deterministic; SMS allowlist fixed in prod-live; rebuild BLOCKED on the Dialpad migration

CHANGED
- Backend suite is GREEN and DETERMINISTIC on branch audit/lpt-account-2026-09-15, and the cross-file
 interference is solved: cause was `process.env = originalEnv` (binding reassignment) in two health tests.
 Baseline: 6212 passed | 363 skipped (6575), 0 failed, 447 files at --maxWorkers=1 (same at 8 forks).
 Control proven: injecting one bare procedure into faq.ts turns the green run into exactly 1 failure named
 faq.zzGreenCheck. guard-coverage.test.ts is a live detector inside a full run.
- ui.ts guarded in SOURCE (ui.navigationContext.get, ui.contextualSuggestions.get -> protectedProcedure).
 They were bare and guarded in prod only by a dist patch, so a rebuild would have reopened them. Verified
 prod first: guard-check.sh on lpt-apps -> POSTURE INTACT, both return 401. 16 tests converted to FORBIDDEN
 pins, a 17th vacuous one repaired, new pin proven non-tautological. ui removed from PUBLIC allowlist.
- context.ts customer path allowlist FIXED: was a substring match on the raw URL with 4 bypasses (query
 decoy, batch, router prefix, auth.refresh substring). Live in production dist, confirmed. Now parses the
 pathname, splits the batch, compares router names by equality, refuses unless EVERY procedure is safe.
 New test customer-path-allowlist.test.ts: 6 of 12 failed pre-fix, 12 pass post-fix.
- Full-site audit FLEET started: 78 never-started parts. process/FLEET-BRIEF.md is the contract. BATCH 1
 (8 parts: 040, 041, 048, 049, 058, 060, 064, 066) ALL LANDED.
- Commits: cdd7bca2e (batch 1 + ui guard), 967396b48 (allowlist fix + corrections).

IN FLIGHT
- 8 part docs from batch 1 exist; 70 parts (039, 042-047, 050-057, 059, 061-065, 067-116) NOT dispatched.
- Batches 2-N not started. Manifest section C tracks this.

BROKEN / BLOCKING
- THE PRODUCTION REBUILD IS BLOCKED (decision D179). Production SMS works on Dialpad (224/240 outbound in
 14 days, all SIDs prefixed "di"). But source removed Twilio and left ~15 call sites on smsService.sendSMS,
 which always returns success:false against the NoOp stub WITHOUT throwing. Rebuilding from source today
 would silently break repair-status texts, payment links, MFA codes and AI-workspace customer SMS.
 Finish the Dialpad migration of smsService FIRST, then rebuild with a named rollback tag.
- guard-coverage.test.ts has two blind spots, both open: (a) PUBLIC cannot see that an allowlisted procedure
 is a WRITE (faq.incrementView is allowlisted); (b) BARE matches only the literal token `procedure`, while
 GUARDED matches `publicProcedure`, and publicProcedure = procedure (context.ts:620) - so
 `x: publicProcedure.query(...)` is unauthenticated and fails nothing.

NEXT (highest value first)
1. Tighten guard-coverage.test.ts for the two blind spots; delete publicProcedure (0 router-file users).
2. Apply the batch-1 fixes serially: customer-account sendInvite protectedProcedure -> managerProcedure;
 changeCustomerPassword missing the exceptToken argument (signs customers out on password change);
 faq's two public writes have no live consumer -> remove from the public surface.
3. Dispatch fleet batch 2 (next 8 parts).
4. Ably: POST /api/ably/token returns the raw globally-scoped key to any authenticated caller, and all
 realtime rides one unscoped channel carrying SMS content and customer records.

EVIDENCE
- Full suite: npx vitest run --root backend --maxWorkers=1 -> 6212 passed | 363 skipped (6575), 0 failed.
- Guard control: inject zzGreenCheck into faq.ts -> exactly 1 failure, named. faq.ts restored clean.
- Prod posture: bash /opt/lpt/guard-check.sh on lpt-apps -> POSTURE INTACT, 9 endpoints 401.
- Prod allowlist present: /app/backend/dist/trpc/context.js:60-64 on lpt-apps (Sep 10 00:50).
- Prod SMS: lpt_prod.communications, 112351 rows, newest 2026-09-15 21:29Z; 14d 218 DELIVERED/6 SENT/16
 FAILED; 503/503 SIDs prefix "di".
- Owner decision #56 filed (A2P opt-out).

<!-- j2 tags=lpt,audit,fleet,auth,sms refs= alias_of= legacy_id= sha=b63a8697d1d1806c -->
