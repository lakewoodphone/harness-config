<!-- e:handoff|H272|2026-09-15 19:46 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:46 UTC · ZABZ-TECH · CLOSED the guard-revert recurrence: the six guards are baked into an image, verified across TWO forced recreates

CHANGED
- **`0d245bcc3`** — the six guards are now **baked into an image**, so a `docker compose` recreate can no longer
 silently revert them. This closes the recurrence that bit at 19:35 and re-exposed a live Stripe token mint.
- Also committed: `docs/audit/bake-guards.sh` and `docs/audit/pin-guarded-image.sh` (the two scripts, rerunnable).

THE FIX, AND WHY THIS SHAPE
- The guards lived in the container's **writable layer**, so a recreate replaced them from the image. That is
 the whole mechanism of the incident.
- **Method:** extract the six patched `dist` files from the running container → bake them into a **derived
 image** over the current one (`lpt-lpt-backend:guarded-20260915-194308`, from a 7-line Dockerfile that only
 `COPY`s them) → pin the compose service with an `image:` line.
- **Second benefit, worth noting:** compose now uses the pre-built image instead of the `build:` context. That
 context is the **stale unsourced tree** from round 6, so building from it was never wise — this quietly
 removes the accidental risk of someone running `docker compose build` and shipping March-2026 source.

VERIFIED, IN THE ORDER THAT MATTERS
1. Each of the six files was checked **`bare=0` inside the new image** before it was used (not just in the
 container).
2. `guard-check.sh` on the old container → `POSTURE INTACT`.
3. **`docker compose up -d lpt-backend`** → `Recreated` + `Started` + healthy — **the exact operation that
 wiped the guards at 19:35**.
4. `guard-check.sh` **after** the recreate → all nine 401, three public paths reachable, **`POSTURE INTACT`**.
5. **A second `--force-recreate`** → healthy, **`POSTURE INTACT`** again. Two independent recreates, because a
 mechanism that failed once should not be believed on one success.

ROLLBACK, one line
- Pre-bake image tagged **`lpt-lpt-backend:pre-guard-bake-20260915-194308`**; compose backed up at
 `/opt/lpt/docker-compose.yml.bak-guardbake-20260915-194308`. Change the `image:` line back and
 `docker compose up -d lpt-backend`.

WHAT THIS DOES **NOT** FIX — stated so it is not mistaken for the rebuild
- It protects **these six files only**. The **session-preservation change (`9eaa0eb4a`)** and anything else
 committed after the 2026-09-10 build are still **not deployed**.
- The build context is still an **unsourced tree whose `backend/src` predates March 2026**, and the real
 rebuild is still owed. This closes the *recurrence of the known security patches* — the part that had
 already bitten twice — and nothing more.

MEASUREMENT FIRST, WHICH IS THE HABIT THAT MATTERS NOW
- I opened this round with `guard-check.sh` and a container inspection **before touching anything**, because
 last round's lesson was that my own four-hour-old "all nine still 401" had aged into a false statement.
- I also measured the build environment before committing to a rebuild: **14 GB free**, github and npm
 reachable, and **1.2 GB of stale `node_modules` inside the build context**. That is why the rebuild is
 *possible* but still a session of its own — and why the cheap recurrence-fix came first.

BROKEN
- Nothing. Production healthy, posture intact across two recreates. Workspace clean at `0d245bcc3`.

NEXT
1. **The proper rebuild** (`docs/audit/DEPLOYMENT-GAP-2026-09-15.md`) — still needed for `9eaa0eb4a` and
 everything else post-2026-09-10. Run `guard-check.sh` before and after; it now has a real chance of
 passing because the guards are in the image.
2. The scoped `ui` task in `_worktrees/lpt-guard-ui2` (needs `pnpm install` there; 11 tests to fix by reading
 each assertion).
3. PART-127 (customer portal experience) and the remaining parts.

<!-- j2 tags= refs= alias_of= legacy_id= sha=711c924d6a7a0905 -->
