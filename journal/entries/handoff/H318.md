<!-- e:handoff|H318|2026-09-15 22:18 UTC|ZABZ-TECH|open -->
## 2026-09-15 22:18 UTC · ZABZ-TECH · personality-system DEPLOYED: AI layer works for the first time (real output, fallbackUsed false); operator surface fails closed; real CAS on saves

## 2026-09-15 late (ZABZ-TECH) - personality-system: audited, fixed, DEPLOYED. AI layer works for the first time.

CHANGED - and this is live in production, not a branch
- Repo `C:\Users\ezabz\Code\personality-system`, main = f164ec1, pushed to GitHub.
- Deployed to Hetzner `lpt-apps`: image `personality-test:f164ec12300e...` (tag = commit sha),
 container healthy, `docker ps` now shows `(healthy)` (it never could before - no healthcheck existed).
- The running commit is FINALLY knowable: `GIT_COMMIT` env + OCI labels + `/_next/static/deploy-info.json`
 all report f164ec12300e69b7b63068fc0a5a50c8b6e17018. Rollback target tagged:
 `personality-test:db0c0ec...` (the previous build). Procedure + watchdog: `deploy/README.md`,
 `deploy/watchdog/` (watchdog is WRITTEN, NOT INSTALLED).
- 6 fleet branches integrated on an integration branch, serially, gate re-run after every merge.
 103 tests -> **302 tests / 28 files**, tsc exit 0, eslint exit 0 (first time ever), next build exit 0.

THE HEADLINE: the AI layer had NEVER worked in production
- Measured before: 4 recorded AI events, ALL failed, last 2026-09-07. Users got deterministic
 fallback text and nobody knew, because the fallback reads fine.
- Root cause: `secretary-provider.ts` hardcoded `deepseek-v4-flash`/`deepseek-v4-pro`. I probed the
 live gateway: real catalog ids are provider-prefixed, and the gateway's ROUTE ALIASES
 (`secretary-auto/-fast/-smart`) answer a trivial prompt with 200 but **500 on every real payload**
 (both a 1.3KB question and the report), all at exactly the gateway's own 30s timeout. So the
 "prefer aliases" design was right in principle and WRONG in fact - I measured it before shipping it.
- Fix: defaults are models I measured passing the app's REAL Zod contracts:
 question/explanation/checkpoint `deepseek/deepseek-v4-pro -> openai/gpt-4.1-mini -> openai/gpt-5.4-mini`;
 report family `deepseek/deepseek-v4-pro -> openai/gpt-4.1-mini`. Evidence is in code
 (`DEFAULT_MODEL_EVIDENCE`, exposed via `describeModelPolicy()`).
- VERIFIED LIVE after deploy: POST /api/assess -> 200 in 13.7s, `type: question-render`,
 `fallbackUsed: false`, `modelUsed: deepseek/deepseek-v4-pro`, first rung succeeded. Before the fix
 the same real request hung past 180s. This is the single most important fact in this entry.

Also live and verified
- Operator surface is gated and fails CLOSED: unauthenticated `?view=records|record|live-cache|ai-metrics`,
 `DELETE /api/persistence` and `GET /api/feedback` all return **403**. Before: 200 with 18KB of
 participant data. `ASSESSMENT_OPERATOR_KEY` (64 hex chars) is set on the box; I generated it and it
 is in `/opt/personality-test/.env` (600). **I do not have it in this journal on purpose.**
- Malformed POST /api/assess -> **400 in 111ms with zero gateway calls** (before: 6 billable calls).
- Data safety: `expectedRevision` is now a real compare-and-swap (null = "must not exist"), saves
 serialise under SELECT FOR UPDATE, a stale write is refused with **409 + current revision** instead
 of silently replacing the shared library, snapshots are pruned to 20 revisions, erasure covers all
 seven tables, 34 `any`/lint errors fixed, `scripts/assessment-db-admin.mjs backup` now dumps
 complete rows with a manifest + SHA-256 and a `restore` exists.
- `/_next/image` disabled (`images.unoptimized`) - unused route that carried a critical advisory.
- Hardened live: `.env` and `.env.local` chmod 600 (was 666 world-writable), and an UNUSED
 `GITHUB_TOKEN` removed from the runtime env of the container.

IN FLIGHT / NOT DONE
- **`agent/interpretation-truth` is the only workstream still running** (1 line of status: alive,
 reproducing defects, 0 commits). It owns the "stop asserting findings from empty profiles" work in
 `participant-interpretation.ts` / `session-insights.ts` / `runtime.ts` / `routing.ts`. I sent it the
 differentiation-threshold retune (scoring now divides by 4 not 2.4, so `adaptive-selection.ts:144`
 and `routing.ts:31` are miscalibrated). If it never lands, that work is the next thing to do.
- **Residual leak, verified still open:** the coarse unauthenticated `GET /api/persistence` still
 returns `currentSessionCache`, which contains one real participant name from the 2026-09-07 session
 (registry.sessions is now 0). Fix = scope the library key to a signed per-device cookie so an
 anonymous caller reads an empty library. Designed, not built. This is the top security follow-up.
- The restore drill is written (`docs/deploy/backup-restore.md`) and UNEXECUTED. No backup has ever
 been restored.
- e2e: `npm run test:e2e` was never run green in this session (needs a browser + a live server).
- Gateway substitution is real: the gateway answered a request for `deepseek-v4-pro` with a FLASH
 model, and discards its own `secretary_trace_id`/`secretary_cost`. The provider now records
 requested-vs-served, but the gateway churn itself is unfixed.

BROKEN-BY-ME-AND-FIXED (worth knowing, these were invisible)
- Two files began with a `#!/usr/bin/env node` shebang and are imported by tests: Vite's transform
 parser throws "SyntaxError: Invalid or unexpected token" and SILENTLY SKIPS THE WHOLE TEST FILE,
 while `node --check` passes because Node strips shebangs. This hid 22 tests once and then an entire
 new test file again. See lesson L1613. Any script with a shebang that a test imports is a trap.

EVIDENCE
- `_scratch/`: gateway-models.sh, gateway-alias-test.sh, model-matrix.mjs, model-matrix2.mjs,
 contract-test.mjs (the schema-contract probes), deploy.sh, verify-deploy.sh, witness*.sql.
- Journal: H301 (the audit+fleet), D177 (model decision), L1606 (model procedure), L1613 (shebang).

<!-- j2 tags= refs= alias_of= legacy_id= sha=52a35d4cb7bfcd74 -->
