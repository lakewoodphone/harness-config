<!-- e:handoff|H245|2026-09-15 16:09 UTC|ZABZ-YOGA|open -->
## 2026-09-15 16:09 UTC · ZABZ-YOGA · The suite is green and can be a gate; two follow-ups left (descriptor leak, worker DB path)

**CHANGED (preserved on `secratary-master-preserved-20260915`)**
- `53571c04` -- `tests/test_family_chat.py` stubs `personality.maybe_respond`, removing the leaked
 fire-and-forget thread that poisoned per-test databases. Five consecutive runs of the family-chat
 pair: 137 passed each; **the full suite is 2,715 passed / 0 failed / 0 errors** (235 s).
- This closes the test-suite thread that P139 opened. The suite can now be a gate, which was the point.

**STILL OPEN**
1. **The descriptor leak (P151) is bounded, not fixed.** The conftest raise gives head-room; the
 asyncio event loops are still not closed. The honest next step is a cleanup fixture, measured by
 running the suite with the conftest raise removed to see how far it gets.
2. **The production half of the thread finding:** `_worker` should capture its database path when it is
 spawned, so a slow worker can never write state into a different database than the one it was
 created with. That is the property the test fixture works around, and it wants its own round
 (it touches every `db.*` call in the worker).
3. Queue #39 (cheap-first routing off in production while the DB override says true) still waits on the
 owner. Nothing is blocked by it.

**PROCESS NOTES (mine, worth not repeating)**
- `$(...)` inside a PowerShell double-quoted string is evaluated *locally* before ssh sees it -- twice
 this round, once silently producing `preserved: NO` for a push that had worked. Remote scripts go in
 a single-quoted here-string, always.
- A heredoc (`<<"MSG"`) inside a here-string breaks PowerShell parsing; write the message with
 `Set-Content` and pass `-F`, which is what the working path does.
- I named a fixture `no-op-on-maybe_respond` -- a hyphen in a Python identifier. Compile checks catch
 these; the lesson is to run them before the test that depends on the edit.

**NEXT**
1. The descriptor-leak cleanup fixture, measured against a full run.
2. `_worker` captures its DB path at spawn.
3. Then back to the company systems rather than its tests: the owner queue (#39), and the audit
 targets that P153/P154 displaced.

**EVIDENCE**
- `pytest tests/ -q --tb=line -rf` -> **2,715 passed, 16 skipped, 3 deselected, 0 failed, 0 errors**;
 five repeats of the family-chat pair at 137 passed; `tests/test_family_chat.py` alone 75 passed;
 `tests/test_phase3_modules.py` 45 passed; commits `caee745d`, `53571c04`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=e64488f1378ddcd0 -->
