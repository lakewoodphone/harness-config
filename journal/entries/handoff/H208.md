<!-- e:handoff|H208|2026-09-14 23:39 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:39 UTC · ZABZ-YOGA · 2026-09-14 23:40Z ZABZ-YOGA: full view into customer comms -- identity, email, waiting, agent discovery

## 2026-09-14 23:40Z ZABZ-YOGA: full view into all customer comms — identity, email, waiting, and agent discovery

### CHANGED

* **The person layer.** `people` + `person_alias` + `people_fts` are rebuilt on every
 refresh: 1,225 people (921 named), 2,199 aliases, 0 carrier labels merged, 49% of 166,902
 communications attributed to a person. Every alias carries `reason` and `source`.
* **Email is the sixth channel**: 2,846 records (Gmail triage sender+subject and the three
 messages actually sent; drafts excluded on purpose). Text is labelled `mail-subject-only`
 because bodies are not stored locally.
* **Four read modes** in `comms-search.py` / the `comms_search` dynamic tool:
 `search` (now with FTS match snippets and optional bm25 ranking), `thread` (identity-aware,
 reports which person and identifiers it used), `person` (a customer's whole history),
 `waiting` (customers whose last word was theirs), `health`.
* **`ps_comms_person` and `ps_comms_waiting`** MCP tools; `register-comms-tool.py` puts
 `comms_search` in the tool registry so agents can *find* it.
* Live index: **166,902 communications, 0 missing, base/FTS/trigram equal**, refreshed by
 cron `7,37 * * * *`, 60 s per rebuild. Verified through `/tools/run/comms_search` and
 `/tools/summary`.

### IN FLIGHT

* `~/supervised-retry.sh` on the authority — the transcript backfill, now past the Windows
 path repair: **247 successes** (was 175), 1,054 calls still without a transcript,
 supervisor on attempt 3+. `failed` also rises (1,188) because some recordings genuinely
 are not audio; those rows now record the *repaired* path so the reason is visible.

### BROKEN / NOT DONE

* **July–September calls still have no audio downloaded** (1,948 calls, no media row at all;
 the download leg stopped mid-June). 1,375 hold a `recording_url` — 34.7 audio-hours,
 ~$12.5 to download and transcribe. This is the last piece of "all the calls".
* No "waiting" line in the owner's digest yet (**D142** explains why it is its own change).

### NEXT

1. Restart the recording-download leg for 2026-07..09 and transcribe what it fetches.
2. Then decide the 3,181 "softer" re-transcripts (calls that already have Dialpad text,
 ~$44) once this round has reported its spend.
3. Add the index-age metric to `ck/sentinel.py:check_comms_freshness` (P131) — still
 unverified from a laptop, so still not shipped.

### EVIDENCE

`personal-secretary-mvp` @ `8526fa4d4`; `harness-config` @ `e383047` (the person layer rode
into that merge commit — check `git show e383047:scripts/commsindex.py`); live index state
`~/.fsearch/comms-state.json`; `comms-refresh.py --check` exits 0.

<!-- j2 tags=comms,handoff refs= alias_of= legacy_id= sha=3cae3cbe68562aa7 -->
