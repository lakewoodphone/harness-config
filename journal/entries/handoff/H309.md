<!-- e:handoff|H309|2026-09-15 21:48 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:48 UTC · ZABZ-TECH · personality-system scoring-truth: F1-F24 fix complete on agent/scoring-truth (unpushed)

CHANGED
Branch agent/scoring-truth in C:\Users\ezabz\Code\_worktrees\personality-system\scoring-truth, commit cdbc2bf 'fix(scoring): stop reporting fabricated numbers to real people'. Files: src/lib/assessment/{scoring,types,recommendations,career-fit,presentation}.ts, new src/lib/assessment/scoring.test.ts, src/components/assessment-scores.tsx (line 44 only).

IN FLIGHT
Nothing running. No push, main untouched, clean working tree.

BROKEN / OPEN
Nothing broken. Known residuals, all reported in the commit message and to the parent:
- profile.scores still carries 0 for an unanswered dimension for unowned consumers (adaptive-selection.ts, routing.ts, runtime.ts:1429, ai-payloads structuredProfile, assessment-scores.tsx rows). readings/dimensionValue are authoritative. Fixing the consumers needs edits in files this task forbade.
- 'differentiation' is now range/4 instead of range/2.4, so it drops ~40% versus the old inflated scale. Hard thresholds calibrated against the inflated value: adaptive-selection.ts:144 (<=0.45) and routing.ts:31 (>=0.4). They should be re-tuned; not changed here (unowned).
- recommendations.ts:48-197 optional-field fallbacks are dead (112/112 occupations set all 5 optional fields); trainingPathToJobZone's 'varies' branch is unreachable and the export is unused. Kept as catalog-edit guards.
- Catalog content findings NOT fixed (need research): 112 roles share only 80 distinct 34-dimension profiles (20 duplicate groups, largest 6 roles); all 16 kleiKodesh roles flattened to compensationLane 'support-now' with jobZone 4.
- The instrument is still not psychometrically validated: no norms, no reliability estimate, no validation sample. The fixes are internal correctness only.

NEXT
1. Parent: merge agent/scoring-truth serially in risk order with the other assessment agents; reconcile assessment-scores.tsx against the lint agent (my single line-44 change vs their unescaped-entity lines).
2. Then: re-tune adaptive-selection.ts:144 / routing.ts:31 for the corrected differentiation scale, and decide the catalog de-duplication with real research.
3. Then: hand a diff for the unowned consumers that still read the 0 sentinel if the owner wants the sentinel gone everywhere.

EVIDENCE
npx vitest run -> 16 files, 160 tests passed (was 103). npx tsc --noEmit -> 28 errors before, 28 after, none in a production file. npx eslint -> no new errors. Before/after numbers reproduced by executing the modules: HEAD mirrored via 'git show' into C:\Users\ezabz\Code\_scratch\before with vitest.before.config.ts, working tree via vitest.after.config.ts, both running the same harness C:\Users\ezabz\Code\_scratch\before-harness\before.test.ts.

<!-- j2 tags=scoring refs= alias_of= legacy_id= sha=8769d2188aaf2aa8 -->
