<!-- e:handoff|H226|2026-09-15 14:04 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:04 UTC · ZABZ-YOGA · The owner queue forgets now, the badge tells the truth, and a six-month-dead reminder poll exists

**CHANGED**
- **`owner_message_queue` had no ageing code anywhere** -- 163 undelivered rows, 42 of them
 urgent/critical, 40 of those 42 daily briefings from 2026-08-06 onward, 95 `ask_owner` rows
 58-109 days old. The `expired` rows that existed came from one-off scripts. Now a registered
 lifecycle-sweep phase (`_expire_stale_owner_messages`, runs every 120 s) with a per-reason
 horizon -- briefing 36 h, comms_freshness/sync_breaker 24 h, ask_owner 7 d, urgent_email 14 d,
 default 7 d -- plus supersede for briefings. Ages on `COALESCE(last_seen_at, created_at)`, the
 observation clock, and `record_owner_message_send_failure` no longer writes `last_seen_at`
 (a send attempt is not a re-observation; without that the policy would have died on day one).
 Applied to the authority: **held 163 -> 20, urgent/critical 42 -> 13, total rows 1745 unchanged**
 -- status plus a `status_note` recording why, bodies never deleted. Commit `a6b08392`.
- **`clear_owner_sync_alerts`, called from `_record_subsystem`** -- the one place every sync
 outcome passes through -- so an alert clears when the subsystem that raised it works.
 Commit `a6b08392`.
- **The kernel's `check_delivery` now measures the owner's chosen channel.** It reads the
 owner-SMS kill switch (path derived from `AUTHORITATIVE_DB`, so kernel and sender cannot
 disagree) and the badge payload's freshness. CRITICAL now means "there is no channel to him at
 all": SMS off AND payload missing or older than 45 min. Live verdict: `info` -- "owner SMS off by
 his decision (OWNER_SMS_KILL_SWITCH present, written 2026-07-20 17:57 UTC); the app surface is
 the channel -- app surface last written 1 min ago, 20 held item(s) for him, none of them a
 delivery fault". Commit `6565c94`, which also carries 123 lines of orphaned `_headline` work
 that had sat uncommitted in `ck/sentinel.py` since 2026-09-14 17:02.
- **`_run_reminder_poll` written.** `agent_bus.py:482` has imported that name every 5 minutes
 since `533267d6` (2026-02-25) and it never existed, so the loop logged
 "cannot import name '_run_reminder_poll'" for six and a half months and that path never
 delivered a reminder. It now delegates to `personal_ops.process_due_reminders`; live poll on the
 authority: `{'checked': 0, 'sent': 0, 'delivered': 0, 'sms_sent': 0, 'failed': 0}` (0 due).
 Commit `85e97f17`.

**IN FLIGHT / BROKEN**
- **`attention_debt` is still `critical`**, now on 2 six-day-old Google security alerts
 (`urgent_email`, horizon 14 d) plus "oldest question 134d old". `agent_questions` (313 pending)
 rots exactly the way `owner_message_queue` did and the new policy does not cover it.
- **The 2 `sync_needs_login` rows are still `held`, deliberately.** I first read the 04:30 ingest
 as amazon/ebay work; it is **Plaid** (`plaid-sync-20260915043005/6`). The sessions were
 re-authenticated and `state.json` written 04:01/04:15, but I have no evidence that
 `amazon_sync`/`ebay_sync` themselves succeeded, so I did not hand-clear them. The next genuine
 success clears them through the new hook, and that is the right mechanism.
- **Reminders: 396 rows, 14 pending, 0 due, and recent rows are `sent` with
 `delivery_channel='dashboard'`.** Nothing I have found renders that dashboard. If nothing does,
 "sent" is another silent drop and the reminder feature is a lie -- measure it, do not assume.
- **A second live session is working the same repo right now** (`5a6c3b66` at 14:02, ~17 modified
 files that are not mine). Every commit of mine used explicit pathspecs and I verified the hunk
 headers first, so no foreign work was swept in.

**NEXT**
- Extend the shelf-life policy to `agent_questions` -- same disease, 313 pending, oldest 134 d.
- Make `attention_debt`'s severity scale with the freshness of what is live, so a week-old alert
 cannot keep the badge permanently red (an alarm that can never go green gets ignored).
- Observe the sync-alert hook firing: `autopilot: cleared N owner alert(s) after <name> succeeded`.
- Find out whether anything renders reminder channel `dashboard`.

**EVIDENCE**
- Queue before/after measured on the live database; badge payload re-read after
 `run-sentinel.sh` (written 2026-09-15T14:02:24Z); kernel verdicts printed from
 `ck.sentinel.delivery()`/`attention_debt()`; 13 new tests pass
 (`tests/test_owner_message_shelf_life.py` 8, `tests/test_owner_sync_alert_resolution.py` 5);
 health 200 after restart; every commit hash above is in `git log` on the authority.

<!-- j2 tags= refs= alias_of= legacy_id= sha=2e816f9a44cdb481 -->
