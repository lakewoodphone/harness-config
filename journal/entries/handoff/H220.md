<!-- e:handoff|H220|2026-09-15 03:10 UTC|ZABZ-YOGA|open -->
## 2026-09-15 03:10 UTC · ZABZ-YOGA · Her machines: full power, company access, self-learning loop live

STATE OF PLAY 2026-09-15 ~02:00-03:15 UTC, from ZABZ-YOGA. Owner instruction being executed: make
Yocheved's agent as powerful as the owner's, fully self-learning, do NOT cripple it, and label
everything that comes from her machines. Her laptop and the office Mac mini are the targets.

CHANGED (all verified on the machines, not assumed):
* Both her machine configs dropped `permission.defaultPreset: workspace-write` for danger-full-access,
 and gained `agent-loop.maxParallelToolCalls: 20`. Her presets had ZERO MCP blocks where the owner's
 have seven; that gap is now documented and the company-access path is delivered instead.
* Both machines run the vision model. The Mac's settings had `provider: deepseek-direct` while
 llm-pi-ai defined only `deepinfra` -- a provider that did not exist -- so images did nothing there.
* scripts/harness-sync.mjs -- the sync duty in pure Node (the two existing appliers could not reach her
 Mac at all). It parses the merged settings back and refuses to install a document naming a provider
 no route defines.
* scripts/ps-system.mjs -- her agent's read client for the company's own systems. Proven FROM both her
 machines: it returns the owner's archived sessions and sees 5 machines.
* Session shipping: laptop 9 sessions / 462 rows / 1798 KB (re-run ships 0 -- idempotent); the Mac
 ships hourly via launchd. Archive now shows desktop-fgv6kmh and lakewooecsmini.
* Recurring jobs on both machines: PersonalSecretary-PushDSHSessions (hourly) and
 PersonalSecretary-HarnessSync (15 min) on Windows; dsh-session-sync (hourly) and harness-autosync
 (15 min) on the Mac. All verified running with result 0.
* Self-learning loop CLOSED and demonstrated: her Mac appended a journal entry, autosync rebased it
 over the remote, applied settings, and pushed the entry back to the shared remote, where this
 machine can read it tagged LAKEWOOECHSMINI.
* Her agent's instructions now cover: which machine it is on, full access on purpose, the company
 archive (ps-system), the journal as a working habit, and origin labelling.
* Laptop web search fixed (was dead every call -- see L1501) and her engine is running in her session
 with the window visible.

BROKEN / CAUTION:
* Her laptop cannot reach the authority over Tailscale (logged out; NeedsLogin). Its harness-config
 copy is therefore delivered via the exec channel, not by git pull. The Mac pulls properly.
* YochevedHarness NSSM service is gone; stale `engine.cmd` errors remain in an old service log only.
* I delivered a parse-broken dshw.ps1 to her laptop for a short window and then fixed it (L1502).

NEXT:
* Confirm the search test result (headless run in flight).
* Expand her agent's tool-belt: her laptops currently reach the company through ps-system (read-only).
 The secretary MCP (ps_* tools) is the richer path and is proven reachable from the Mac via Tailscale.
* Feed findings from her real sessions into her instructions continuously -- that IS the learning loop.

EVIDENCE: laptop engine pid 10896 session 1 task result 0 port 3099 LISTENING; Mac engine pid 3468;
Mac autosync status.json result ok; fleet stats show 5 machines.

<!-- j2 tags= refs= alias_of= legacy_id= sha=7672ffbdcb33a211 -->
