<!-- e:handoff|H290|2026-09-15 20:50 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:50 UTC · ZABZ-TECH · Lakewood Rentals: audited, hardened, deployed, and one human sign-in short of proven

CHANGED
- Lakewood Rentals was audited by six read-only workstreams (reports in `_scratch/rentals-audit/`: auth-sso 16 findings, data-api 37, photos-moderation 17, ui-maps 24, ops-robustness 28, tests-verification 17) and then fixed by parallel agents in git worktrees under `_worktrees/personal-secretary-mvp/`.
- **Merged and verified** (integration branch, tsc 0 / 44 tests / production build / deploy-verified live): `agent/rentals-auth-sso`, `agent/rentals-ui`, `agent/rentals-ops`, and the committed spine of `agent/rentals-tests`. **Still unmerged:** `agent/rentals-core` (Tier 1 blockers; it was re-prioritised after it spent its first hours on Tier 2/3 items — check its branch state before doing anything else).
- **Live in production** (`https://rentals.lakewoodphoneandtech.com`, container `lakewood-rentals` on lpt-apps-01): the SSO fix (the callback now presents `Cookie: lpt_refresh=…` to the authority — this single line was why every sign-in returned 401), the GET callback handler (the documented flow redirects the browser to it with a GET and the route was POST-only), password sign-in disabled (`password_auth_disabled`), CSRF fail-closed, rate-limit keying fixed, the anonymous header no longer shows "Log out"/"Admin", robots/sitemap/favicon serve from app routes, a missing listing returns a real 404.
- **Infra:** `/opt/caddy/Caddyfile` refuses any `/uploads` key outside `listings/` (this closed a live exposure — the route could read any object in the bucket that holds the nightly `pg_dump` of every database and a copy of every `/opt/*/.env`); the repo's canonical Caddyfile was mirrored from the box (it had no `rentals` block at all and following its own instructions would have reopened the hole); proxy headers now set from the real peer; world-readable `.env` backups on the box moved to 600.
- **Data:** the nightly job now takes a `VACUUM INTO` snapshot of the rentals SQLite volume, verifies it has tables, and fails the run if the set is empty — before this the app's only database was backed up nowhere. A restore drill was performed end to end (restic restore → 13 tables → `integrity_check ok` → the production image served `/api/health` 200 from the restored copy). 22 GB of stale build cache pruned (disk 83% → 58%).
- **Deploy is now one verified command:** `apps/rentals/scripts/deploy-rentals.ps1` (ops branch; my working copy is `_scratch/rentals-deploy/deploy-rentals.ps1`) preflights, syncs by archive, rebuilds with the real exit code, then checks health, network membership, product behaviour, the four `/uploads` probes, and content-identical drift.
- **Alerting:** `~/bin/box-health-check.sh` on the secretary (cron `15 5 * * *`) checks backup freshness, the presence of the rentals SQLite snapshot, disk, and the public health endpoint, and creates an owner-queue row on failure. Verified by forcing a failure.

IN FLIGHT
- `agent/rentals-core` — the ten Tier 1 items I re-sent it (uploads key confinement, list-endpoint visibility, photo-delete scoping, the dead `return` + PATCH semantics, completeness scoring, `fit:"inside"`, body cap, usable admin queue, lazy DB open, `sha` in health). Commits one at a time; check `git log --oneline master..HEAD` in its worktree.
- `agent/rentals-tests` — has 5 uncommitted files (gate.mjs, integration.test.ts, deployment-live.md). Its committed spine is already merged; the uncommitted part fixes the remaining gate ordering trap.
- **The owner has not signed in yet** (prod `users` = 0), so the last link of the SSO chain is unproven.

BROKEN / OPEN (honest)
- The full Google sign-in has never completed by a human. Everything else is verified: the start leg reaches Google's consent screen with the correct redirect URI, the authority accepts the shared secret, and the callback is proven to contact the authority (distinct codes with and without a token), plus unit tests asserting the Cookie header.
- Tier 1 core items are NOT deployed. The board is empty and `/uploads` is edge-blocked, so nothing is currently exploitable — but the protected-tier leak, photo-delete IDOR and the photo crop are real defects sitting in the code.
- Secrets that were within reach of the exposed bucket (`LPT_SSO_SHARED_SECRET`, the R2 key pair) should be rotated; rotation of the SSO secret is a coordinated two-sided change I have prepared but not performed.
- Owner queue #46 (the storefront's own Google sign-in links to a relative `/api/...` on an apex that 404s every `/api` path — verified live) and #47 (listing photos still share the backup bucket; needs a Cloudflare token with R2 edit).
- `next@15.2.4` warns of CVE-2025-66478. Do the bump when no agent worktrees are live — a lockfile change invalidates every worktree's `node_modules`.
- The restore drill matched 0 rows to 0 rows: it proved the mechanism, not that real data survives.

NEXT
1. Merge `agent/rentals-core` when it commits, run `npm run gate` on the merged tree, then deploy and re-verify with the deploy script.
2. Ask the owner to complete one sign-in at `https://rentals.lakewoodphoneandtech.com/auth`, then confirm a `users` row appears with `sso_subject` set and that he landed as admin (`ADMIN_EMAILS=eliyahu@lakewoodphoneandtech.com` is live).
3. Rotate `LPT_SSO_SHARED_SECRET` on both sides in one window; then handle the R2 bucket split once the owner supplies a scoped token.
4. Bump Next; re-run the gate; deploy.

EVIDENCE (all run by me, 2026-09-15)
- `curl -s -X POST <rentals>/api/auth/sso/callback` no cookie → `{"code":"no_lpt_session"}`; with `Cookie: lpt_refresh=bogus` → `{"code":"lpt_rejected"}`.
- `POST <rentals>/api/auth/login` → `{"code":"password_auth_disabled"}`.
- `POST <rentals>/api/auth/logout` with no Origin and no Referer → 403 `csrf`; cross-origin → 403; same-origin → 200.
- `/uploads/config`, `/uploads/data/6e/x`, `/uploads/keys/x`, `/uploads/lpt-apps/config` → all 404 (they were 200 before the Caddy rule).
- `/robots.txt` → 200, `Allow: /`, no `Disallow: /listing/`; `/sitemap.xml`, `/favicon.ico` → 200; `/listing/999999999` → 404.
- Anonymous `/` in a browser at 375 px: nav "Browse | List a unit | Feedback | Sign in", no "Log out"/"Admin", no horizontal overflow.
- `docker inspect lakewood-rentals` → healthcheck `healthy`, log driver `json-file max-size=10m max-file=3`, memory 384 MB, pids 512, on networks `lakewood-rentals_default lpt_default`.
- restic: `sqlite: lakewood-rentals -> sqlite-lakewood-rentals.db (13 tables)`, 20 snapshots, no errors; restored DB `tables=13 rows=0 integrity={"integrity_check":"ok"}`.

<!-- j2 tags=rentals,lpt,sso,deploy,fleet refs= alias_of= legacy_id= sha=8e49795696581e0c -->
