<!-- e:handoff|H213|2026-09-14 23:49 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:49 UTC · ZABZ-YOGA · 2026-09-15 00:05Z ZABZ-YOGA: round 2 -- freshness with two halves, voicemail audio unblocked, all-time proven

## 2026-09-15 00:05Z ZABZ-YOGA: round 2 — freshness with two halves, voicemail audio unblocked, "all time" proven

### CHANGED

* **`health` reports ingestion age per channel, not just index age** (`comms-search.py`,
 committed `94f5755`). Budgets enforced only on the channels that flow daily (sms, call:
 30 h); voicemail/email reported; the frozen UI-crawl channels labelled supplementary.
 Problem text names the fault: "the index is current but the database is not being fed
 (P29)". Verified live through `/tools/run/comms_search` and installed to `~/.fsearch`.
* **Voicemail audio is now reachable.** A voicemail call's only media URL is
 `https://dialpad.com/v/<call_id>`; the downloader only read `recording_url` /
 `admin_recording_urls`, so 693 voicemail calls had no audio *by construction*.
 `voicemail_link` is now a source URL and the transcriber accepts any audio kind
 (`recording%` or `voicemail`). Measured: 693 → 682 voicemail calls without audio inside a
 minute, and falling. `personal-secretary-mvp` @ `da3c0f8b6`.
* **"All time" is proven for calls.** Probed 7-day windows in 2024-10, 2025-01, 2025-04,
 2025-07 and 2025-09 through the real harvest path: `read=0 stored=0 errors=0` in every
 one. The API answered and the answer is "nothing there", so 2025-10-20 is the account's
 first call, not the harvest's horizon.

### MEASURED BACKLOG (the numbers the budget is sized against)

| bucket | calls | audio minutes | ~cost |
|---|---|---|---|
| **no transcript at all** | 6,763 | **2,551** | **$15.3** |
| local audio, no successful re-transcript (includes calls that already have Dialpad text) | 4,415 | 7,537 | $45 |
| a URL but no local audio (being downloaded now) | 3,038 | 3,060 | $18 |
| voicemail calls without audio | 682 | ~short | ~$2 |

The **cumulative transcription cap is 6,000 minutes (~$36)**, so the entire *no-transcript*
gap (2,551 min) fits with room to spare; the cap exists for the softer "quality swap" class
(~$45), which stays deferred.

### IN FLIGHT

`~/supervised-download.sh` and `~/supervised-retry.sh` on the authority, both idempotent and
both restarted at 23:48Z with the new selectors. Recording files 3,232 and rising; 253 calls
transcribed / 352 minutes (~$2.1 spent of the cap).

### NEXT

1. Let both jobs converge; then re-measure the backlog and report the actual spend.
2. Re-run the downloader's dry run until it selects zero, which is the signal that every
 media URL we know about has been fetched.
3. Still not shipped: the kernel's index-age metric (P131) — `ck/sentinel.py` is being
 edited by another session (` M ck/sentinel.py` on the authority at 23:45Z), so a change
 there would collide. The information is available to any agent through `health` in the
 meantime.

### EVIDENCE

`health` output above is live from `/tools/run/comms_search`; `comms-refresh.py --check`
exits 0 with `166,902 indexed, 0 missing`; both supervisor logs on the authority;
`harness-config` @ `94f5755`, `personal-secretary-mvp` @ `da3c0f8b6`.

<!-- j2 tags=comms,dialpad,handoff refs= alias_of= legacy_id= sha=8ad95c78cff355a5 -->
