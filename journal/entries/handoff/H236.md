<!-- e:handoff|H236|2026-09-15 14:49 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:49 UTC · ZABZ-YOGA · The round-35 sync hook is observed in production, and three more pains resolved by measurement

**CHANGED**
- **The sync-alert clear is verified live** (round 35's open claim, closed). Triggered the real
 endpoints: amazon -> "cleared 3 owner alert(s) after amazon_sync succeeded", ebay -> "cleared 4".
 All seven `sync_needs_login`/`sync_breaker` rows are `resolved`, each with the sync's own success
 message as its note. Badge: held 20 -> 13, urgent 11 -> 9.
- **Both browser syncs work**: `status: ok`, `download_rc: 0`, `ingest_rc: 0`, 0 new transactions
 (nothing new since 04:30). The two alerts I declined to hand-clear in round 35 were stale, and are
 now cleared by the mechanism rather than by a guess.
- **P130 fixed in the reader** (`journal.py`): `_maybe_refresh_questions()` refreshes a mirror older
 than an hour and the page prints its age. Verified: page 12 open (was a stale 11), mirror
 provenance "38 row(s) read, 12 pending, generated 2026-09-15 14:48 UTC", authority stats 12.
- **Three pains resolved with measurements**: P135 (append --dry-run 3,422 ms, not 3 minutes),
 P146 (827 entries, 0 duplicate ids; cross-machine allocation H232-H235 landed beside H229-H231
 with no collision), P130 (above).

**IN FLIGHT / BROKEN**
- **P-new: `run_subsystem_once` cannot run the five browser syncs** -- the endpoint built to run one
 subsystem on demand does not know them, so a hook on any of them is unverifiable through the tool
 designed for it. The working trigger is `POST /finance/{amazon,ebay}/sync`.
- **P-new: only `journal.py` refreshes the mirror.** A hand `cat` of `state/owner-questions.md`
 still sees whatever is on disk (its Provenance line carries the stamp). Accepted: the alternative
 carriers were worse.
- Open pain now **88** (was 96 when the triage began in round 37; two resolved in round 38 and three
 in this one).

**NEXT**
1. Continue the triage: the remaining entries have not been read at all, and this round's three
 resolutions came from measuring rather than reasoning. Cheap next candidates: P139 (test suite
 cannot run green), P133 (`log/**` still writable), P59 (the phone gate's death is unexplained).
2. Teach `run_subsystem_once` the sync names, so the next verification of a sync hook does not need
 a detour through a different route.

**EVIDENCE**
- The two endpoint responses and the two `cleared N owner alert(s)` log lines quoted above; the
 seven `resolved` rows with their notes; badge payload `2026-09-15T14:48:56Z` (held 13);
 `check` 0 errors; five resolutions recorded in `state/status.tsv`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=27b996935ccbf1d9 -->
