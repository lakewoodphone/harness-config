<!-- e:handoff|H236|2026-09-15 16:35 UTC|ZABZ-TECH|open -->
## 2026-09-15 16:35 UTC · ZABZ-TECH · Newman virus case: AutoIt USB worm confirmed on both drives, 12.7 GB data intact and copied off, deliverable medium awaits the owner

## 2026-09-15 ~17:15 UTC · ZABZ-TECH · Newman: the virus is real (AutoIt USB worm), data intact, copy running, one owner decision outstanding

CHANGED
- Newman case (347-768-0413, two flash drives, "virus") went from intake to **confirmed diagnosis**.
 Both drives carry the **AutoIt USB-shortcut worm**: `Microsoft/MicrosoftSecurity.exe`,
 **SHA-256 `51fe204cc7fdaf39c7f55fe537e2538a315ba27c83025c24ee1dc34b69936025`, identical on both drives**,
 **ClamAV `Win.Malware.Autoit-9880096-0`**, plus `Microsoft/Microsoft.a3x` (obfuscated AutoIt script) and
 32 (Kingston) / 56 (Cruzer) decoy `.lnk` files whose command line is
 `cmd.exe /c start Microsoft\MicrosoftSecurity.exe /AutoIt3ExecuteScript ..\Microsoft\Microsoft.a3x …`.
 The identical decoy directory set on both drives (`My Pictures`, `My Videos`, `Reports`, `Statments`,
 `Microsoft`, `WinddowsUpdateCheck`) is the worm's signature. Cruzer also has
 `WinddowsUpdateCheck/WinddowsUpdater.exe` — ClamAV "OK" on that one, which is **not** a clearance.
- **The customer's data is intact**: Kingston 1.7 GB / 90 files, Cruzer 11 GB / 519 files, ~**12.7 GB**,
 mostly 334 MP3 recordings (verified real MPEG layer III, ID3v2.3, 192 kbps, `ffprobe` reads full durations).
 Lifted read-only to `/mnt/atlas-recovery/newman-3477680413/recovered/{kingston,cruzer}` excluding only
 worm artifacts + Windows system folders (`notes/excludes.txt`), after proving those decoy folders held no
 user file.
- Case file updated with the full evidence table; sync record updated. Verification pass (checksum diff vs
 source, per-file magic bytes, `clamscan -r`, SHA-256 manifest) was **queued to run itself** when the copy
 ends — log `logs/verify.log` in the engine case dir.
- Bench: ClamAV + exfatprogs + fatattr + mtools installed; ddrescue images for both drives **paused at
 ~1 % / ~1.3 %** with mapfiles kept (resume with `lpt-recovery ddrescue-start … retry`), deliberately, so
 they would stop competing with the customer copy for the same USB controller.

IN FLIGHT
- rsync copy of ~12.7 GB (detached, `logs/extract.log`) then the verification pass (`logs/verify.log`).
 Nothing depends on me watching it. **Next session: read both logs, then finish the deliverable.**

BROKEN / OWED
- **One real owner decision outstanding: how the data is handed back.** (a) wipe their Kingston + Cruzer and
 return the data on the Kingston — free, no purchase, but the wipe needs the owner's explicit per-action yes;
 or (b) fresh shop USB, leave their drives as they are — the $15/USB line he set on Abramczyk 2026-09-14.
 Recommendation is (a). Asked in the session that diagnosed this.
- **The customer's PC is probably still infected** — this worm re-infects every removable drive it sees, so a
 clean drive is re-infected on first use. Not priced, not quoted, owner's call whether to offer.
- Prior impediments unchanged: this ZABZ-TECH lpt-hub is `ahead 29, behind 2442` (commit `1e532c120` is
 local-only); stale `czermak-7183147956` case lock; `/mnt/shlesinger-live` I/O error.

NEXT
1. Read `logs/extract.log` + `logs/verify.log`; the copy must show ZERO checksum differences against the
 sources and `clamscan -r` must report 0 infected.
2. Get the owner's medium decision, then build the clean deliverable (32 GB is enough for 12.7 GB) and put the
 manifest on it.
3. Optional, only if the owner says yes: resume the ddrescue images for the record; wipe the drives per his
 answer; offer the PC cleanup.

EVIDENCE
- Engine case `/srv/recovery/cases/20260915-122509-newman-3477680413` (agent token `c50388db48b70ccae84250d49b0cf52a`),
 `notes/` holds `intake.md`, `excludes.txt`, `lnk-files.txt`, `lnk-verdicts.txt`, `mp3-bad-headers.txt`,
 `deliverable-manifest.sha256`; `maps/` holds the resumable ddrescue mapfiles.
- Case file `lpt-hub/docs/customer-operations/cases/flash-drives-newman-3477680413-2026-09-15.md`.

<!-- j2 tags= refs=newman,linux-pc,lpt-hub alias_of= legacy_id= sha=eccb2f1520edbac1 -->
