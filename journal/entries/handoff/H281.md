<!-- e:handoff|H281|2026-09-15 20:19 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:19 UTC · ZABZ-TECH · fleet-api silent-failure fixes committed (829a7b97), not pushed, not deployed

CHANGED
- worktree C:\Users\ezabz\Code\_waze-fleet\psm-fleetapi, branch fix/fleet-api-silent-failures, from origin/master tip e17f2374.
- Commit 829a7b97 "fix(waze-mdm): make the fleet-api's silent failures visible". NOT PUSHED.
- deploy/waze-mdm/fleet-api/fleet_api.py — F1..F7 of the SYSTEM-AUDIT "silently does nothing" class.
- deploy/waze-mdm/fleet-api/test_silent_failures.py (new, 24 tests), conftest.py (new), test_fleet_monitor.py (3 assertions updated for the new 3-tuple).
- Before: 119 passed / 27 failed. After: 146 passed. Deleted no data.

IN FLIGHT
- Nothing. The fix is committed and the suite is green. Deployment is somebody else's decision: the running
 container on the Hetzner box still has the old code, so NOTHING here has taken effect in production yet.

BROKEN / NOT FIXED (deliberate, each with the reason)
- `telnyx_billing.cents()` still coerces an unparseable amount to 0 (audit finding 8). Outside the restricted
 file scope for this task (deploy/waze-mdm/fleet-api/** was the only region permitted, and I read that file
 to confirm — the same shape as F6, so it should be next).
- The monitor's `INSERT INTO fleet_device_health_history` still writes the schema defaults `0, 1` for
 `queue_depth, push_ok`: the monitor never probes the queue, so those two are claims it cannot support, but
 making them NULL is a migration and the monitor's own measured columns (serial, state, last_checkin_age)
 are correct today. The snapshot endpoint — the one that DOES probe — now writes NULL on failure.
- `docker-compose.yml` is out of scope. F2 needed no compose change: the healthcheck uses
 `urllib.request.urlopen`, which raises on a non-200, so a 503 from `/health` is already caught.

NEXT
- Push 829a7b97 when someone owns the deploy, then diff the running container's `fleet_api.py` against it.
 Finding 22 in the audit (fleet_api_remote.py is a 2803-line near-duplicate that is NOT built) is the trap
 here: do not "restore the other file".
- Two contract changes a caller must hear about: (1) `POST /fleet/monitor/run` gains `alert_errors` and can
 now answer `complete: false` where it previously said true; (2) `/waze/device/{serial}` returns
 `consumed_data_mb`/`remaining_mb`/`percent_used` as `null` (not 0) and `cap_band: "unknown"` when the
 Telnyx usage read fails, plus a new `usage_known` boolean. I grepped the portal repo (phone-and-tech-full,
 10,602 tracked files) with `git grep -I` for `cap_band|consumed_data_mb|percent_used|waze/device` and got
 zero hits, so nothing tracked there reads these fields yet.

EVIDENCE
- `python -m pytest deploy/waze-mdm/fleet-api -q` → BEFORE (HEAD fleet_api.py, tests present): "27 failed,
 119 passed in 12.02s". AFTER: "146 passed in 11.12s".
- New bundle alone: "24 passed in 0.50s". Per-finding failure lines quoted in the report to the parent agent.
- Re-derived, not trusted: the audit's `nanomdm_gateway_compat.py:304-319` citation for `all_queue_health`
 returning only `{"devices": [...]}` is CORRECT (I wrote that to the "already fixed or wrong" bucket
 prospectively and had to take it back out). The `fleet_queue_snapshots` zero-INSERT claim is confirmed by
 grep in the shipped file: CREATE at :197, DELETE at :959, UPDATE at :3820, no INSERT.
- `_adapt_datetime` verified to convert the new stale query to `NOW() + %s * INTERVAL '1 hour'` under
 `USE_PG = True` and to leave it byte-identical under SQLite.

<!-- j2 tags=waze-mdm,fleet-api,personal-secretary-mvp refs=commit:829a7b97 alias_of= legacy_id= sha=76cfd7e5b0c20b5a -->
