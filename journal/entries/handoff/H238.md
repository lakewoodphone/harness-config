<!-- e:handoff|H238|2026-09-15 15:06 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:06 UTC · ZABZ-YOGA · Config templates now match the code; the pre-existing failure set is 9 to 6 with two clusters left

**CHANGED**
- **Three of the nine pre-existing suite failures are fixed** (`01ab2dc1`, preserved on the GitHub
 branch): `tests/test_config_contract.py` 5 passed, and the four-file set went 9 failed -> 6 failed
 / 106 passed. Two root causes: the contract ignored `validation_alias`, and both env templates were
 incomplete (21 undocumented settings in `.env.example`, and `web/.env.local.example` missing
 entirely). `missing_keys` 24 -> 0, `extra_keys` 3 -> 0.
- `web/.gitignore` now un-ignores `.env.local.example`, so the fix survives a fresh clone.
- My round-41 correction stands as written in L1519; this round implemented the fix it described.

**REMAINING FAILURES (measured after the fix, 27 s to reproduce)**
`test_ai_gateway` 3 (`test_sanitize_tool_call_out_of_workspace`,
`test_sanitize_response_tool_calls_blocks_unsafe_file_tool_cleanly`,
`test_sanitize_response_tool_calls_preserves_safe_and_blocks_unsafe_mix`),
`test_vscode_chat_extractor` 1 (`test_discover_workspaces_aggregates_matching_workspace_profiles`),
`test_assistant_chat_prompt_profiles` 2 (routing-telemetry tests). All are one cluster of two --
the ai_gateway trio is one sanitiser, the prompt-profile pair is one telemetry path -- so two
focused rounds should clear P139 rather than six.

**NEXT**
1. The `test_ai_gateway` sanitiser trio: three assertions about the same function, so diagnose once.
2. Then the prompt-profile telemetry pair, then `test_vscode_chat_extractor`.

**EVIDENCE**
- `missing_keys: 0 []`, `extra_keys: []`, `duplicate_keys: []` from
 `build_config_contract_report`; `pytest tests/test_config_contract.py -q` 5 passed;
 `git ls-files web/.env.local.example` lists it; commit `01ab2dc1`; worktree still holds the other
 session's 79 modified files, untouched.

<!-- j2 tags= refs= alias_of= legacy_id= sha=48534f54f239e4f4 -->
