<!-- e:handoff|H317|2026-09-15 22:12 UTC|ZABZ-TECH|open -->
## 2026-09-15 22:12 UTC · ZABZ-TECH · 2026-09-15 final: four of five interface workstreams integrated, and the fix surfaced a hidden 511-item queue backlog

Four of the five audit workstreams are integrated and pushed; the fifth (portal frontend
robustness) is still running. Final refs tonight: personal-secretary-mvp origin/master
4c3ab1e8 (16 commits this session), phone-and-tech-full origin/test ecd34e82c (11 commits),
lpt-hub origin/main 10fec5f12 (the audit itself).

INTEGRATED AND LIVE
- fleet-api: liveness fields deployed (image revision 95c562ba). 65 devices -> ever_replied
 true 50 / false 13 / null 2; DRN 5, 15, 27, 39, 54 have never once replied. Before tonight the
 staff panel called all 65 "Never seen" and the silent count was structurally zero.
- fleet-dashboard: command honesty deployed (image c222d9cf), 37/37 files byte-identical to the
 commit blobs. Erase/lock no longer toast success on an ok:false response; bulk lock/restart
 report real counts; /api/health probes; the three 404 endpoints answer real derived data.
- The dashboard's build context had been broken on the box, which is why its image was frozen at
 2026-07-05: four months of dashboard changes had never shipped. Documented in compose (12aa9e1f).
- .gitattributes: the *.py eol rule existed but was SCOPED to scripts/waze/*.py, so it protected
 exactly the files someone had already been hurt by and left every other Python file exposed -
 which is why 21 .py files arrived CRLF during the deploy. Global rule added (4c3ab1e8).

INTEGRATED, NOT DEPLOYED
- Portal contract hardening (origin/test ecd34e82c): queue_issues/push_issues null no longer
 coerced to 0; getMyWazeDevices now reports devices it could not read instead of silently dropping
 them; a fleet-api outage is BAD_GATEWAY instead of the false statement "DRN X has no provisioned
 Waze SIM"; the invented /profiles/observed route replaced with the real one; all four env
 templates now say the canonical base https://mdm.abletelsolutions.com/api, and a base ending in
 a route family is rejected at startup rather than silently returning an empty fleet.
- The proxy/fleet-page auth fix (psm master 321f167e): /shadow deleted; fleet reads go through
 session-checked handlers that inject the token server-side.

A REAL NUMBER THE FIX IMMEDIATELY SURFACED: live /api/fleet/health now reports queue_issues 511
(source "gateway"), push_issues 0. The staff page had been rendering "0 queue issues" from a
coerced null. 511 is a real command-queue backlog that nobody could see. Next session should look
at what those 511 are.

STILL OPEN: the portal frontend workstream (error branches on the staff fleet page, the missing
navigation entry, the 4xx retry predicate that consumes four rate-limit attempts per typo, and the
CustomerSettings failure that erases a customer's address on Save). Also: the portal needs
deploying before any of the API-level fixes reach a screen, and the portal collapses JSON null to
false so the 2 no-enrolment devices will read "No reply on record" instead of "cannot tell".

<!-- j2 tags=portal,fleet-dashboard,handoff,final refs= alias_of= legacy_id= sha=7747be1da2f0742b -->
