<!-- e:handoff|H218|2026-09-15 02:27 UTC|ZABZ-YOGA|open -->
## 2026-09-15 02:27 UTC · ZABZ-YOGA · lpt-sync: three defects fixed, verified dry-run only, pushed to lpt-sync-precreate-guard-20260915

CHANGED
 personal-secretary-mvp, secratary, /home/zabz/personal-secretary-mvp
 Pushed additively: 7378193d + 858ce073 -> origin refs/heads/lpt-sync-precreate-guard-20260915
 (was e0a1311d, now 858ce073). master NOT touched (still 7a566b6e on origin,
 local master 8a68ac4b). No force, no pull, no rebase, no service restarted.

 app/services/website_db.py +48/-15 create_order(customer_id=...)
 scripts/lpt-sync-to-production.py +543/-60 the three fixes
 tests/test_lpt_sync_precreate_guard.py +504/-... 39 -> 61 tests

 A. DUPLICATE CUSTOMER ROWS. push_to_production created the customer, then
 create_order called create_customer AGAIN; with an unmatchable phone both
 INSERT (weichbrud: 2610 orphan + 2611 with the order; the tool printed
 2610). Fixed by handing Step 1's customer id to create_order as
 customer_id, and reporting the id the ORDER references.
 B. GUARD HOLE. _sibling_claim returned early when the NORMALISED phone was
 empty, so the rule did nothing while sibling_checked still said True.
 Case identity is now same-caseId OR same-phone, AND >=3 shared device
 tokens; sibling_checked reports whether a candidate was actually compared
 and the sweep prints the count.
 C. WRITE-BACK. Now references.websiteOrder only; productionOrder and the
 workflow keys are preserved when they hold anything. Only the changed
 container is re-serialized, at the file's own indent (no whole-file dump).

VERIFIED (dry-run / read-only only, ZERO production writes)
 * the 3 false CREATEs (chaim-margulies-...-15ijl7--82lx-2026-07-23,
 laptop-kaplan-2026-08-24, nikon-godfrey-2026-08-24) now SKIP/sibling,
 naming WO26072412522 / WO2608130006 / WO2608140001, matched_on=caseId;
 * weichbrud and dell-latitude-3540 (already pushed) still SKIP/linked_order;
 * FULL SWEEP 368 records: CREATE 14 -> 12 (after the 2 live pushes) -> 9.
 sibling 13 -> 16, linked_order 142, overlap 27, should_sync_false 174,
 ERROR 0. 9 == 14-3-2 exactly, no new record appeared;
 * control snapshot around the dry runs IDENTICAL: customers 540, orders 584,
 tickets 613, status_history 1164, customer_notes 954, max customers.id
 2611, max orders.id 2876, ticket_counters.lastValue 12711;
 * no file in the lpt-hub corpus changed (git status clean there);
 * 61 passed with LPT_SYNC_DB_TESTS=1 (was 39 passing).
 Artifacts: /tmp/lptrecon/sweep-fixed.json, sweep-fixed-lf.json,
 control-before.json, control-after.json, control-final.json,
 diff_sweeps.py. ONE artifact dir worth adding: /tmp/lptrecon/diff_sweeps.py
 re-prints the whole before/after proof.

BROKEN / NOT DONE
 * WEICHBRUD RECORD IS STILL DAMAGED ON DISK. The 2026-09-15 live push
 replaced its references.productionOrder {system: ebay, orderNumber:
 09-15137-18879, linkedAt 2026-09-07} with the website order. Pre-push state
 is in lpt-hub git 75e78012^. Not repaired -- the mandate was code + dry
 verification and the corpus repo was explicitly off limits to touch. Needs a
 one-field restore under a mandate that covers the lpt-hub corpus.
 * The push also carried 6 UNRELATED commits that were already between the
 remote branch tip and local HEAD (da83fc60..27d7d51c, work-loop/tasks).
 Capping them would have meant rewriting the branch, which the instruction
 forbade, so the branch now contains them. Not my change, but the branch is
 no longer only-lpt-sync.

NEXT
 * Restore weichbrud's eBay supplier PO (under a corpus mandate).
 * Re-verify the customer fix against LIVE production on the next real push:
 the one thing a dry run cannot show is that a real push now produces exactly
 one customer row. Read max(customers.id) before and after and confirm it
 advances by exactly 1 for an unmatchable-phone record.
 * The 9 remaining CREATEs are the real backlog (data-recovery-347-534-6208,
 eli-green-visible-s222, iphone-13-14-mordecai, laptop-chaya-brodie,
 laptop-weingarten, lp140wfh-gutterman, pixel-6a-bloch,
 samsung-tab-s7-customer, sd-card-czermak). Four of them (weingarten,
 pixel-6a-bloch) were in the double-customer set, so they would have orphaned
 a row on every push before this fix.

EVIDENCE
 journal L1491-L1493; sweep numbers and control snapshots as above; commit
 858ce073 diff.

<!-- j2 tags=lpt-sync,secratary,production refs= alias_of= legacy_id= sha=487dfba2a40fa29a -->
