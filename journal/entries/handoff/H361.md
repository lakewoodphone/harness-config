<!-- e:handoff|H361|2026-09-16 00:25 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:25 UTC · ZABZ-YOGA · Device-info leak closed: the case push never passed device_info, so all 62 orders from the 2026-08-27 run have none; fixed and anchored, end-to-end proof is the next push

## 2026-09-16 00:45 UTC · ZABZ-YOGA · Addendum to H359: the device-information leak is closed (62 orders, 0 with deviceInfo)

CHANGED
- **`scripts/lpt-sync-to-production.py` now passes `device_info` to `create_order`.** It never did, and
 `create_order` writes `device_json` only when given one
 (`app/services/website_db.py`: `device_json = psycopg2_json(device_info) if device_info else None`),
 so **every order this tool has ever created carries NO device record**. Measured: **62 orders created
 in the 2026-08-27 push window, 0 with `deviceInfo` set** — exactly axis A's number, and axis A showed
 coverage was already falling (18/18 Dec 2025 -> 38/161 Aug 2026 -> 0/5 Sep 2026).
 Note `devices` (1 row) is a dead table and irrelevant to this; the live device record is
 `orders.deviceInfo`, which the intake path does populate (337/339 `repair_intake_cases` promoted).
- Patch: `apply/patch-sync-device-info.py`, two hunks, both anchors asserted exactly once, compiled,
 backup `scripts/lpt-sync-to-production.py.bak-w4-deviceinfo-20260916`, md5
 `ce7b28016edfffec25f1f129dc9759c0` -> `efb4a1213c802e1ed77373b585a95471`. One file, no DB touched.
- The record's OWN `device` block is passed through unchanged, so nothing is invented. Verified the
 data is real rather than a stub: `2026-09-14-lenovo-ideapad-slim3-15abr8-screen.json` carries
 `brand`/`machineType`/`model`/`mtm`/`summary`; the Weingarten and Eli Green records carry `summary`.
- **The patch's own exactly-once anchor assertion refused my first attempt**, because
 `source="cli_lpt_sync_to_production"` appears THREE times in that file (`create_customer`,
 `create_order`, `insert_customer_note`). Nothing was written on that refusal — verified afterwards by
 an unchanged md5, a clean `git status` and the absence of `_device_info`. The anchor now carries the
 preceding `skip_dedup=skip_dedup,` kwarg, which exists only in the `create_order` call. **A patch that
 would have added device_info to `create_customer` or `insert_customer_note` was stopped by the gate,
 not by my reading.**

BROKEN / NOT TRUE (stated so nobody believes otherwise)
- **The device_info fix is NOT proven end to end.** The chain is proven by reading
 (`device_info` -> `psycopg2_json(device_info)` -> the `"deviceInfo"` column) and the value is proven
 non-None for real records, but **no create push has happened since the patch**. The proof is the next
 push; I did not manufacture a junk order in the production database to test it.
- Nothing in `plan.json` for axes C, E, F is applied. `scripts/ps_mcp_server.py` (B-006) remains DIRTY
 and must wait for the other operator — it matters because that file's docstring is the schema hint
 every agent reads, and it steers queries at the stale `call_log`/`contacts` lineage.

NEXT
- C-001..006 (canonical person surface: 2 tables, ~4,447 rows, 3 views) — the largest remaining apply.
- F-010 (schedule `~/.fsearch/comms-refresh.py --check`, which exits non-zero on staleness and is in no
 crontab) plus F-001/F-005/F-007.
- `lpt_hub_production_sync_runner.py:34` builds `Path(lpt_hub)/"sync-records"`; the corpus is at
 `docs/customer-operations/sync-records`.

EVIDENCE
- `sed` of the patched call site (line ~1600) showing `device_info=_device_info,`.
- `website_db.py` device_json line quoted above; the 62/0 window measurement.
- Report committed to lpt-hub `docs/customer-operations/reconciliation/2026-09-15-full-reconciliation.md`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=8ce939be1ca54372 -->
