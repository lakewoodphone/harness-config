<!-- e:handoff|H301|2026-09-15 21:27 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:27 UTC · ZABZ-TECH · personality-system audit + 12-agent fix fleet (prod AI never worked; unauthenticated PII; silent-overwrite CAS; one-click erasure)

## 2026-09-15 ~21:40 UTC - ZABZ-TECH - personality-system (formerly "personality test") audit + fix fleet

CHANGED
- Repo located & restored: `C:\Users\ezabz\Code\personality-system` freshly cloned from
 `lakewoodphone/personality-test` (private; verified anonymous access fails). The old checkout
 had been archived to `_archive/personality test` at the May commit. Live prod = Hetzner
 `lpt-apps` container `personality-test`, /opt/personality-test at db0c0ec, Caddy, port 3006,
 DB `personality_test` on shared `lpt-postgres`.
- Committed b3aa0e3 on main (lockfile reconciled with npm 11) so the fleet base is clean.
- HARDENED LIVE: `/opt/personality-test/.env.local` was mode 666 root:root holding the gateway
 key, DB password and a GitHub token; any local process could READ AND REWRITE it.
 `chmod 600` applied to it and `.env`. Verified 600 after; container still Up, prod still 200.
- 12 agent worktrees created under `_worktrees/personality-system/`, node_modules junctioned in.

BROKEN - confirmed by my own measurement, not reported to me
1. THE AI LAYER HAS NEVER WORKED IN PRODUCTION. `assessment_ai_events` has exactly 4 rows, all
 status='failed', error='AI provider request failed.', 2026-09-06..09-07. Every real AI call
 ever recorded failed; users got deterministic fallback text silently.
 ROOT CAUSE PROVEN by probing the gateway: `secretary-provider.ts` hardcodes
 `deepseek-v4-flash` / `deepseek-v4-pro`. The gateway's real catalog ids are prefixed
 (`deepseek/deepseek-v4-flash`), and `deepseek-v4-flash` is not a catalog id at all - it only
 resolves via an undocumented fuzzy match. `deepseek/deepseek-v4-flash` -> HTTP 500.
 The gateway exposes model-agnostic ROUTE ALIASES built for exactly this churn:
 `secretary-auto` 200 (served deepinfra DeepSeek-V4-Flash-0731), `secretary-fast` 200,
 `secretary-smart` 200, `secretary-genius` **502** (do not use).
2. UNAUTHENTICATED PII LEAK, live. `GET https://personality.abletelsolutions.com/api/persistence`
 -> 200, 18,374 bytes, no auth: participant names + AI-written personality summaries + the
 full AI request/response history. `OPTIONS` returns `Allow: DELETE,...` so an anonymous
 `DELETE` also wipes it. Live data is thin right now (1 real name, 8 "Person N" placeholders,
 0 answers) but the path is structural.
3. CAS BUG = silent data loss with a success message. `expectedRevision` is only used to choose a
 merge strategy, never to reject; the client sends `null` after ANY transient GET failure
 (`remote-persistence.ts` returns revision:null on failure), and null takes the REPLACE branch.
 One network blip + one autosave replaces the whole shared library and the UI says "All saved".
4. ONE-CLICK FULL ERASURE, no confirmation. `participant-column.tsx` renders a full-width
 `Reset {label}` button immediately beside `Continue {label}` -> `handleParticipantReset`
 (assessment.tsx) -> `resetCrowdParticipantSession` -> `createAssessmentSession()` (runtime.ts:3160)
 discards the entire run including finalReport. I verified all four hops myself.
5. Blank white page forever if the boot GET hangs: `if (!hydrated) return null` (assessment.tsx:2097).
6. `GET /api/feedback` 500s on EVERY request: `WHERE ($1::text IS NULL OR id < $1::text)` at
 assessment-persistence.ts:1667 against a BIGSERIAL id. Feedback is write-only; consent_to_contact
 unreadable.
7. README.md line 97 says all four validation commands pass. Measured: vitest 103 pass, but
 `tsc --noEmit` = ~34 errors and `eslint --max-warnings=0` = 6 errors + 2 warnings. No CI exists.
8. Scoring: a dimension that was never answered scores 0, which is BELOW the 1-5 scale, and every
 consumer reports it as a result (fabricated strengths, a named dominant blocker, a Holland code
 of "RIA", trainingWillingness "open" - all from an EMPTY response set). Work-value trade-off
 adjustments push scores to 0.5..5.5. UI renders those as a 113%-wide bar / -25% width.

IN FLIGHT - 12 parallel agents, each in its own worktree and branch (agent/*), none pushed
Wave 1: ai-model-registry (model layer redesign), delivery-ci (tsc/lint to zero + CI + scripts),
 docs-truth (README/MODELS/OPERATIONS truth)
Wave 2: auth-egress (operator key, fail-closed; rate limit; body cap; 400-before-gateway-call),
 persistence-integrity (real CAS + FOR UPDATE + feedback SQL + disarm recovery script +
 real backup + erasure path), scoring-truth (answered-vs-unanswered, nulls, clamps,
 scoring.test.ts which does NOT exist today)
Wave 3: ui-resilience (409 handling, no "All saved" lie, honest zeros, a11y, one pure extraction),
 interpretation-truth (stop asserting from empty profiles, negation-blind keywords,
 cross-version retakes, threshold table), deploy-consistency (compose into git, sha labels,
 healthcheck, watchdog that would have caught the AI outage)
Auditors done: scoring, AI layer, persistence/API, question bank, UI/UX. Delivery auditor still running.

NEXT
- Integrate serially in risk order (docs -> new isolated files -> shared edits -> schema), re-running
 tests + tsc + lint after EVERY merge. Never merge a branch whose tests I have not run myself.
- Then deploy the verified build to lpt-apps and CONFIRM the AI layer actually answers
 (the whole point: a real gateway 200 with a model name recorded in assessment_ai_events).
- DEFERRED until ui-resilience lands (it owns both files): the reset-erasure confirmation, the
 blank-page boot timeout, raw-error-text leaks to users, export completeness, and the
 "Confidence" badge which measures answer SPREAD not confidence (all-1s and all-5s both read "high").
- Owner question candidates: (a) is this a public site or a closed link? It decides how much auth
 vs rate-limiting the participant flow needs. (b) ratify "models are never hardcoded".

EVIDENCE
- Gateway probes and DB reads are in `C:\Users\ezabz\Code\_scratch\` (gateway-models.sh,
 gateway-alias-test.sh, pdiag*.sql). Worktrees: `_worktrees/personality-system/`.
- Journal: this entry. Audit reports: docs/AUDIT-2026-09-15.md (agent docs-truth, in flight).

<!-- j2 tags= refs= alias_of= legacy_id= sha=dfb428507ef5f2b8 -->
