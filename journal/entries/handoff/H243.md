<!-- e:handoff|H243|2026-09-15 18:13 UTC|ZABZ-TECH|open -->
## 2026-09-15 18:13 UTC · ZABZ-TECH · Newman flash-drive case COMPLETE - both drives cleaned, data on the Kingston, ready for pickup ( uncollected)

## 2026-09-15 ~18:30 UTC · ZABZ-TECH · Newman flash-drive virus case COMPLETE: both drives cleaned, 12.97 GB verified onto the customer's Kingston

CHANGED
- **Newman (347-768-0413) is done and ready for pickup.** Two flash drives in at 11:23 EDT, both
 infected with the AutoIt USB-shortcut worm (proved: `MicrosoftSecurity.exe` SHA-256
 `51fe204c…6025` identical on both, ClamAV `Win.Malware.Autoit-9880096-0`, 88 decoy `.lnk` running it
 as an AutoIt script), both now wiped and clean.
- **Data:** 609 source files → **505 delivered, 12,973,709,026 bytes**, on the customer's Kingston
 (label `NEWMAN`) as `Kingston-drive/` + `SanDisk-drive/`, plus `README-NEWMAN.txt` and
 `MANIFEST-sha256.txt`. The Cruzer came back clean and empty as `NEWMAN-2`. Owner approved the wipe
 (2026-09-15); both formats were gated behind verification.
- **Evidence, not assertion:** per-file SHA-256 source-vs-copy — kingston 51 compared, cruzer 454
 compared, **0 hash mismatches, 0 extras, 0 unexplained losses**; all 104 absent files classified as
 worm/Windows artifacts; 334 MP3s with 0 bad headers; `clamscan -r` 0 infected on the bench set AND
 on the drive; the drive's own manifest recomputed from the drive matched; **0** `.lnk`/`.a3x`/
 `autorun*` anywhere; Hebrew filenames intact end to end; recordings play (`ffprobe` 879/1909/1393 s).
- Docs: case file `lpt-hub/docs/customer-operations/cases/flash-drives-newman-3477680413-2026-09-15.md`
 (status `ready`, price 150, payment `uncollected`), sync record (schema-valid), `recovery-index.md`
 row completed, and a **$75-per-drive USB-flash data-retrieval rate** now recorded in the pricing table.
- Bench: **`clamav` + `exfatprogs` + `fatattr` + `mtools` installed** on linux-pc; the worm retained as
 evidence at `/srv/recovery/cases/20260915-122509-newman-3477680413/evidence/worm/` (41 files +
 `EVIDENCE.sha256`, do-not-run notice).

IN FLIGHT
- Nothing. The job is closed except the handover, which is physical: drives are unmounted and ready to
 unplug from the Linux PC.
- **Held on the bench until pickup:** the verified copy at `/mnt/atlas-recovery/newman-3477680413/recovered/`
 (1.7 GB + 11 GB) — do not delete until the customer has the drive in hand.

BROKEN / OWED
- **Money: $150, uncollected** ($75 × 2). No USB/media charge — the data went back on the customer's own
 drive, so the Abramczyk $15 convention does not apply.
- **The customer's PC is still the loose end**, and it is the thing that makes this fix temporary: the
 worm re-infects every drive it sees. I told them so in `README-NEWMAN.txt`. Whether LPT quotes cleaning
 that machine is an owner question, not priced.
- The ddrescue images for the two drives were left **paused at ~1 %** (`maps/` retained). They are
 worthless as images and were kept only so nothing was silently deleted; the drives they describe are
 now wiped, so they can be discarded whenever someone wants the space.
- Pre-existing, unchanged: this ZABZ-TECH lpt-hub is `ahead N, behind 2442` of origin (my commits are
 local-only: `1e532c120` and the completion commit); stale `czermak-7183147956` case lock; a leftover
 bash from my verify3 that survived `kill -9` and had to be killed by explicit PID; `/mnt/shlesinger-live`
 I/O error.

NEXT
1. Owner hands both drives to the customer and collects $150.
2. If the owner wants to sell it: quote cleaning the customer's PC (SOP:
 `docs/customer-operations/compromised-windows-recovery-sop.md`, wipe-vs-cleanup table).
3. Optional bench tidy-up: the paused `newman-*` mapfiles/images, and the 12.7 GB bench copy once pickup
 is confirmed.

EVIDENCE
- Case file + sync record above; bench logs `/srv/recovery/cases/20260915-122509-newman-3477680413/logs/`
 (`extract.log`, `verify2.log`, `verify3.log`, `compare5.log`, `deliver2.log`), manifests and comparison
 output in `notes/` (`VERIFIED-OK`, `final-compare-{kingston,cruzer}.txt`, `source-*.txt`, `copy-*.txt`,
 `mp3-bad-headers.txt`, `deliverable-manifest.sha256`), worm in `evidence/worm/`.
- Related: **L1513** (manager case-id = directory basename), **L1514** (FAT charset / `pkill -f` self-kill),
 **L1515** (gating verification passes), **W** entry for this case.

<!-- j2 tags= refs=newman,lpt-hub,linux-pc alias_of= legacy_id= sha=0d20eb2467853563 -->
