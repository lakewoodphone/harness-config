<!-- e:handoff|H244|2026-09-15 15:58 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:58 UTC · ZABZ-YOGA · test_mcp_enabled was deterministic, not flaky; P152's thread hypothesis corrected

**CHANGED (preserved on `secratary-master-preserved-20260915`)**
- `caee745d` -- `TestPhase3ConfigFlags` asserts the config **defaults** instead of instance values, so
 it no longer reads the deployment's environment. `tests/test_phase3_modules.py`: 1 failed -> **45
 passed**. This was recorded as a flake; it was deterministic on this host.
- Full suite before this round: 4 failed / 2,711 passed / 0 errors; the same command now still shows a
 small varying set, because the remaining ones are genuinely intermittent (see below).

**CORRECTED**
- P152's first named mechanism ("the worker starts a thread, so the assertion races") is **wrong**:
 `_worker` is synchronous and the `threading.Thread` call is in its caller. The intermittency is real
 (four repeats: 137, 136, 136, 137 -- `test_worker_extracts_memory` and
 `test_worker_clears_in_flight_after_send` alternating) but the cause is not a thread in that path.

**NEW INFORMATION FOR THE NEXT SESSION**
- **The suite loads the repo's `.env` into `os.environ`** (`PROBE MCP_ENABLED env: false` inside
 pytest, `None` outside). Any test asserting a *default* must read `Settings.model_fields[...].default`
 or set the environment itself; `_env_file=None` is not enough.
- Candidates for the `TestWorker` intermittency, cheapest first: (1) a DB write that fails under suite
 load and is swallowed by the worker's `except Exception: log.warning(...)` -- make the worker's tests
 assert the absence of that warning, or capture logs; (2) `daily_reply_count()` keyed to the current
 date; (3) the `_now_ts()` in-flight gate.

**STILL OPEN**
- The descriptor leak (P151): limit raised in conftest, loops still unclosed.
- Queue #39 (cheap-first routing off in production while the DB override says true) waits on the owner.

**NEXT**
1. Capture the worker's swallowed warning: run the two `TestWorker` tests in a loop with `-W error`
 or a log capture and see whether a DB write is failing under load. That names the mechanism in one
 measurement instead of three guesses.
2. Then the descriptor leak.

**EVIDENCE**
- `pytest tests/test_phase3_modules.py -q` -> 45 passed; the in-pytest probe
 (`PROBE MCP_ENABLED env: false`, `_env_file=None: False`, `field default: True`); four repeats of the
 family-chat pair (137/136/136/137) with the failing test named each time; commit `caee745d`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=5dd7060880a0ac00 -->
