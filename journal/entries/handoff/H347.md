<!-- e:handoff|H347|2026-09-15 17:05 UTC|ZABZ-YOGA|open -->
## 2026-09-15 17:05 UTC · ZABZ-YOGA · P196 closed: hub cleaned 42 to 6 with every sha recorded; next rounds should be design work, not cleanup

**CHANGED**
- `ce651b3` (harness-config) -- `scripts/branch-audit.py`: classify a remote's branches by reachability
 (`claimed from master`, `claimed from another branch`, `UNIQUE`), with `--apply` deleting only the first
 two and recording every sha to a keep-file first. Verified from both the authority and the laptop.
- The shared hub: **42 branches -> 6**, 36 redundant refs deleted with zero commits lost. Deleted shas:
 `/home/zabz/harness-config-branches-deleted-20260915.txt` (36 lines).

**P196 IS CLOSED** -- it recorded the debris; the hub is now readable as a lineage, which is what the
P134 fork question needs it to be.

**A NOTE FOR WHOEVER PICKS UP P134**
The `diverged-*` refs that autosync's status names are gone (their commits survive). If autosync reports
divergence again it will create a fresh one, and its stored `diverge_refs` from before today now name
nothing. Not a fault -- but a stale name in a status file is exactly the kind of thing that gets quoted
as evidence later, so it is written down here.

**NEXT**
1. The owner queue (14 pending) is the standing item: oldest **#20 [high]**, plus **#39** and **#40** from
 this session. Nothing is blocked by any of them.
2. With the suite green, the descriptors fixed and the hub clean, the remaining open pain is mostly
 *design* (P4 the improvement loop, P1/P9 the owner-attention questions, P74/75 the self-management
 ratio, which is now #40). The next round should pick one of those rather than another cleanup.

**EVIDENCE**
- The classification table above; `python scripts/branch-audit.py --remote origin` from the laptop after
 the operation -> "branches on origin: 6 ... UNIQUE: 5 ... nothing redundant"; the keep-file with 36
 shas; commits `ce651b3`, and the hub's surviving refs listed by sha.

<!-- j2 tags= refs= alias_of= legacy_id= sha=de89ae42700eae61 -->
<!-- renumbered H253 -> H347 on 2026-09-15 (ZABZ-YOGA): id H253 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
