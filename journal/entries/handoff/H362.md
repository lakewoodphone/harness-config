<!-- e:handoff|H362|2026-09-16 00:33 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:33 UTC · ZABZ-YOGA · Round 4: canonical identity surface built (1581 persons / 2866 phone bindings / 1380 unresolved); C-005 referenced a table that does not exist; my conflict view was 8x wrong until the sample exposed it

## 2026-09-16 01:00 UTC · ZABZ-YOGA · Round 4: the canonical identity surface is BUILT (1,581 persons / 2,866 phone bindings / 1,380 unresolved), a missing dependency in axis C's plan was found, and my own conflict view was wrong by 8x before I fixed it

CHANGED
- **C-001..C-006 applied: the canonical person surface exists in secretary.db.**
 `person_canonical` **1,581** rows · `person_phone` **2,866** (1,486 attached to a person +
 **1,380 traffic-carrying numbers that reach no identity record anywhere**) · three indexes ·
 `v_person_canonical` 1,628 · `v_identity_conflict` **172** · `v_contact_duplicate` 10.
 Every count read back through a separate READ-ONLY connection. Additive only: two new tables, one
 new mirror, three views; **no existing table was modified and nothing was deleted**.
 `recon` check still `16 ok · 0 FAIL · 0 could not measure`; 8 views total. Built by
 `apply/apply-C-identity-surface.py` (one transaction per unit, `busy_timeout` 120 s, retries on
 lock only, idempotent via `INSERT OR REPLACE`).
 **The two table DDLs in `plan.json` were NOT executable as written**: C-002/C-003 carry only
 `CREATE TABLE`, while the 2,866 rows live in the 1 MB `identity-truth.json` axis C produced, so the
 INSERTs had to be generated.
- **A MISSING DEPENDENCY in axis C's plan, found by reading before running:**
 **C-005's `CREATE VIEW v_identity_conflict` references `prod_customer_mirror`, which DOES NOT EXIST
 in secretary.db** — verified: no such table, no `%customer_mirror%`, nothing `%production%` except
 `website_write_log` and `website_sync_cursor`. The view would simply have failed. Instead the mirror
 was created from production Postgres (**542 customers, read-only session**), which is worth having
 anyway: it is what lets the identity layer reconcile all four stores locally. **This is the second
 time this session that a `plan.json` failed on contact with the database** (the first was C-005's
 same family - no agent tested its own SQL).
- **A view of MINE was wrong and the sample caught it.** The first `v_identity_conflict` reported
 **1,431** phones with >1 name claim. Its own sample exposed it: `8482453600 claims=6
 dialpad:(848) 245-3600, dialpad:+18482453600, ...` — six "claims" that are all the same number in six
 formats. Two faults, both inherited from C-005: the join used the **raw** phone value, and it counted
 Dialpad's `customer_name` as a name when that column frequently holds **the phone number itself**.
 Fixed by normalising both sides and excluding claims whose digits equal the phone's: **1,431 -> 172**,
 and the top hit is now axis C's W4 — `7326745878` carrying `Mom Zabrowsky` / `Mommy` /
 `Shmuel Avigdor Zabrowsky` / `Adeena Zabrowsky`: **one line, four names, two humans, in the owner's own
 hand-written book.** Recorded as **L1679**.
- **The 673 vs 172 gap explained, not waved away:** axis C normalised the phone but still counted
 phone-as-name claims; fixing only the first gives ~673, fixing both gives 172. So **~501 of axis C's
 673 "conflicts" were junk labelling, not disagreements about who a human is** — which is evidence FOR
 axis C's own separate finding that `customer_name` carries device labels and numbers.
- **`is_dialable = 0` count is 0, and that is CORRECT, not a bug:** all six `excluded_shop_numbers` are
 absent from `person_phone` entirely, because the shop's own lines were excluded from the person layer
 upstream (axis C: "shop numbers that became people: NONE (correctly excluded)"). Nothing needed
 marking non-dialable.
- **`lpt_hub_production_sync_runner.py` is DEAD CODE — nothing invokes it.** Verified: no cron entry,
 no caller anywhere in `app/`, `scripts/`, `data/tools/`, or any `.sh`/`.mjs`. So P82's proposed fix
 ("repair its source path") is moot: the live pusher is `lpt-sync-to-production.py`, and **its failure
 surface is already closed** by `lpt-recon-check.sh`, which runs `--dry-run-all` on a 4-hourly clock
 and reports `push_would_create`. Do not spend time repairing a script nothing runs.

IN FLIGHT
- **F-010** — schedule `~/.fsearch/comms-refresh.py --check` (it already exits non-zero on staleness and
 is in no crontab), plus F-001/F-005/F-007 repoints. `scripts/ps_mcp_server.py` (B-006) is DIRTY and
 must wait for the other operator; it matters because that docstring is the schema hint every agent
 reads and it steers queries at the stale lineage.
- C-007/C-008/C-009 — the three concrete `commsindex.py` fixes are NOT applied (they are diffs against a
 file in `~/.fsearch`, another live component; each needs its own careful pass).
- 18 of axis C's operations need the owner (13 production merges, 5 identity splits). Queue 65 asked;
 66 and 67 wait their turn.

BROKEN / NOT TRUE
- Nothing in `plan.json` for axes E and F is applied. B, C (surface) and D (views) are applied.
- **The conflict view's 172 and axis C's 673 are both "right" answers to slightly different questions.**
 Neither is the number of humans per phone; that is not derivable from these stores and axis C said so.

NEXT
- F-010 next (small, closes a known "scheduled nowhere" gap), then the `commsindex.py` trio, then
 axis E's money operations once the owner answers.

EVIDENCE
- `apply-C-identity-surface.py` verification block (read-only read-back, quoted counts).
- `fix-v-identity-conflict.py` output: 172 rows, 363 claims, top conflicts listed.
- Report committed: `34021bc39` in lpt-hub `docs/customer-operations/reconciliation/`.
- `journal.py check` -> 0 errors.

<!-- j2 tags= refs= alias_of= legacy_id= sha=efeb6214ce8441e0 -->
