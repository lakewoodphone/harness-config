<!-- e:handoff|H314|2026-09-15 22:05 UTC|ZABZ-YOGA|open -->
## 2026-09-15 22:05 UTC · ZABZ-YOGA · chumash-search: honest API contract, real Hebrew calendar, indexed gematria

CHANGED
- agent/chumash-search (worktree C:\Users\ezabz\Code\_worktrees\chumash-timeline\chumash-search), 4 commits on top of main.
- src/lib/hebrew-year.ts: real molad + four dehiyyot calendar; Rosh Hashanah was on a forbidden weekday for most years.
- src/app/api/{search,timeline,feedback,submissions}/route.ts + new src/app/api/_lib/{http,pesukim-query}.ts: JSON body on every path, three-state store errors, honest count/total/offset, includeText honoured, per-method era events, control-character validation.
- src/lib/methods/{methodA,types}.ts: status "beyond_range" for years past pasuk 5846.
- New tests: src/lib/hebrew-year.test.ts (10) and src/app/api/_lib/{http,api-contract}.test.ts (23). Suite 178 pass on merged main.

IN FLIGHT
- Nothing of mine. My branch was merged into main by the manager (0232f87) except the last two commits (466ddbb, 53671ba), which are rebased on 0232f87 and verified.
- The final `npx next build` for commit 53671ba could not be re-run because the shared node_modules (main clone junction) was being rebuilt by another process at 18:04 and lost typescript/vitest/react-dom/.bin. Gates for the same content passed at 18:03; a background job polls for restoration and re-runs all four.

BROKEN
- Free-text search is still a sequential scan in production: db/schema.sql has no trigram index. Measured on a seeded scratch DB: 371.808 ms seq scan -> 0.407 ms with 3 GIN pg_trgm indexes (functional index on regexp_replace(hebrew, niqqud, '', 'g') plus transliteration/translation). DDL handed to the data-layer owner; NOT fixed by me (db/** is not my scope).
- Method D still cannot reach every Devarim pasuk: measured on merged main, a 5000-6000 sweep returns 860 of 956; the 96 missing are inside the six parshiyot longer than the 100 years of their century (112/103/106/122/124/130 pesukim per 100-year bucket). Full coverage needs the year spans to be allocated proportionally to parsha length, which moves the 1840/1940 anchors - a content decision, not mine.

NEXT
- Whoever owns db/**: add the three indexes above and re-measure with EXPLAIN (ANALYZE).
- UI agent: render method status "beyond_range" instead of a pasuk, and read per-method `eraEvents` (the top-level map is kept only for compatibility).
- Re-run `npx next build` on 53671ba once node_modules is healthy.

EVIDENCE
- Live before: /api/search?gematria=895 median 487 ms, {count:20, no total}, true total 70; ?q=%00 -> 500, 0-byte body; ?includeText=false -> same 6181 bytes as true.
- After (production build + scratch DB seeded with db/seed.ts): count 20 / total 70, offset 60 -> the same 10 global indices; ?q=%00 -> 400 with a message naming U+0000; includeText 9052 -> 5327 bytes; 503 store_unavailable on the three store-backed shapes while ?year= stays 200.
- Hebrew calendar: 21/21 years 5780-5800 and 1001/1001 years 5000-6000 agree with @hebcal/core for 1 Tishrei RD, year length, leap status and every month length; 1827/1827 days of 2024-2028.

<!-- j2 tags= refs= alias_of= legacy_id= sha=e2ea074c367dcf35 -->
