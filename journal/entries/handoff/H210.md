<!-- e:handoff|H210|2026-09-14 23:40 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:40 UTC · ZABZ-YOGA · Journal v2 verified on Linux and merged to master: 240 checks, 0 errors, FAIL=0 WARN=0 - and a real id collision (D143) proved P132 during the merge

CHANGED
- Pushed and merged: `origin/master` now carries journal v2 (995f9db). The main checkout was reconciled with it in a worktree, then fast-forwarded; the one merge conflict was the generated cache and it was resolved by regenerating it, not by choosing a side.
- **Linux is verified.** The authority pulled a detached worktree at `origin/master` and ran under Python 3.14.4: `selftest` all 240 checks pass; `check` 0 errors (and it rebuilt the cache on a fresh tree, proving the self-heal path on another OS); `status` 4,842 B with `LEGACY DRIFT 0`; `costs` - status 22 ms, list 19 ms, show 3.4 ms, search 20 ms, newest 19 ms. No POSIX-only path, no unguarded `os.uname`, LF everywhere.
- The independent gate on the final tree: **FAIL=0 WARN=0**. 707 entries, `check` 0 errors, `dedupe` nothing to collapse, `import-legacy` new 0.
- The id collision P132 predicts happened during this merge, not in theory: SECRATARY and ZABZ-YOGA both allocated `decisions D143` in the same hour (Fios network record vs rebuild-and-carry). The shared line kept SECRATARY's; mine was re-filed by the tool as **D145** and the citation in NOW.md updated. Recorded as **L1466**.
- Two worktree scratch files that a merge staged at the repo root (`_scratch_lesson.md`, `_scratch_mine.md`) were removed from git.

IN FLIGHT
- **The authority's checkout is diverged**: `/home/zabz/harness-config` sits on local commits (1c3f9a5 "H207 ... P132 is closed; resolve() flattens its reason") that are not on `origin/master`, with uncommitted edits under `scripts/`, and it runs its own edits to `tools/journal.py`. Nothing is lost; the next session on that machine should `git fetch` and reconcile (rebase or merge), and if it merged a `P132` of its own the merge will produce an add/add conflict exactly like D143's - `repair-ids`/re-file is the resolution, not a choice of side.
- `gc-legacy --quiet-hours 24 --apply` has not been run; `log/**` and the six flat files stay frozen in place (**P133**).
- The 26 entries above id 500 are the scar of ceilings that ran ahead of the real maximum on two machines. Ids are never reused, so they stay.

BROKEN
- No cross-machine lock or central id allocation (**P132** is still open, and its evidence now includes a real collision).
- Until every machine pulls, a v1 writer can still append to `log/**` or a flat file; `check` counts that drift by identity and `import-legacy` absorbs it.
- `check` prints 31 warnings: dangling refs from history and one empty-bodied handoff (H83). Honest residue, no action.
- The selftest's six-way concurrency case needed the lock wait raised from 30 s to 240 s: each append runs a `git grep` over every ref for the id ceiling, so six serialised writers on a loaded machine (two agent sessions, a build) can exceed 30 s and fail on a machine that is merely busy.

NEXT
1. Reconcile the authority's checkout (its local commits + its `journal.py` edits) with `origin/master`.
2. Run `gc-legacy` after a quiet day, then retire the v1 writer on every machine by pulling.
3. The owner's queue: **#17** (a live Twilio SID and auth token committed to a repo) and **#31** (two provider keys leaked into a transcript) are one question each and both are his.

EVIDENCE
- Linux: `python3 -V` 3.14.4; `selftest` "all 240 checks passed"; `check --quiet` "0 error(s), 31 warning(s), 75 info" after "cache rebuilt (703 entries)"; `status` tail "LEGACY DRIFT 0"; the `costs` table above.
- `journal.py verify` on the merged tree: "FAIL=0 WARN=0"; legacy 1,235 entries all present by identity; index header matches v1; frozen v1 tool reads the v2 index; `next-id lessons` L1472, above the real maximum.
- Merge: `CONFLICT (add/add): journal/entries/decisions/D143.md`; D145 written by `append`; `journal.py index --force` after the merge had committed the cache with conflict markers (0 markers left).

<!-- j2 tags= refs= alias_of= legacy_id= sha=d7c64ed1f227eea2 -->
