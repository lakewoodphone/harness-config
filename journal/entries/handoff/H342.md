<!-- e:handoff|H342|2026-09-15 16:17 UTC|ZABZ-YOGA|open -->
## 2026-09-15 16:17 UTC · ZABZ-YOGA · Evolution offers dedup on content and refuse node_modules; five findings reduced to two real ones

**CHANGED**
- `05d46f23` (app, preserved on `secratary-master-preserved-20260915`, deployed, health 200) --
 `log_evolution` records an identical unapplied offer once and refuses vendored/generated targets.
 6 tests in `tests/test_evolution_log_guards.py`; four hand-run probes on a temp DB
 (identical offers -> 1, 1; node_modules-only -> 0; mixed -> keeps `["app/services/y.py"]`; two
 applications -> 3, 4).

**STATE OF THE FIVE FINDINGS FROM THE SWEEP**
1. `evolution` (HIGH) -- **cause fixed**, badge clears itself on 2026-09-20 as the old rows age out of the
 check's 7-day window.
2. `agent_dead_weight` (HIGH) -- 5 agents completing ~nothing, worst `engineering_indexer` 172 ticks /
 0 completions. Next: read what they are asked to do before retiring them. Worth noting the overlap:
 the offer generator that produced the 38 duplicates may be one of these.
3. `config_sync` (HIGH) -- the authority's `~/harness-config` ahead 12 / behind 104.
4. `tick_completion` / `session_archive` -- cleared in rounds 50-51 (both were false alarms).
5. `attention_debt` (HIGH) -- the known 2 critical emails, understood.

**PROCESS SLIPS OF MINE THIS ROUND (both caught before commit)**
- I wrote `log.info(...)` in `app/database.py`, where the logger is `_log` -- a `NameError` waiting for
 the first vendored target. The hand-run probes raised it immediately; a compile check would not have.
- `cp ... database.py.$(date -u +%Y%m%d-%H%M%S)` was mangled by PowerShell (`-u` parsed as a parameter),
 so the backup did not happen. Git is the real backup here, but the habit is to prefer a stamp
 computed in PowerShell (`(Get-Date).ToUniversalTime().ToString(...)`) over `$(date ...)` inside a
 double-quoted string.

**NEXT**
1. The five dead-weight agents -- and whether the duplicate-offer generator is one of them.
2. The harness-config divergence (preserve the 12 commits, then decide the lineage).

**EVIDENCE**
- The measured table above (queried directly from `data/secretary.db`); `pytest
 tests/test_evolution_log_guards.py -q` -> 6 passed; the four probe lines; commit `05d46f23`; health
 200 after restart; `.runtime/deployed-commits.log` updated.

<!-- j2 tags= refs= alias_of= legacy_id= sha=b92e93e89661dbc1 -->
<!-- renumbered H248 -> H342 on 2026-09-15 (ZABZ-YOGA): id H248 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
