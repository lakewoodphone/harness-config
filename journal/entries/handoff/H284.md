<!-- e:handoff|H284|2026-09-15 20:32 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:32 UTC · ZABZ-TECH · Family-chat robustness fleet: four tracks merged and deployed; Chip's context bug was the tip of nine defects

CHANGED 2026-09-15
Authority (~/personal-secretary-mvp) fast-forwarded from 6a1fd872 to the integrated
tree and deployed; service restarted, health 200, Family Chat hub mounted, webhook
re-synced, resilience loop running, 0 tracebacks since. Latest commit cb3c1572.

Four workstreams, each isolated in its own git worktree, briefed as a contract with a
frozen interface, integrated serially and gated on tests after every merge:
 robust/storage -> db.py: durable outbound writer (record_outbound), schema
 column-order guard, outbox retry visibility, status ladder
 robust/clients -> twilio_client / security / secretary_tools: no blind retry of
 a non-idempotent POST, HTTP timeouts, permanent errors by
 Twilio CODE not substring, UCS-2 segment counting,
 forgeable-signature fix, tool-output redaction
 robust/hubrecord -> hub.py: one send door, so no send can skip its record
 robust/chipcontext-> personality/config: context de-duplication, corrupt-state
 guards, sensitive-message guard, reply sanitation

Integration fixes I made myself (in none of their scopes):
 - the trusted-hosts allowlist now names BOTH published hosts, or my new
 sms_fallback_url would have been 403'd by the clients' own security fix
 - the one shared test that asserted the OLD retry policy, corrected
 - an ambiguous send no longer gets queued to the outbox and re-POSTed two layers
 up (new 'unconfirmed' status, 3 tests)
 - a throttle test that was asserting on process-global leaked state

RESULT: 513 tests pass on the deployed tree (144 existing + 369 new).
Pre-fix evidence per track: storage 24 failed, clients 34 failed, hubrecord 27
failed, chipcontext 88 failed against their own new tests.

EVIDENCE / VERIFICATION I DID MYSELF (not taken on report)
 - the storage track REFUTED my cluster premise and was right. Twilio for
 +18482407257 on 2026-08-19/20: 117 entries, 109 failed with code 21610, 8
 delivered earlier while still subscribed; 27 bodies, 18 attempted SIX times.
 Nothing was delivered to her; our outbox 'dropped' was substantively right.
 - their outbox mechanism, verified in SQL: comparing an ISO next_retry_at against
 a space-separated _now() as TEXT returns 0, so an ISO timestamp can never come
 due inside the same date. All 18 live rows are ISO, all created 08-19, all
 updated at 2026-08-20 00:00:10.
 - the clients' forgery claim, verified by reading 6a1fd872: candidates were built
 from client-supplied Host / X-Forwarded-Proto, so a forger could sign their own
 URL. Real defect.
 - deployed allowlist reads both hosts trusted, attacker.example not trusted
 - a POST to EITHER public host returns 403 'Missing X-Twilio-Signature', i.e. the
 route is reachable and the signature check runs
 - the column-order guard accepts the live schema (read-only)
 - default_db_path now resolves to the production DB from any cwd
 - live DB still 1593 messages, 0 unreadable inbound rows

IN FLIGHT / OPEN
 - The unknown producer (P166) is still unknown: something sent to real family
 phones on 09-15 without recording or updating Chip's own state.
 - 35 delivered messages in the last 30 days have no row (13 admin notices, 22 Chip
 replies) - historical; new sends record now.
 - 18 message rows stuck at 'queued' with no SID, and 25 rows at Twilio's initial
 'queued', are left untouched deliberately (a human should decide).
 - recover_stale_outbox can still re-send a message Twilio already accepted if the
 process dies between send and mark-sent (hubrecord D9). Twilio's messages.create
 has no idempotency key; the client sends Idempotency-Key as belt-and-braces but
 Twilio does not document honouring it on that endpoint.
 - chipcontext's de-dupe depends on hub._send_to_member prefixing forwards as
 '<Sender name>: <body>'. If that prefixing is ever removed, update both.
 - Not pushed to origin: the authority's master is behind origin/master by many
 commits and a pull --rebase conflicts on app/autopilot.py (another agent's
 in-flight work). The merged fix is on the authority's master only.

NEXT
 - Watch the next real family message and confirm its stored body is readable and
 that Chip's reply reads in-context.
 - Run scripts/reconcile_family_chat_sends.py --hours 24 after a day of real
 traffic: the missing figure should be 0 for messages sent after this deploy.
 - Consider db.kind ('member'|'admin'|'system') so admin notices can be recorded
 with their real cost without consuming the family cap (hubrecord records them at
 cost 0.0 today, which understates spend by about $0.05/month).
 - The 220-char Chip cap caps characters, not segments: 220 plain GSM-7 chars is 2
 segments but 4 with a smart quote. Not changed.

<!-- j2 tags=fleet refs= alias_of= legacy_id= sha=69d7863ad458168e -->
