<!-- e:handoff|H233|2026-09-15 14:34 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:34 UTC · ZABZ-YOGA · Questions get a shelf life and the badge can go green again: attention_debt now counts live debt and states ages

**CHANGED**
- **`agent_questions` has a shelf life** (`9943817e`). Measured before: 313 pending, oldest
 3226 h (134 d), newest 1405 h (58 d), nothing created in 30 days, all `agent_id=orchestrator`
 -- SMS-era approval asks plus four paraphrase variants of one Pesach question. Now a lifecycle
 sweep phase expires pending rows older than 14 days (`status_note` says why; the table's own
 CHECK constraint already allowed `expired`). Applied: **pending 313 -> 0, expired 0 -> 313,
 total rows 507 unchanged, all 507 questions keep their text, 313 notes written.** Live in the
 running process: every sweep logs `agent_questions_expired': 0`. 7 tests.
- **`attention_debt` counts live debt, and severity follows freshness** (`b822350`). It counted
 the dead channel's backlog as owner work and went CRITICAL on a bare count, so two six-day-old
 Google security alerts held the owner's whole badge red for five weeks with no way to go green.
 Now: questions count only inside `QUESTION_DEBT_HOURS = 336`; CRITICAL requires a
 critical-severity row *observed* within `FRESH_CRITICAL_HOURS = 48` (the shelf life's own
 clock), otherwise HIGH with the age stated. Live verdict moved **critical -> high**:
 "2 critical message(s) held, newest 5.9d old; 11 urgent message(s) held; waiting: ...".
- **Caught a wrong number in my own patch, by reading its output**: the critical line first said
 "newest 0.1d old" -- the newest held row of *any* urgency beside a count of critical rows that
 were 140.6 h old. Ages are now tracked per urgency and the critical line uses the critical one.
 L1514.
- Badge payload regenerated (`2026-09-15T14:31:54Z`, 14 checks): `attention_debt` high,
 `delivery` info, `app_voice` info.

**IN FLIGHT / BROKEN**
- The two critical held rows are Google security alerts for his own two Gmail accounts, observed
 5.9 days ago, and will expire at the 14-day `urgent_email` horizon in about eight days. No
 action taken on them: they are his accounts and the mail is in his inbox.
- **P148 (new): reminders are marked `sent` into a `dashboard` nothing has been shown to render.**
 396 rows, 14 pending, 0 due; dashboard-channel reminders are marked delivered and send nothing.
 If no surface renders them, "sent" is a status the system awards itself for work nobody received.
- P145 (two live writers / the 61-138 divergence), P147 (journald retention has no explicit cap),
 and the sync-alert clear (still unobserved -- the two `sync_needs_login` rows stay `held` until a
 genuine `amazon_sync`/`ebay_sync` succeeds) all remain open.

**NEXT**
1. **Triage the open pain list itself.** It stands at 93 open entries and has not been read as a
 whole in this session; some are certainly fixed (P6's message half, P144's two halves) and each
 one costs a future session a read. Closing the ones already fixed is cheap and compounding.
2. Observe the sync-alert clear on the next real sync; set the journald cap from the measured rate.

**EVIDENCE**
- Commits: `9943817e` (app, on `secratary-master-preserved-20260915`), `b822350` (ceo-kernel, pushed).
 Live sweep log carries `agent_questions_expired': 0`; kernel metrics
 `held_critical_fresh: 0`, `newest_critical_age_hours: 140.6`, `pending_questions: 0`;
 health 200 after restart; `.runtime/deployed-commits.log` updated.

<!-- j2 tags= refs= alias_of= legacy_id= sha=8d202c25b05ca6e5 -->
