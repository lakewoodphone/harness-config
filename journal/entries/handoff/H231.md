<!-- e:handoff|H231|2026-09-15 14:28 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:28 UTC · ZABZ-YOGA · Her Mac: mesh access, fleet tooling, and the plain-language rule; laptop unreachable

STATE 2026-09-15 ~15:00 UTC from ZABZ-YOGA. Owner's latest instructions were three: (1) the repo list
was too narrow -- "why only 2 repos / what about lpt flip phone, what about home assistant, what about
the waze system"; (2) her machines must reach the MESH and read info and .env keys from the office
desktop and the yoga laptop, "full full access ... i can't have it crippled in any way"; (3) she is
fully trusted as the manager but is NOT a developer, so her agent should not do anything very dangerous
unless she realises what she is doing and clearly authorises.

DONE AND VERIFIED ON HER MAC:
* ps-mesh.mjs -- fleet access as a tool. Measured: desktop-ts, secratary-ts, laptop-ts and linux-pc-ts
 all answer; it reads 319 configured variables on the office desktop, 281 on the yoga laptop, 285 on
 the authority. Her Mac IS on the tailnet (utun 100.126.146.121) and reaches the office LAN.
* A destructive-command guard that refuses the two things that actually went wrong here (pm clear on a
 customer phone; pm clear com.whatsapp, ~90GB of media) and still allows the SAFE alternative
 (pm trim-caches) -- because a guard that blocks the safe path teaches people to bypass it.
* Secrets are masked by default; a value prints only with --show-secret.
* The persona now explains how to work with a non-developer who is nonetheless fully in charge, and
 documents the fleet and the guard.
* Five LaunchAgents running: yocheved-harness, harness-autosync, dsh-session-sync, repo-sync (hourly),
 journal-daily (09:00 + at load). Engine restarted, pid 69411.
* ps-case.mjs -- writes the customer's case file AND its sync record, with [from <host>] on every entry.
* repo-sync's manager list widened to 12 repos (lpt-flip-phone, ha-config, kosher-filter-ai, the
 schematics, tesla-lin-chip, the knowledge base, quickbooks-agent and more), and clone-manager-repos.sh
 puts them on a workstation.

TWO THINGS I GOT WRONG AND FIXED, both worth remembering:
* The Windows quoting problem took FOUR attempts (grep; inline PS with escaping; simpler inline PS;
 stdin) and two of them returned "0 variables" INSTEAD OF ERRORING, which reads as an answer. It is
 solved with PowerShell -EncodedCommand (base64 UTF-16LE), which no layer can mangle.
* I asserted in a commit that `--list` had pushed to production; it had not, and I had to write a
 correction commit. My own unguarded test probe did the pushing.

OPEN, IN PRIORITY ORDER:
1. HER LAPTOP IS NOT REACHABLE. Its Cloudflare exec tunnel returns 530 and its Tailscale is logged out,
 so it has NO mesh access, and the repo-sync read-only fix plus clone-manager-repos.sh are not
 deployed there. The laptop does have ps-shop/ps-case/ps-system/journal and two working tasks.
 Everything needed for it is committed and waiting; it needs the machine up.
2. JOURNAL IDS COLLIDE ACROSS MACHINES (P146, with evidence). Both machines independently allocated
 H228/H229/L1510/P145 today. Fix properly by allocating from the authority, or by putting the host in
 the id; do NOT hand-renumber.
3. The extra repos are NOT cloned on her Mac (it has lpt-hub and the secretary repo). Her GitHub access
 is one deploy key per repo, so each additional repo needs a deploy key added on GitHub -- the owner's
 action, not mine. The tooling to clone them is in place.
4. `master` in harness-config is diverged from origin because of (2); this session's work is on
 refs/heads/manager-access-2026-09-15.

<!-- j2 tags= refs= alias_of= legacy_id= sha=fa11f7b9f11ee47f -->
