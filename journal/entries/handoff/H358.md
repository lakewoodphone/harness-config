<!-- e:handoff|H358|2026-09-16 00:12 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:12 UTC · ZABZ-YOGA · Round 2: the new push clock failed on its first cron run (database is locked) and now retries with a sanity gate; 16 ok / 0 FAIL; A-007 rejected on evidence; four applies named

## 2026-09-16 00:12 UTC · ZABZ-YOGA · Round 2: the push clock failed on its first cron run and the wrapper now survives it; check is 16 ok / 0 FAIL; the four remaining applies are named

CHANGED
- **The website feed is live and verified.** `communications` 116,558 -> **116,685**; newest message
 **11:34:24.649Z -> 23:43:48.221Z**. 127 messages restored (28 calls + 99 SMS), 0 errors.
- **The first cron-triggered run FAILED — and that is the most useful thing this round produced.**
 ```
 2026-09-16T00:05:07 CRON[601354] (zabz) CMD (... website-comms-push.py --json ...)
 {"calls": {"ok": false, "error": "OperationalError: database is locked"},
 "sms": {"ok": false, "error": "OperationalError: database is locked"}, "ok": false}
 ```
 Production was unchanged (116,685, newest 23:43:48Z) so it failed cleanly with no partial state —
 but it failed silently from the operator's view, which is the exact defect the reconciliation exists
 to remove. "It is configured" is not evidence; a cron line that has never fired is a hypothesis.
- **`/home/zabz/bin/website-comms-push-cron.sh`** — a wrapper that mirrors the pattern
 `scripts/dialpad-harvest-cron.sh` already documents for this exact contention (3 attempts, 45 s
 backoff, retry the whole pass because the push is idempotent, rather than widening a global timeout
 of a running application). The company store logs ~3,322 `database is locked` events a week, so
 contention is the normal weather, not an exception.
- **The wrapper also carries the sanity gate that matters**: `_get_dialpad_calls_for_push` /
 `_get_dialpad_sms_for_push` are written `except Exception: return []`, so a locked database makes
 them return an EMPTY list and the sync then reports `ok: true, synced: 0` — a green result meaning
 *"I could not read anything"*. The wrapper treats "0 candidates while the local store demonstrably
 holds rows in the window" as FAILURE and retries it. That is this system's signature defect
 (L1484: a job that runs, exits 0 and produces nothing) caught structurally instead of by memory.
- **A cwd trap found and removed.** Running the wrapper from `/tmp` first failed with
 *"no PHONE_TECH_FULL_* DSN available"* purely because it did not `cd` into the repo and `app.config`
 loads `.env` relative to the working directory. Fixed on both sides: the wrapper now `cd`s, AND
 `website-comms-push.py` reads `.env` by absolute path (`dsn_from_env_file()`). A tool that works
 interactively and fails under cron because of an unstated cwd dependency is a trap, so the dependency
 was removed rather than documented. **Re-tested from `/tmp`: rc=0, ok:true, 252 skipped as duplicates.**
- **Cron final state** (backup `~/crontab.backup-20260916T001034Z`; exactly one push entry, old `5,35`
 removed):
 - `9,39 * * * *` `/home/zabz/bin/website-comms-push-cron.sh`
 - `*/15 * * * *` `production-comms-freshness.py`
 - `23 */4 * * *` `/home/zabz/bin/lpt-recon-check.sh`
 - **The schedule has been observed firing**, not merely installed:
 `CRON[463225] ... production-comms-freshness.py` at `23:45:01` and `CRON[578519]` at `00:00:01`,
 each with its status file written.
- **`lpt-recon.py` -> `16 ok · 0 FAIL · 0 could not measure`**; `production-comms-freshness.py` ->
 `fresh`; `journal.py check` -> **0 errors**.
- **Axis A's A-007 is REJECTED on evidence, recorded permanently.** A-007 proposed setting the
 Weingarten record's phone from `PENDING` to `9179026334` — which would have unblocked A-006. Its own
 precondition was *"the case file must confirm it"*. The case file does not: it reads
 `- **Phone:** PENDING (need number to create backend contact/work order)` and the case is
 **CLOSED (2026-09-02)** with the owner's decision *"no fix"*. There is no phone to set, no contact,
 no order and no work; setting it by surname inference would manufacture the duplicate A-007 existed
 to prevent. Recorded as **L1677** and in the report. **Do not apply A-007.**
- The six held case pushes are now individually justified in the report, each with its evidence.

IN FLIGHT — the four remaining applies, in the order I would do them
1. **B-004, repoint `app/communications_contract.py`** at the Dialpad tables. **File verified clean**
 (md5 `6a7babfc3b8e3c489bc95f4e0acc2e8d`, matching what axis B analysed) so the edit is safe; the
 spec is prose in `plan/B-comms/PLAN.md` §4, NOT in `plan.json`, which is why it was not applied
 blind. Needs an API restart afterwards. This is the fix that turns the owner's own
 `/communications/summary` from `ok:true, cutover_ready:true` with `recent_call_count:0` into the
 truth. `scripts/ps_mcp_server.py` (B-006) is **dirty — must wait** for the other operator.
2. **B-001..003 and D-001..002 — five `create_view` operations**, pure additive DDL, all specified in
 the plans. They give the canonical Dialpad surfaces. Note axis D's ruling: **view, never delete**
 (2,282 call rows are two legs of one call each holding a different recording).
3. **C-001..006 — the canonical person surface** (`person_canonical` 1,581 + `person_phone` 2,866 +
 three views). 18 of axis C's 28 operations need the owner (13 merges, 5 splits); the 10 that do not
 are the surface and three concrete `commsindex.py` fixes.
4. **F-010 — schedule `~/.fsearch/comms-refresh.py --check`**, which already exits non-zero on
 staleness and is in no crontab. Same class as the 11.5-hour silence.
 Plus: **pass `device_info` in `lpt-sync-to-production.py`** (all 62 orders from the 2026-08-27 run
 have none) and **`lpt_hub_production_sync_runner.py:34`** builds `Path(lpt_hub)/"sync-records"`
 when the corpus is at `docs/customer-operations/sync-records`.

BROKEN / NOT TRUE
- **74 of the 111 missing messages still cannot reach the website**: 24 phone threads match no
 production customer and `website_db.py:1628`/`:1720` do `if not customers: continue`. That refusal is
 *correct*; widening it is a business decision about manufacturing customer records from unknown
 numbers (axis G's G-008), so it is named and left alone.
- Nothing from `plan.json` in axes B, C, D, E, F is applied. They remain specifications and **no agent
 tested its own SQL**.
- `website_db: batch customer lookup failed: list index out of range` — observed twice, a real defect
 in the batch lookup, unrelated to my changes, unfixed.
- Two of my own checks were wrong in ways only running them revealed (`no such column: name`, then an
 IndentationError from a dangling string, then a cwd-dependent DSN read). All three fixed.

NEXT
- Do the four applies above, starting with B-004 on the clean file.
- The owner is owed ONE question at a time; queue 65 (the deposit question) is the one asked, 66 and 67
 wait their turn.

EVIDENCE
- `~/.lpt-recon/website-push.log` (the 00:05 failure AND the later rc=0 success), `production-freshness.log`,
 `check.log`, `status.json`.
- syslog `CRON[463225]` 23:45:01, `CRON[578519]` 00:00:01, `CRON[601354]` 00:05:07 (the failure).
- `lpt-recon.py` -> "16 ok · 0 FAIL · 0 could not measure"; `journal.py check` -> 0 errors.
- crontab backups `~/crontab.backup-20260915T234441Z` and `~/crontab.backup-20260916T001034Z`.
- Weingarten case file line 7: `- **Phone:** PENDING (need number to create backend contact/work order)`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=53103dac31b200d7 -->
