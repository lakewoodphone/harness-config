<!-- e:handoff|H256|2026-09-15 19:08 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:08 UTC · ZABZ-TECH · android inference telemetry emitter: client half shipped, server endpoint is the blocker

CHANGED: branch agent/android-telemetry-emitter (worktree C:\Users\ezabz\Code\_worktrees\kf\android-telemetry-emitter), commit ea29459. New android/app/src/main/java/com/kosherfilter/client/telemetry/ (InferenceTelemetry.kt vocabulary+wire encoder, InferenceTelemetryEmitter.kt batching+fail-soft, InferenceTelemetryOutcome.kt verdict mapping, InferenceTelemetryReporter.kt, InferenceTelemetryTransport.kt) plus 5 new JVM test files under android/app/src/test/java/com/kosherfilter/client/telemetry/. Wired at 4 decision sites (mitm_image, a11y_screenshot, share_intent, notification_big_picture) and into BrokerHeartbeatService for transport attach/flush. docs/WS_PROTOCOL.md now carries the inference_telemetry endpoint contract.

IN FLIGHT: nothing. Not pushed (task forbade pushing).

BROKEN: the server has no receiver for the new message. PhoneMessage in server/app/models.py has no inference_telemetry member, so phone_message_adapter raises invalid_message and the batch is discarded. Verified directly: TypeAdapter(PhoneMessage).validate_python({'type':'inference_telemetry',...}) raises. Nothing is stored on the authority until a server-side model + handler + dispatch lands. The full contract is written in docs/WS_PROTOCOL.md section 'inference_telemetry'.

NEXT: (1) server agent: add InferenceTelemetryBatch/InferenceTelemetryEvent models with type Literal['inference_telemetry'], a handler calling record_inference_event per event (skip+count a malformed one, never fail the batch), dispatch in ws_phone_endpoint.py, and an inference_telemetry_ack. (2) A per-device inference budget (ledger + egress gate) so cap_hit has a caller. (3) The escalation transport that actually sends a frame for a second opinion — until it exists, every ESCALATE is recorded as local_fallback with no remote_verdict and verdict_changed absent, so the D39-11 self-improvement measure (measure 3) has no data by construction.

EVIDENCE: git show --stat ea29459. Kotlin tests NOT executed: ./gradlew.bat --offline testDebugUnitTest fails in 12s with 'SDK location not found' (no Android SDK on this machine). Verified instead by: 20/20 shared constants matching server/app/inference_telemetry_schema.py exactly, a named-argument sweep over all call sites, and the encoder's exact JSON literals + escaping reproduced against Python's json parser. Python side: server tests test_inference_telemetry.py + test_ws_protocol.py = 53 passed in 56.88s (docs-only change there).

<!-- j2 tags= refs= alias_of= legacy_id= sha=7c8e915016286fdd -->
