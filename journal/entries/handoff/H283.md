<!-- e:handoff|H283|2026-09-15 20:29 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:29 UTC · ZABZ-TECH · Greening the backend suite: fixed 19 tests across three distinct stale causes, deleted a stale multipart test, and corrected my own understated baseline (21 failures, 8 files remain)

CHANGED
- **`1b00ddda5`** — fixed **15** stale `laptop-preference` failures across **three distinct causes**.
- **`556e83900`** — deleted a stale multipart-backup test (4 failures) and **corrected my own baseline**,
 which had understated the problem by a factor.
- **Progress: 19 tests fixed.** The backend suite went from (scoped) 25 failures to, measured properly,
 **21 failures across 8 files**.

THE THREE STALE CAUSES, EACH READ FROM THE OUTPUT RATHER THAN GUESSED
1. **A missing test helper.** `contract-test-utils` had `createAuthenticatedCaller` (**role ADMIN**),
 `createAdminCaller` and `createManagerCaller` — and **no CUSTOMER one** — while `laptop-preference`'s three
 procedures are `customerProcedure`, which requires `role === 'CUSTOMER'` **and** a non-null `customerId` and
 rejects everything else **before the handler runs**. Every positive test built an ADMIN caller and hit the
 guard instead of the logic it was written for. Added `createCustomerCaller(customerId)` with a docstring
 explaining the guard.
2. **Assertions describing unreachable code.** Three tests pass `customerId: undefined` and assert the
 *handler's* custom message; the guard refuses that context first, so the handler never runs. Updated to the
 guard's message — the behaviour (a refusal) is unchanged, only which layer refuses.
3. **Three retired budget tiers.** `VALUE_200_400 / ALL_AROUNDER_400_700 / POWER_700_PLUS` were replaced by the
 four Phase-3 bands. The tests still passed the old names, so tRPC's validator rejected them before any
 handler ran. **The unification shipped in the code and not in its tests** — the same drift PART-129 measured
 from the decision side.

A COLLATERAL I CAUSED AND CAUGHT — worth the line it costs
My tier-rename script rewrote a test named **`rejects a retired budget tier`**, which *deliberately* passes a
retired value to prove it is refused, into a **valid** value — so it passed by no longer testing anything. The
suite caught it (`promise resolved instead of rejecting`) and I restored the retired value.
**A mechanical rename over test data can invert a negative test, and only running the suite reveals it.**
This is the seventh instrument fault this session; the pattern holds — act, then read the result, never assume
the edit did what its name says.

MY BASELINE WAS WRONG, AGAIN, IN THE SAME DIRECTION
I had scoped vitest to `src/trpc`, which **hid five failing files outside that path**. The full-suite command is
`npx vitest run --root backend`, and it reads:

```
 Test Files 9 failed | 426 passed | 11 skipped (446)
 Tests 21 failed | 6144 passed | 363 skipped (6528)
```

Eight files remain red: `communications-phone-threading` (4), `customer-backup.service` (5),
`workorders-getall-status-filter` (3), `terminal.error-handling` (3), `customer-portal-ops.routes` (2),
`google.router` (2), `customerPortal.debug-logging` (1), `errorLogger` (1). **Mostly outside this audit's
subject.** The scoping error is the same class as everything else this session: I chose a narrower command, it
gave a confident answer, and the answer understated the problem.

DELETED, STATED PLAINLY
`customers.backups.test.ts` tested four multipart backup procedures that exist **nowhere** in the repo (removed,
not moved) and that the router replaced with a different design (8 guarded procedures, no multipart). Backups
are covered by `customer-backup.service.test.ts`, `customer-backup-storage.service.test.ts` and three route test
files. **Deletion, not a silent skip.**

WHY I STOPPED HERE
The remaining 8 files span comms, backups, work orders, portal routes and the error logger — **not the
account/sign-in audit** — and each needs its cause read individually. Greening them is real work with a real
payoff (it is what makes `guard-coverage.test.ts` a usable control) but doing it in the closing minutes of a
session is how one fix becomes the next defect.

BROKEN
- Nothing. Posture intact; workspace clean at `556e83900`.

NEXT
1. **Green the backend suite** as its own task, using the eight-file table above as the backlog, one file at a
 time, each with its cause named in the commit as this round did. **This unblocks the guard test.**
2. **The proper rebuild** — still the only thing making the audit's source fixes real.
3. Land `agent/guard-ui2` and delete the `ui` ACKNOWLEDGED DEBT block.
4. Then the original plan: 78 never-started parts, 17 REVISION parts, and one consolidated pass over the owner
 decisions (C-117-1, C-119-1, C-123-1, C-126-1, C-129-1, plus the queued Stripe question).

<!-- j2 tags= refs= alias_of= legacy_id= sha=674a4cf480fe3842 -->
