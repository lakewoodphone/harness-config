<!-- e:handoff|H205|2026-09-14 23:18 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:18 UTC · ZABZ-YOGA · Journal v2 is live and verified: one file per entry, a self-healing cache, and the record rebuilt from the frozen sources after a concurrent migration corrupted the derived tree

CHANGED
- The journal is v2 and live. `entries/<kind>/<id>.md`, one file per entry (690 now), `index/` a generated cache that validates a signature of `entries/` and rebuilds itself without blocking a read, real flags on every read command, `costs` that re-measures the read path, `doctor`, `check --fix`, `dedupe`, `repair-ids`, `gc-legacy`, `import-legacy`, `pairs`, `backlinks`.
- `journal/AUDIT.md` (measured defects) and `journal/SPEC-v2.md` (the contract) are new; `journal/README.md` and `journal/index/README.md` are rewritten around them.
- The migration ran on the real tree: 1,235 legacy entries (789 shard + 446 flat) became 674 distinct entries with **0 B residue across all nine shards**, 383 alias rows, 837 exact + 398 same-identity matches, and `check` 0 errors.
- The derived tree a concurrent session's migration had left behind (563 files whose name/marker/heading disagreed, 33 entries of real work that existed nowhere else) was repaired by parking it, re-migrating from the frozen sources, and carrying those 33 back (2 of them re-filed: L320 -> L1084, W67 -> W106).
- Patched after review: LF on every write, `check --fix` re-emits through the write path (it had left a stale sha), `newest` answers from the cache (397 -> 100 ms), NOW.md gets its own cap so it cannot starve the rest of the page, the check summary names its rule set, drift counts by identity and treats alias rows as absorbed, and the owner-question mirror now refreshes over `ssh secratary-ts` on Windows (it never could before - the owner's laptop had been showing a 17-hour-old count).

IN FLIGHT
- The preset text for this system lives in `scripts/make_zabz_preset.py`; it now describes v2 and must be regenerated and committed in the same commit, or `--check` drifts.
- Push to `origin`, then pull on the authority and run selftest/status/check there: **Linux execution is still unverified** (Windows and 3.12 only).
- `gc-legacy --quiet-hours 24 --apply` has not been run; `log/**` and the six flat files are still in place, frozen by convention and scanned by the importer.

BROKEN
- Id allocation across machines is still local-ceiling-plus-git-refs with no shared lock, so two machines can still choose the same number at the same moment. `check` will name it; nothing prevents it (P132).
- Until every machine pulls this commit, a v1 writer can append to `log/**` or a flat file. `check` counts that drift by identity and `import-legacy` absorbs it, so it is recoverable, not silent (P133).
- `check` still prints 30 warnings: dangling refs from history (mostly ids cited in old prose) and one empty-bodied handoff (H83). No action needed; they are the honest residue of a hand-merged record.
- The selftest's six-way append concurrency case is load-sensitive: it failed once while the machine was busy and passed on the next two runs.

NEXT
1. Regenerate `presets/zabz/agent.cordis.yml` from the generator and commit with the journal.
2. Pull on `secratary`, run `journal.py selftest`, `status`, `check` there (Linux proof), then `gc-legacy` after a quiet day.
3. The queue holds 13 rows; #17 (a live Twilio SID and auth token committed to a repo) and #31 (rotate the two leaked provider keys) are the owner's, and they are one question each.

EVIDENCE
- `journal.py costs`: status 5,986 B / 97 ms, show 9 ms (one file), search 105 ms, newest 100 ms, list 96 ms - all within the targets in AUDIT.md.
- `_scratch/j-verify.py`: FAIL=0; legacy 1,235 = 837 exact + 398 same-identity; 0 missing; 0 ids with conflicting content; index header matches v1; 60/60 legacy ids resolve; frozen v1 `show P46` rc=0 and `next-id lessons` above the real maximum.
- `journal.py check`: 0 errors. `import-legacy` dry run: identical 1073, aliased 534, **new 0**. `status`: LEGACY DRIFT 0.
- `journal.py selftest`: 240 checks pass. `doctor`: format 2, 690 files, cache fresh, lock free, fts5 yes, origin reachable.

<!-- j2 tags= refs= alias_of= legacy_id= sha=aef80ff8cfcfcb24 -->
