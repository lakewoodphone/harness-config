<!-- e:handoff|H225|2026-09-15 14:02 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:02 UTC · ZABZ-YOGA · comms: the reader now answers, and the owner can see who is waiting

## 2026-09-15 ~14:05 UTC · ZABZ-YOGA · comms: the reader now answers, and the owner can see who is waiting

CHANGED (all pushed, both lineages where they diverge)
- `harness-config` 30974cf -> 38549fb: query path can no longer dead-end (aliases + `**extra`
 + unknown modes named + every query tokenised and quoted); loose fallback drops stopwords,
 orders by bm25, and reports `match_mode` and the terms it used; `_party_filter` shared by
 search and thread, reporting identity, confidence and other candidates; `search --party X`
 with no keywords works; CLI `q` optional; opt-outs get their own bucket
 (`closed_by_optout`); `--line` gives one age-carrying summary line per mode.
- `personal-secretary-mvp`: 8d4968d21 (endpoint drops nothing but null) and de925d742
 (owner digest section 1c). The authority's own lineage: d8603d17 and 5a6c3b66.
- `scripts/comms-verify.py` is now in the repo: 13 assertions through the live HTTP
 dispatcher and the CLI. ALL PASS (re-run after every change below, last run 14:01Z).
- Deployed to the authority: `~/.fsearch/comms-search.py`, patched `app/main.py`
 (API restarted, PID 4092497 -> 4101260 -> 4101631), digest script.

THE THING THAT MATTERS MOST IN THIS BATCH: the owner digest now prints
`🔔 16 customer(s) waiting on a reply (last 3 days) — <command to see them>`, and prints
nothing when the number is zero. Verified by running the digest exactly as cron does:
6.1s end to end. That is the first time this archive has changed anything the owner can
see without being asked.

IN FLIGHT
- A background subagent is investigating where the business's own records hold
 name<->phone mappings, to give names to the ~304 unnamed people (43 of 60 `waiting` rows
 currently show a bare number). Read-only investigation; no writes authorised.

BROKEN / MEASURED-BAD
- `people.display_name` can be a source LABEL rather than a person: `7325036369` has
 1,669 communications and is displayed as "Microsoft Word", because
 `dialpad_sms_cache.customer_name` literally says that. Not a merge error — the source
 says it — but any agent reading `person: "Microsoft Word"` on a laptop-repair message
 will be misled. 38 of 1,226 display names look non-human by a rough filter; the rest are
 legitimate businesses (Merck, banks, carriers). Needs a deliberate design pass, not a
 blocklist guessed in a hurry.
- The authority's app checkout is still 136 behind origin/master with its own lineage
 (P143). Its 57 unpushed commits are now backed up on the remote as
 `backup/secratary-checkout-20260915`, so the single-copy risk is gone.
- Permanent ceiling unchanged (L1481): 6,055 calls can never have words.

NEXT
1. Names for the unnamed (subagent result above) -- biggest single lever on how useful
 every other view is.
2. Give the fleet a reason to call `comms_search` unprompted: recent comms for the
 counterparty of an inbound message or a task, in the context an agent already reads.
 Measured: 34 calls, all mine, none from an agent.

EVIDENCE
- `/tmp/cv.py` 13/13 PASS; `/tmp/ot.py` 23/23 opt-out cases; `comms-refresh.py --check`
 exit 0 (166,939 records, 0 missing, 24 min old).
- `waiting` before: top row `8482996583 Stop`. After: `3473702677 Where are you located?
 What are your hours?`.
- Loose search before: `It's a iPhone 16 pro max`. After: `do you repair cracked iphone
 screens?`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=02671c717818890e -->
