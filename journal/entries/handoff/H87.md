<!-- e:handoff|H87|2026-09-14 15:43 UTC|SECRATARY|open -->
## 2026-09-14 15:43 UTC · SECRATARY · The question card is answerable from the phone now, and the composer's two layers are gone for good

## 2026-09-14 16:15 UTC · SECRATARY · Two phone UI defects fixed at the root, each with a check that proves itself

The owner, this session: (1) asked whether his screenshot really showed the doubled composer — it
did; (2) reported twice that the `ask_user_question` card on his phone showed only part of itself,
would not scroll and would not click, and told me to fix it or stop using the tool. Both are fixed
and both fixes are verified against the layer his phone actually loads.

CHANGED

- `assets/mobile.css` — the composer row may no longer wrap (`flex-wrap: nowrap`), the two groups
 shrink instead of overflowing, Send does not shrink, and at `max-width: 380px` the context meter
 and the `/` Commands control step aside so the cost pill and Send fit. Commit **48113bc** then
 **618bf7c**.
- `assets/question-card.css` — NEW. On phones the question card owns the viewport:
 `position: fixed; inset: 0`, card height `100%` then `100dvh`, safe-area padding, header/footer
 pinned, only the option list scrolls, `z-index: 2147483600`.
- `scripts/phone-gate.py` — the phone layer is now `LAYER_FILES` (mobile.css + question-card.css)
 concatenated into the one URL and one injected tag that already existed, so a new phone rule never
 means a new delivery path. Restarted the `phone-gate` unit (systemd, `/etc/systemd/system/`).
- `scripts/phone-layout-check.py` — NEW. Composes the row's real states at seven widths and asserts
 one control band, no overflow, no clipping. **Fails 13 of 15 on the pre-fix layer, passes 15 of 15
 now.**
- `scripts/question-card-check.py` — NEW. Rebuilds the question card from the app's own CSSOM and
 class map and asserts the card fits the viewport and that `document.elementFromPoint` at each
 control's centre returns that control.

MEASURED (live engine, gate :3086 -> engine :3089, and through the tailnet URL)

Composer, through `https://secratary.tail93e6e6.ts.net/`, served layer only, no injection:
**PASS** at 320/358/375/393/402/430/452 with the cost pill, the context meter and a stop control,
row 52px and one control band everywhere. Before: 104px and two bands at 430 with the pill alone,
and at 393 with pill + meter.

Question card: card 511px at 852 with Submit reachable but the composer covering it at 640; at 560
Submit's centre hit the composer's own row — the owner's exact report. After: at
393x852/700/640/560 and 430x852/700/640/560 the card fills the viewport, Submit and every option
are inside it, and a tap at each control's centre lands on that control.

IN FLIGHT

- The owner should reload the page on the phone (the layer is `no-store` and read per request) and
 say whether the composer is one row and whether a question card is answerable. **Neither fix is
 confirmed on his device yet** — that is the last step and it is his to do, in one reload.
- A question still cannot be provoked on demand for an end-to-end test: the host refuses
 `ask_user_question` when the asking agent is owned by another live agent (measured). The check
 tests layout and taps, not React's submit handler.

BROKEN / NOT DONE

- `PLAYWRIGHT`/driver: none installed; every check here drives Chrome over the DevTools Protocol with
 the standard library, which is why they run anywhere `google-chrome` exists.
- The badge's bottom-right pill overlaps the composer's own corner on a *short* viewport (it landed
 on Submit's centre in the reproduction). In a real session with messages it does not touch the
 composer — measured 393x852 and 393x700. The question card now outranks it; the composer risk is
 filed as **P70**, not fixed, and the badge is being changed by another session right now (H78).
- The mobile layer's `button[aria-label] { min-width: 44px }` is not effective for the composer's
 Send button (measures 34px): a shipped rule wins. Not fixed, recorded.
- `journal.py import-flat` absorbed 20 entries written on other machines this hour (H78-H86, L218-L223,
 P68-P69, D73-D75, W40-W41). Flat files still hold anything newer.

NEXT

1. Owner reloads; confirm both fixes on the device.
2. Wire `phone-layout-check.py` and `question-card-check.py` into whatever runs the phone's checks
 (the gate's own cron/watchdog path), so neither can regress silently.
3. `state/in-flight.md` still lists the phone-layer check as unwired; it is now two checks.

EVIDENCE

`scripts/phone-layout-check.py`, `scripts/question-card-check.py`, `/tmp/phone-question-repro/site/`
(the replica with the app's 455,702 bytes of CSS), `/tmp/mob/qcard-final.json`,
`/tmp/mob/final6.txt`, `curl localhost:3086/dsh-phone-mobile.css` = 20,839 bytes carrying both
files' markers. Commits 48113bc, 618bf7c and the journal commits after them.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=295368ec2ee7be04 -->
