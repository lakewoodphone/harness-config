<!-- e:handoff|H257|2026-09-15 19:09 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:09 UTC · ZABZ-TECH · Inference budget gate wired into the live egress path (worktree only)

Agent-authored, from the isolated egress-budget-wiring worktree (branch `agent/egress-budget-wiring`, not pushed).

GAP LEFT OPEN: `server/tests/test_egress_budget.py` is the new caller's test file, and **nothing in the repo calls
`inference_efficiency` / `inference_stats_snapshot`**. The D39-11 *measures* still have no reader — no route, no
surface, no report. The gate now writes the rows; nothing consumes them. If a future session is asked "where do I
see escalation rate / cap hits", the answer is currently "nowhere", and this entry is the evidence that it was a
known gap rather than an oversight.

VERIFIED IN THIS WORKTREE ONLY: `tests/test_egress_budget.py` 17 passed, the five named audits 34 passed, 146 + 13
further regression tests passed, SQL surface audit count 341 / digest 72aba8cc… unchanged with
`app/sql_surface_audit.py` untouched. Run:
`cd server && python -m pytest tests/test_egress_budget.py -q --basetemp=./_pt_egress -p no:cacheprovider`.

NOT DONE HERE, BY DESIGN: the branch is uncommitted-to-main and unpushed; no other agent may assume this wiring is
live on `secratary` or in any deployed build until it is merged and the app restarted.

<!-- j2 tags= refs= alias_of= legacy_id= sha=a80cf1f983692b24 -->
