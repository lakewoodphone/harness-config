<!-- e:handoff|H363|2026-09-16 00:37 UTC|ZABZ-YOGA|open -->
## 2026-09-16 00:37 UTC · ZABZ-YOGA · F-010 applied: the comms index check is now on a 4-hourly clock and proven able to fail; four clocks live; report table repaired

## 2026-09-16 01:05 UTC · ZABZ-YOGA · Addendum to H362: F-010 applied (four clocks), and two report-table repairs

CHANGED
- **F-010 APPLIED — the comms index check is now on a clock.** `~/.fsearch/comms-refresh.py --check`
 already implemented a non-zero-on-stale failure surface and was **scheduled nowhere**: the cron ran
 the rebuild half only (`7,37 * * * *`), with no `MAILTO` in any of the 35 lines, which is half of why
 11.5 hours of website silence passed unnoticed. Now `12,42 * * * *` with `--max-age-minutes 45`.
 Backup `~/crontab.backup-20260916T003539Z` (46 -> 48 lines, nothing removed).
- **Proven it can actually fail, not just exist:** with `--max-age-minutes 1` against a 24-minute-old
 index it exits **1** ("last run was 24 min ago (limit 1)"); with `--max-age-minutes 120` it exits **0**.
 A check that has never been observed failing is a hypothesis.
- **Four clocks now, all tested by running them exactly as cron does:**
 `9,39` website-comms-push · `*/15` production-comms-freshness · `12,42` comms-refresh --check ·
 `23 */4` lpt-recon-check. Plus the `*/30` harvest that already existed.
- **Stated limitation, not hidden:** `--check`'s liveness budget names only `sms`/`call`, so it CANNOT
 see the four dead `dialpad_ui_*` surfaces (2,116-2,239 h). Those are now surfaced instead by the
 `dialpad_ui_surface_stale` runtime warning in the endpoint B-004 made honest. Between the two, the
 three failure modes - index staleness, website staleness, dead UI surfaces - each have a surface.
- Housekeeping: my report edits twice clobbered a table row header, leaving rows rendered as loose
 paragraphs. Both repaired and verified programmatically (table spans lines 168-193, 26 rows, 0
 malformed). Commits `34021bc39`, `dc09a0a43`, and the repair. The lesson is small but real: when
 editing a Markdown table with a replacement whose text ends in `|`, re-read the SAME boundary
 afterwards - I checked the row COUNT and it was right both times, which is exactly why the check has
 to look at the boundary and not the total.

NEXT
- Axis E's money operations and axis C's 18 owner-gated merges/splits wait on the owner (queue 65 asked).
- The `commsindex.py` trio (C-007/008/009) are diffs against a live file in `~/.fsearch`; each needs
 its own careful pass.

<!-- j2 tags= refs= alias_of= legacy_id= sha=e4a78c56d0573fc1 -->
