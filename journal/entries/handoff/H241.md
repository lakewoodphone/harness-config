<!-- e:handoff|H241|2026-09-15 15:26 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:26 UTC · ZABZ-YOGA · Two platform path bugs fixed from failing tests; pre-existing failures 9 to 1

**CHANGED (all preserved on `secratary-master-preserved-20260915`)**
- `681bc507` -- `scripts/vscode_chat_extractor.py` decodes workspace folder URIs into the host's path
 form. `test_vscode_chat_extractor.py` 1 failed -> 3 passed. This was a live data bug: on this host
 every workspace path was `home\zabz\code\...`.
- `f34df52f` -- the gateway's sensitive-path segments split on both separators, so the AppData rule
 works for a Windows path read on POSIX.
- Pre-existing suite set: **9 failed / 103 passed -> 1 failed / 111 passed**.

**REMAINING (1, known and understood)**
- `test_ai_gateway.py::test_sanitize_tool_call_out_of_workspace` -- four assertions, all encoding
 Windows-host semantics. On POSIX the guard blocks in every case (safe direction, L1520). It needs a
 platform-aware rewrite (assert the host's outcome, as I did for one of its assertions before
 restoring the file to avoid a half-edit). Until then P139 cannot close, and the honest statement is
 "the pre-existing set is one known test wide, not nine".

**NEXT**
1. Rewrite that test platform-aware, carefully, with a compile check between edits -- then measure the
 **full** suite once to see whether P139's other half (order-dependent errors) still stands.
2. Queue #39 (cheap-first routing off in production) is still waiting on the owner; nothing is blocked.

**EVIDENCE**
- `pytest <four files> -q` -> 1 failed, 111 passed (10.3 s); `pytest tests/test_vscode_chat_extractor.py -q`
 -> 3 passed; the decoder probe `posix: '/tmp/tmpaao8_wc5/Code/x' | match: True`; the reason string
 now "path is under AppData, denied by default"; commits `681bc507`, `f34df52f`; worktree still holds
 the other session's 79 files untouched.

<!-- j2 tags= refs= alias_of= legacy_id= sha=a024bf2346b2d150 -->
