<!-- e:handoff|H221|2026-09-15 03:17 UTC|ZABZ-YOGA|open -->
## 2026-09-15 03:17 UTC · ZABZ-YOGA · Her Mac got the secretary MCP; laptop engine back in session 1 with search working

STATE, 2026-09-15 ~03:15 UTC, ZABZ-YOGA. Continuing the same objective: her agent as powerful as the
owner's, self-learning, not crippled, everything from her machines labelled.

ADDED SINCE THE LAST HANDOFF:
* Her preset now carries the secretary MCP row (was ZERO MCP rows against the owner's seven). It runs
 ps_mcp_server.py as a local child process ON the authority over ssh, so her Mac needs no Python
 environment and nothing local can break it. Gated to non-Windows deliberately: her laptop has no
 route to the authority (Tailscale logged out, separate networks), and a row that cannot start is
 noise; the laptop reads the company through ps-system.mjs over HTTPS instead.
 EVIDENCE IT MOUNTED: after restart the engine log shows the server answering a ListToolsRequest
 ("Processing request of type ListToolsRequest", server.py:727) and ssh to secratary-ts returning
 SSH_OK from the Mac.
 NOTE ON VERIFICATION: a `--profile headless` run cannot see MCP tools at all -- headless mounts the
 base bundle, not the yocheved preset. I used it once and the model correctly reported the generic
 harness toolset, which looked like a failure and was not. Do not use headless to test preset tools.

VERIFIED WORKING NOW:
* Her laptop engine: pid 10896, SESSION 1, task result 0, port 3099 listening -- so the window is on
 her desktop, not in session 0 where I could not see it.
* Her laptop web search: real search snippets (GBP 61.14 at Laptop-LCD-Screen.co.uk for NV156FHM-T07),
 previously dead on every call.
* Both machines: danger-full-access, vision model, maxParallelToolCalls 20, hourly session shipping,
 15-minute config convergence. Mac autosync status result "ok"; Mac session sync ships on the hour.
* Her Mac's journal round trip: append -> autosync rebases -> applies -> pushes back to the shared
 remote, readable here tagged LAKEWOOECHSMINI.

OUTSTANDING / HONEST GAPS:
* Her LAPTOP's tool-belt is still thinner than the Mac's: ps-system (read-only HTTPS) + web search,
 against the Mac's secretary MCP. Closing that needs either the laptop on Tailscale or an
 HTTPS-reachable company MCP endpoint.
* The Mac mini is ONLINE right now but was offline earlier in the session; her machines come and go.
* YochevedHarness NSSM service no longer exists; stale engine.cmd lines survive only in an old log.
* Rotation of leaked provider keys (owner queue #31) is still pending and is genuinely his.

<!-- j2 tags= refs= alias_of= legacy_id= sha=ec359acc05920b13 -->
