<!-- e:handoff|H279|2026-09-15 20:13 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:13 UTC · ZABZ-TECH · Built the tree-level guard test PART-131 F-1 asked for - 4 tests, 485ms, proven to fail on a new unguarded procedure - and it corrected my own round-7 count

CHANGED
- **`307cf3d62`** — the tree-level auth guard test that **PART-131 F-1** asked for. `backend/src/trpc/__tests__/guard-coverage.test.ts`. **4 tests, 485 ms, and proven to fail on a new unguarded procedure.**

WHY THIS WAS THE RIGHT NEXT THING
PART-131's F-1 was the single finding that explained the most others: during the audit, 22 procedures across
four routers used the bare `procedure` builder (no authentication), two routers were **reachable from the public
internet**, and **every relevant test was green throughout**. The defect was never inside a procedure — it was
in a **declaration**, spread across 93 router files, so nothing could notice. This converts the audit's method
into a **permanent control**.

WHAT IT ASSERTS
- Every bare `procedure` declaration is either on an explicit **PUBLIC** list with a written reason, or it fails CI.
- Every PUBLIC entry still exists as a bare declaration — so the exemption set cannot **rot into stale exemptions**.
- A scan that read too few files or declarations **fails**, so an empty scan can never read as health.
- The six routers that were open on 2026-09-15 have **no bare procedure left** (regression pin).

PROVEN TO BITE — before claiming it works
This session has produced **four checks that gave confident answers while structurally unable to be right** (the
`esbuild` check that never ran, the census from a stale tree, mention-counts mistaken for connectivity, and a
broken uniqueness check). So the control came first:
- injected a bare `zzControlProbe` into `faq.ts` → **1 test failed and named it**: `faq.zzControlProbe (faq.ts)`
- reverted → **4 passed**
Without that step this would have been a fifth unverified green line.

TWO INSTRUMENT MISTAKES I MADE AND FIXED, both recorded in the file
1. **The first version imported `appRouter`** and read `_def.procedures`. It worked, but pulled the whole
 backend service graph into the test — **140 s of import** — and depended on tRPC internals that moved
 between versions. Heavy and brittle for a check that only reads declarations. Replaced with a **source
 scan**, the same method the census used, now **485 ms**.
2. **`process.cwd()` is the repo root, not the backend**, so the scan pointed outside the backend and threw
 `ENOENT`. It now resolves from `import.meta.url`. **The throw was the good outcome** — a scan returning
 nothing would have made every assertion vacuously true.

THE PUBLIC LIST IS THE POINT, AND IT CAUGHT MY OWN RECORD
21 of the 22 bare declarations are legitimate public surfaces — 13 auth sign-in procedures, 3 customer-account
sign-up procedures, 5 faq published-content ones — each now carrying a one-line reason. **Adding a public
procedure is a deliberate act.** The 22nd is `ui.ts`, still bare, listed as **ACKNOWLEDGED DEBT** with the
reason and the branch holding its fix.

Writing that list also **corrected my own round-7 handoff**: I had counted bare procedures with a PowerShell
grep inside a pipeline and got unreliable numbers, then wrote DEBT entries for `smart-fields` and `systemLog`
which my own round-7 commit had **already guarded**. The test caught the contradiction — it reported those six
as *"listed PUBLIC but no longer bare"*. The true state is **22 bare declarations in 4 files** (`auth` 12,
`faq` 5, `customer-account` 3, `ui` 2), verified by an independent per-file count.

ALSO FIXED ALONG THE WAY
- **Built `packages/shared-types`**, whose missing `dist` is the cause of the **22 pre-existing
 `@phone-and-tech/shared-types` resolution errors** that dominate `tsc` in this repo. Tests that import the
 router tree could not previously run at all. This is a real improvement beyond the test itself: those 22
 errors were noise masking any genuine type error in that area.

WHAT IT DOES NOT DO, stated in the file
It cannot see a bare procedure whose **body** manually checks `ctx.user` — `ui.navigationContext.get` was exactly
that case. The deployed half is `/opt/lpt/guard-check.sh` (nine anonymous probes against a live host); this is
the CI half. **Both are needed; neither replaces the other.**

BROKEN
- Nothing. Posture intact, workspace clean at `307cf3d62`.

NEXT
1. **The proper rebuild** — still first; still the only thing making the audit's source fixes real.
2. Land `agent/guard-ui2` (needs `pnpm install` in that worktree and a scoped pass over 16 stale tests), then
 delete the `ui` ACKNOWLEDGED DEBT block.
3. Wire `guard-coverage.test.ts` into the repo's CI so it runs on every push, not only when someone remembers.
4. Then the original plan: 78 never-started parts, 17 REVISION parts, and one consolidated pass over the owner
 decisions (C-117-1, C-119-1, C-123-1, C-126-1, C-129-1, plus the queued Stripe question).

<!-- j2 tags= refs= alias_of= legacy_id= sha=040906af28c5b583 -->
