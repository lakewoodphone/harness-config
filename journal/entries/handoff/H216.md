<!-- e:handoff|H216|2026-09-15 01:50 UTC|ZABZ-YOGA|open -->
## 2026-09-15 01:50 UTC · ZABZ-YOGA · lpt-sync-to-production: pre-create guard + idempotency + real dry run (committed, push blocked)

CHANGED (secratary, /home/zabz/personal-secretary-mvp)
 commit ed4fdcca "fix(lpt-sync): pre-create guard, order write-back, a real dry run"
 Local commit ONLY — the push was rejected (see BROKEN).
 scripts/lpt-sync-to-production.py 976+/17- sha256 a3e6e021d497…
 tests/test_lpt_sync_precreate_guard.py (new, 380 lines) sha256 8c4069bfa85f…
 Previous version of the tool is preserved at /tmp/lptrecon/lpt-sync-to-production.py.ORIG
 Full diff: /tmp/lptrecon/lpt-sync-precreate-guard.diff (secratary) and
 C:\Users\ezabz\code\lpt-sync-fix\lpt-sync-precreate-guard.diff (ZABZ-YOGA).
 No production write was made: every run was --dry-run against a read-only session.

WHAT THE DEFECT WAS
 push -> order created -> order closed -> website_db.find_recent_order skips
 COMPLETED/CANCELLED/UNFIXABLE -> record carries no orderId -> looks unsynced ->
 pushed a SECOND time. Two of the five duplicates of 2026-09-15
 (WO26082712644, WO26082712640) came from this tool on 2026-08-27.

WHAT THE FIX IS
 1. Pre-create guard, read-only, before ANY write (customer row included):
 refuse when operational.shouldSyncToWebsite is false | the record references
 a live production order | the matched customer already has ANY order (any
 status, any age) whose title matches exactly or shares >=3 significant
 tokens with the record's device+issue | a sibling record on the same phone
 already put this case in production. Checking ALL phone-matched rows, not
 create_customer's arbitrary fetchone(), and also dashed/+1 phone variants.
 2. Idempotency: on a successful create, orderId/orderNumber are written back
 into the record JSON atomically (workflow.orderId/orderNumber +
 references.productionOrder).
 3. Real dry run: opens production with default_transaction_read_only=on, runs
 the identical matching + guard, prints CREATE/SKIP+reason. New
 --dry-run-all / --report.

MEASURED (read-only, all 368 records)
 CREATE 14, SKIP 354 = 174 shouldSyncToWebsite=false + 140 already linked to a
 live order + 27 existing-order match + 13 sibling record.
 All 8 incident records SKIP naming the existing order.

BROKEN
 `git push origin master` -> REJECTED (non-fast-forward). Local master is
 41 commits ahead and 134 behind origin/master (origin tip 7a566b6e,
 2026-09-14 21:27 -0400, authored mostly by zabz68). The 41 local commits are
 2026-09-15 01:00-01:06 by Secretary Autopilot/Zabz/zabz68 and were never
 pushed. I did NOT merge, rebase or force: that history belongs to other
 sessions. My commit sits on local master on secratary and is not on GitHub.
 Remedy: `git pull --rebase origin master && git push origin master` from
 secratary — but that replays the other 41 commits, so it is not mine to run.

NEXT
 - Reconcile the diverged branch (owner/other session), then push ed4fdcca.
 - The backlog is NOT cleaned: the 5 known duplicate pairs still exist in
 production, and 174 records are marked shouldSyncToWebsite:false and are now
 silently never pushed (behaviour change — reverse deliberately if wanted).
 - The 14 CREATE records are genuinely absent; pushing them is a separate,
 still-unmade decision.

EVIDENCE
 /tmp/lptrecon/verify_c.py + its output (row counts before/control/after ==
 identical), /tmp/lptrecon/dryrun_all.out (per-record decisions),
 /tmp/lptrecon/decisions.json, verify_d_audit.py / verify_separator.py /
 verify_twins.py (false-positive and false-negative audits).
 `LPT_SYNC_DB_TESTS=1 pytest tests/test_lpt_sync_precreate_guard.py` => 33 passed.

<!-- j2 tags=lpt-sync,production-db,duplicates,secratary,git-divergence refs= alias_of= legacy_id= sha=7c16e175ae076d23 -->
