<!-- e:handoff|H234|2026-09-15 14:37 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:37 UTC · ZABZ-YOGA · The pain count itself was wrong: three callers counted marker status, and five entries are resolved

**CHANGED since H233**
- **`journal.py` counted open pain three times over from the frozen marker status.** `cmd_status`
 (the page every session reads), `cmd_stats`, and `cmd_state` (which writes
 `state/open-pain.md`, the offline fallback) all asked `marker != "done"`, so `superseded`,
 `retracted` and `blocked` entries were open work forever and the page's number drifted further
 from the truth with every entry anybody resolved. One `open_entries()` built on
 `effective_status` now serves all three (`d16360b`). Page 95 -> **91**, `open-pain.md` says
 "91 open of 110", `stats` 784 open / 36 closed, `check` 0 errors -- all four agree.
- **Five pain entries resolved with evidence**: P6 and P128 **done** (message-and-question rot
 fixed by `a6b08392` + `9943817e`; `check` exits 0 and `absorb_all` no longer aborts), P55 and its
 legacy duplicate P92 **superseded** (owner SMS off is his decision and the sensors were re-aimed
 at the app channel), P140 **superseded** by P145 (same divergence, fresher numbers, plus the
 preservation branch). P144 was already resolved for the same reason.
- The open pain count is therefore **91**, not the 95 the page used to claim -- expect the number
 to keep dropping as older entries are checked against evidence.

**NEXT**
1. **Finish the triage.** The open list is 91 and this session only read the first 52 entries. The
 method that worked: read the entry's own claim, then verify it against the live system, then
 `resolve --status done|superseded --why "<the evidence>"`. Entries that are already fixed and
 still counted as open are the cheapest thing to remove from a future session's reading list.
2. Observe the sync-alert clear; set the journald retention cap from the measured rate (P147).

**EVIDENCE**
- Commit `d16360b` and the four-surface agreement quoted above; `check` 0 errors / exit 0.

<!-- j2 tags= refs= alias_of= legacy_id= sha=3a36047a591a17ae -->
