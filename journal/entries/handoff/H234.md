<!-- e:handoff|H234|2026-09-15 15:30 UTC|ZABZ-TECH|open -->
## 2026-09-15 15:30 UTC · ZABZ-TECH · RattleByte #188880 logged and task reopened; install procedure confirmed correct and the USB stick is the ranked cause; ps_task_update/ps_memory_search fixed (8e8616e5)

## CHANGED
- **RattleByte ticket #188880 / Panasonic DMP-BD84 region-free — fully logged and task reopened.** Case log:
 `lpt-hub/hardware-diagnostics/panasonic-dmp-bd84-rattlebyte-diagnostic-2026-09-12.md` (the vendor's reply, the
 timeline, a ranked cause analysis, a ready-to-send portal reply, and a 10-source research brief). Task `24658`
 reopened via a direct `PATCH /ceo/tasks/24658` after the MCP tool for it proved dead.
- **Two dead MCP tools fixed.** `scripts/ps_mcp_server.py` — `ps_task_update` and `ps_memory_search` had been
 calling routes that do not exist (`/api/v1/tasks/update`, `/api/v1/memory/search`), returning **HTTP 404 on every
 call**. Repointed at `PATCH /ceo/tasks/{id}` and `GET /ceo/db/memories/search`. Committed on the authority as
 **`8e8616e5`**; 17 deterministic checks pass and both routes were proven live.
- Research landed and corrected the working assumption — see `NEXT` below.

## IN FLIGHT
- Nothing of mine. The background research subagent `0a8dbdba` finished and its brief is folded into the case log.

## BROKEN
- Ticket #188880 is **Closed** by the vendor and reopens only when we post to the rattlebyte.com portal. Do **not**
 email `info@rattlebyte.com`; the vendor said that mailbox is not monitored for support.
- Two content-free auto-drafts still sit unsent in Gmail thread `1a094aafde65d7fb` (2026-09-13, 2026-09-14).
 Neither answers the vendor. Superseded — do not send.
- **`secretary.db` write contention is real and reproducible** (see the new pain entry, P48 follow-up): ten
 consecutive `PATCH /ceo/tasks/24658` returned `500 internal_error` / `database is locked` in 0.02–0.04 s while the
 Dialpad harvesters ran, 8 s apart, from two different processes — while `GET /health` stayed **200 in 0.043 s**.
 Do not read a fast 500 from the company API as "the request was wrong". Read `journalctl -u secretary-api`.
- **`personal-secretary-mvp` is diverged on both Windows checkouts**, so this fix cannot be pushed cleanly:
 secratary is **140 ahead / 71 behind** origin with **79 dirty files**; the desktop checkout is **60 behind /
 3 ahead**. The fix is committed on the authority but will be lost if that checkout is ever force-reconciled.
 This is P140/P85, unchanged and still nobody's job.

## NEXT
1. **Owner, ~1 minute at the player.** Photograph the back plate (to pin the variant: `EB`/`EG` = Europe,
 `GN` = Oceania/SE Asia, `P` = USA). Read the **OEM Main Version** at: HOME MENU → Setup → Player Settings →
 System → System Information → Firmware Version Information → Main Version. Note connectivity (Panasonic's BD84
 specs say Ethernet/LAN only, no Wi-Fi — confirm on the unit).
2. **Post those to ticket #188880 on the rattlebyte.com portal** (posting reopens it), plus two questions back:
 (a) is the `BD83` Initial archive right for this exact BD84 variant, and (b) what triggers the Initial install.
 Ready-to-send text is in the case log. Outbound is **not sent** — no per-message instruction.
3. **Cheapest fix, needs nobody: try a 256 MB–2 GB USB 2.0 stick, single partition, FAT32, formatted on Windows.**
 Panasonic's spec for this firmware family is 256 MB–2 GB; a Panasonic BD player is on record accepting a 4 GB
 FAT32 stick where a 16 GB one gave exactly this error. The message names the *device*, not the file.
4. Watch the mod board's LED during the retry — after a successful install the board pairs with a **flashing red**
 LED, then a brief green flash. Do not power off until pairing completes; pull the stick on reboot.

## EVIDENCE
- Vendor reply captured only from the portal page; no mailbox has it. `email_triage_log.id 2965` (2026-09-12T08:38:50Z,
 `salesbdmods@rattlebyte.com`, `action_taken=task_created`) is the **notification**, not the reply.
 `email_thread_state` thread `1a094aafde65d7fb`: `message_count=1`, `needs_response=1`. Gmail `in:anywhere
 rattlebyte|188880|BD84` over all four connected accounts → no agent reply. `info@lakewoodphoneandtech.com` is not
 a connected account and that is where osTicket addressed the ticket.
- Task `24658`: created 2026-09-12T08:38:50Z, completed **09:07:57Z**, reopened 2026-09-15T15:25:06Z, 2,915-char
 description, tags `email, followup, action_needed, blocked_on_owner, rattlebyte, dmp-bd84`.
- MCP fix: `8e8616e5` on `secratary:/home/zabz/personal-secretary-mvp`; `git show --stat` = 1 file, +33 −5;
 backup at `scripts/ps_mcp_server.py.bak-20260915`. Search is now `grep -n 'api/v1/tasks'` → none.
- Install procedure (correcting the assumption that a service menu was needed): RattleByte's own fitting PDF says
 unpack to FAT32 → insert in the **front** USB slot **at the Home Screen** → it starts automatically.
 No RattleByte or forum source describes a service-menu trigger for BD83/BD84.
- `journal.py check` → 0 errors. New entries this session: H233, H234, L1512, P146, P147, P148, D154, W(new).
- Case log: `lpt-hub/hardware-diagnostics/panasonic-dmp-bd84-rattlebyte-diagnostic-2026-09-12.md`

<!-- j2 tags=rattlebyte,dmp-bd84,mcp,handoff refs= alias_of= legacy_id= sha=b3aa16d8428e6bba -->
