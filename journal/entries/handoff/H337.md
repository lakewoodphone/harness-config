<!-- e:handoff|H337|2026-09-15 15:54 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:54 UTC · ZABZ-YOGA · Quiet-hours timezone fixed; the suite is 2711 passed / 4 flaky / 0 errors

**CHANGED (preserved on `secratary-master-preserved-20260915`)**
- `6947d9d8` -- the four quiet-hours failures were the test helper's timezone: `_local_ts` builds
 server-local epochs while `in_quiet_hours` evaluates in America/New_York. Added `_family_ts` and
 moved nine call sites. `test_family_chat_personality.py` 4 failed -> 62 passed.
- Full suite: **4 failed / 2,711 passed / 0 errors** (from 21 failed / 153 errors / 2,123 passed when
 this work began).

**WHAT IS LEFT**
- **P190: the residual failures are flaky, ~six known members, one to four per run, membership varies.**
 Three mechanisms are visible and each is cheap to test: the server-local `_now_ts` patch in a
 cooldown test; an in-flight marker asserted right after a threaded worker (a race); and
 `test_mcp_enabled` asserting a global setting that another test can mutate (state leakage, and the
 most persistent flake). Fix order: shared settings first, then the timestamp, then the race.
- **The descriptor leak** (P189): the limit is raised in conftest, the loops are still not closed.
- Queue #39 (cheap-first routing off in production while the DB override says true) still waits on the
 owner. Nothing is blocked by it.

**METHOD NOTE FOR THE NEXT SESSION**
Run the full suite **twice** before concluding anything about failures: the set moved between runs
(7 -> 4, different members), and a single run would have sent me chasing the wrong test. `pytest tests/
-q --tb=line -rf` takes ~3:20 and prints the exact failing ids.

**EVIDENCE**
- `pytest tests/test_family_chat_personality.py -q` -> 62 passed; `pytest tests/ -q --tb=line -rf` ->
 4 failed, 2,711 passed, 0 errors (203 s); the six-invocation flake table above; commit `6947d9d8`;
 the four-file pre-existing set remains 112 passed.

<!-- j2 tags= refs= alias_of= legacy_id= sha=c5191d1c1366a233 -->
<!-- renumbered H243 -> H337 on 2026-09-15 (ZABZ-YOGA): id H243 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
