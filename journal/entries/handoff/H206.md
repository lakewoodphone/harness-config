<!-- e:handoff|H206|2026-09-14 23:23 UTC|SECRATARY|open -->
## 2026-09-14 23:23 UTC · SECRATARY · Fios fiber home install recorded: photos, credentials, ONT serial, and four open items

### CHANGED
- New durable record: `personal-secretary-mvp/docs/network/verizon-fios-home-2026-09-14/` —
 `README.md` (the facts, split into measured / quoted / inferred), `MANIFEST.json` (sha256, bytes
 and pixel size per photo) and `photos/` (six byte-identical copies of what the owner sent).
- Local commit `52129b53`; pushed to origin/master as **`2848f5e3`** and verified with `git ls-tree`
 (the local branch could not push directly — it is 20 ahead / 108 behind origin; see lessons).
- Memory id **19173** in `secretary.db` (`fact`, importance 8, tags include `home_network`,
 `fios`), written through `ps_action save_memory` then corrected in place (`memory_type` came back
 `observation`, importance 3, tags `["owner_context","from_chat"]` — the action ignores those
 parameters).
- Tasks **25112** (claim the $100 Amazon gift card, due 2026-11-18), **25113** (verify the service
 address), **25114** (measure the two-router topology).

### THE FACTS, so they do not have to be re-derived
- Verizon Fios **300 Mbps**, order `NJ12338123101`, installed **2026-09-14**. **$30.00/month**
 ($40 plan − $10 Auto Pay + Paper Free; access-point fee waived, $0 tax) + `$99` setup; first bill
 estimated `$129`. Auto Pay drafts on the **5th**, first on **2026-10-05**, bank `…7930`. Account
 `XXXXXX-674-0001`. Promos: **$100 Amazon gift card** and a 3-year price guarantee.
- Hardware: **Nokia ONT S/N `ALCLECEF1FB204`**, P/N `3FE52343ABRB` and `3MV00743AD`, Model 02,
 12VDC 2.5A, with its paired Verizon power/battery unit (blue CONT/BAT lit) — Ethernet out to the
 **10G WAN** of a **Verizon router, model on the label reads `CHR30A`**, SSID **`Verizon_ZYFY4N`**,
 admin `http://mynetworksettings.com`. Plus a second router, **ASUS RT-AX1800S V2**, SSID
 **`ASUS_B4`**, SN `S9IG7530707865` — the network the owner's own phone was joined to.
- Nothing about Fios existed anywhere before this: zero hits in `memories`, `docs/`, the journals.

### INFERRED, NOT MEASURED (do not repeat as fact)
Fiber → ONT → Ethernet → router 10G WAN, since that is the only cabled port in photo 2. Whether the
ASUS is in router mode (double NAT) or access-point mode is unknown, as is whether home devices can
all see each other. Home Assistant (`homeassistant.local:8123`) failed reachability on 09-12 and
09-13 and has not been re-tested since the cutover. This session could not measure any of it: the
server is on the **office** LAN (`192.168.50.77`, gateway 192.168.50.1) and every home-range probe
was refused/down.

### NEXT
1. When a home-side machine is reachable over Tailscale, settle the topology (ASUS mode, subnets,
 HA reachability) and replace section 3 of the README with measured readings.
2. Watch task 25112 — the gift card has a real deadline and no sign it was registered.
3. `journal.py questions` after queue changes.

### EVIDENCE
Photo sha256s are in `MANIFEST.json` and each matches the attachment hash the harness reported.
Email facts are quoted with Gmail message ids in README §2 (e.g. order email `1a06e5969904fde4`,
install-complete `1a0a17b4d7e807e4`, Auto Pay `1a0a19aeb6db9740`), read read-only on 2026-09-14.

<!-- j2 tags= refs= sha=29069f9de8aceefa alias_of= -->
