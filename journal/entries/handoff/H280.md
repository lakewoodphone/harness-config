<!-- e:handoff|H280|2026-09-15 20:15 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:15 UTC · ZABZ-TECH · 2026-09-15: credential exposure closed, fleet API hardened - what changed and what is left

CHANGED
- Public plaintext fleet API closed: 8003 is loopback-only; TLS /api/fleet/* plus a new
 /api/waze/* route live on mdm.abletelsolutions.com.
- Live secrets scrubbed from 19 tracked/untracked files; telnyx_usage_freshness.py now
 refuses to run without PG_DSN instead of shipping a password default.
- Rotated and verified: NanoMDM API key, NanoDEP API key, Postgres password.
- Two dead scripts repaired: push-drn26.sh (embedded a stale `admin:<pw>` and called an
 unpublished port plus a non-existent route) and Get-DeviceResponsive.ps1 (a "$DRN:" parse
 error meant the whole script failed to load under pwsh 7).
- Pushed to origin/master 6f403923 on lakewoodphone/personal-secretary-mvp (7 commits),
 including the scripts/waze CLI fixes and the fleet-client TLS URL migration.

IN FLIGHT
- Subagent 94533da3 is implementing the fleet-api silent-failure class from the audit:
 _raise_alert swallowing its own exception, /health ignoring gateway.ok, three health
 fields that can only read zero, queue snapshots read but never written, health-history
 writing push_ok=1 on failed probes, and a failed usage read presented as "0 MB / ok".
- _waze-fleet/ptf-security (portal router guards) and ptf-portal (read-only customer waze
 bridge) are built and unmerged. phone-and-tech-full `test` carries 0 guards on terminal.ts
 and a2pAutoReply.ts while the deployed build has 13 and 6.

RISK
- Redeploying deploy/waze-mdm/docker-compose.yml from any ref older than 6f403923
 republishes plaintext 8003. The fix is in git; the trap is deploying stale.
- web/next.config.ts forwards /shadow/* with no Authorization header to a token-gated API.
- The merged scripts/waze/onboard.py (Activation Lock step + honest-exit work) has never
 been run against a real device.

NEXT
- Integrate ptf-security + ptf-portal into phone-and-tech-full `test` with the suite green.
- Move the Caddyfile bcrypt hash into .env (see the pain entry).
- Run `diagnose.py profiles <drn> --refresh` once on a wake device; the enqueue path has
 never been exercised end to end.

EVIDENCE
- Public :8003 -> connection refused; TLS /api/fleet/devices -> 200 (65 devices);
 /api/waze/device/FFYGNQ8AN72J -> 200 with token / 401 without; localhost:8003/health -> 200;
 /version -> 200. pytest scripts/waze -> 139 passed. Rotation matrices as above.
- Server backups: /opt/waze-mdm/.env.bak-20260915-195003 (compose + Caddyfile pre-change),
 .env.bak-20260915-195255 (pre nanomdm/nanodep), .env.bak-pg-20260915-201417 (pre postgres).

<!-- j2 tags=waze-mdm,security refs= alias_of= legacy_id= sha=192efc58fb764407 -->
