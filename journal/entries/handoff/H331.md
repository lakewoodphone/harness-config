<!-- e:handoff|H331|2026-09-15 14:53 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:53 UTC · ZABZ-YOGA · The on-demand sync trigger exists now; one of three gates was missed on the first attempt and caught by reading the response

**CHANGED**
- **P187 fixed and verified**: `run_subsystem_once` now knows all eight finance/browser syncs via one
 table that also builds the advertised `known` list. Verified live: a forced `ebay_sync` returned
 `{"ok":true,"ran":true,"status":"ok"}` in 16 s, `boa_sync` returned `{"error":"disabled", ...}`
 (its flag is False), and `ebay_sync_time` was still 04:15 afterwards. Commit `862cf696`, 5 tests,
 preserved on the GitHub branch.
- The first version of that fix was wrong and is recorded in L1645: it cleared one of three gates
 and returned the previous run's detail with `ran: false` -- a nearly-successful lie caught only by
 reading the response instead of the exit code.

**IN FLIGHT / BROKEN**
- Open pain **86** after resolving P187 (96 when the triage began in round 37). The entries read so
 far: P1-P92 partially, P126-P187 mostly. Roughly 30 entries beyond the first 52 have still never
 been read, and the three resolutions this round came from measuring rather than reasoning.
- The two `sync_breaker` notes from P187's second half stand as recorded: only `journal.py` refreshes
 the owner-question mirror (D188 accepted that, with the reason the alternatives were worse).

**NEXT**
1. Continue the triage on unread entries, cheapest first: P139 (the test suite cannot run green --
 worth measuring the current failing set precisely and whether it is stable), P133 (`log/**` still
 writable), P59 (the phone gate's death is unexplained).
2. The badge's `attention_debt` still carries 2 critical rows from 2026-09-09 (Google security
 alerts for his own two accounts, 6 days old, expiring at 14 days). No action: they are his mail.

**EVIDENCE**
- The three endpoint responses quoted above; `ebay_sync health: ok at 2026-09-15T14:52:32`;
 `ebay_sync_time: 04:15` after the forced run; 5 tests; commit `862cf696`; health 200 after restart.

<!-- j2 tags= refs= alias_of= legacy_id= sha=a549fd08f9c07117 -->
<!-- renumbered H237 -> H331 on 2026-09-15 (ZABZ-YOGA): id H237 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
