<!-- e:handoff|H291|2026-09-15 20:58 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:58 UTC · ZABZ-TECH · W2 applied to live and verified; the residual restart rate is deliberate external restarts, not a fault - and W2's leak effect is suggestive but unproven

MEASURED 2026-09-15 20:53-20:57 UTC on secratary, after applying W2 (agent/w2-conns 04689550) to the live tree.

APPLIED AND VERIFIED: 14/14 tests pass on the LIVE tree (repo venv, 12.12s); both files compile; the security guard survived (grep -> 1); `threading.enumerate` (2 refs) and `CONN_CACHE_IDLE_TTL_S` (6 refs) are present, so the shipped design is the one that avoids the count-cap crash; app healthy 28s after restart; 0 HTTP 5xx; and BOTH layers work together -- loopback /api/v1/receipts 200, public 401. Backups *.bak-w2-20260915-205344. An automatic-revert-if-unhealthy guard was built in and did not need to fire.

3-MINUTE OBSERVATION (offered as suggestive ONLY): handles 183 -> 185 -> 186, lock errors 0, api starts 0 per minute. For contrast, the pre-observation baseline was 187 handles and 7 lock errors/30m, and W2 measured 326 -> 451 handles in 41 min. So handle growth looks slower (~+1/min vs ~+3/min) and locks are zero across three minutes -- BUT THREE MINUTES CANNOT ESTABLISH THIS. P170 says a window must be an order of magnitude longer than the fault period; the leak's period was ~40 minutes. Do NOT record W2 as having fixed the leak or the lock rate yet. The confirming measurement is an hour without a restart, and it has not been taken.

FINDING WORTH MORE THAN THE FIX: THE ELEVATED RESTART RATE IS NOT A FAULT, NOT A CRASH LOOP, AND NOT MY WATCHDOG. In 30 minutes: 8 explicit `Stopping secretary-api.service` lines, 8 `Scheduled restart job`, 0 watchdog `restarting` lines, 0 watchdog `health failed` lines, and exactly ONE real crash (`code=killed, status=9`). Paired `Stopping -> Deactivated successfully -> Started` with no traceback between them means an EXTERNAL actor is calling systemctl stop/restart roughly every 4 minutes. Most likely the second operator session that has been editing this box all session. That reframes the thing I spent round 1 fixing: the original storm was genuinely the watchdog (339 starts/7d, 53/hour, its own log naming itself), but a residual ~16 starts/30m is somebody deliberately restarting, and no watchdog tuning will touch it. Before treating any restart metric as a health signal on this host, separate DELIBERATE restarts from crash restarts -- `Scheduled restart job` vs `Main process exited`, and `Stopping` vs `Deactivated` -- because they look identical in a raw start count.

ALSO UNEXPLAINED, PRE-W2: `Exception Group Traceback` pairs at 20:53:18 and 20:53:28 from python pid 261604, i.e. the process BEFORE the W2 restart. Not caused by W2. Unexplained and worth a look.

<!-- j2 tags=database,lock,restart,w2,unproven refs=P170,L1588,H285 alias_of= legacy_id= sha=fae3136b169d31e4 -->
