<!-- e:handoff|H232|2026-09-15 14:28 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:28 UTC · ZABZ-YOGA · The app can narrate itself again (and the dead sweep was never dead): logging configured, credentials redacted, app_voice check added

**CHANGED**
- **The application can narrate itself again.** Measured before: the running service's
 root logger was at **WARNING with zero handlers**, so every `logger.info` app-wide was
 discarded and WARNING+ reached the journal only through Python's `lastResort` handler
 (bare message, no level, no logger name, no sub-second time). After: root INFO, one
 formatted handler, **124 INFO lines in the first minute**, 152 of 1600 journal lines
 in 30 minutes in the app's own format. `app/logging_setup.py` (new) +
 `configure_logging()` at app/main.py:47 (before the first `from app.` import so
 import-time records count). Commits `c9f11107`, `01a15bfd`.
- **Credentials are redacted at that handler** (`01a15bfd`), because INFO is now on
 app-wide and this codebase logs provider URLs and OAuth flows at INFO: Bearer tokens,
 key/value pairs whose key names a credential, `sk-`/`rk-`/`pk-`/`ghp_`/`xox*` keys,
 credential query parameters. Two flaws were found by hand-testing it rather than
 trusting it -- the first key list only had the underscore form so `X-Api-Key=...`
 walked through, and `Bearer` was matched as the value, leaving the token in the line.
 Both are regression tests now. 0 leaks over 8 samples; ordinary diagnostics unchanged.
- **Two silent swallows in `_maybe_run_lifecycle_sweep` now speak**: `except
 ImportError: pass` became a WARNING, the error branch moved from DEBUG to WARNING, and
 a refusal states its reason once (disabled / waiting for the interval). Those lines
 produced the answer below.
- **`check_app_voice` in the kernel** (`b06e02b`): reads the service journal and looks
 for the app's own formatted INFO lines, so the logging regression cannot return
 unnoticed. An unreadable journal is a refusal (UNKNOWN), a stopped service is not
 called silent, and a green reading carries the newest formatted line's timestamp.
 Live: `info -- "the application is audible: 152 of 1593 journal line(s) in the last
 30 min carry its own INFO format"`. The kernel is at **14 checks**.

**CORRECTION -- my premise this round was wrong.** I reported to myself that the
lifecycle sweep had silently died (last line 13:30:09, across four restarts). It had
**not**. It was running every 120 s throughout, and running correctly: the first sweep
after the logging fix returned `task_lifecycle_updated=37`,
`priority_task_goals_created=3`, `terminal_waiting_steps_cleaned=1` with the new
`owner_messages_expired` / `owner_messages_superseded` keys. What died was the *log
line*, because the process that had been formatting INFO (Twilio's HTTP client calling
`logging.basicConfig()` at import) was replaced. I built two diagnostics on the wrong
premise before checking what silence was made of; both are worth keeping, and neither is
evidence that a sweep was down. See L1512 and P3.

**IN FLIGHT / BROKEN**
- **`agent_questions` rots exactly as the owner queue did**, now measured: **313
 pending**, oldest **3226 h (134 d)**, newest 1405 h (58 d), **nothing created in 30
 days**, all `agent_id=orchestrator`. They are SMS-approval asks from the SMS era
 ("Ready to send this SMS to +1848... Mordecai's iPhone 14 repair"), a Gmail
 re-authorisation ask, and at least four paraphrase variants of the same Pesach
 question. The same shelf life applies; the channel looks vestigial, and the kernel
 still reads its count as owner-attention debt ("oldest question 134d old") -- a
 permanently red line about a channel nothing writes to.
- **`attention_debt` is still `critical`** on 2 six-day-old Google security alerts.
 Severity-versus-freshness is still the open fix: a week-old alert should not keep the
 owner's badge permanently red.
- **Journalled retention has no explicit cap** (P147): `journalctl --disk-usage` 916 MB,
 `SystemMaxUse`/`MaxRetentionSec` unset, filesystem 54% full. Choose a number from the
 measured rate after a day of the new logging.
- **The sync-alert clear is still unobserved**: the two `sync_needs_login` rows remain
 `held`, and the 04:30 ingest I once attributed to them was Plaid. The next genuine
 `amazon_sync`/`ebay_sync` success clears them through `_clear_owner_sync_alerts` and
 logs `autopilot: cleared N owner alert(s) after <name> succeeded`.
- **My own documented lesson recurred**: I put `$(date +%Y%m%d-%H%M%S)` inside a
 PowerShell double-quoted string destined for bash, for the fourth time; the backup
 landed with an empty suffix. Cost: nothing (the patch applied, compile passed), but
 the rule is mechanical -- remote bash goes in a single-quoted here-string, always.

**NEXT**
1. Give `agent_questions` a shelf life and stop the kernel counting a dead channel's
 backlog as live owner debt.
2. Make `attention_debt`'s severity follow freshness.
3. Observe the sync-alert clear; check the journald cap.

**EVIDENCE**
- Commits on the authority: `c9f11107`, `01a15bfd` (personal-secretary-mvp, pushed to
 `secratary-master-preserved-20260915`), `b06e02b`, `6565c94` (ceo-kernel). Live
 readings quoted above; 8 logging tests and 5 shelf-life tests pass; health 200 after
 each restart; `.runtime/deployed-commits.log` carries the deploy lines.

<!-- j2 tags= refs= alias_of= legacy_id= sha=4c96fd65e9b0b49c -->
