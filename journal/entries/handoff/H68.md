<!-- e:handoff|H68|2026-09-14 05:49 UTC|ZABZ-YOGA|done -->
## 2026-09-14 05:49 UTC · ZABZ-YOGA · The journal is a tree of documents with reads now — 389 entries instead of six files nobody could afford to open

**CHANGED**
- The journal is now a tree, not six flat files: `log/<kind>/<month>.md` (append-only, sharded),
 `state/` (NOW.md, open-pain.md, owner-questions.md, in-flight.md, status.tsv), `index/entries.tsv`
 (generated), `tools/journal.py` (the only writer and the cheapest reader), `archive/` (the
 pre-rebuild record verbatim plus MIGRATION.md). `journal/README.md` is the map.
- 389 entries migrated from the **union of all three machines and both stray copies**; 17 sources
 audited with `journal.py audit`, **0 entries missing**. Collided ids repaired with suffixes.
- `QUESTIONS.md` retired: open owner questions are the `owner_decision_queue` table and nothing else.
 Five live ones reconciled into it (#11 nothing has reached him since 2026-07-19, #12 the email reply
 loop, #13 Yocheved's write access, #14 iPhone location, #15 Google Voice); sixteen closed with a
 disposition each in `archive/README.md`.
- `scripts/owner-queue.py` is now tracked in the harness instead of living only on `secratary`
 (PAIN P47), gained `--json` for the mirror, and is deployed and verified there.
- The `zabz` preset's journal and queue sections were rewritten **in the generator as well as the
 output**, and running the generator was verified to reproduce the file byte-for-byte.

**IN FLIGHT**
- The flat files are still live and one other session is writing them: all five were dirty in git at
 01:19-01:23 while the migration ran. `journal.py import-flat` absorbs anything new and `check` warns
 when a flat file holds an entry the log lacks. Retire them once a full working day passes with
 nothing new.
- `state/open-pain.md` lists 56 of 57 as not-proven-done, which is not the same as live. Nothing in the
 record marked old entries finished, and `journal.py resolve` is now the way to say so.

**BROKEN**
- Nothing broken by this change. Unchanged and still true: the company has delivered nothing to the
 owner since 2026-07-19 (**P55**), and its replacement channel is question #11.

**NEXT**
- Read the queue one question at a time, starting with **#11**; it is the one that unblocks every other
 finding reaching him at all.

**EVIDENCE**
- `journal.py status` -> **6,590 chars** (bounded at 12,000; it fails loudly past it).
 `journal.py newest handoff 1` -> 4 KB against a 198 KB shard.
- `journal.py check` -> **0 errors, 4 warnings** (the four are the collision-ID classes, one per kind).
- Migration counts: yoga 63/153/53/61/41, tech +3/+5/+1/+3/+1 new, secratary +1 lesson, stray `~/code`
 PAIN +3 and DECISIONS +2 that existed in no journal copy. `archive/MIGRATION.md` has the table.
- Two silent losses were caught by verification rather than by the script's own report: the first run
 dropped **37** entries with wrapped bold headings, the second dropped **7** whose whole content is on
 the heading line and whose bodies are empty. Both are now lessons.
- Fleet at the time: `ZABZ-TECH` 6 commits behind `origin/master`, `secratary` 1 — stale, not forked,
 both working trees clean.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=716018e1eeaaef5d -->
