<!-- e:handoff|H287|2026-09-15 20:40 UTC|ZABZ-TECH|open -->
## 2026-09-15 20:40 UTC · ZABZ-TECH · The backend suite is NON-DETERMINISTIC - four files pass alone and fail in the full run, and fixing 19 tests made the failure count go UP (21 to 38)

CHANGED
- **`3b46e2fad`** — fixed the comms phone-threading contexts (4 failures → 4 passing) and deleted the
 abandoned multipart backup tests, preserving the one live test.
- **`cb5c29bd2`** — **the backend suite is NON-DETERMINISTIC**, confirmed by isolation. This outranks every
 other test fix in this series.

THE FINDING THAT MATTERS
Fixing 19 tests made the failure count **go up** (21 → 38). That is impossible if the suite were
deterministic, so I tested rather than asserted: four files that fail in the full suite were run **alone**.

| File | Alone | Full suite |
|---|---|---|
| `communications-phone-threading` | **4 passed** | 1 failed |
| `workorders.debug-logging` | **8 passed** | 2 failed |
| `inventory.debug-logging` | **15 passed** | 2 failed |
| `workorders-create-service-errors` | **3 passed** | 2 failed |

**All four pass in isolation and fail in the suite**, across unrelated areas. Definitive: they are not stale
assertions against changed code — they are **files whose result depends on what else ran.** The
`errorLogger` file taking **122 seconds** in the full run is corroborating evidence.

WHY IT OUTRANKS THE STALE TESTS
The stale failures are **deterministic** — fix them and they stay fixed. These are not, and three consequences
follow:
1. **Any "tests pass" claim about this repo is unfalsifiable** until the nondeterminism is bounded — including
 my own. My round-15 "3 files, 39 passed" was a true measurement of three files run together and says
 nothing about that code under the full suite.
2. **The guard test cannot be trusted either way.** It passes alone and scoped, but whether it would *report* a
 bare procedure during a full run depends on ordering.
3. **Some of the 21 failures I triaged as "stale" may be interference instead**, and I could not tell which
 without running each alone. The ones I fixed had legible causes in the assertion text (wrong guard context,
 retired enum, removed procedure), so those fixes are correct — but the interference class was **hiding inside
 the same number**.

FIXED THIS ROUND, WITH CAUSES
- **`communications-phone-threading`, 4 → passing.** The test calls `communications.getConversations` directly
 on the procedure and hand-built its context with only `{ id, email }`. That procedure is a **`managerProcedure`**
 (router line 288), which requires `role in ['ADMIN','MANAGER']`, so the guard rejected all four calls before
 any handler ran. Added `role: 'ADMIN'`.
- **`customer-backup.service.test.ts` deleted (5 failures).** It tested a multipart design that no longer
 exists — `startMultipartUploadSession`, `createMultipartPartUploadUrl`, `completeMultipartUploadSession`,
 `abortMultipartUploadSession` are referenced **nowhere outside tests** — and the router that used them was
 replaced by a batch-ready endpoint. **One test was live and was preserved**, not deleted with the rest:
 `registerBatchReady`, extracted to `customer-backup-batch-ready.test.ts`, passing. Its mock needed
 correcting: the service calls `assertCustomerExists` and builds the package inside `prisma.$transaction`,
 and the fixture date had to become live because `expiresAt` is computed from `Date.now()` (a past fixture
 made the returned package already `EXPIRED`).

A PROCESS FAULT OF MINE, TWICE IN ONE ROUND
I reached for `python -c` **inline through PowerShell** and it mangled the script both times — once a newline
escape, once a quoting break. The **file-based** script worked first try both times. Reading the result is what
caught it; the commands reported no error. This is the eighth instrument fault this session, and the mitigation
is already written down and was simply not followed: **put the script in a file.**

BROKEN
- Nothing in production. Posture intact; workspace clean at `cb5c29bd2`.

NEXT
1. **Bound the nondeterminism** before any further test triage: run each failing file alone, split the list into
 `stale` vs `interference`, then investigate shared mutable state (module-level Maps in
 `account-change-guard.ts:19` and `security.ts:20` are the first suspects — `pool: 'forks'` with
 `isolate: true` is already set, so likely a shared file or env var rather than module memory).
2. Then green the deterministic half, which is what makes `guard-coverage.test.ts` usable.
3. **The proper rebuild** — still the only thing making the audit's source fixes real.
4. Land `agent/guard-ui2`; then the original plan's 78 never-started parts and 17 REVISION parts.

<!-- j2 tags= refs= alias_of= legacy_id= sha=b81fbb93cfd57dbd -->
