<!-- e:handoff|H276|2026-09-15 19:55 UTC|ZABZ-TECH|open -->
## 2026-09-15 19:55 UTC · ZABZ-TECH · The account/sign-in audit series is COMPLETE - all fifteen parts 117-131 documented (129 auth drift, 130 filter site, 131 test blind spots)

CHANGED
- **The account and sign-in series is COMPLETE: all fifteen parts (117–131) documented.** Four parts landed this
 round — PART-129 (auth drift), PART-130 (filter site), PART-131 (test coverage) — plus the ledger close:
 **`fb114ce51`**, **`3bdb68c3d`**, **`10b9fb61a`**.

PART-129 — AUTH DRIFT (the pivot's decision table, checked row by row)
Scored against the shipped code: **10 hold, 5 partial, 0 contradicted.** Every partial runs the same direction —
the decision describes something **stronger** than what shipped, so the code is **behind** the decisions, not
defying them. 15 of 16 rows verified by command with output quoted.
- **F-1 Medium, the one to act on:** decision 5 says retire the Google Form once the website path works. The
 website path is **proven and in production**, so the precondition is met — and instead the Form is **neither
 retired nor processed**. Its ingest exists (`personal-secretary-mvp/scripts/process-computer-form-leads.py`,
 24 KB, mtime 2026-09-14) and is **not scheduled** (`crontab -l` has no `computer-form` or `laptop` entry). **A
 submission to the still-live Form would create nothing — no contact, no task, no lpt-hub record.** Live to
 customers, dead to the business: worse than either option the decision offered.
- **F-2:** decision 8 says *phone canonical preserves the phone→secretary bridge*, and the resolver that works
 matches on **email** (`google-oauth.service.ts:383,414`) while the phone lookup is the one that cannot find
 70% of users (PART-125 F-1). The identity that works is the one the decision made optional.
- **F-3:** decision 9 names Google OAuth the *default*; it was switched off in production until PART-118, and
 `customerLogin` still requires a password the only customer account does not have.
- **F-5:** the master index credits the Google Form ingest scripts to a repo they are not in.
- **F-6, a correction to this series' own record:** I had written "the pivot's 17-row decision table". It has
 **exactly 16** numbered rows and the master doc **never says 17** — the phrase came from my own earlier
 summaries. Recorded rather than quietly fixed.

PART-130 — THE FILTER SITE
**Built (8 files), tested (33), deployed and healthy (`Up 6 hours`), documented (D38-01…07) — and reachable by
nobody.** No `filterapp` DNS record; the Caddy block is present but not loaded. Its LPT sign-in is correctly
dark and **blocked on the authority's credential model** — it needs a secret issued *for it*, which PART-123
F-1 established the single-shared-secret design cannot provide. **Recorded as good:** `SITE_SESSION_SECRET`
under 32 chars refuses to start, a missing broker URL degrades loudly — the same rule as `assertRefreshToken`'s
503, implemented independently in a second repo.

PART-131 — THE SUITE, AND WHAT IT DOES NOT PROVE
~6,400 lines across 20 auth test files, and **every blind spot was observed in this series while the suite was
green** — not a coverage percentage, a list of defects a green run tolerated:
- **F-1 High:** nothing enumerates the router tree and asserts every non-allowlisted procedure rejects an
 anonymous call. That is how `terminal` and `a2pAutoReply` sat open to the internet.
- **F-2 High:** no test reads container configuration, so Google-OAuth-absent-in-production and
 `JWT_EXPIRES_IN=7d`-vs-`15m` were both invisible.
- **F-3:** no test asserts the token-reuse revocation happens; delete it and the suite stays green.
- **F-4:** no test resolves the component graph, so 12 unrouted portal pages are invisible.
- **F-5:** no test asserts a declared policy is enforced (the account gate; `requireMfaForAdmin` with a class
 never instantiated).
- **F-7:** test names drift from what they assert, and no tool catches it.
**The one test that would have caught the most:** enumerate the router tree and assert every procedure off the
public allowlist rejects an anonymous call. Its shape already exists — `isCustomerAccessibleTrpcRequest` is an
allowlist — and it is the same nine probes now living in `/opt/lpt/guard-check.sh`, in CI instead of by hand.

MY OWN DEFECTS THIS ROUND, THREE ON ONE FILE
I corrupted the manifest twice and aborted a repair once:
1. **A NUL byte replaced the leading `0` of commit `0d245bcc3`.** Cause: PowerShell expands a literal
 backtick-0 inside a **double-quoted replacement string** to NUL. `Set-Content -NoNewline` wrote it through.
 Fixed in Python; the file is now NUL-checked.
2. **Duplicate/!unique rows reappeared** from earlier appends — caught by counting rather than by eye.
3. **My first repair script aborted on a line-ending mismatch**, so it wrote nothing while appearing to run.
 The replacement now matches each row independently and **asserts exactly one match per pattern**.
 Also learned: my "unique rows" check was itself broken (splitting on `|` collapses different statuses to the
 same first token), which is why it briefly reported 13 unique. Verified the fix with a regex over the whole
 text and printed all 15 statuses individually.

BROKEN
- Nothing. Posture intact, workspace clean at `10b9fb61a`.

NEXT
1. **The proper rebuild** — still the first item, and now the *only* thing standing between this series' fixes
 and production (`9eaa0eb4a` and everything post-2026-09-10). Run `guard-check.sh` before and after.
2. **The tree-level guard test** (PART-131 F-1) — converts this audit's method into a permanent control.
3. Then the original plan's backlog: 78 never-started parts, 17 REVISION parts.
4. And **one pass over the accumulated owner decisions** — C-117-1, C-119-1, C-123-1, C-126-1, C-129-1, plus
 the Stripe-rotation question already in the queue. They are numerous enough now to deserve a single
 consolidated approach rather than one at a time across sessions.

<!-- j2 tags= refs= alias_of= legacy_id= sha=6371154c0b097be7 -->