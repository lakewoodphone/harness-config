<!-- e:handoff|H293|2026-09-15 21:01 UTC|ZABZ-TECH|open -->
## 2026-09-15 21:01 UTC · ZABZ-TECH · waze-mdm fleet dashboard gate: hash moved out of the repo Caddyfile into .env (live, verified)

CHANGED
- waze-mdm-01 (ssh hetzner, 87.99.141.172), /opt/waze-mdm: Caddyfile now has `ezabz {$DASHBOARD_PASSWORD_HASH}` on both gates (/fleet/* and /api/v1/fleet*), inode preserved (259905), md5 823d6271feec203dddfa71b450956827; docker-compose.yml caddy service got `DASHBOARD_PASSWORD_HASH: ${DASHBOARD_PASSWORD_HASH:?set DASHBOARD_PASSWORD_HASH}` (md5 62259f853a06cf7cf701db44d72dbc70); .env holds the unchanged hash as `DASHBOARD_PASSWORD_HASH='<bcrypt>'` (single-quoted, mode 600).
- repo personal-secretary-mvp: commit de0f7ceb (deploy/waze-mdm/Caddyfile, docker-compose.yml, .env.example), NOT pushed. Repo worktree Caddyfile and compose are byte-identical to the live files.
- Backups (byte-identical to pre-change, md5-verified): Caddyfile.bak-hash-20260915T205824Z (83e4a7642bae002167a1b23a2b1b4104), docker-compose.yml.bak-hash-20260915T205824Z (1709f541962448b164538f15dbc21ea9), .env.bak-hash-20260915T205719Z (4253 bytes, the true pre-change .env).
- Rollback: `cd /opt/waze-mdm && cp -p Caddyfile.bak-hash-20260915T205824Z Caddyfile && cp -p docker-compose.yml.bak-hash-20260915T205824Z docker-compose.yml && docker compose up -d --force-recreate caddy` (rollback was exercised once for real and restored 401 + 200 in seconds).
IN FLIGHT: none.
BROKEN
- The password itself is NOT retracted: the same bcrypt hash is still in git history, in the GitHub remote (origin/master blob carried 2 occurrences), in the `secratary` remote, and in the older Caddyfile.bak-* copies on the server. Only rotating the password remediates that; it is queued as an owner decision (queue row added 2026-09-15).
NEXT
- On the owner's yes: `docker run --rm caddy:2 caddy hash-password --plaintext '<new>'` -> update only /opt/waze-mdm/.env -> `docker compose up -d --force-recreate caddy` -> re-verify md5 + 401/200.
EVIDENCE (all on waze-mdm-01, 2026-09-15 20:57-21:00Z)
- host md5 == container md5 == 823d6271feec203dddfa71b450956827.
- `docker exec waze-mdm-caddy-1 caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` -> `Valid configuration`.
- container Caddyfile: `{$DASHBOARD_PASSWORD_HASH}` x2, literal `$2a$` x0; container env var sha256 == .env value sha256.
- running Caddy admin API (127.0.0.1:2019/config/) effective hash fingerprint 0090e2ba79dc before AND after the change -> the gate is the SAME password.
- 401 https://mdm.abletelsolutions.com/fleet/ ; 401 /api/v1/fleet ; 401 /api/v1/fleet/devices ; 200 /version ; 200 yocheved.abletelsolutions.com ; 200 emt.abletelsolutions.com. Wrong credentials also 401 with `www-authenticate: Basic realm="restricted"` (gate evaluates, is not blanket-refusing).
- guard: with DASHBOARD_PASSWORD_HASH removed, `docker compose config` fails: "required variable DASHBOARD_PASSWORD_HASH is missing a value: set DASHBOARD_PASSWORD_HASH".
NOT VERIFIED: no authenticated dashboard session (no plaintext password available) — the gate was never opened with valid credentials, only refused.

<!-- j2 tags=waze-mdm,security,secrets refs= alias_of= legacy_id= sha=aff1213cba93fa52 -->
