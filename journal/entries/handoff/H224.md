<!-- e:handoff|H224|2026-09-15 13:57 UTC|ZABZ-YOGA|open -->
## 2026-09-15 13:57 UTC · ZABZ-YOGA · comms read-path: the index was reachable, the tool was not

## 2026-09-15 ~14:00 UTC · ZABZ-YOGA · comms read-path: the index was reachable, the tool was not

CHANGED
- `harness-config` 30974cf (pushed): `comms-search.py` -- parameter aliases + `**kwargs`
 absorption + unknown modes named; every query tokenised and quoted so punctuation
 cannot reach the FTS5 parser; loose fallback drops stopwords and is bm25-ordered, and
 reports `match_mode` + the terms it used; `_party_filter` shared by search and thread
 with identity/confidence/other-candidates reported; `search --party X` with no keywords
 works (filter-only); CLI `q` is optional. New `scripts/comms-verify.py` = 13-assertion
 regression battery, all passing.
- `personal-secretary-mvp` 8d4968d21 (pushed, origin/master) + d8603d17 (the authority's
 own lineage): `app/main.py` `/tools/run/{name}` drops nothing but null, so
 `{"limit": 3}` is no longer silently discarded before the tool sees it.
- `dialpad-transcript-gap.py` committed and pushed (bdf39e526) -- the selector's
 `_audio_file_problem` check that lets the transcript queue actually reach 0.
- The authority's 57 unpushed commits pushed to origin as
 `backup/secratary-checkout-20260915`. Non-destructive insurance only.
- Deployed to the authority: `~/.fsearch/comms-search.py` (the live dynamic tool) and the
 patched `app/main.py`; API restarted, healthy, verified through the live dispatcher.

IN FLIGHT
- Nothing of mine is mid-change. Both repos clean on my files.

BROKEN / MEASURED-BAD
- NEW PAIN: the authority's app checkout is a third lineage -- 57 commits reachable from
 no remote (now backed up), 136 behind origin/master, 75 dirty files. Needs a dedicated
 reconciliation session, not a drive-by.
- `waiting` still surfaces an opt-out ("Stop") as a customer waiting and includes
 carrier shortcodes; 1,226 people but only 922 named. Both are next.
- The fleet still does not reach for `comms_search` on its own: 34 calls, all mine. The
 tool now cannot dead-end; nothing yet puts it in front of an agent at the moment a
 customer speaks.
- Permanent ceiling stands (L1481): 6,055 calls can never have words.

NEXT
1. `waiting` noise: drop opt-outs (STOP/UNSUBSCRIBE), carrier shortcodes, and anything
 already answered -- then put ONE deduplicated "<n> customers waiting on a reply" line
 into the owner's attention digest, and only when non-zero (P40 alert fatigue).
2. Give the fleet a reason to call it without being told: recent comms for the
 counterparty of an inbound message/task, in the context it already reads.
3. Identity coverage: name the 304 unnamed people from the business side (repair jobs,
 invoices, contacts) rather than guessing.

EVIDENCE
- `/tmp/cv.py` output: 13/13 PASS through `POST /tools/run/comms_search` and the CLI.
- `comms-refresh.py --check` exit 0, 166,939 records, 0 missing, index 19 min old.
- Loose query before: "It's a iPhone 16 pro max". After: "do you repair cracked iphone
 screens?" -- same corpus, same software, one fix.
- API restart verified by PID change (4092497 -> 4101260) and HTTP 200 at 10s; the
 ConnectionRefused I hit was a 6-second window where something else SIGTERMed the unit
 (restart counter 1), not my patch.

<!-- j2 tags= refs= alias_of= legacy_id= sha=078ffebd11d3015c -->
