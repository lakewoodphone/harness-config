<!-- e:handoff|H202|2026-09-14 23:01 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:01 UTC · ZABZ-YOGA · 2026-09-14 23:05Z ZABZ-YOGA: Dialpad comms end to end -- complete index, scheduled, agent-reachable

## What happened

The desktop session's brief (2026-09-14 11:26) was: *"set up the full dialpad ingest of all
dialpad sms and phone calls and more from all time, and have it fully indexed, categorized and
readable and all that for ai agents to look things up"*. It built `commsindex.py` and indexed
174,622 communications, then the session moved on and **nothing ever ran the index again** — six
hours later it was already missing 19 texts and 62 calls.

This session finished the job: the index is complete by measurement, refreshed by cron on the
authority, and reachable by agents through the live API.

## Measured state (authority `secratary`, 2026-09-14 22:57Z)

| | before | now |
|---|---|---|
| communications indexed | 165,880 (6.7 h stale, unverified) | **164,052** (0 min stale, coverage-verified) |
| coverage vs source | 19 SMS + 62 calls missing; 215 history rows silently dropped | **0 missing, 0 orphan, all 8 source tables** |
| base vs FTS vs trigram | 166,192 / 175,885 / 175,885 — 9,693 dead FTS rows | **equal, checked on every run** |
| voicemail range | …2026-06-14 (crawl froze) | **…2026-09-14, live** |
| phone-number search | returned nothing | works (address is indexed) |
| transcript noise | 38.6% of lines were Dialpad analytics labels | stripped at ingest |
| duplicate calls | same conversation returned twice | collapsed (2,186 redundant rows) |
| agent access | SSH + a CLI | `/tools/run/comms_search`, `ps_comms_*` MCP tools |

## What is now scheduled

`7,37 * * * * /usr/bin/python3 /home/zabz/.fsearch/comms-refresh.py` on the authority — five
minutes after the Dialpad harvest, so the index is never more than one harvest behind. The run
rebuilds (29 s), replays the extractors to prove coverage, writes `~/.fsearch/comms-state.json`,
and exits non-zero if anything is missing. `--check` is the cheap observer: exit 1 if the index is
older than 90 min, failed, or incomplete.

## Files

* `harness-config/scripts/commsindex.py` — extractors, de-duplication, artifact stripping (`bda19d2`)
* `harness-config/scripts/comms-coverage.py` — the audit; imports the extractors and the dedup key
* `harness-config/scripts/comms-refresh.py` — rebuild + verify + state + `--install`
* `harness-config/scripts/comms-search.py` — read-only reader: search / thread / health
* `harness-config/scripts/dsh-sessions.py` — read another machine's DSH transcripts
* `personal-secretary-mvp/scripts/ps_mcp_server.py` — `ps_comms_search|thread|health` (`5e635479b`)
* `personal-secretary-mvp/scripts/dialpad-transcript-gap.py` — the transcript backfill

## In flight when this was written

`dialpad-transcript-gap.py --limit 1200` running on the authority (pid recorded in
`~/dialpad-transcript-gap.log`): 1,117 calls that hold a recording and **no transcript at all**,
2,125 audio-minutes, ~**$12.75** at whisper-1 rates, capped by `--max-minutes 2400`.

<!-- j2 tags=comms,dialpad,handoff refs= alias_of= legacy_id= sha=73a187f956f5d522 -->
