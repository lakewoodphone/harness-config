<!-- e:handoff|H323|2026-09-15 14:26 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:26 UTC · ZABZ-YOGA · comms: 1,204 people named from the shop's own records, and one wrong name removed

## 2026-09-15 ~14:30 UTC · ZABZ-YOGA · comms: 1,204 people named, and one wrong name removed

CHANGED
- `harness-config` (commsindex.py, this session): two new name sources plus a trust
 ranking. Verified on the live index: **1,226 -> 1,479 people, 922 -> 1,204 with a real
 name**. The waiting list now reads "Goldie Atlas / Stroh / Sruly Sonenblick" where it
 read bare numbers, and the busiest correspondent (1,669 comms) no longer reads
 "Microsoft Word" -- it reads its number, because the shop's own case record for it says
 the customer's name is "Pending customer".
- Names now come from: (a) the shop's 368 case records in
 `lpt-hub/docs/customer-operations/sync-records/*.json` (213 named numbers, highest
 trust), and (b) the repair-order SMS templates inside the corpus itself ("Great news
 <NAME>! Your repair order #WO...") -- 9,782 messages, 252 numbers, and the only thing
 that names the three busiest people the corpus left bare (599, 494, 440 comms).
- `name_trust` per slug (0 case record / 1 order template / 2 observed label) decides the
 display name, and a case record saying the name is UNKNOWN suppresses an untrusted label
 from becoming a name. Coverage gate 166,943 / 0 missing; battery 13/13.

BROKEN / MEASURED-BAD
- **NEW P143-adjacent finding, and it is a negative result worth keeping:** the system
 that actually knows these customers' names is the Prisma Postgres `phonetech_dev` behind
 `repos/phone-and-tech-full` (its `repair-notification.service.ts` renders the named
 templates). It is **not installed on the authority at all** -- `postgresql.service` does
 not exist as a unit and there is no `postgres` user, so `ss -ltn | grep 5432` is empty
 and there is nothing to start. Six heavy unnamed numbers
 (7326642417/448, 7326146974/404, 3472154686/315, 8483897895/292, 8454282434/174,
 5163302373/162) are the ones it would name. Until that database is reachable those
 customers have no readable name anywhere, and that is a property of the data, not of
 the query.
- **267 of the 295 unnamed stay unnamed**, and ~90% of the waiting list will keep showing
 a number. The index should say that plainly rather than implying a lookup failed.

NEXT
1. Give the fleet a reason to call `comms_search` unprompted (recent comms for the
 counterparty of an inbound message or task, in context an agent already reads).
2. Voicemail/call self-identification ("my name is X") -- measured ~10-12 usable names
 after review, ASR-noisy, and the owner's own "... from Lakewood" line sits on 40+
 numbers and must be screened out first. Deliberately not applied yet: unverified ASR
 naming is exactly the failure this session removed.
3. Label the unnamed explicitly in `waiting` ("no name on file") so a bare number reads
 as a known state rather than a broken lookup.

EVIDENCE
- `/tmp/vn.sh` output: people 1,479, named 1,204; Elchonon Zelman 599, Nashi Neuman 494,
 Spitz 440, Danny Cohem 308, Reiss 284, Yossi Bernstein 151, Moshe Wilner 29;
 7325036369 -> its own number; `comms-refresh --check` 0 missing; `/tmp/cv.py` 13/13 PASS.
- The trap, measured: every Dialpad table overlaps 269-288 of the unnamed and the union
 covers 295/295 with **zero** real names, because that name field holds the phone number
 when it has no name (422 of 288 overlaps normalise to the number itself).
- Lesson recorded as **L1505**: carry a trust grade on any fact merged from sources of
 different quality, and treat "I do not know" as evidence.

<!-- j2 tags= refs= alias_of= legacy_id= sha=77df793b790a15cc -->
<!-- renumbered H229 -> H323 on 2026-09-15 (ZABZ-YOGA): id H229 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
