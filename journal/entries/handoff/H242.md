<!-- e:handoff|H242|2026-09-15 15:43 UTC|ZABZ-YOGA|open -->
## 2026-09-15 15:43 UTC · ZABZ-YOGA · Nine pre-existing failures closed; pytest tests/ is now 7 failed / 2708 passed / 0 errors

**CHANGED (all preserved on `secratary-master-preserved-20260915`)**
- `1164281a` -- the last pre-existing failure `test_sanitize_tool_call_out_of_workspace` is
 platform-aware: three of its six assertions presupposed an approved Windows root is usable, which
 is true on Windows and impossible on POSIX, where the guard fails closed. Both branches assert the
 security property, so nothing is skipped. **The nine pre-existing failures are now zero.**
- `c60f75b2` -- `tests/conftest.py` raises `RLIMIT_NOFILE` to 16384, so the suite's errors (74 fixed
 order, 1,726 random order) are gone: **`pytest tests/` is 7 failed, 2708 passed, 0 errors** on a
 default shell (soft limit 1024).

**THE SUITE'S STORY, CORRECTED**
P139 said "8 pre-existing failures, 153 order-dependent errors" and blamed whole-suite interference.
The failures were 9 and are 0; the errors were descriptor exhaustion -- the soft limit is 1024 and the
suite opens a thousand-plus event loops -- and are 0 with the conftest raise. The 153 was never
interference, and the wrong explanation had been in the record for a day.

**STILL OPEN**
1. **The descriptor leak itself**: the limit is raised, the loops are still not closed. That is a
 mitigation, not a fix, and it is written down as such.
2. **Seven failures, a different set**: four quiet-hours gates (leading hypothesis: wall-clock
 dependence, since two runs gave 6 and 7), `test_twilio_sms_timeout_sends_async_followup_reply`,
 `test_duplicate_webhook_deliveries`, `test_phase3_modules::test_mcp_enabled`.
3. Queue #39 (cheap-first routing off in production by `.env`, while the DB override says true) is
 still waiting on the owner. Nothing is blocked by it.

**NEXT**
1. Measure the quiet-hours four: do they build their times from `datetime.now()`? If so, that is why
 the failure count moves, and it is a one-fixture fix.
2. Then the descriptor leak: a cleanup fixture that closes lingering event loops, measured by running
 the suite with the conftest raise removed.

**EVIDENCE**
- `pytest tests/ -q --tb=no -p no:randomly` -> 7 failed, 2708 passed, 0 errors (272 s); the four-way
 table above; `ulimit -n` 1024 soft / 524288 hard; commits `1164281a`, `c60f75b2`;
 `tests/test_ai_gateway.py` 98 passed; the four-file set 112 passed.

<!-- j2 tags= refs= alias_of= legacy_id= sha=50b15fde00f15b8c -->
