<!-- e:handoff|H313|2026-09-15 22:03 UTC|ZABZ-TECH|open -->
## 2026-09-15 22:03 UTC · ZABZ-TECH · 2026-09-15 ~22:05Z ZABZ-TECH: fleet-dashboard honesty (1b0c91d6) deployed and verified on waze-mdm-01

CHANGED
* `waze-mdm-fleet-dashboard-1` recreated on waze-mdm-01 (87.99.141.172): b6d127cd2f84, image
 fleet-dashboard:local = sha256:c222d9cf8206, health **healthy**, restartcount 0, started
 2026-09-15T22:01:14Z. Healthcheck now probes `/api/health` (committed value, kept).
* `/opt/waze-mdm/fleet-dashboard/` now holds commit 1b0c91d6's tree (+ routes/, services/, models/,
 tests/ which did not exist on the box before) with LF endings; `backend/frontend-dist/` replaced by
 a freshly built bundle (entry index-CDn7wpMF.js, sha256 934d7267…b710).
* `/opt/waze-mdm/docker-compose.yml`: installed 1b0c91d6's version (healthcheck → /api/health with
 timeout 10s; FLEET_API_URL/FLEET_API_TOKEN added to the dashboard service) AND corrected the build
 context to `./fleet-dashboard/backend`. Owner's `DASHBOARD_PASSWORD_HASH: ${…:?…}` (line 25) and
 `127.0.0.1:8003:8003` (line 212) both intact. Normalised to LF.
* Backups: `/root/waze-mdm-deploy-20260915T215825Z/` (compose ×3 stages, override, full pre-deploy
 source tar, container-inspect-before.json, baselines). Old image pinned as
 `fleet-dashboard:pre-deploy-20260915T215825Z` = sha256:cd198db48619 (2026-07-05).

IN FLIGHT
* Nothing on the dashboard. The authenticated Caddy→dashboard path was NOT exercised end to end:
 the only credential on the box is the one-way bcrypt `DASHBOARD_PASSWORD_HASH` (no plaintext
 anywhere in .env or its backups), so the three endpoints were verified on port 9020 instead.

BROKEN (pre-existing, found not caused; new pain entry filed)
* An unwritable/read-only `/data` makes the dashboard CRASH at startup (init_db → `PRAGMA
 journal_mode=WAL` → `sqlite3.OperationalError: unable to open database file` → "Application
 startup failed. Exiting.") and `restart: unless-stopped` loops it. Present in the OLD code too.
 Fix proposed: make init_db swallow the failure so /api/health answers 503 and the container reads
 unhealthy instead of looping.
* The repo cannot express the box compose build context (see the decisions entry) — the next person
 who copies the repo compose to the box re-breaks `compose build`.

NEXT
* Deploy the staff portal so fleet-api liveness and these honest dashboard numbers become visible
 in a UI (carried over from H311).
* Decide the init_db tolerance fix above.
* Reconcile the repo compose build-context/layout question.

EVIDENCE
* 37/37 files byte-identical to `git cat-file blob 1b0c91d6:<path>`; container-vs-host hashes equal
 for main.py, routes/health.py, routes/fleet_health.py, routes/commands.py, services/db_service.py,
 frontend-dist/index.html and the entry JS.
* /api/health = HTTP 200 `status: degraded` (`ok:false`), db probe ok+verified, rows {devices:0,
 locations:0}, nanomdm reachable (404 on /health), fleet_api not configured.
* /api/v1/fleet/{health,stats,alerts} = 200 with derived:true and named nulls (were 404).
* Unauthenticated via Caddy: /api/v1/fleet/* = 401, /fleet/ = 401, /version = 200.
* `docker ps`: 11/11 up; all other container ids and image ids identical to the pre-deploy baseline.
* Live DB sha256 b978d6d9…c542 unchanged. `pytest tests -q` on the deployed tree: 37 passed.
* origin/master has since moved to 321f167e; 1b0c91d6 is an ancestor and ZERO files under deploy/
 changed in between, so the deployed dashboard code is still the tip for that path.

<!-- j2 tags=waze-mdm,deploy,dashboard refs=H311,H312,1b0c91d6 alias_of= legacy_id= sha=240fe989a79dad9c -->
