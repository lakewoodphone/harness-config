<!-- e:handoff|H198|2026-09-14 22:42 UTC|ZABZ-YOGA|open -->
## 2026-09-14 22:42 UTC · ZABZ-YOGA · Engine froze: a wedged engine the watchdog could not replace for 100 minutes, plus Tailscale dead since boot

CHANGED
- `multi-window/dshw.ps1` (harness-config, clean tree before the edit, 1613 -> 1647 lines):
 - `Test-EngineAlive` probe budget 1500 ms connect / 2500 ms read -> defaults 6000/12000, now
 parameterised (`-ConnectMs`, `-ReadMs`).
 - NEW `Test-EngineWedged`: two patient probes (10 s / 30 s) five seconds apart; only a port
 silent to BOTH may be taken away. Every destructive path is gated on it.
 - `Ensure-Engine`: reclaim is gated on `Test-EngineWedged`; a failed start whose error matches
 already-in-use/EADDRINUSE now refreshes the listen table with the cache bypassed, reclaims the
 real node holder if it is wedged, and retries instead of giving up.
 - `Invoke-Ensure` (what the 1-minute scheduled task runs) now DELEGATES to `Ensure-Engine`
 instead of carrying its own start-only copy. That copy is why the watchdog could not recover a
 wedged engine and only ever logged "port already in use".
 - `dshw new` and `dshw restore` no longer call the destructive path when a listener exists.
 - `Get-ListenTable`: an empty result is treated as a failed read and retried 3x.
 - `Invoke-Health`: transcript is one appended file per day instead of one per run. Nothing deleted.
- Verified: AST parse OK, and `dshw.ps1 ensure` -> "ensure: engine is answering on 3099", exit 0.
- Isolation test `%TEMP%\dshw-probe-test.ps1`: live 3099 alive=True (431 ms cold / 49 ms warm),
 wedged=False (47 ms); free port 39999 alive=False, wedged=True; port 3000 (Next dev) alive=True.
- L505, D139 recorded.

IN FLIGHT
- The wedged-engine path itself is not provable live: there is no wedged engine to reclaim right
 now, so the two-probe gate is verified by the isolation test and code reading only.
- `dshw.ps1` is edited but NOT committed, and the push to `secratary` is blocked (see BROKEN).

BROKEN
- Tailscale on ZABZ-YOGA is dead: `BackendState: NoState`, no tailnet IPs, `secratary.tail93e6e6.ts.net`
 does not resolve. Daemon came up at 16:42:06 and its own record is "You are logged out. The last
 login error was: fetch control key: Get https://controlplane.tailscale.com/key?v=138: context
 canceled" -- it tried before the network was up and never recovered. `tailscale up` and
 `tailscale login` return with no effect; `netcheck` is healthy (UDP true, DERP New York), and the
 control plane answers on 443 in 206 ms, so the network is fine and the daemon is wedged. The
 authority and the owner-queue read path are unreachable from home until an ELEVATED
 `Restart-Service Tailscale` (or a reboot). This session is not elevated.
- DNS damage on top of it: at 17:20:30 Microsoft-Windows-DNS-Client 1023 "Name resolution policy
 table has been corrupted" (rule {5abe529b-675b-4486-8459-25a634dacc23}, error 0x57) plus 1012
 "error while attempting to read the local hosts file" and 1014 for
 `_ldap._tcp.dc._msdcs.tail93e6e6.ts.net`. It has cleared itself; no NRPT rules remain and public
 DNS works. The engine's stderr carries the fallout either way:
 `ssh: Could not resolve hostname secratary.tail93e6e6.ts.net: No such host is known.` x15.
- Boot 16:41:46 (three Kernel-Power 109 shutdown transitions at 16:38:56 / 16:40:29 / 16:41:27).
- Load: 31.6 GB RAM, 24.09 GB committed, CPU 84%, 379 processes, 97 msedge. The
 personal-secretary uvicorn worker (pid 6536) has burned 3060 s CPU in 110 min (~46% of a core).

NEXT
- Commit dshw.ps1; push to secratary once Tailscale is back.
- Get an elevated Tailscale restart (owner action, one click) -- everything that reads the
 authority is blocked behind it.
- Decide what to do about the elevation path found in this audit: `DSH Engine Watchdog (1m)` runs
 `RunLevel: Highest` and executes `scripts/hidden-tasks/DSH_Engine_Watchdog__1m_.vbs`, a file any
 process running as this user can rewrite. That is admin every 60 seconds for anything that can
 write one repo file. I did NOT use it to restart Tailscale, deliberately.

EVIDENCE
- `engine-recovery.log`: 17:49:33 "port 3099 not answering, attempt 1/3" -> 17:49:38 "start FAILED -
 port 3099 already in use" (NO reclaim line: `Get-PortOwner` returned null), then 18:27:26 ->
 18:27:31 "port 3099 bound but not answering - reclaiming from pid 19556" -> 18:28:59 "engine
 answering on 3099 (pid 21124)". The replacing engine needed 71 s to answer.
- `watchdog.log`: ~11 failed starts between 16:45:50 and 18:22:45, all "port 3099 already in use",
 plus 18:30:49 "did not report a URL within 180s".
- `logs\3099-20260914-164252.err.log` (the dead engine, started 16:42:52): clean -- MCP bridges came
 up, no fatal error, only a jina SSE disconnect. It reported a URL at 16:43:25 and was already
 silent at 16:45:50, so it wedged on its own; the watchdog then failed to replace it for 100 minutes.
- `windows.log`: slot 5 at 18:28:43; slot 10 at 18:29:01, 18:31:14, 18:32:51 -- the reopen storm the
 owner saw. w9/w10/w11/w12 are `enabled: false` in windows.json yet w9 and w10 are open, because
 `Invoke-New` force-enables every slot before picking the first free one.
- `Get-ScheduledTask`: "DSH Engine Watchdog (1m)" LastRun 18:34:33, next 18:35:32, RunLevel Highest.

<!-- j2 tags=harness,multi-window,dshw,tailscale,freeze refs= alias_of= legacy_id= sha=036a721285d9adcb -->
