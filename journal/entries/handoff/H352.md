<!-- e:handoff|H352|2026-09-15 23:05 UTC|ZABZ-YOGA|open -->
## 2026-09-15 23:05 UTC · ZABZ-YOGA · Full reconciliation applied: 4 production orders created and verified from a 7-axis fleet; Chaya Brodie duplicate hazard closed; four inherited claims corrected including a false 'broken re'

## 2026-09-15 23:40 UTC · ZABZ-YOGA · Full reconciliation with the production database: fleet of 7, four case orders created in production, one duplicate hazard closed, four inherited claims re-measured

CHANGED
- Owner asked for a full reconciliation with the production database using all Dialpad data, all
 lpt-hub repo info, and everything else, methodically, with the sub-agent flow.
- **Fleet of 7 read-only axes** on disjoint stores, plans at `~/code/lpt-recon/plan/<axis>/`,
 shared rules in `~/code/lpt-recon/CONTEXT.md`, full record in
 `~/code/lpt-recon/reports/2026-09-15-full-reconciliation.md`. Two of my own briefs were corrected by
 agents who read the source instead of the document (the DSN key is `PHONE_TECH_FULL_DATABASE_URL`,
 not `..._DIRECT_URL`; the Prisma schema is under `backend/`, not the repo root).
- **APPLIED to production Postgres, verified live: customers 540 -> 542, orders 584 -> 588,
 tickets 613 -> 617.** Four unblocked, owner-free lpt-hub cases pushed with the existing tool:
 WO26091512712 (Lenovo IdeaPad Slim 3 15ABR8, customer 2609), WO26091512713 (Eli Green, new
 customer 2642), WO26091512714 (Chaya Brodie, new customer 2643), WO26091512715 (Shlesinger
 ThinkPad X201, customer 2082). Order numbers came out exactly as axis A predicted.
- **A duplicate hazard found and closed.** The Chaya Brodie push CREATED its order and then FAILED
 its own idempotency write-back ("the edited text did not verify ('references')"), leaving
 `references` null. `references.websiteOrder` IS the guard, so a re-push would have created a SECOND
 order for the same case. Wrote the link by hand in the tool's exact shape
 (`apply/repair-chaya-writeback.py`), then re-ran the tool's own guard and it now reports
 `Decision: SKIP ... already references live production order WO26091512714`. Committed the four
 record links to secratary's lpt-hub clone as `173a411ee`; not pushed to origin.
- **A second production defect observed live:** `WARNING: website_db: write-log insert failed after
 retries (orders/WO26091512712/create): database is locked` — the business write committed while the
 audit-log write was lost, so WO26091512712 has no push audit row. Same lock storm axes B/F measured.
- Rollback points, both verified: `secretary-pre-recon-20260915T224525Z.db` (5.5 GB, SQLite online
 backup API, 107 s) and `prod-pg-pre-recon-20260915T224857Z.dump` (17 MB, 147 TABLE DATA entries
 confirmed by `pg_restore -l`, including communications/customers/orders/tickets).
- Built and self-tested an **apply gate** (`apply/recon-apply.py`): kind whitelist, row pre-image
 before any UPDATE/DELETE, `--confirm-op <id>` required per destructive op, refuses an unbounded
 UPDATE/DELETE and any plan op with no `affected` count or no evidence, warns when rows-affected
 contradicts the plan's prediction. Proven on a synthetic plan built to be refused: 4 refused,
 1 awaiting per-op confirmation, 2 dry-ran and rolled back; production confirmed untouched after.
- Built **`lpt-recon.py`** (the reconciliation check, per-check provenance + deadline, exit 1 on FAIL,
 2 on unmeasurable) and **`/home/zabz/bin/lpt-recon-check.sh`** (the failure surface: asks the push
 tool what it would create, read-only, and runs the check). Deliberately a REPORTER, not an
 auto-writer — a cron must not create production customers. **Not yet in crontab.**
- Baseline `lpt-recon.py`: **13 ok · 2 FAIL · 1 could not measure** — FAIL on production comms age
 (11.41 h) and on `dialpad_sms_cache` duplicates (127,879 rows / 125,135 message_ids).

IN FLIGHT
- **Axis G (production comms pipeline) is still running.** It is the one axis whose fix is still
 outstanding and it is customer-facing: the shop website's customer message history has been silent
 since **2026-09-15T11:34:24Z** (`metadata.source='dialpad_sync'`, 109,367 rows, 0 duplicate
 external_refs) while the Dialpad harvest is live (newest text 0.6 h). Production holds 4,873 calls
 against 12,552, and 4,873 is exactly `dialpad_call_link`'s row count.
- Nothing in `plan.json` from axes B/C/D/E/F has been applied. Those are specifications, and no agent
 tested its own SQL.

BROKEN / NOT TRUE — four inherited claims re-measured and corrected
- **"Reconciliation has failed every run since 2026-08-23 (33 consecutive 600 s timeouts)" is FALSE.**
 There were 22 successes in that window; the timeouts are 1500 s, a configured bound. The genuinely
 broken trigger is **`autopilot`: no successful run since 2026-08-20**. `comms_freshness` recovered
 today (runs 258/259 succeeded). Real `database is locked` rate **3,322 events / 7d**, not growing;
 the old "7,461" was a journal LINE count and `error_log` undercounts by 27x.
- **"`/health` cannot report a write-path failure" is FALSE as stated.** It sets `ok=db_ok`, so a
 timeout returns `ok:false`. True: the probe is a `SELECT 1` so write contention cannot appear in it,
 and the handler answers HTTP 200 while degraded (4,845 times in 21 days).
- **`/communications/summary` is not "all zeros"** — it is auth-gated (401 without `x-ps-api-key`) and
 with the key it returns `ok:true, cutover_ready:true` with `recent_call_count:0`, because `:748`
 makes zero the PASS condition. `"dialpad"` appears 0 times in its 972 lines. **35 lying readers**,
 the highest-leverage being `scripts/ps_mcp_server.py:464`, whose docstring is the schema hint every
 agent sees and which advertises no Dialpad table.
- **"Axis E found the local Python `re` module is broken" — I tested it and it is FALSE.** `re` behaves
 identically and correctly on ZABZ-YOGA (3.12.10) and secratary (3.14.4), including my applier's own
 FORBIDDEN/WHERE_REQUIRED regexes. `WO2512190001` is 12 characters; `^WO\d{6}\d{5}$` requires 13.
 The test string was malformed, not the module. Axis E caught the symptom honestly and misdiagnosed
 the cause; its numbers are unaffected because it used string slicing.

WHAT THE RECONCILIATION ACTUALLY FOUND (the load-bearing numbers)
- Production was **10 cases behind the corpus, not 372**; the tool's own guard says
 `CREATE 11, SKIP 361, ERROR 0`. 175 skips carry `shouldSyncToWebsite: false` and 143 already name a
 live order. The 2026-08-27 report is internally inconsistent (categories sum to 301, not 289).
- `devices = 1 row` is not data loss: the live device record is `orders.deviceInfo`; the sync tool never
 passes `device_info`, so all 62 orders pushed on 2026-08-27 have none.
- The two secretary.db comms lineages are **different telephone lines** (AI line +17324447361 vs
 business line +17326552355), so a backfill would CONFLATE, not reconcile. ID-level intersection with
 every Dialpad id column is 0 of 70, four ways. Only working key:
 `dialpad_call_link.call_id -> dialpad_call_full.call_id` (4,873/4,873).
- The identity union is **2,849 phone numbers, intersection of all four stores 65**, and **1,463
 traffic-carrying numbers reach no identity record at all** — 90 of them production customers,
 including the four busiest. 4 demonstrable wrong merges, one of which puts the OWNER'S OWN LINE
 inside a customer's identity (`7325691594`, Dialpad "Zabz", production "eliyahu zabrowsky", merged
 into "Yael Herskowitz" by a case record).
- **Do not delete the "duplicate" Dialpad rows.** 2,441 of the 12,029 collapsed rows carry facts no
 other row carries, and **2,282 are the two LEGS of one call each holding a DIFFERENT recording**
 (operator vs admin); 0 pairs have byte-identical media columns, 99.9% are joined by an exact
 operator/entry-point call id. Deleting either leg destroys customer audio.
- Money: **$6,489.94 one-directional gap** (production records LESS than lpt-hub quoted), top three
 Ginsberger $1,210 / Reider $390 / Pinter $270, plus Tzipora Moses' $800 check recorded in lpt-hub and
 absent from production entirely. **No money record exists** in production — four partial ones, seven
 tables at 0 rows. Only `order_status_history` can honestly record why money changed.
- The Weissman E4610 sync-record points at **another customer's order** (WO2608310001 / orderId 2837 is
 Shmuel Birnbaum, "airtag speaker") — a mislink from the 2026-09-15 linkage pass.

NEXT
- Collect axis G, then decide its fix (the only customer-facing item still open).
- Two owner questions are owed and must be asked one at a time: E's deposit question (recommendation
 (a), insert as PENDING against the real order) and the 5 committed customer-credential files.
- Schedule `lpt-recon-check.sh` and `comms-refresh.py --check` in cron; the latter already implements a
 non-zero-on-stale failure surface and is scheduled nowhere, which is why 11.5 h of silence was invisible.

EVIDENCE
- `~/code/lpt-recon/` (CONTEXT.md, plan/<7 axes>/, apply/, reports/2026-09-15-full-reconciliation.md).
- Production before/after counts read live through `PHONE_TECH_FULL_DATABASE_URL`, read-only sessions.
- `git log --oneline -1` in `/home/zabz/repos/lpt-hub` -> `173a411ee`.
- apply log: `/home/zabz/recon-20260915/apply-A-cases.log`; self-test: `recon-apply.py --plan
 _selftest-plan.json --dry-run` -> "refused/error: 4 awaiting your per-op yes: 1".
- `re` test: `apply/_test_re_arithmetic.py` -> WO2512190001 len 12 / 10 digits / no match; WO26091512712
 len 13 / 11 digits / match.

<!-- j2 tags= refs= alias_of= legacy_id= sha=a8b7e46266af6e3d -->
