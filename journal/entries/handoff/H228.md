<!-- e:handoff|H228|2026-09-15 14:11 UTC|ZABZ-YOGA|open -->
## 2026-09-15 14:11 UTC · ZABZ-YOGA · Verification addendum: the reminder error is gone by observation, and the stale Pesach override is inert

**CHANGED since H226 (verification addendum)**
- **The recurring reminder error is gone, by observation.** The 14:05:21 failure that my own
 background check reported as "STILL FAILING" predated the restart; a check window of "the last 6
 minutes" contained lines from before the change point. Re-checked from the restart timestamp
 (`ActiveEnterTimestamp` = 2026-09-15 14:07:56 UTC): **0 `_run_reminder_poll` failures**, with the
 ~14:10:21 tick covered. `app.autopilot` imports with the function present.
- **The aged-out question about the Pesach override needed no fix: it is inert.** `owner_state.current`
 still carries `manual_availability_override` labelled "Pesach 2026" with offline windows
 2026-04-01..04-04 and 2026-04-07..04-09. `owner_model._owner_state_snapshot` returns
 `{"status": "inactive", "label": "none", ...}` today because `_window_is_active` compares against
 absolute dates -- so it cannot misroute current availability, the `manual_availability_override_active`
 finding does not fire, and clearing it would change nothing functional. Recorded so no future
 session re-asks. (This was question #1179/#1194 in the 95 aged-out rows -- the aging was right: the
 premise had expired with the dates.)

**NEXT (unchanged from H226)** -- extend the shelf life to `agent_questions` (313 pending, oldest
134 d); make `attention_debt`'s severity follow freshness so a week-old alert cannot keep the badge
permanently red; observe the sync-alert hook firing; find whether anything renders reminder channel
`dashboard`.

<!-- j2 tags= refs= alias_of= legacy_id= sha=64fd3e674cbf16c7 -->
