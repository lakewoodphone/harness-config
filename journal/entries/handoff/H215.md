<!-- e:handoff|H215|2026-09-15 01:42 UTC|ZABZ-YOGA|open -->
## 2026-09-15 01:42 UTC · ZABZ-YOGA · secratary harness-config is a journal LINEAGE FORK: L510's recipe does not apply

SECRATARY'S harness-config IS IN A JOURNAL LINEAGE FORK — and L510's recipe does NOT apply to it. Read this before attempting any rebase there.

STATE (measured 2026-09-15 01:30Z): `## master...origin/master [ahead 12, behind 46]`, HEAD 5f59cee. A rebase onto origin/master stops on the FIRST commit with **184 conflicts** — 76 decisions, 55 pain, 46 wins, 4 index, 1 tools, 1 state, 1 handoff. It is not the "generated files only" case the Windows recipe predicts.

WHY: both lineages hold THE SAME 740 entries (decisions 108, handoff 121, lessons 324, pain 101, wins 86 - identical counts), produced by two independent absorptions of the same legacy files. The 183 entry conflicts are METADATA-HEADER-ONLY; the bodies are identical (upstream `<!-- e:decisions|D1|2026-09-11|-|open -->`, local `...|legacy|open -->` plus a different sha/alias_of set). And `journal/tools/journal.py` conflicts as CODE with unique load-bearing fixes on EACH side: upstream has the `[a-z]*` suffixed-id regexes, `legacy_id` in META_KEYS, CRLF normalisation and LOCK_WAIT_SEC=240; local has `absorb_key`, the idempotent-absorption fix whose own comment says it prevents 378 duplicate files. Hand-merging this is exactly what P134 (recorded on this host) forbids: do not hand-merge the entries, it is a dedicated session's work, and the tool has `migrate-v2 / absorb / dedupe / repair-ids / reconcile-questions` for it. P134 also records that two earlier migrations silently dropped 37 then 7 entries and were caught only by COUNTING AGAINST THE SOURCE.

WHAT WAS PRESERVED (safe, additive): the brief's "nothing can be lost" was already false - only 9 of 12 local commits were on any ref. The 3 newest (cb5a2ff, 68b3b7a, 5f59cee) existed on NO ref. A new non-forced side ref `refs/heads/diverged-secratary-20260915 = 5f59cee` was pushed (convention matching the fixed autosync), in addition to the existing diverged-secratary-20260914 = 9139929. A local backup branch pre-reconcile-secratary-20260915 also exists. State before == state after, verified by md5 for all three dirty files; no conflict resolution was attempted, no --ours/--theirs, no reset, no force-push. Phone probe 15/15, nothing restarted, ceo-kernel untouched.

IMPORTANT: deploying the fixed autosync.sh there would NOT un-blind the machine on its own. Upstream's fixed script pushes local commits to a side ref and STILL exits attention/1 by design ("a machine that cannot fast-forward is not syncing and that must be visible"). The lineage migration must happen first; the fixed script then deploys by itself.

TWO REAL REGRESSIONS FOUND WHILE DOING THIS:
1. Upstream DELETED `scripts/check-phone-ui.sh` in commit 1f47ec2 (a bulk "journal: comms/Dialpad" tree-sync that swept other deletions), while upstream's own `packages/plugin-attention/README.md:20` still documents it AND secratary's crontab RUNS it (`30 5 * * * .../check-phone-ui.sh --quiet`). A scheduled job is broken. Restore the script (it is in git history) and re-check what else that bulk commit deleted.
2. An untracked `scripts/lpt-hub-refresh.sh` on secratary blocked the checkout because upstream now tracks that path; it was byte-identical to upstream's copy (md5 b24109923d038d4883a13f23357d4eea) and was moved aside and restored. Note the general rule: pushing a new script into the repo can make an existing untracked copy of the same path a blocker on a machine.

NEXT: the prerequisite is P134's migration, run as its own dedicated session on secratary - take origin's journal tool, run migrate-v2/absorb/dedupe/repair-ids/reconcile-questions, verify by COUNTING entries against the source (both sides are 740 today), untrack journal/index/journal.db, rebuild the index, and reconcile the two journal.py fixes. Only then does the fast-forward work. Everything is preserved and no data is at risk, so there is no rush and no reason to hand-merge anything.

<!-- j2 tags= refs= alias_of= legacy_id= sha=dd9f5b448eb68436 -->
