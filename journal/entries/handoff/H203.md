<!-- e:handoff|H203|2026-09-14 23:05 UTC|ZABZ-YOGA|open -->
## 2026-09-14 23:05 UTC · ZABZ-YOGA · Engine freeze: a wedged engine the watchdog could not replace for 100 minutes (fixed, pushed as 444c3b3); Tailscale was never down, MagicDNS was

CHANGED
- `multi-window/dshw.ps1` -- committed **444c3b3** and PUSHED to origin/master (secratary), after an
 earlier local commit d28e793 was destroyed by another session's `git reset` (see BROKEN).
 - `Test-EngineAlive`: 1500 ms connect / 2500 ms read -> 6000/12000 defaults, parameterised.
 - NEW `Test-EngineWedged`: two patient probes (10 s / 30 s) five seconds apart; only a port silent
 to BOTH may be taken away, and every reclaim path is gated on it.
 - `Ensure-Engine`: reclaim gated on it; an EADDRINUSE start failure refreshes the listen table with
 the cache bypassed, reclaims the real holder, and retries.
 - `Invoke-Ensure` delegates to `Ensure-Engine` -- the 1-minute watchdog could previously only START
 an engine, so on a bound port it could do nothing but log "port already in use".
 - `dshw new` / `restore`: never reclaim a bound port. They open WINDOWS; they must not kill engines.
 They were, bizarrely, the only reachable engine-recovery path.
 - `Get-ListenTable`: empty result = failed read, retried 3x.
 - `Invoke-Health`: transcript one appended file per day, not one per run (288/day). Nothing deleted.
- Verified: AST parse clean (1647 lines); `dshw.ps1 ensure` -> "engine is answering on 3099", exit 0;
 isolation test of both probes against the live engine, a free port and a non-node listener.
- Tailscale restored on ZABZ-YOGA. The authority and the owner queue are reachable again.

IN FLIGHT
- The wedged-engine path is not provable live (no wedged engine to reclaim); it is verified by the
 isolation test and by reading. The next real wedge is the test.
- Nothing explains WHY pid 19556 wedged. Its stderr is clean; it reported ready 16:43:25 and was
 silent by 16:45:50. The fleet can wedge again -- it will now be replaced in about a minute, not 100.

BROKEN
- CONURRENT SESSIONS IN ONE REPO. At ~19:00 another DSH session on ZABZ-YOGA ran `git reset` to
 `origin/master` in `~/code/harness-config`, which discarded my unpushed commit d28e793 AND the six
 journal entries written in this session (L505, D139, H198, P126, P127, P128 -- all unpushed, all
 gone from disk). Reflog: `f31de89 HEAD@{3}: reset: moving to origin/master`, then `bda19d2` and
 `1f47ec2` committed on top by that session. `scripts/autosync.ps1` is NOT the cause -- it documents
 that it never stashes, resets, cleans or force-pushes and only does `pull --ff-only` + push.
- The journal therefore reflects one session's view. This entry may itself be reset away; the durable
 record of the fix is the pushed commit message 444c3b3. `journal.py check` was failing with 318
 errors before this session and still is; the classes and the missing repair tool are written up in
 this session's pain entry P128 (which the reset removed -- re-derive from the check output).
- Two faults found in the freeze audit beyond the launcher, both still true:
 1. `DSH Engine Watchdog (1m)` runs `RunLevel: Highest` and executes
 `scripts/hidden-tasks/DSH_Engine_Watchdog__1m_.vbs`, a file writable by any process running as
 this user: write access to one repo file is admin every 60 seconds. Deliberately NOT used.
 2. `Invoke-New` force-enables every slot before picking the first free one, so `dshw new` opens
 slots that `windows.json` marks `enabled: false` (w9 and w10 are open this way).

NEXT
- Re-record this session's lost entries once the journal is owned by one session only.
- Decide what to do about the elevated-task/VBS elevation path.
- `Invoke-New` should respect `enabled: false` or say out loud that it does not.

EVIDENCE
- engine-recovery.log: 17:49:33 "port 3099 not answering, attempt 1/3" -> 17:49:38 "start FAILED -
 port 3099 already in use" with NO reclaim line (Get-PortOwner returned null), then 18:27:26 ->
 18:27:31 "reclaiming from pid 19556" -> 18:28:59 "engine answering on 3099 (pid 21124)".
- watchdog.log: ~11 failed starts 16:45:50 -> 18:22:45, all "port 3099 already in use".
- logs\3099-20260914-164252.err.log: the dead engine's stderr, clean, no fatal error.
- windows.log: slot 5 at 18:28:43, slot 10 at 18:29:01 / 18:31:14 / 18:32:51 (the reopen storm).
- Boot 16:41:46; 84% CPU, 379 processes, 97 msedge, 24.09 GB of 31.6 GB committed.
- DNS-Client 1023 "Name resolution policy table has been corrupted" at 17:20:30, rule
 {5abe529b-675b-4486-8459-25a634dacc23} = the MagicDNS NRPT rule; the Tailscale service restart
 reinstalled it. Elevated `tailscale status` BEFORE that restart already showed
 `secratary 100.84.72.88 active; direct ... tx 1146600 rx 2460616` -- the tunnel was never down.

<!-- j2 tags=harness,dshw,freeze,tailscale,concurrency refs= alias_of= legacy_id= sha=91d2b29690c1dcb0 -->
