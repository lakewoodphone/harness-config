<!-- e:wins|W124|2026-09-14|ZABZ-YOGA|open -->
**W124 · 2026-09-14 · 42 urgent owner alerts were held and unseen because a failed SMS was silent.**

42 urgent owner alerts were held and unseen because a failed SMS was silent

MEASURED on secratary 2026-09-15, the mechanism behind pain P6. The owner-message flush runs at
most once a minute, takes the two NEWEST held rows, and ended with:

 res = send_message(settings=self._settings, to=owner_phone, body=body)
 if res.get("ok"):
 mark_owner_message_sent(self._db_url, mid)
 # <- no else

A failed send logged nothing, counted nothing and changed nothing, so it was indistinguishable
from a send that was never attempted. What that hid:

 * owner_message_queue held 162 rows; owner_sms_allowed admits only urgent/critical (threshold
 "urgent"), so the 95 `ask_owner` rows -- all `normal`, oldest 2026-05-29 -- can never be
 sent, and they are 95 of the 162;
 * 42 held rows WERE sendable. An urgent evening briefing sat at position 2 of the flush order
 and had been re-attempted thousands of times over 5.7 hours, still held;
 * 2 messages were sent in 7 days, and no record anywhere explained why.
Twilio credentials are configured and sms_log shows successful recent sends, so the transport
works: the failure is specific, real and unrecorded.

FIX: `send_attempts` and `last_send_error` columns (lazy migration, the same pattern as
repeat_count/last_seen_at) plus an else-branch that logs at WARNING and records the reason on
the row. VERIFIED: the migration adds both columns; the recorder fires on the first real failure.
Commit abb7a1d8, four tests.

DELIBERATELY NOT FIXED, and this is the important part: the flush only ever attempts the two
newest held rows, so everything older is starved -- and widening that selection would send owner
SMS that is not being sent today. Outbound needs the owner's per-message word, so the starvation
is HIS question, not mine to resolve underneath him. The test asserts both properties: that
failures are recorded, AND that the selection is still two newest, so a later session cannot
"helpfully" widen it.

TWO CORRECTIONS I MADE TO MYSELF WHILE FINDING THIS, both from measuring instead of reasoning:
 1. I first assumed the flush took the OLDEST held rows (its code comment says "oldest first");
 the SQL is ORDER BY created_at DESC, newest first. The comment is wrong.
 2. I then assumed the head was un-sendable and blocked everything: position 2 is urgent and
 sendable. It is attempted, fails, and is silent.

<!-- j2 tags= refs= alias_of= legacy_id= sha=ce9a2a107ffb87e4 -->
