<!-- e:wins|W159|2026-09-15|ZABZ-YOGA|open -->
**W159 · 2026-09-15 · Seven of nine pre-existing failures fixed, three of them defects in the tests themselves.**

**W-new · 2026-09-15 · Seven of the nine pre-existing failures fixed, and three of them were defects in the tests.**

| reading | round-40 baseline | now |
|---|---|---|
| the four-file pre-existing set | **9 failed**, 103 passed | **2 failed**, 110 passed |
| `test_config_contract.py` | 3 failed | 5 passed |
| `test_ai_gateway.py` | 3 failed | 1 failed (deliberate, L1647) |
| `test_assistant_chat_prompt_profiles.py` | 2 failed | **6 passed** |

The last two were fixed without touching an assertion, because all three problems were in the tests:

1. **The Settings fixture read the deployment's real `.env`**, which sets `USE_DYNAMIC_REGISTRY=false`.
 That closed the semantic-routing gate in `run_assistant_chat`, so `choose_model_dynamic` was never
 called and `calls[0]` came back `('ceo','email_draft',None)`. Instrumented to prove it:
 `SEM-GATE tier='ceo' owner=False sem=True dyn=False` -- and `with .env: False` vs
 `_env_file=None: True` settled it. The fixture now passes `_env_file=None`.
2. **The module imports `RouteDecision` and `app.router` in order to stub the router, and never did.**
 With the gate open the real router ran against fake settings and returned no provider/model. Both
 tests now stub `app.router.choose_model_dynamic` with the cheap-first decision they assert about.
3. **My own first stub said `source="test"`** where the tests assert the decision is passed through as
 `"registry"`; the stub now reports what the real dynamic router reports.

**And the investigation found a live production disagreement, which is now queue row #39.**
`.env` sets `USE_DYNAMIC_REGISTRY=false`; the `settings_overrides` table says `true` (written
2026-03-06); the gate reads the `.env` value, so nothing honours the override and **semantic cheap-first
routing never runs** -- 0 `Semantic routing` log lines in six hours. Cheap-first would send simple
tasks to models the smart router prices at 63x cheaper, so "off" costs money and "on" costs some
quality on easy calls. That trade is his, so I queued it with a recommendation rather than switching
it myself.

<!-- j2 tags= refs= alias_of= legacy_id= sha=8bd420be4c004a4f -->
<!-- renumbered W137 -> W159 on 2026-09-15 (ZABZ-YOGA): id W137 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
