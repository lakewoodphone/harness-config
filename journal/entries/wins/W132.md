<!-- e:wins|W132|2026-09-15|ZABZ-TECH|open -->
**W132 · 2026-09-15 · The door security chain is verified end to end - all three contacts flip correctly and the lock is back.**

**W (2026-09-15) - The door security chain is verified end to end, not just transmitting. All three contacts flip correctly and the lock is back.**

Closes the question W131 left open. Owner shut two doors at ~13:40; read back from the automations'
own template sensors:

 outside door off = closed, 0.6 min ago
 interior control room off = closed, 0.9 min ago
 exterior control room on = OPEN (correct -- the shop is open for business)

Both closed doors flipped **within a minute of being shut**, which is the test that distinguishes a
working contact from a misaligned magnet. All three cells read **100%**, and
`lock.0x002446fffd0a4705` is live again at **100%** after its AA change.

**The full chain, all of it verified today:** three SmartThings `IM6001-MPP01` contacts returned on
CR2450 swaps alone and were re-bound and re-configured; the Kwikset `SMARTCODE_DEADBOLT_10_W3`
returned on AA batteries; the NVR camera evidence path was repaired (D147); and the health surface
that watches all of it stopped reporting ghosts as critical (L1520).

**RECORDED SO A FUTURE SESSION DOES NOT ALARM ON IT: `binary_sensor.phoenix_control_room_exterior_door_contact`
reads OPEN during business hours by design.** The shop opens that door. A session that sees a
control-room door open at midday and treats it as an intrusion has misread normal operation. The
signal to watch is the *change* while `binary_sensor.phoenix_everyone_away` is on, which is what the
intrusion automations already key on.

**Still open, and they are cleanup rather than faults:** 377 ghost entities estate-wide; the retired
Keymaster generation (47 automations still unavailable, and it is a second access-control logic
tree -- P76); and the RTSP clip path still has no breaker.

<!-- j2 tags= refs= alias_of= legacy_id= sha=89450943ee0201ac -->
