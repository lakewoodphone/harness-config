<!-- e:wins|W141|2026-09-15|ZABZ-YOGA|open -->
**W141 · 2026-09-15 · pytest tests/ is GREEN: 2715 passed, 0 failed, 0 errors.**

**W-new · 2026-09-15 · `pytest tests/` is green: 2,715 passed, 0 failed, 0 errors.**

| reading | when this work began | now |
|---|---|---|
| full suite failures | 21 | **0** |
| full suite errors | 153 | **0** |
| full suite passed | 2,123 | **2,715** |
| the four-file pre-existing set | 9 failed / 103 passed | 112 passed |

Reached by fixing causes, never by relaxing an assertion. In order:

1. **The pre-existing nine** -- three were a config contract that ignored `validation_alias` plus two
 incomplete env templates; two were a **fail-open** workspace guard that resolved a foreign absolute
 path *inside* the workspace and allowed the call; one was a Windows-only URI decoder that turned
 every VS Code workspace path on Linux into `home\zabz\code\...`; one was a Settings fixture that
 read the deployment's `.env`; one was a router stub the test imported `RouteDecision` for and never
 wrote; one was a Windows-only cross-platform assertion.
2. **The 153 "order-dependent errors"** -- not interference at all: the soft file-descriptor limit here
 is 1024 and the suite opens a thousand-plus asyncio event loops, so it hit `Errno 24` and every later
 test failed at setup. `tests/conftest.py` raises `RLIMIT_NOFILE` to 16384.
3. **The quiet-hours four** -- `_local_ts` built epochs in the *server's* timezone while the code
 evaluates in the *family's*, so "02:00 is quiet" was really "22:00 is quiet" on a UTC host.
4. **`test_mcp_enabled`** -- recorded as the most persistent flake, and it was deterministic: the suite
 exports the repo's `.env` into `os.environ`, so `MCP_ENABLED=false` reached the Settings instance.
 The class now asserts field defaults.
5. **The residual flakiness** -- fire-and-forget threads. `hub.handle_inbound_sms` ->
 `personality.maybe_respond` spawns a daemon running `_worker`, no test joins it, and `_worker`
 writes `chip_in_flight_until` through a database whose path is resolved **at call time**. A worker
 from one test woke during the next and set *that* test's in-flight marker, so its worker returned
 early and the send it asserted never happened. The hub tests now stub the spawn (`53571c04`);
 five consecutive runs of the family-chat pair are 137 passed, and the full suite is green.

Two of my five explanations for that last one were wrong before the right measurement (isolated: 0
failures in 24 iterations; in-file: intermittent; the code path: leaked threads). The wrong ones are
in the record as corrections, which is why the right one is believable.

<!-- j2 tags= refs= alias_of= legacy_id= sha=95a0b7129b857976 -->
