<!-- e:wins|W122|2026-09-14|ZABZ-YOGA|open -->
**W122 · 2026-09-14 · The ghost-agent detector scanned goal_plans and goal_steps but not tasks, where live work is assigned.**

The ghost-agent detector scanned two of three tables, and missed the one live work is in

MEASURED on secratary 2026-09-15. Task #25083 -- "Review CHEMED test results and radiology
reports", a personal medical item whose own description says "Owner needs to review" -- had sat
`in_progress` for 5.7 hours assigned to `dept_executive`, which exists NOWHERE: zero occurrences
in company_blueprint.py, and canonicalize_assignment_agent returned it unchanged.

Two mechanisms existed and neither could see it:

 * self_healing_monitor._check_ghost_agents scans only goal_plans.assigned_agent and
 goal_steps.assigned_agent. It never looked at tasks.assigned_to -- the table the LIVE work
 is in -- so its own resolution hint ("reassign to a live department agent or add the alias
 to _LEGACY_ASSIGNMENT_AGENT_ALIASES") could not be produced for this row.
 * _normalize_assignment_alias_rows does repair tasks and does run at schema init, but only for
 names already in the alias map. `executive`, `dept_ceo` and `dept_executive_assistant` all
 map to orchestrator; the bare form did not -- and the map's own comment discusses this very
 CHEMED plan, so the intent was there and the entry was missing.

So the name fell between a detector that does not scan the table and a normaliser that only
knows names someone already thought of.

FIX: the missing alias, plus the detector's scope extended to live `tasks.assigned_to`.
VERIFIED: after the restart that runs the normaliser, task #25083's assigned_to is
`orchestrator` and `select count(*) from tasks where assigned_to='dept_executive'` is 0.
Committed b3aa1d42, four tests.

ALSO MEASURED, and it corrects the pain P5's premise: the blueprint names 28 agents and 27 have
a live trace; the oldest pending job across the whole queue was 0.8h old, so no work is stranded
waiting for an agent that never runs; and only TWO live tasks were assigned to anything
untraceable. P5 said "work silently vanishing" and "the blueprint and reality disagree" -- what
is actually true is that ONE name was missing from an alias map, in the one table nothing
scanned.

<!-- j2 tags= refs= alias_of= legacy_id= sha=7f2c6d24f24baab6 -->
