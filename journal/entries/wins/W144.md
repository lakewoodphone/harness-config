<!-- e:wins|W144|2026-09-15|ZABZ-TECH|open -->
**W144 · 2026-09-15 · The fleet's liveness is real at last: 50 replied / 13 never / 2 unknown, deployed and hash-verified.**

Deployed 2026-09-15 21:38:55Z. fleet-api c029f049 -> 95c562ba (in-container hash verified against
the file I installed, so the running code is the reviewed code). origin/master 03e7fb50; suite
179 passed on the integrated tree (was 169 + 10 new).

THE FLEET'S LIVENESS IS NOW MEASURABLE, for the first time. Live read of all 65 devices:
 ever_replied: true 50, false 13, null 2 with a last_seen value: 63
 DRN 31 -> last_seen 2026-09-15T20:52:58+00:00, ever_replied true (live)
 DRN 40 -> last_seen null, ever_replied null (no enrolment: cannot tell)
 active devices that have NEVER replied: DRN 5, 15, 27, 39, 54
Before this change the staff panel rendered "Never seen" for all 65 and the silent-device count
was structurally zero, because the portal reads last_seen/ever_replied and the API never sent
them - fleet_devices.last_seen is NULL for every device by the control plane's own admission.

The three-state design is the part worth keeping: true / false / null, where null means "there is
no enrolment row to consult, so we cannot tell", NOT false. A fabricated false there would have
been the same defect in a new place.

TWO THINGS STILL OPEN, both in the portal, neither deployed:
1. The staff UI still needs the portal deployed to show any of this. The API is truthful; the
 page is not yet reading it in production.
2. The portal collapses JSON null -> false (Boolean(null ?? undefined)), so the 2 no-enrolment
 devices render as "No reply on record" instead of "cannot tell". The API is honest; the UI
 bucket is its least-alarming one. Out of the backend agent's scope and reported, not silently
 accepted.

Not touched, reported: fleet_api_remote.py in the same directory carries the identical defect
(no liveness in its SELECT) - it is not in the Dockerfile and ships nowhere, which is the only
reason it is harmless. The live service runs fleet_api:app.

<!-- j2 tags=waze-mdm,liveness,deploy refs= alias_of= legacy_id= sha=0f3209bd6f1b5bab -->
