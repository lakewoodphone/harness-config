<!-- e:wins|W146|2026-09-15|ZABZ-TECH|open -->
**W146 · 2026-09-15 · fleet-dashboard honesty deployed to waze-mdm-01 with byte-verified provenance (37/37 files == git blobs, 37 tests green).**

The dispatcher dashboard on waze-mdm-01 no longer lies about erase, lock and health, and the deploy
is byte-verifiable end to end.

DEPLOYED (2026-09-15 22:01Z): commit 1b0c91d6 `deploy/fleet-dashboard/**` → container
`waze-mdm-fleet-dashboard-1` = b6d127cd2f84, image `fleet-dashboard:local` = sha256:c222d9cf8206,
healthy on first probe, restartcount 0. Frontend bundle rebuilt on the workstation (the repo's
`backend/frontend-dist/` is untracked/gitignored and the previous agent had NOT rebuilt it — a
rebuild without it ships a stale UI): entry `assets/index-CDn7wpMF.js`, 394303 bytes,
sha256 934d7267c6b09773d60bcc7018f6a4ccd08d6e6ef107b075001557a364cbb710.

EVIDENCE THAT ACTUALLY DISTINGUISHES THIS DEPLOY FROM ANY OTHER:
* All 37 installed files hash identically to `git cat-file blob 1b0c91d6:<path>` — the deployed tree
 is the reviewed commit, not a lookalike.
* Container content == host content: /app/main.py, /app/routes/health.py, /app/routes/fleet_health.py,
 /app/routes/commands.py, /app/services/db_service.py and the bundle all hash equal inside and out.
* Served UI is the new one: `GET /` references /fleet/assets/index-CDn7wpMF.js, that asset is 200 /
 394303 bytes, and the OLD bundle in the rollback image contains NO occurrence of the new
 destructive-confirmation strings (`ERASE 1`, `LOCK <n>`) while the new one does.
* The commit's own test suite, run in a throwaway container against the host tree mounted read-only:
 **37 passed** including `test_erase_that_never_dispatches_is_not_a_success`.
* /api/v1/fleet/health, /stats, /alerts were 404 before and now answer HTTP 200 with
 `measured: true, derived: true`, real `measured_at` timestamps, and unmeasurable values as null
 and NAMED (`avg_queue_depth`, `max_queue_depth`, `snapshot_rows`).
* Caddy still gates: unauthenticated /api/v1/fleet/{health,stats,alerts} = 401, /fleet/ = 401;
 /version (the phone check-in path) = 200.
* No collateral: caddy, fleet-api, postgres, nanomdm, nanodep, nanodep-syncer, restore, dns-filter,
 yocheved, emt-quiz all still up with byte-identical container ids and image ids.
* The live SQLite file is unchanged (sha256 b978d6d9…c542), verified before and after.

WHAT MADE IT SAFE TO DO: the 503/healthcheck risk was measured before recreating (200/degraded on
the live config → healthcheck kept as committed); the old image was pinned to a dated tag;
the compose, the whole dashboard source tree and the container's full inspect JSON were backed up
under /root/waze-mdm-deploy-20260915T215825Z/.

THE KNIFE-EDGE MOMENT: `docker compose build` FAILED first — the compose's build context
`../fleet-dashboard/backend` resolves to the nonexistent `/opt/fleet-dashboard/backend` on this box
(the repo layout has fleet-dashboard as a sibling of waze-mdm; the box has it as a child). Fixed and
documented in place; see that day's decisions entry. The box compose, with that one deviation
undone, is byte-identical to the repo blob 211f8b84.

<!-- j2 tags=waze-mdm,deploy,dashboard,honesty,verification refs=H311,1b0c91d6 alias_of= legacy_id= sha=a724db3c78bca4a2 -->
