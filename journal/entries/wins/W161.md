<!-- e:wins|W161|2026-09-15|ZABZ-YOGA|open -->
**W161 · 2026-09-15 · The nine pre-existing failures are closed; the full suite is 6 failed / 74 errors / 2635 passed.**

**W-new · 2026-09-15 · The nine pre-existing test failures are closed, and the full suite moved with them.**

| reading | P139's claim (2026-09-15) | now (full `pytest tests/`, 3:07) |
|---|---|---|
| the four-file pre-existing set | **9 failed** / 103 passed | **0 failed** / 112 passed |
| full suite failures | 21 | **6** |
| full suite errors | 153 | **74** |
| full suite passed | 2,123 (the stale "green" claim) | **2,635** |

Every pre-existing failure is fixed, and none of the nine was fixed by weakening an assertion:

- **3 were one root cause**: the config contract ignored `validation_alias` (so it demanded three env
 names the code never reads and called the three it does read "unexpected") and both env templates
 were incomplete -- 21 undocumented settings in `.env.example` plus a missing
 `web/.env.local.example`. Resolves P188.
- **2 were a fail-open security guard**: `_resolve_tool_path` rooted any non-absolute string inside
 the first approved root, so a Windows path on Linux (and `/etc/passwd` on Windows) was resolved
 *inside* the workspace and allowed. It now fails closed (L1647).
- **2 were a Windows-only URI decoder**: `file:///tmp/x` decoded to `tmp\x`, so on this Linux
 authority every VS Code workspace path was `home\zabz\code\...` -- a string matching nothing, which
 made per-workspace chat attribution silently find nothing. Fixed (`681bc507`).
- **1 was a non-hermetic test**: the Settings fixture read the deployment's real `.env`, which sets
 `USE_DYNAMIC_REGISTRY=false` and closed the semantic-routing gate the test was exercising. Fixed
 with `_env_file=None`; the same value costs money in production and is queue #39.
- **1 was a test that imported `RouteDecision` in order to stub the router and never did**, plus my
 own first stub reporting the wrong `source`.
- **The last one** (this round) had six assertions of which three presupposed that an approved
 Windows root is usable -- true on Windows, impossible on POSIX. It now asserts each host's outcome,
 with the security property asserted in both branches rather than skipped.

The remaining 6 failures and 74 errors are **different** from the nine: the errors are `OSError`s
under whole-suite load, which is P139's other half and still open.

<!-- j2 tags= refs= alias_of= legacy_id= sha=8653415cf1d0a2bb -->
<!-- renumbered W139 -> W161 on 2026-09-15 (ZABZ-YOGA): id W139 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
