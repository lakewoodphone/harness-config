<!-- e:wins|W153|2026-09-15|ZABZ-YOGA|open -->
**W153 · 2026-09-15 · The once-read page and the offline state file now agree with the resolver: 95 to 91, and the number can no longer drift.**

**W-new · 2026-09-15 · The always-read page and the offline state file now agree with the resolver, and can no longer drift.**

| surface | before | after |
|---|---|---|
| `journal.py status` -> OPEN PAIN | **95** | **91** |
| `state/open-pain.md` provenance | generated from the same wrong set | "91 open of 110 pain entries" |
| `journal.py stats` open | inflated by the same rule | 784 open / 36 closed |
| `catalog()` effective status | 91 open / 15 done / 4 superseded | unchanged (it was the reference) |

Three callers asked `marker status != "done"`, so `superseded`, `retracted` and `blocked` entries
counted as open work: `cmd_status` (the page every session reads), `cmd_stats`, and `cmd_state`
(which writes `state/open-pain.md`, the file a session reads when the authority is unreachable --
and whose own header claimed that a corrected entry drops out of it). One `open_entries()` built on
`effective_status` now serves all three, so the next resolution drops the number instead of
quietly inflating it. `check` still exits 0 with 0 errors.

<!-- j2 tags= refs= alias_of= legacy_id= sha=1b3875d0f80741ac -->
<!-- renumbered W131 -> W153 on 2026-09-15 (ZABZ-YOGA): id W131 was independently allocated on another machine for a different entry; upstream keeps the id. Original content unchanged. -->
