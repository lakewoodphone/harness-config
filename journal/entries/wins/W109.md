<!-- e:wins|W109|2026-09-14|ZABZ-YOGA|open -->
**W109 · 2026-09-14 · The migration proved it lost nothing, and the frozen v1 tool still reads the tree.**

The rebuild of 1,235 legacy entries (789 shard + 446 flat) into 674 distinct entries proved its own completeness rather than asserting it:
- byte-preservation residue **0 B across all nine shards**;
- every legacy entry present by identity - 837 exact, 398 the same entry under another id, 0 missing (independent gate `_scratch/j-verify.py`, frozen v1 parser);
- 383 alias rows, so every collapsed copy is still reachable by id and `show` says where it went;
- no id means two different entries;
- the frozen v1 tool still `show`s an entry through the v2 index and `next-id lessons` returns above the real maximum (so a stale machine cannot re-create the collision scar);
- `check` 0 errors; `import-legacy` now reports new 0 and is idempotent.

<!-- j2 tags= refs= alias_of= legacy_id= sha=6d78fb08cd92c3b0 -->
