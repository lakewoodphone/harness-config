<!-- e:pain|P172|2026-09-15|ZABZ-TECH|open -->
## P172 — Scheduled reminders fail on an import error: agent_bus imports a function autopilot no longer defines

FOUND 2026-09-15 16:57 local on ZABZ-TECH, in the desktop secretary API's own error log while I was restarting it for an unrelated reason.

EVIDENCE
- api.err.log line 9: 'Scheduled reminder check failed: cannot import name \\'_run_reminder_poll\\' from \\'app.autopilot\\' (C:\\\\Users\\\\ezabz\\\\Code\\\\personal-secretary-mvp\\\\app\\\\autopilot.py)'.
- app/agent_bus.py:482 does rom app.autopilot import _run_reminder_poll and line 484 calls it via asyncio.to_thread.
- app/autopilot.py no longer defines it: the only reminder-shaped symbol left is _maybe_send_calendar_reminders (line 8406). So the import can only ever raise ImportError - whatever calls that path is dead, and it fails silently into a log line rather than into anything the owner sees.
- The log had been rotated (796 bytes) so frequency is unmeasured; the code path is unconditionally broken whenever it runs.

WHY IT MATTERS
- This is the reminder path on the machine the owner works on. A reminder system that raises on every poll is exactly the class of defect this system has paid for before: the job runs, exits without doing anything, and nothing says so (the family of ten defects recorded in the comms work, L1484).

WHAT WOULD FIX IT
1. Decide which of the two it should be: restore _run_reminder_poll in autopilot, or repoint agent_bus at whatever replaced it (_maybe_send_calendar_reminders looks like the successor but takes different arguments). Check git history for the rename before writing new code.
2. Prove it afterwards the way the rest of this system demands: run one poll and show a reminder that was due actually being sent, not just an import that stopped raising.
3. Then look for the same symbol in the other checkouts (secratary, yoga, mac mini) - the same repo is deployed on every node, so the break is fleet-wide, not local.

<!-- j2 tags= refs= alias_of= legacy_id= sha=19f3d30890deb47a -->
