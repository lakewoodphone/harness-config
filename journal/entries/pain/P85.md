<!-- e:pain|P85|2026-09-14|ZABZ-YOGA|open -->
## P85 — P: desktop personal-secretary-mvp repo diverged - two task installers stuck old; Cloudflare git flaky

ZABZ-TECH's personal-secretary-mvp repo has diverged from origin/master: `git pull --ff-only` on the
desktop returns "fatal: Not possible to fast-forward, aborting". Consequence: the desktop still runs the
OLD scripts/mesh/install-node-agent.ps1 and scripts/install-vscode-chat-sync-task.ps1, so those two
installers would register VISIBLE console tasks if re-run. It is currently masked by the daily
self-heal in dshw.ps1, so it is not hurting anyone today, but the divergence itself is unresolved.

Second annoyance: the Cloudflare path to secratary is flaky for git. One push returned
"Please make sure you have the correct access rights and the repository exists", yet the ref had in
fact landed ("Everything up-to-date" on retry); a later `ls-remote` timed out during banner exchange.
Tailscale DNS is down on ZABZ-YOGA only (`tailscale status` = NoState) while ZABZ-TECH resolves
`secratary.tail93e6e6.ts.net` to 100.84.72.88, which is why the desktop (not yoga) could autosync.

FIX. Resolve the desktop's psm divergence (a merge, by whoever owns that branch), then let the desktop
pull. Prefer pushing harness-config to secratary and letting each machine's autosync pull, rather than
scp-ing files into a repo — scp strands untracked copies that then block the next `pull --ff-only`.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=5403a7899596ad04 -->
