<!-- e:pain|P84|2026-09-14|ZABZ-YOGA|open -->
## P84 — Authority DB: 3.76 GB with lock flapping, 326 stale-key rows, and a conflicted production checkout

The authority database is a 3.76 GB SQLite file with lock contention that is now visibly affecting health: "sqlite3.OperationalError: database is locked" at 21:30:22 today, /health flapping on the same contention (open pains P63/P66/P68), and secretary-db-errors.log showing "DB corruption detected" roughly every 6 hours through 2026-09-10 23:45 and then silence (either the detector or the corruption stopped - unknown, and silence is not evidence of health). It also still contains the DEAD DeepSeek key sk-7e8b...96cf 326 times against 14 occurrences of the live key, and the credentials table is empty (0 rows) while settings_overrides has no key entries - so anything that reads the key from the database rather than .env will keep failing with 401 even though the live .env was rotated at 17:54 today. Related: the production checkout /home/zabz/personal-secretary-mvp is mid-merge ("UU app/autopilot.py" plus ~10 modified files) on the tree the running service executes from.
WHAT WOULD FIX IT: (a) find and rewrite the 326 stale key rows from a single source of truth, (b) instrument the ingest wait time as P48 already proposed, (c) resolve the conflicted checkout on the authority deliberately rather than leaving it mid-merge, (d) decide whether the DB-vs-.env credential fork is removed (one store) or documented.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=d1db00fe960e7bb3 -->
