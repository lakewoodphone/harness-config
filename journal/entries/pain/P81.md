<!-- e:pain|P81|2026-09-14|ZABZ-YOGA|open -->
## P81 — Tailscale wedged on ZABZ-YOGA: NoState with no state file, needs a reboot

Tailscale on ZABZ-YOGA (the home laptop, this session's host) is wedged and the home->office path is down. Symptoms measured 2026-09-14 evening: `tailscale status` -> "unexpected state: NoState", no IPv4 address, self listed offline while secratary sees "zabz-yoga-1 offline, last seen 1m ago" (so the node still heartbeats to the control plane). `tailscale debug prefs` answers and shows LoggedOut false; `tailscale netcheck` runs and reports the gateway/self IP changed to 192.168.12.1/192.168.12.104. curl -4 and -6 both reach https://controlplane.tailscale.com (HTTP 302, ~0.45 s), so this is NOT a network or DNS problem. There is NO tailscaled.state anywhere (C:\ProgramData\Tailscale was created today and is empty), the service restarted cleanly several times, a down/up cycle did not clear it, resetting the Tailscale Tunnel adapter did not clear it, and `tailscale debug daemon-logs` produces no output and hangs. Two tailscaled processes are the service and its child (normal), and this shell is not elevated so they cannot be killed.
WHAT WOULD FIX IT: a reboot of the laptop, which is the owner's to time because it drops this session; if that fails, an elevated repair (stop service, delete/repair state, re-auth). WHAT IT COSTS TODAY: the git remote `secretary-ts` is unreachable from this machine, so pushes go through the Cloudflare tunnel (`secretary-cf`) instead, and any home->office SSH would too.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=182ffbf01be508b8 -->
