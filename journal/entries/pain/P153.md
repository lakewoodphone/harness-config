<!-- e:pain|P153|2026-09-15|ZABZ-TECH|open -->
## P153 — lpt-hub on ZABZ-TECH is 2446 behind / 30 ahead of origin/main -- nothing can be pushed from this machine, and a workstation sync bot rewrites the same sync records

MEASURED 2026-09-15 on ZABZ-TECH: `git rev-list --left-right --count origin/main...HEAD` in `~/code/lpt-hub` = **2446 behind, 30 ahead**. So this machine cannot push at all (`git push origin HEAD` -> rejected, non-fast-forward), and it never could have for a while: 29 of the 30 local commits were already unpushed before this session's case commit (19e8e9ffd).

Consequences:
- Case work done on ZABZ-TECH lands in the working tree and in a LOCAL commit only. The remote therefore lags: at 2026-09-15 `origin/main`'s copy of `docs/customer-operations/cases/usb-files-disappeared-tbd-2026-08-06.md` still had NO chip-off section, while the local working tree had the full 09-01/09-14/09-15 activity log.
- The remote is not idle either: `origin/main` contains a "manager workstation" sync pipeline (`chore(<host>): sync N path(s) from the manager workstation`) that REWRITES customer sync records. It had already bumped the same case's `updatedAt` to 2026-09-15 with none of this session's content in it. So there are two writers to the same file -- a local session and a mounted-workstation sync bot -- and no merge rule.

Do NOT fix this by rebasing a 30-commit divergence over a working tree that carries a dozen other sessions' uncommitted case files. The safe shape is a worktree-based integration: `git worktree add ../lpt-hub-integ origin/main`, cherry-pick the local-only commits there, verify, push from there, then fast-forward the local branch. Before doing it, decide the ownership question: if the manager-workstation sync bot is authoritative for sync records, sessions should stop hand-editing them and edit only the case file, letting the contract's "case file wins" rule regenerate the mirror.

<!-- j2 tags=lpt-hub,git,repo-sync,push-blocked refs= alias_of= legacy_id= sha=ec2a1c9a310cb1bd -->
