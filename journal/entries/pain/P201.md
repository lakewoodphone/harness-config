<!-- e:pain|P201|2026-09-15|ZABZ-YOGA|open -->
## P201 — GBP is unreadable and unwritable from the fleet: both access paths are dead while a doc claims they work

MEASURED 2026-09-15 23:14-23:32Z. (1) data/gbp_token.json DOES NOT EXIST -- not on ZABZ-YOGA, not on secratary (No such file or directory). The OAuth GBP write path is gone, not merely rate-limited. (2) GOOGLE_MAPS_API_KEY is not configured anywhere; .env has only GOOGLE_API_KEY, and GOOGLE_MAPS_API_KEY is an empty placeholder in .env.example line 896. Live endpoint on the authority returns ok=false with error GOOGLE_MAPS_API_KEY not configured. (3) Places API (New) is SERVICE_DISABLED for project 765434853416 (the GOOGLE_API_KEY project), so any Places read 403s. Meanwhile docs/marketing/google-business-profile-management.md lines 42-45 and its section 7 still assert that the rating path WORKS reliably and that Hours are current (content pack matches) -- both false: the profile serves the OLD Mon-Thu schedule with Wednesday OPEN, and the content pack still lists Wednesday 10:30-5:30. FIX: either re-mint data/gbp_token.json (needs owner browser consent) or restore GOOGLE_MAPS_API_KEY and enable Places API (New) on the intended project. Until then LPT hours drift is undetectable without a human plus a JS-rendering scraper. Closely related: P-pain about a doc asserting a capability that measurement contradicts.

<!-- j2 tags= refs= alias_of= legacy_id= sha=ec6bd02e183c7595 -->
