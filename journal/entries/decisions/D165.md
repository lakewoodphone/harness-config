<!-- e:decisions|D165|2026-09-15|ZABZ-TECH|open -->
**D165 · 2026-09-15 · Lakewood's own Twilio auth token is rotated and the leaked one is dead.**

2026-09-15, approved by the owner (option: 'Rotate Lakewood's token today, production updated and verified'). Owner's other Twilio decision, the Outworq client account, is a separate open question.

WHY
The live auth token for account AC274c4dd8af35aa8cbcdf8abf5f7e71b6 ('Lakewood Phone & Tech') was committed in plaintext in phone-and-tech-full (backend/eslint-results.json, backend/simple-server.js, backend/archived-servers/simple-server.cjs, archive/fixes/TWILIO_ACCESS_TOKEN_COMPLETE.md, archive/fixes/TWILIO_CONFIGURATION_FIX.md, docs/development/setup.md, docs/features/twilio/TWILIO_CONFIGURATION_COMPLETE.md, docs/reference/reports/SECURITY_AUDIT_ENV_VARIABLES.md) and was in live use by production. Git history cannot be un-published; rotation is the only fix (P160).

HOW (the no-downtime path, Twilio's own facility)
1. Created a SECONDARY auth token: POST https://accounts.twilio.com/v1/AuthTokens/Secondary, authenticated with the old primary. Verified it authenticates (GET account 200) and verified the probe can fail (bogus token 401).
2. Stored it 0600 at secratary:/home/zabz/.secrets/twilio-lakewood.json.
3. Patched TWILIO_AUTH_TOKEN in every consumer env file with a one-time backup (*.bak-twilirot-20260915): secratary /home/zabz/personal-secretary-mvp/.env, lpt-apps /opt/lpt/.env and /opt/lpt-test/.env, mac-mini /Users/lpt/personal-secretary-mvp/.env, ZABZ-TECH C:\\Users\\ezabz\\Code\\personal-secretary-mvp\\.env, ZABZ-YOGA C:\\Users\\ezabz\\code\\personal-secretary-mvp\\.env.
4. Restarted the consumers so they hold the new value: secretary-api (systemd, secratary), lpt-backend and lpt-test-backend (docker compose --force-recreate on lpt-apps; both healthy afterwards), local secretary API on ZABZ-TECH.
5. PROMOTED the secondary to primary: POST https://accounts.twilio.com/v1/AuthTokens/Promote authenticated with the SECONDARY credential (HTTP 200). The old primary became invalid at that moment.

VERIFIED AFTER PROMOTION (not assumed)
- New token: GET account 200, status active, name 'Lakewood Phone & Tech'.
- Old token (fp ...1efa): 401 rejected. Control bogus token: 401 rejected.
- secratary: /health 200; the app's own settings path reads fp ...c4e6 and returns the account plus messages (real read through app.config.settings).
- lpt-apps: both containers healthy and their live env shows token fp ...c4e6.
- The comms index keeps rebuilding clean (167,012 rows, 0 missing).

LEFT OPEN, DELIBERATELY
- ZABZ-YOGA's patched .env is not yet picked up by its running stack: the host went unreachable (Tailscale name stopped resolving) at the moment of the restart. Its old token is dead, so yoga's local Twilio calls will fail until that stack is restarted. NEXT session: restart yoga's local API (scripts/restart-api.ps1) once reachable.
- The plaintext strings stay in the repos for now. They are DEAD and therefore harmless, but they should still be stripped and gated by a pre-commit check (follow-up, deliberately not done inside another session's dirty tree).
- Outworq LLC's token (AC17e2...) is still live in personal-secretary-mvp/deploy/twilio-studio-flow/.env.vogel - it is the client's account and needs Shimon; that is queue row #17.
- API keys are NOT affected by an auth-token rotation: SK7196... (lpt production) and the two profiles in ~/.twilio-cli/config.json (chaya-production, ps-mvp) remain valid and are their own exposure class to handle separately.

<!-- j2 tags= refs= alias_of= legacy_id= sha=ac7aa03e29a988f8 -->
