<!-- e:decisions|D81|2026-09-14|imported|open -->
**D81 · 2026-09-14 · Correction to the round-6 verification: one of the two "regressions that are mine" was environmental, and the method that said otherwise was flawed.**

I recorded two regressions from my own work and named leads for both. One was wrong. `test_integrity_anchor`'s email-target failure is caused by an **untracked `server/.env`** in this checkout that configures a real partner email and SMTP host, so `publish_integrity_anchors` attempts a send no test environment can complete. Proof: re-running with `LPT_ACCOUNTABILITY_EMAIL`, `LPT_PARTNER_EMAIL_TO` and `LPT_PARTNER_EMAIL_SMTP_HOST` blanked gives **5 passed**. My base-comparison was invalid because a `git worktree` does not carry untracked files — the very thing the behaviour depended on (`L162`).
The second, `test_mtls_handshake_validation::[client_self_signed]`, remains **unattributed rather than mine**: it failed in the full-suite run and in one six-file run, and passes alone, with the consent tests, and with its own file — a concurrency-sensitive TLS assertion, not a deterministic effect of any change here. Recorded as unattributed because "must be mine" and "must be theirs" are both guesses without a reproduction.

<!-- j2 tags=legacy-import refs= alias_of= legacy_id= sha=fffd3e6497c89da4 -->
