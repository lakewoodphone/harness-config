<!-- e:lessons|L890|2026-09-14|SECRATARY|open -->
**L890 · The lpt-hub production sync is lpt-sync-to-production.py with no caller: P82 named the wrong artifact**

THE LPT-HUB PRODUCTION SYNC: my P82 was aimed at the wrong artifact. Corrected here.

P82's two premises are BOTH WRONG, and the real cause is different:
- WRONG ARTIFACT. `data/tools/lpt_hub_production_sync{,,_runner,_executor,_auditor}.py` are INERT STUBS. `lpt_hub_production_sync.py:39-41` warns on a missing dir and RETURNS; `run()` still returns {"ok": True} at line 118; nothing calls the runner. They have never synced anything, and they cannot "fail on a missing dir". P82 measured these.
- WRONG SOURCE. The real source dir EXISTS: `lpt-hub/docs/customer-operations/sync-records` (371 JSON files on remote main). It was never `lpt-hub/sync-records`.
- THE REAL SYNC is `scripts/lpt-sync-to-production.py` (735 lines; local copy mtime 2026-08-27 17:53 EDT, the day it last ran). `main()` line 555, `--push-only` line 508, path resolution `_resolve_lpt_hub_root()` lines 81-101, and it exits 1 with "Cannot find lpt-hub repo" at 198-201 only when EVERY candidate fails.

THE ACTUAL CAUSE OF THE 18-DAY STOP: NO CALLER EXISTS. `grep -rn "lpt-sync-to-production"` across the whole secretary repo returns ZERO matches; crontab has no line; no systemd timer; no workflow; no wrapper. It is hand-invoked only, and no hand ran it. Production agrees: newest `order_status_history` row with reason 'Order created via secretary (cli_lpt_sync_to_production)' is 2026-08-27 18:30:26Z (WO26082712695, 62 rows that day); afterwards orders use ordinary intake numbering. Also note the server's lpt-hub clone claims 8253dcbf as HEAD only because it is STALE (last fetch 2026-08-28); remote main is now 9158 commits ahead and 8253dcbf is an ancestor, not lost.

NEW DEFECT, AND IT DEGRADES THE LIVE SYSTEM RIGHT NOW: a SHADOW `personal-secretary-mvp/lpt-hub/` tree appeared 2026-09-14 14:40 holding exactly 1 record. Because `_resolve_lpt_hub_root()` tries `Path(__file__).resolve().parents[1] / "lpt-hub"` and then `Path.cwd() / "lpt-hub"`, and uvicorn's cwd IS the repo dir, the shadow WINS on the server: the sync would write records where they never reach GitHub, and `app/services/lpt_hub_sync.py` (resolver line 87) now reads 1 record where it used to fall through to the GitHub API and see 371. `LPT_HUB_LOCAL_REPO_PATH` is NOT set in the uvicorn env, so nothing overrides the bad candidate. Anything that resolves a repo root by walking `cwd` is one stray directory away from silently reading the wrong tree.

TO FIX, IN ORDER (verified steps, host ZABZ-YOGA not the server, because its canonical C:/Users/ezabz/Code/lpt-hub exists and is the lineage that last worked):
1. Refresh the 24-day-stale local clone (267 -> 371 records).
2. Confirm PHONE_TECH_FULL_DATABASE_URL in the local `.env` - the local repo has NO `.env` at all, and without it the push prints an error and RETURNS without raising (silent no-op).
3. Build the read-only gap list (371 records vs production by references.orderNumber, then phone). NOT yet computed; backlog size unknown.
4. Dry run exists: `python scripts/lpt-sync-to-production.py --push-only "<record>.json" --dry-run`. There is NO bulk mode - a backlog is a loop.
5. The real run inserts into LIVE production customers/orders/order_status_history/customer_notes with NO rollback path in website_db. That is the owner's per-action yes, not mine.
6. Prove each push via a new order_status_history row carrying the cli_lpt_sync_to_production reason plus website_write_log rows.
7. Cron a READ-ONLY daily gap report. Never cron the push itself.
ALSO OWED: deal with the shadow dir so the resolver cannot pick it (move it aside, or set LPT_HUB_LOCAL_REPO_PATH in the service env), and only then re-enable any caller.

<!-- j2 tags= refs= sha=25ceda4d6f161ec5 alias_of= -->
