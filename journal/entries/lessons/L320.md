<!-- e:lessons|L320|2026-09-14|SECRATARY|open -->
**L320 · The 326 stale-key rows premise is falsified: they are ingested transcripts, and the real leak is elsewhere**

THE "326 STALE-KEY ROWS IN THE AUTHORITY DB" PREMISE IS FALSIFIED, and the real cause is more serious.

Measured 2026-09-14 22:02-22:25 UTC on secratary:
- `grep -abo` over the 3.76 GB data/secretary.db for the dead key prefix = 0 byte matches; over its 280 MB WAL = 0. An SQL scan of every TEXT column of all 203 tables for '%sk-7e8b%' = 0 rows.
- The dead key APPEARS only as ingested DSH transcript text, in dsh_session_events.raw (plus FTS mirrors), source_machine=zabz-yoga, in rows created 22:08:02-22:09:39 TODAY - i.e. written by the audit conversation that was searching for it. It grew in real time: 0 matches at 22:02, 386 byte matches at 22:25, and the DB grew 3.758 -> 3.932 GB in that window via POST /api/v1/owner/dsh-sessions/ingest (~13 MB/min).
- The Aug 16 and Aug 23 DB backups contain 0 occurrences. credentials table = 0 rows. settings_overrides = 11 rows, none key-related. The credential store is .env via systemd EnvironmentFile -> app/config.py:50, NOT the database.
- DB health: PRAGMA quick_check = ok (exit 0, well inside 300 s). page_size 4096, page_count 927,507 -> 959,952, freelist 0 -> 657, journal_mode=wal, schema_version=513. NOT corrupted.

=> No DB rewrite is warranted. Do not "fix" those rows: they are transcripts, and any count of them measures the audit rather than a leak.

REAL PLAINTEXT LEAK LOCATIONS (files, verified): .env.bak-deepseek-20260914-175406 and .env.bak-cfsecret-20260914-143857 on secratary, and /home/zabz/.fsearch/index.db (8 GB, 2 occurrences, because the file index had indexed those backups).

THE GENERAL HAZARD THIS EXPOSES: every session transcript is ingested into the authority DB and indexed by fsearch, so ANY secret ever printed inside a session is permanently in the ~3.9 GB DB and in the search index. Reading a credentials file into a session is therefore a WRITE into the company record, not a private read. This session did exactly that (two live keys read from .credentials.yaml in full), which is why the rotation question matters more than its first answer suggested.

Also measured in the same window: 1343 "database is locked" in 24 h, still occurring after restarts; and the API was restarted 4 times at 22:06-22:10 by another session (deliberate, matching .runtime/deployed-commits.log).

<!-- j2 tags= refs= sha=c0ab475cb9596ccc alias_of= -->
