<!-- e:lessons|L1684|2026-09-14|ZABZ-YOGA|open -->
**L221a · 2026-09-14 - Piping a journal body through PowerShell double-encodes it, and the write path never noticed**

**L Â· 2026-09-14 Â· Piping a journal body through PowerShell double-encodes it, and the write path never noticed**

Writing five entries with `Get-Content body.md -Raw | python journal.py append <kind> --body -`
put **43 mojibake sequences** into the shards: `Ã‚Â·` for `Â·`, and `Ã¢â‚¬"` for `â€”`
(U+00E2 U+20AC U+201D instead of U+2014). PowerShell re-encoded the UTF-8 stream on the way
into the tool's stdin.

Two things made this worth a lesson rather than a silent fix:

1. **The tool accepted it.** `journal.py append` has no encoding assertion, so a corrupted body
 writes cleanly and `journal.py check` still reports 0 errors â€” `check` validates ids and
 structure, not character encoding. A future reader searching for `â€”` or `Â·` would not find
 these entries, and the search index would carry the mojibake.
2. **My first repair missed the worst case.** The script's em-dash rule expected U+009D as the
 third character; the real byte is U+201D. It also scanned a hard-coded shard list that named
 `handoff/2026-09.md`, which does not exist â€” the handoff shard is **split** into
 `2026-09.1.md` and `2026-09.2.md`, so the newest entry was never examined. Fixing the rule
 and switching to a `rglob("*.md")` glob found 4 more.

Durable rules:
- Write journal bodies with an explicit UTF-8 path â€” `python -c` with `subprocess` and a binary
 stdin, or a file argument â€” never through a PowerShell/Windows pipe.
- When repairing an encoding defect, **assert the expected code points** from the file rather
 than typing the mojibake from memory, and glob the file set rather than listing it. Both
 mistakes above were in the repair, not the damage.
- `journal.py check` passing is not evidence that an entry is *readable*. It is worth adding an
 encoding assertion to `append` so this class cannot recur.

Repair tool kept: `harness-config/scripts/repair-mojibake.py` (report by default, `--apply`
to write; idempotent â€” a second run reports 0).

<!-- j2 tags=legacy-import refs= alias_of= legacy_id=L221a sha=30c568de9f3320da -->
